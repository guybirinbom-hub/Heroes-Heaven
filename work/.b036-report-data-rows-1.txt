BATCH 036 — family data-rows-1 (chunk 1 of 2 of "data-rows"), WG-comparison lane
Spec: work/.b036-rows-data-rows-1.json   Test: test/batch036-data-rows-1.test.ts
Assigned findings: 10, all CONFIRMED, all closed with authored data rows. No src/ or scripts/ file edited.

DATA STILL NEEDED
(none — every row this chunk's findings need is in work/.b036-rows-data-rows-1.json)

CROSS-FILE GAPS
- blessed-swiftness#mounted: there is no mount-Speed carrier anywhere in the engine (src/rules/derive.ts:5177 sums landSpeedBonus onto the CHARACTER unconditionally, and no companion/mount Speed lane exists to redirect it to) — WG has no carrier for it either. This chunk discloses the redirect in the record's modeAdjust note instead of mis-modelling it; a real redirect would be an engine-family change in src/rules/derive.ts plus a mount-aware Speed lane, and is not scoped to batch 036.

PER FINDING
- lesson-of-protection#familiar-spell — row {classFeatures/lesson-of-protection, grantedSpells, ["mystic-armor"]}. AoN lesson-19 prints "You gain the Blood Ward hex, and your familiar learns Mystic Armor."; only the hex half had a carrier. Reader already live at src/rules/build.ts:4046-4055 (flat grantedSpells branch of the choice-owned-feature walk) → featureGrantedSpells → grantedByRank merge at build.ts:4295, the lane the shipped lesson-of-vengeance row proved in batch 035. No supersedes (existing overlay rows for the record are focusSpells + edition only). Pinned by test/batch036-data-rows-1.test.ts "lesson-of-protection teaches Mystic Armor" (field) and "a 2nd-level witch who picks lesson-of-protection has Mystic Armor in the spellbook" (built character, with the hex as the control that the pick took effect).
- lesson-of-dreams#familiar-spell — row {classFeatures/lesson-of-dreams, grantedSpells, ["sleep"]}. AoN lesson-16. Same reader, same shape, no supersedes. Pinned by "lesson-of-dreams teaches Sleep".
- lesson-of-life#familiar-spell — row {classFeatures/lesson-of-life, grantedSpells, ["spirit-link"]}. AoN lesson-18. Same reader, no supersedes. Pinned by "lesson-of-life teaches Spirit Link".
- lesson-of-shadow#familiar-spell — row {classFeatures/lesson-of-shadow, grantedSpells, ["chilling-darkness"]}. AoN lesson-22. Same reader; spells['chilling-darkness'] is rank 3, so the build.ts:4295 merge files it in the rank-3 bucket, matching WG's NORMAL giveSpell rank 3. No supersedes. Pinned by "lesson-of-shadow teaches Chilling Darkness".
- lesson-of-decay — row {classFeatures/lesson-of-decay, grantedSpells, ["insect-form"]}. AoN lesson-26. Same reader, rank 3, no supersedes. Pinned by "lesson-of-decay teaches Insect Form" (field) and "a 6th-level witch who picks lesson-of-decay has Insect Form at rank 3 in the spellbook" (built character — the rank-3 half of the merge, with the rank-3 hex Mycological Malady as the control).
- lesson-of-mischief — row {classFeatures/lesson-of-mischief, grantedSpells, ["mad-monkeys"]}. AoN lesson-21. Same reader, rank 3, no supersedes. Pinned by "lesson-of-mischief teaches Mad Monkeys".
- lesson-of-snow — row {classFeatures/lesson-of-snow, grantedSpells, ["wall-of-wind"]}. AoN lesson-23. Same reader, rank 3, no supersedes. Pinned by "lesson-of-snow teaches Wall of Wind".
- lesson-of-the-shark — row {classFeatures/lesson-of-the-shark, grantedSpells, ["feet-to-fins"]}. AoN lesson-28. Same reader, rank 3, no supersedes. Pinned by "lesson-of-the-shark teaches Feet to Fins".
- blessed-swiftness#mounted — row {classFeatures/blessed-swiftness, modeAdjust, <whole array>} with supersedes: true, replacing the existing effect-backfill.json modeAdjust row on the same record+field; the superseded value is carried whole and only the trailing parenthetical is changed, from "(The +5-foot Speed is yours wherever you stand and is already on your sheet.)" to "(The +5-foot status bonus is yours while on foot; if you're mounted, your mount gains the bonus instead.)". AoN class-feature-877 (Blessing of the Devoted, Player Core 2 pg. 86) prints "If you're mounted, your mount gains the bonus instead." and the record's own core-descriptions entry repeats it, so the shipped note contradicted the record's own prose. landSpeedBonus:5 deliberately unchanged — see CROSS-FILE GAPS. Pinned by "the blessed-swiftness mode note states the mounted redirect instead of denying it", which also asserts the +2-defenses half of the note and landSpeedBonus survive.
- blessed-swiftness#aon-parent — two rows, both on classFeatures/blessed-swiftness: {aonParentId, "class-feature-877"} and {aonSection, "Blessed Swiftness:"}. The shipped parent equipment-2320 is Bracers of Devotion (Item 11, Treasure Vault Remastered), whose same-named section reads only "Blessed Swiftness The bonus to Speed is +10 feet." — a name collision on a different mechanic in a different book. The section key moves with the parent because class-feature-877 spells its headings with a colon, exactly as the sibling classFeatures/blessed-shield already joins (class-feature-877 / "Blessed Shield:"). Two fields, so two rows and no merge conflict; neither field has an existing overlay row, so no supersedes. Not player-visible: aonParentId/aonSection are read by the print/lane passes (render-check.mjs, lane-sample.mjs, wg-field-catalogue.mjs). Pinned by "blessed-swiftness joins its print to Blessing of the Devoted, not to Bracers of Devotion", which also asserts the correct sibling join does not move.

NOTHING I COULD NOT DO
All 10 findings are closed by rows. No finding of mine belongs to another family; no row of mine collides with a sibling chunk (the other 22 confirmed findings touch none of my 9 records — checked against work/.b036-read.json, and work/.b036-specs.json held no other spec when this chunk ran).

STATE OF THE TEST FILE
test/batch036-data-rows-1.test.ts: 12 tests, all 12 currently RED and each for exactly the right reason (grantedSpells undefined on the eight lessons; the spellbook line only, with the hex control green, on the two built characters; the old parenthetical and equipment-2320 on the two blessed-swiftness tests). They go green when the driver applies work/.b036-rows-data-rows-1.json. No settle, no teach, no ratchet, no comparer touched — nothing here needs a mutation-proof marker.
Verified: node scripts/test-flip-audit.mjs --batch 036 → clean, every flip cited. npx tsc --noEmit → clean.
