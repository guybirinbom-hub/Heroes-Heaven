[
 {
  "id": "elysiums-cadence",
  "verdict": "CONFIRMED",
  "reason": "Re-derived all three sides independently and the finding survives intact. THEIRS: `node scripts/wg-show.mjs elysiums-cadence --raw` returns \"No ability_block row\" because their name slugs to `elysium-s-cadence` (apostrophe); addressing the row directly via `--wgid 22701` (and via the corrected slug) yields exactly one op, verbatim: `addBonusToValue variable=SKILL_DIPLOMACY value=1 type=circumstance text=\"checks to Make an Impression, or +2 if the target is holy.\"` PRINT: C:/wonderers guide/aon-2e-archive/data/by-category/feat/feat-8407.json reads \"You gain a +1 circumstance bonus to checks to [Make an Impression], or a +2 circumstance bonus if the target is holy. You gain the Set Free reaction.\" I also checked the legacy printing feat-3752 (same feat, legacy_id link): identical clause with \"+2 if the target is chaotic good\". Gather Information appears in NEITHER printing nor their op, so our string has no source anywhere — it is an authoring error, not a legacy artifact. OURS: `grep -rn \"elysiums-cadence\" src/` returns exactly one carrier, C:/trying ai 2/pf2e codex/src/rules/situationalBonuses.ts:292, matching the cited text character-for-character; `grep -c` in that file is 1, so the find is unique; no featGrants*/modes.ts/counterMods.ts carrier delivers the +2 tier elsewhere. Both halves of the difference are real: (1) `when: \"Make an Impression or gather-information\"` extends the star to a check neither WG nor print covers, and (2) the printed +2-if-holy tier is dropped. NOT ASK-OWNER: WG and print agree exactly and ours is the sole outlier, so the fix picks no side; work/rulings/DECISIONS.md, DECISIONS-round2.md and work/owner-questions.json carry no Elysium entry. FIX SHAPE VERIFIED: `bonus: \"+1 circumstance (+2 if the target is holy)\"` is the dominant neighbour convention in this exact file (lines 541, 563, 655, 727, 784, 807, 1050, 1053 and ~20 more all use \"+N type (+M if ...)\"), and `when` renders as a trailing clause (`${bonus} from ${source} — ${when}`, explain.ts:775) so a bare \"Make an Impression\" reads correctly, matching bare-clause neighbours such as line 497. Registry is display-only, keys are per-id, so no sibling shares the lane and no new engine field is needed. ALSO CHECK (Set Free) — no gap, and the finding correctly did not claim one: public/core.json feats.elysiums-cadence already carries `grantsActions: [\"set-free\"]`, backed by the existing scripts/data/effect-backfill.json row (category feats / id elysiums-cadence / field grantsActions — the only backfill row for this id, so a new overlay row would collide), `actions.set-free` exists as a real record, and src/sheet/MainTab.tsx:268-270 collects and renders feats' grantsActions. The reaction reaches the character today. Finding and fix are both exactly right as specified."
 },
 {
  "id": "clandestine-cloak",
  "verdict": "CONFIRMED",
  "reason": "Re-derived all three sides and they agree with the reader. THEIRS: `node scripts/wg-item.mjs \"Clandestine Cloak\"` (their item id 6798; wg-show --raw correctly reports no ability_block row, so the item table is the only source) prints EXACTLY four addBonusToValue ops — SKILL_STEALTH +1 item \"when the hood is up\", SKILL_DECEPTION +1 item \"to Impersonate a forgettable background character when the hood is up\", SKILL_DIPLOMACY -1 item, SKILL_INTIMIDATION -1 item. PRINT: aonId equipment-3067-2998 from public/core.json, mirror file C:/wonderers guide/aon-2e-archive/data/by-category/equipment/equipment-3067-2998.json plus prose in public/core-descriptions.json — same four modifiers; the 2-action Cloak Identity activation grants veil of privacy (counteracts detection/revelation/scrying), which confers no Deception bonus. So there is NO WG-vs-print disagreement and this is not owner territory. OURS: src/rules/situationalBonuses.ts:2465 verified verbatim — the array ends with `{ targets: [{ kind: 'skill', detail: 'deception' }], when: \"never — placeholder removed\", bonus: \"+2 status\" }`, a fifth row present in neither source. It is LIVE: entriesFor() at line 3478 filters only by suppressedIds (whole-id), there is no per-row filter anywhere; core.json items['clandestine-cloak'] has no `situational` and no `situationalReplaces`, so nothing overrides the shipped list; and grep of src/ for the id returns ONLY situationalBonuses.ts lines 1837 (the -greater sibling, which correctly carries exactly four) and 2465 — no featGrants*/featFeatGrants/featPickGrants/featCantripGrants/companionGrants/modes CATALOG_MODES/consumable-modes.json/counterMods.ts carrier, so no other lane already delivers or cancels it. A player with the cloak invested sees a Deception star reading \"+2 status — never — placeholder removed\". The find is unique in the file (line 1837 is the distinct `clandestine-cloak-greater` key) and no sibling shares the lane. WHERE THE FIX IS INCOMPLETE — and the finding's own warning points at the wrong script: apply-situational-lane.mjs CANNOT resurrect this. It builds existingIds from the live file and skips any id already present (alreadyAuthored), and clandestine-cloak is skipped twice more besides — it is in work/situational-lane/corrections-content.json (correctedIds) and spec.json marks it needsHumanDecision:true (escalatedIds). CLAUDE.md's \"it is DELETIONS that die\" note is about removing a whole ID KEY (then the generator re-emits it from spec.json); this fix keeps the key, so no exclusion-list entry is needed or even possible there. The REAL second writer is scripts/apply-corrections.mjs: line 2465 sits below the \"corrected records (verifier-disputed readings, revised)\" banner at line 2314, i.e. that script wrote it. It reads work/situational-lane/patches.json, and for action 'replace' it (a) explicitly drops any prior registry line for the id (\"Drop any prior line for these ids first\") and (b) re-emits EVERY object in `bonuses` verbatim — and patches.json:2447-2452 still contains the `+2 status` / \"never — placeholder removed\" object. Its `reason` field (\"NOTE: the 5th row must be ignored/deleted\") is inert prose the script never reads, which is exactly how the placeholder shipped in the first place. So a re-run of apply-corrections.mjs restores the bad row. Finding correct, fix right but under-specified -> CONFIRMED with correctedFix. Checked work/rulings/DECISIONS.md, DECISIONS-round2.md and work/owner-questions.json: no existing ruling mentions this record. (spec.json does carry a separate needsHumanDecision question about whether the registry should hold conditional PENALTIES at all — that concerns rows 3 and 4, which already ship, and is orthogonal to deleting row 5.)",
  "correctedFix": "TWO edits are required; the deletion alone is undone by the next run of scripts/apply-corrections.mjs.\n\n(1) src/rules/situationalBonuses.ts, line 2465 — delete the trailing fifth array element so the entry ends after the intimidation row. Remove exactly this substring (including the leading comma-space):\n  , { targets: [{ kind: 'skill', detail: 'deception' }], when: \"never — placeholder removed\", bonus: \"+2 status\" }\nThe line must end `..., { targets: [{ kind: 'skill', detail: 'intimidation' }], when: \"while the cloak's hood is up\", bonus: \"-1 item\" }],` — matching the shape of `clandestine-cloak-greater` at line 1837, which already carries exactly these four. The find is unique in the file; do NOT touch line 1837.\n\n(2) work/situational-lane/patches.json, inside the `\"id\": \"clandestine-cloak\"` patch (`\"action\": \"replace\"`, entry begins line 2420) — delete the fifth object of `bonuses`, lines 2447-2452:\n   {\n    \"bonus\": \"+2 status\",\n    \"when\": \"never — placeholder removed\",\n    \"targetKind\": \"skill\",\n    \"detail\": \"deception\"\n   }\nand change the preceding `},` (line 2446, closing the intimidation object) to `}` so the array stays valid JSON. Also reword the `reason` string (line 2454), which currently says \"NOTE: the 5th row must be ignored/deleted\" — apply-corrections.mjs never reads `reason`; it ships every object in `bonuses`, so the instruction has to be executed in the data, not written next to it. Suggested replacement text: \"Cloak Identity grants Veil of Privacy, which counteracts detection/scrying and gives no Deception bonus, so only the four hood-up modifiers are real; the invented 5th Deception row has been removed from this patch.\"\n\n(3) Do NOT add anything to scripts/apply-situational-lane.mjs. Its exclusion mechanism is id-level and this fix keeps the id, so the entry is already protected there (alreadyAuthored, plus correctedIds and escalatedIds); an entry there would be dead weight.\n\nOptional verification: `node scripts/apply-corrections.mjs --dry` after edit (2) should no longer emit the `+2 status` row for this id."
 },
 {
  "id": "harbingers-protection",
  "verdict": "CONFIRMED",
  "reason": "Re-derived from scratch; every claim holds and the fix is exactly right as specced.\n\nTHEIR OPS (re-run myself). `node scripts/wg-show.mjs harbingers-protection --raw` prints \"No ability_block row\" — a FALSE NEGATIVE, because slug(\"Harbinger's Protection\") is \"harbinger-s-protection\" and the script's `n.includes(target)` fallback cannot bridge the apostrophe. Querying by name (`node scripts/wg-show.mjs \"Harbinger's Protection\" --raw`) resolves their id 39543, type=feat, level=6, with exactly the 13 ops claimed: `adjValue HEAVY_ARMOR value={\"value\":\"T\"}`, then six `conditional` ops, each with two conditions — `IF LIGHT_ARMOR EQUALS E` + `IF HEAVY_ARMOR LESS_THAN E` -> `trueOperations: adjValue HEAVY_ARMOR = E`, likewise M and L, then the same three off `MEDIUM_ARMOR`. No item-table lookup was needed (no ITEM_/ABILITY_BLOCK_ refs). ⚠ UNARMORED is never read as a source; nor is HEAVY_ARMOR (it is only the LESS_THAN guard, i.e. raise-only). Net semantics: heavy := max(heavy, best(light, medium)), floor trained.\n\nPRINT. `C:/wonderers guide/aon-2e-archive/data/by-category/feat/feat-7509.json` (aonId feat-7509 read from public/core.json feats[\"harbingers-protection\"]; identical prose in public/core-descriptions.json): \"You often work on missions alone... You are trained in heavy armor. When you gain expert or greater proficiency in any type of armor, you also gain that proficiency in heavy armor.\" Note it has NO \"(but not unarmored defense)\" parenthesis AND no \"class feature\" restriction.\n\nOURS. `src/rules/featGrantsAuto.ts:408` — `'harbingers-protection': { armor: { heavy: 'trained' } },` — cited line:file is exact, and `grep -c` in that file = 1, so the find is unique. It is the ONLY carrier: grep over src/ returns that file alone, and featFeatGrants.ts / featPickGrants.ts / featCantripGrants.ts / companionGrants.ts / situationalBonuses.ts / modes.ts / counterMods.ts / scripts/data/*.json all return 0. scripts/data/effect-backfill.json has four \"harbinger\" ids (harbingers-claw, harbingers-armament, harbingers-caw, battle-harbinger-dedication) but NO harbingers-protection row, so no overlay collision. Only op #1 is delivered; the mirror is missing outright.\n\nTHE FIX'S SHAPE IS RIGHT (this is where I expected it to fail). The field is declared `armorMirrorBest?: ArmorCategory[]` at src/rules/featGrants.ts:262 — a BARE ARRAY. `armorMirrorBest: ['heavy']` matches. (An earlier draft spec preserved in work/all-findings.json proposed `armorMirrorBest?: { categories: ArmorCategory[] }`; that shape was superseded when the lane was actually built and would not type-check today. The finding uses the shipped shape.) It also matches the neighbour verbatim: featGrantsAuto.ts:530 `'mountain-skin': { armor: { medium: 'trained', heavy: 'trained' }, armorMirrorBest: ['medium', 'heavy'] },`. Resulting line: `'harbingers-protection': { armor: { heavy: 'trained' }, armorMirrorBest: ['heavy'] },`.\n\nREADER + ORDERING. build.ts:5644-5659 loops feats, reads `FEAT_GRANTS[fc.featId]?.armorMirrorBest`, computes `best` by reducing maxRank over `['light','medium','heavy']` of proficiencies.defenses (unarmored deliberately absent), and raises each listed category via maxRank. Its guard `if ((proficiencies.defenses[c] ?? 'untrained') === 'untrained') continue;` is satisfied here because the flat `armor` grant is applied at build.ts:4592, ~1050 lines earlier, and harbinger's heavy grant is unconditional (no conditionalArmor), so heavy is already 'trained' when the mirror runs. Source set {light, medium, heavy} vs their {LIGHT, MEDIUM} is behaviourally identical: heavy is the target and maxRank makes including it a no-op. Placement is after class advancement, so the mirror sees final ranks — which is what \"when you gain expert or greater proficiency in any type of armor\" means.\n\nPLACEMENT (featGrantsAuto vs HAND_AUTHORED_GRANTS). featGrantsAuto.ts is correct. sentinel-dedication's lives in featGrants.ts only because its whole entry does (it needs conditionalArmor + a hand-authored comment block); mountain-skin's lives in featGrantsAuto.ts and works, proving the auto table feeds FEAT_GRANTS (test/armor-mirror-best.test.ts asserts `FEAT_GRANTS['mountain-skin']?.armorMirrorBest` toEqual ['medium','heavy'] and passes). harbingers-protection's entry is already in the auto table; moving it to the hand table would shadow the auto entry (hand wins on id conflict, per the comment at featGrantsAuto.ts:~401) and force duplicating the `armor` map for no gain.\n\nSERIALISER (the loaded-gun check). scripts/aon-verify/_ser.ts `lit()` is genuinely generic — `if (Array.isArray(v)) return \\`[${v.map(lit).join(', ')}]\\`` and a recursive Object.entries walk, with no key enumeration anywhere — and `serGrant()` eval-round-trips and THROWS if JSON.stringify differs, so a whole-file rewrite by apply-reviewed/apply-clear/apply-conditional/apply-fixes/fix-conditional-ranks cannot silently drop `armorMirrorBest: ['heavy']`. mountain-skin is the live proof.\n\nNOT ASK-OWNER. Their ops and our reader AGREE on unarmored: they read only LIGHT_ARMOR and MEDIUM_ARMOR, and the reader hardcodes ['light','medium','heavy']. The print is SILENT on unarmored (\"any type of armor\"), not affirmative — silence is not a contradiction, so there is no WG-vs-print disagreement for the fix to pick a side in. Moreover the exclusion is a pre-existing, documented contract of the field itself (featGrants.ts:257-262 and the build.ts:5637-5642 comment), not a decision this one-line entry makes. Nothing in work/rulings/DECISIONS.md, DECISIONS-round2.md or work/owner-questions.json touches the armour mirror or unarmored feeding it (the only \"unarmored\" hits are the unrelated armor-in-earth and Hardwood Armor questions).\n\nNO REGRESSION. The change is additive and keyed to one id; test/armor-mirror-best.test.ts asserts only sentinel-dedication and mountain-skin, and the mirror is raise-only via maxRank so AC can only rise. Sibling armigers-protection (featGrantsAuto.ts:79) prints the same clause and still lacks the field — a separate, pre-existing gap, untouched by this fix."
 },
 {
  "id": "inscribed-with-elders-deeds",
  "verdict": "CONFIRMED",
  "reason": "Re-derived all four legs; the gap is real, the proposed fix is right in shape but not implementable as written.\n\nTHEIRS (re-run `node scripts/wg-show.mjs inscribed-with-elders-deeds --raw`, feat 51601): one `conditional`, `LEVEL GREATER_THAN_OR_EQUALS 13`; trueOperations = one `select` ABILITY_BLOCK, `abilityBlockType:\"feat\"`, `traits:[\"Orc\"]`, `level.max: 5`; falseOperations = the same with `level.max: 1` (plus a stray `isFromArchetype: true` the reader did not mention — a WG authoring artifact; print governs, so it changes nothing).\n\nPRINT (C:/wonderers guide/aon-2e-archive/data/by-category/feat/feat-7653.json, mirrored verbatim in public/core-descriptions.json): \"granting you a 1st-level ancestry feat with the orc trait until you prepare again… At 13th level, you can instead gain a 5th-level ancestry feat with the orc trait.\" WG and print AGREE, so no side is being picked — not ASK-OWNER. Nothing in work/rulings/DECISIONS.md, DECISIONS-round2.md or work/owner-questions.json touches this feat or a level-stepped picker cap.\n\nOURS: public/core.json feats.inscribed-with-elders-deeds.choice.from = {type:'feat', maxLevel:1, traits:['orc']}; the 13th-level step exists only inside choice.note. I grepped src/ and scripts/data/ for both `inscribed-with-elders-deeds` and `eldersDeedsFeat`: the ONLY hit is scripts/data/effect-backfill.json line 41646 (category \"feats\", field \"choice\"). No featPickGrants/featFeatGrants/featGrants*/companionGrants/situationalBonuses/modes/consumable-modes/counterMods entry — the reader missed no carrier. Exactly one backfill row for this id (of 433 choice-field rows), so no collision; the fix edits that row in place, it does not add one.\n\nMEASURED myself from public/core.json: 31 feats carry the orc trait; 10 at level ≤1, 19 at level ≤5 — the reader's numbers are right. So from 13th level we hide 9 legal picks. That is the exact anti-pattern openChoice.ts:149 already names (\"made a pick the rules allow unreachable\") and the inverse of featPickGrants.ts:11's stated convention (\"at worst slightly permissive, never blocking a legal pick\"). The daily answer is recorded-only (there is no `grantFeat` counterpart to `grantInnate`/`grantLanguage`), so the option list IS the whole mechanic here — that strengthens the finding rather than making it cosmetic.\n\nANCESTRY-CATEGORY question (asked): `from.type:'feat'` does NOT filter category — `featCategory` is the field that does (openChoice.ts:52), and siblings use it (items/ancestors-call, items/ancestral-embrace, feats/scion-of-many-planes). But all 31 orc-trait feats are category 'ancestry', so omitting it admits nothing illegal. Not a required part of the fix.\n\nNEW-FIELD test (the one that usually kills these): there is genuinely no existing way to say a level-stepped cap in this lane. OpenChoiceFrom.maxLevel is `number` only (types.ts:1937) — no 'self'/'half' (that is FeatPickSpec.maxLevel in the separate build-time featPickGrants lane, and neither value can express 1→5 at 13) and no formula-string form (that is DailyItemDef.filter.maxLevel: `number | string` at types.ts:2680, resolved by dailyItems.ts:94-97; resolveFormula supports only floor/ceil/max/min and arithmetic, so `min(5,1+4*floor(level/13))` would technically work but is another lane's vocabulary and unreadable). I enumerated ALL 11 open feat choices in core.json: none carries a level step. And the precedent for adding an OpenChoiceFrom field that reads the built character is established twice in this very file — `excludeOwn` and `excludeOwnTraditions` were both added for exactly this reason (\"the record shipped a note apologising… It can: this resolver already runs against a built character\"). Our record's note is that same apology.\n\nWHY MISSPECCED: (a) \"read in featsMatching\" is not implementable — featsMatching(from, content) at openChoice.ts:48 has no character or level, and `case 'feat'` (line 107) calls it without opts; the level must be threaded from opts?.character?.level. (b) A characterless fallback is mandatory: src/builder/shared.tsx:4207 calls openChoiceOptions with no character, and test/choice-lane-applied.test.ts:36-46 requires every non-`own-*` open choice to resolve non-empty with no character. (c) `maxLevelAt: {level,maxLevel}[]` matches no neighbour: the array precedents are `spellRankByLevel: {level,rank}[]` and `countByLevel: {level,count}[]` (types.ts:2668,2675), and the single-step precedent is `advancedAlchemy.atLevel: {level,items}` (types.ts:2642, read at build.ts:5162). Corrected spec below.",
  "correctedFix": "1) src/rules/types.ts — in `OpenChoiceFrom`, immediately after `maxLevel?: number` (line 1937), add a field named to match its neighbours (`spellRankByLevel`, `countByLevel`), not `maxLevelAt`:\n\n  /** feat: the ceiling RISES at a printed level. Inscribed with Elders' Deeds offers a 1st-level orc\n   *  ancestry feat and \"At 13th level, you can instead gain a 5th-level ancestry feat with the orc\n   *  trait\" — 10 legal picks become 19, and with only a flat `maxLevel` the clause lived in `note`\n   *  and 9 picks the rules allow were unreachable for the rest of the career. Highest threshold the\n   *  character has reached wins; with NO character the base `maxLevel` stands, which is what keeps the\n   *  characterless guard in test/choice-lane-applied.test.ts green. Same shape as\n   *  `DailyItemDef.filter.spellRankByLevel`. */\n  maxLevelByLevel?: { level: number; maxLevel: number }[];\n\n2) src/rules/openChoice.ts — thread the level and mirror `rankOf` (dailyItems.ts:65-68):\n   - line 48: `function featsMatching(from: OpenChoiceFrom, content: ContentDatabase, level?: number): Feat[] {`\n   - inside, replace line 53 with:\n        const stepped = (from.maxLevelByLevel ?? [])\n          .filter((e) => level !== undefined && level >= e.level)\n          .reduce((n, e) => Math.max(n, e.maxLevel), 0);\n        const cap = stepped || from.maxLevel;\n        if (cap !== undefined && f.level > cap) return false;\n     (`stepped || from.maxLevel` is the characterless fallback — do not default `level` to 0 and drop the base.)\n   - line 107: `case 'feat': return featsMatching(from, content, opts?.character?.level).map(...)`\n   Both live call sites already pass a character: src/rules/dailyChoices.ts:68 (`{ character: c }`) and src/builder/Builder.tsx:458/632 (`featPrereqChar`); src/builder/shared.tsx:4207 does not, and takes the fallback.\n\n3) scripts/data/effect-backfill.json — edit the EXISTING row in place (line 41646, category \"feats\", id \"inscribed-with-elders-deeds\", field \"choice\"); do NOT add a second row. In `value.from` add:\n     \"maxLevelByLevel\": [{ \"level\": 13, \"maxLevel\": 5 }]\n   and shorten `value.note` to the part the picker still cannot enforce, dropping the now-modelled clause:\n     \"Temporary, so it can't be a prerequisite for anything permanent, and it can't require a physiological feature you lack.\"\n   Then re-apply to public/core.json (scripts/apply-backfill-now.mjs / npm run data) so the shipped record carries it.\n\n4) Do NOT add `featCategory: 'ancestry'` as part of the fix — all 31 orc-trait feats are already category 'ancestry', so it changes no option list at either cap; adding it is intent-only noise.\n\nExpected result: level ≤12 → 10 options (unchanged); level ≥13 → 19 options; no character → 10 options."
 },
 {
  "id": "confounding-image",
  "verdict": "CONFIRMED",
  "reason": "Re-derived independently and it survives. THEIRS: `node scripts/wg-show.mjs confounding-image --raw` returns exactly one op, `{type:'injectText', data:{type:'spell', id:5142, text:'You can modify its duration to be sustained up to 1 minute and modify its range to 30 feet...'}}`. PRINT: C:/wonderers guide/aon-2e-archive/data/by-category/feat/feat-3377.json carries the identical clause, so there is no WG-vs-print disagreement and rule 4 does not apply. OURS: public/core.json feats['confounding-image'] carries only level/traits/prerequisites/actionCost/archetype/edition/aonId — no mechanical field — and grep of src/ for the id returns zero hits; I also checked featGrants*/featFeatGrants/featPickGrants/featCantripGrants/companionGrants/situationalBonuses/modes CATALOG_MODES/consumable-modes.json/counterMods and no carrier already delivers it.\n\nTHE LANE EXISTS WITH A LIVE READER, and the finding names it correctly. Field is `spellNotes` (plural). Typed at src/rules/types.ts:324 (`spellNotes?: SpellNote[]` on ContentBase) with the SpellNote interface at types.ts:381; character-side shape at types.ts:5249 (`Record<string, {from,note}[]>`). Built at src/rules/build.ts:5442-5460 — `pushSpellNote` plus the `ownedRecords` loop. Read through src/rules/explain.ts:687 `spellNotesFor(c, spellId)`. Rendered at src/sheet/SpellsTab.tsx:2205, `notes={spellNotesFor(character, detail.id)}` into `SpellDetail` (prop documented at SpellsTab.tsx:111 as \"Principle N2\"). Authored shape is exactly `{spellId, note}` — confirmed against feats/spew-tentacles -> black-tentacles. 83 spellNotes rows already live in scripts/data/effect-backfill.json.\n\nDOES IT RENDER WITHOUT THE SPELL BEING GRANTED? Yes. `pushSpellNote` drops a note only when `!content.spells[spellId]`; the `minLevel` gate at build.ts:5455 fires only if the record itself carries an `innateSpells` entry for that spell, which confounding-image does not. So the note lands on `character.spellNotes['mirror-image']` purely from owning the feat, and SpellDetail prints it when the player opens Mirror Image in their prepared/repertoire list — which the prerequisite \"ability to cast mirror image\" guarantees they have.\n\nI attacked the scope objection and it fails. docs/mechanic-lanes.md:97 summarises the lane as \"the record grants a spell and then CHANGES it\", and owner principle N2 as quoted in types.ts says \"on the spell you get from this feature\". Read strictly that would exclude a feat that grants nothing. But the lane's own live content already goes past that summary: feats/overwhelming-harm -> harm (harm is granted by classFeatures/necromancer-spellcasting, NOT by the feat) and feats/cruelty -> touch-of-the-void (granted by NOTHING anywhere in core.json) are both shipping today, both are \"you can modify the spell\" clauses on a spell the character casts from their own spellcasting, and both are the exact structural twin of Confounding Image. feats/mercy and feats/greater-mercy -> lay-on-hands are the same shape. The code has no grant gate and no test asserts one (only test/echo-pickers.test.ts and test/innate-spell-verification.test.ts touch spellNotes, and neither requires a grant). So this is existing practice, not a scope expansion needing an owner call. docs line 97 is a descriptive summary of the common case, not a gate.\n\nI RESOLVE THE TENSION: BOTH GET spellNotes, and the uneasy-rest precedent is the thing that is wrong, not this finding. work/wg-batch-017-parity.json's uneasy-rest evidence rests on a false premise it states outright: \"The spell-modification lane does not exist in our vocabulary and is applied consistently.\" It does exist — 83 rows, a documented lane row, a reader chain, and an owner principle. Its second argument, that core-descriptions.json already carries the prose so the player reads the same rider, is pre-rejected by the owner: types.ts's N2 doc says leaving the clause on the feat alone \"puts it on a page nobody is looking at while casting\", which is the entire reason the lane was built. I verified core-descriptions.json feats['confounding-image'] does carry the full text, and that does not discharge the gap. Sorting the four earlier verdicts: barrier-shield (B12) stays correct — its injectText restates the feat's own text and names no spell, so there is no spell to write onto; agile-hand (B14) stays correct — its one number is already carried at situationalBonuses.ts:2636. But tracing-sigil (B13, injectText onto the sigil spell) and uneasy-rest (B17, injectText onto Sleep id 4837 adding frightened 1) are the same lane as this one and were ruled NOTHING-USEFUL on the same false premise. They are separate findings, not part of this fix, but future batches should treat \"injectText onto a SPELL record that changes how a spell the character can already cast behaves\" as a spellNotes row, and \"injectText restating the feat's own text without naming a spell\" as genuinely nothing-useful. That line, not the grant/no-grant line, is the one the data supports.\n\nFIX SPEC CHECK. category/id/field are exactly right: {category:'feats', id:'confounding-image', field:'spellNotes'}. `mirror-image` is a real spell id (rank 2, edition legacy-era, matching the feat's own legacy-era edition), so pushSpellNote will not discard it. No confounding-image row of any field exists in scripts/data/effect-backfill.json, so no collision. The value shape matches the 83 neighbours exactly. The one defect is that the fix leaves the note text as an unfilled placeholder, and it omits the second half of this repo's overlay pattern — effect-backfill.json is applied to the shipped public/core.json by scripts/apply-backfill-now.mjs, so the row does nothing until that runs. Hence CONFIRMED with correctedFix supplying our own wording in the neighbours' feat-name-prefixed style and the apply step.",
  "correctedFix": "Append to scripts/data/effect-backfill.json (and then apply to the shipped data with `node scripts/apply-backfill-now.mjs`, the same two-carrier pattern every other spellNotes row uses — the overlay alone does not reach public/core.json):\n\n{\n  \"category\": \"feats\",\n  \"id\": \"confounding-image\",\n  \"field\": \"spellNotes\",\n  \"value\": [\n    {\n      \"spellId\": \"mirror-image\",\n      \"note\": \"Confounding Image: you can cast this spell with its duration changed to sustained up to 1 minute and its range changed to 30 feet. Doing so replaces the spell's normal effects: you create a single illusory duplicate of yourself in an unoccupied square within range. The image occupies its space, has a 5-foot reach, and your allies can flank with it. It has no other creature statistics beyond an AC equal to your spell DC and saving throw modifiers equal to your spell DC - 10. Creatures can move through its space freely. The spell ends if the image is hit by an attack, if it fails a save, or if you are ever more than 30 feet from it. When you Sustain the Spell, you can move the image to another unoccupied square within range.\"\n    }\n  ]\n}\n\nWording notes: phrased in our own words rather than copied from WG or AoN — restructured opening (\"with its duration changed to\"), \"a single illusory duplicate\", \"occupies its space\", \"creature statistics\", \"moves through its space freely\", \"another unoccupied square\". The feat-name prefix and the \"you can cast this spell...\" opening match the closest neighbours (feats/overwhelming-harm, feats/cruelty), not the granted-spell neighbours' \"When you cast X from this feat\" phrasing, which would be wrong here because the feat grants nothing. Uses \"spell DC - 10\" with a hyphen to match the existing core-descriptions text rather than the en dash WG ships. No `fromChoice`/`whenChoice`/`fromCantripPick`/`fromAncestrySpells` flags — this is the plain spellId-keyed form."
 },
 {
  "id": "stoney-deflection",
  "verdict": "CONFIRMED",
  "reason": "Re-derived and survives. (1) Raw WG = one op, adjValue STONE_BRAWLER_FEAT_COUNT=1 — an internal tally for Stoney Skin's resistance, no parity signal; WG is silent on the bonus, so the fix picks no side in a WG-vs-print disagreement (not ASK-OWNER). (2) Print (feat-4373, and core-descriptions feats[\"stoney-deflection\"]) grants \"+2 circumstance bonus to AC and to any defenses against being Shoved or Tripped\"; the AoN markdown links Shoved→Actions.aspx?ID=38 and Tripped→ID=40, so the clause names the two ACTIONS, which rules out the broader \"rolled saves too\" reading that Mountain Stance earns only via its extra \"and other forced movement effects\" wording. (3) Ours: grep of src/ for the id gives exactly one hit, situationalBonuses.ts:679, AC only — no featGrants*/modes.ts CATALOG_MODES/toggle-modes.json/consumable-modes.json/counterMods.ts/effect-backfill.json carrier delivers the Shove/Trip half. (4) Targets verified from the repo's own data: actions.shove = \"Athletics check against your target's Fortitude DC\", actions.trip = \"against the target's Reflex DC\"; Inexorable's printed text says it outright (\"+2 circumstance bonus to your Fortitude or Reflex DC against attempts to Shove or Trip you\"), and its entry at :2743 is exactly the cited {kind:'save',detail,dcOnly:true} pair — shape confirmed, not invented. dcOnly is an EXISTING field (declared :69, honoured by the lookup at :3498-3500), so this is not a new engine field. (5) Decisive: work/situational-lane/spec.json for this very record carries engineNote \"The AC half maps cleanly. The 'and to any defenses against being Shoved or Tripped' half is a defensive DC ... with no legal target kind — same gap as Tunnel Wall / Inexorable.\" Ruling D later added dcOnly and BOTH named siblings were authored (inexorable :2743, tunnel-wall :2834); this record is the one left behind. (6) Survival: apply-situational-lane.mjs computes existingIds from the CURRENT file and skips already-authored ids, appending only before the closing brace, so src.slice(0,close) preserves the row verbatim — additions survive; CLAUDE.md:158 warns only that hand DELETIONS get re-added. The find is unique (one occurrence of \"stoney-deflection\"), the new entry matches neighbours (commanders-banner, stalwarts-banner-* all carry multiple entries in one array), and appending touches no sibling. test/situational-bonuses.test.ts guards (slug key, id resolves to a record, non-empty targets/when/bonus) and coverage.test.ts (record-keyed, not bonus-keyed) all still pass. Only defect: the spec'd `when` is a bare fragment, but each entry's `when` renders standalone in the stat popup and must carry its own duration/trigger — corrected below, kept at 109 chars so cap-situational-notes.mjs (trims at >120) leaves it alone.",
  "correctedFix": "In C:/trying ai 2/pf2e codex/src/rules/situationalBonuses.ts, replace the single line 679 (unique in file) with the two-entry form — same id, existing AC entry untouched, second entry appended:\n\n  \"stoney-deflection\": [{ targets: [{ kind: 'ac' }], when: \"until the beginning of your next turn, after you use Stoney Deflection while in Stonestrike Stance\", bonus: \"+2 circumstance\" }, { targets: [{ kind: 'save', detail: 'fortitude', dcOnly: true }, { kind: 'save', detail: 'reflex', dcOnly: true }], when: \"until the beginning of your next turn, after you use Stoney Deflection, against attempts to Shove or Trip you\", bonus: \"+2 circumstance\" }],\n\nChanges from the finding's spec: the `when` is a complete standalone note (each SituationalBonus renders on its own, so a leading-ellipsis fragment would print without its duration or trigger) and is 109 chars, under the 120-char threshold at which scripts/cap-situational-notes.mjs would re-trim it. Shape otherwise exactly as cited: one entry, two dcOnly save targets (Shove→Fortitude DC, Trip→Reflex DC), mirroring \"inexorable\" at :2743. No new field, no overlay row, no script exclusion-list change needed — apply-situational-lane.mjs skips this id as alreadyAuthored, so the edit is durable."
 },
 {
  "id": "advanced-death-dealing",
  "verdict": "CONFIRMED",
  "reason": "The defect is real and both authorities agree, so it is not an owner call. AoN feat-9298 prints \"Special You can select this feat more than once. Each time you select it, you gain another necromancer feat\", and WG's own ability_block row 58089 carries meta_data {\"can_select_multiple_times\":true} plus the same special text — WG DOES encode repeatability, it just lives on the record rather than in the ops (their raw ops are exactly one select, level max 10, trait 5135 = Necromancer, which matches our traits:['necromancer']; 5143 on the record is \"Necromancer Archetype\"). Our public/core.json feats/advanced-death-dealing has no maxTakable, and maxTakes() (src/rules/featGrants.ts:665-668) returns 1 when absent, so the second taking can never be bought. The mechanic is otherwise fully wired: build.ts:4143 counts prior takes against maxTakes before filling a slot, and 4198-4222 reads the answer under the SLOT key with grantedBySlot, so a second taking gets its own pick — nothing else is needed but the flag. 30 of 59 advanced-* siblings already ship maxTakable:null. BUT THE SPECCED FIX IS A NO-OP. scripts/lib/apply-backfill.mjs:63 reads `if (fix.value === null) delete target[fix.field]; else target[fix.field] = fix.value;` — value:null REMOVES the field, it never sets a literal null. Since advanced-death-dealing has no maxTakable to begin with, the proposed overlay row would delete nothing and change nothing. This trap has already been sprung once in-repo: scripts/data/effect-backfill.json already contains {\"category\":\"feats\",\"id\":\"widespread-fascination\",\"field\":\"maxTakable\",\"value\":null} (the file's ONLY maxTakable row), and widespread-fascination — same book, same repeatable Special — still has no maxTakable in core.json, i.e. that row has been silently inert. There is no sentinel in the applier for a literal null. The lane that CAN set one is scripts/data/fixes.json, applied at scripts/import-core-v2.mjs:673-679 with an unconditional `entry[fix.field] = fix.value`, keyed by id (3 existing rows already use `id`), positioned deliberately after the AoN facets and BEFORE applyBackfill(db) at line 717 — and no effect-backfill row targets this id, so there is no collision and nothing downstream (import-archive-buckets, fix-aonid-collisions, backfill-ast-edition) rewrites feats records. No sibling regresses: the field is read only through maxTakes()/the equivalent takes() helpers in scripts/repeatable-pick-check.mjs and scan-repeatable-grant-collisions.mjs, all of which already treat null as unlimited. Note for the owner, out of scope for this row: the same gap hits 7 other repeatable-prose feats with no maxTakable — advanced-rune-magic, widespread-fascination, divine-access, kingdom-assurance, aon-kingdom-assurance, skill-training-kingdom, skill-mastery-rogue — because import-core-v2.mjs (the live AoN importer) never got the proseMaxTakes() pass that import-core.mjs:448-465 has for the Foundry path; 0 of 240 Impossible Magic feats carry maxTakable at all.",
  "correctedFix": "Do NOT add the row to scripts/data/effect-backfill.json — value:null deletes there. Instead add one row to scripts/data/fixes.json (an array of {category,id|name,field,value} applied unconditionally at scripts/import-core-v2.mjs:673-679, before the effect-backfill applier):\n\n{\"category\":\"feats\",\"id\":\"advanced-death-dealing\",\"field\":\"maxTakable\",\"value\":null}\n\nThat yields maxTakable:null on the record, which maxTakes() (src/rules/featGrants.ts:665) reads as Infinity, matching the printed Special and WG's meta_data.can_select_multiple_times. No other change is needed — FEAT_PICK_GRANTS['advanced-death-dealing'] in src/rules/featPickGrants.ts:248 already matches WG (half-level is the printed cap; their static 10 is the same clause), and the per-taking pick lane (slotKey / '<slotKey>#<i>' / grantedBySlot in src/rules/build.ts:4143 and 4198-4222) already gives take 2 its own answer. While in fixes.json, consider the identical row for advanced-rune-magic and widespread-fascination (both same book, both print the same Special, both currently once-only), and DELETE the now-provably-inert effect-backfill row {\"category\":\"feats\",\"id\":\"widespread-fascination\",\"field\":\"maxTakable\",\"value\":null} so the mistaken convention is not copied again."
 },
 {
  "id": "basic-summoner-spellcasting",
  "verdict": "ASK-OWNER",
  "reason": "The contradiction is real and I re-derived both sides. THEIRS (wg-show --raw, id 22481, level 4): giveSpellSlot castingSource=SUMMONER, rank0 amt2 lvl1-20, rank1 amt1 lvl4-20, rank2 amt1 lvl6-20, rank3 amt1 lvl8-20 — three leveled slots from level 8, additive, never replaced. PRINT (rules-1485.json, SoM p.74, verbatim): \"Basic Bounded Spellcasting Feat: Usually gained at 6th level, these feats give you a 1st-level spell slot and a 2nd-level spell slot from that magical tradition… At 10th level, you replace your 1st-level spell slot with a 3rd-level spell slot\" — always TWO leveled slots, with replacement; and feat-2957.json is right=\"Feat 6\" whose whole body is \"You gain the basic bounded spellcasting benefits.\" OURS matches WG on the ladder: no per-feat carrier exists (public/core.json feats.basic-summoner-spellcasting has no spellSlotBonus; grep of src/ for the id returns nothing), the mechanic is generic — src/rules/casterArchetypes.ts:188 'summoner-dedication': mk('arcane','cha',2,'summoner'), mk() at :353 mints basicId 'basic-summoner-spellcasting', and archetypeSlots at :438 walks RANK_UNLOCKS (:406-415) [1,4,'basic'],[2,6,'basic'],[3,8,'basic'] doing out[rank]=1 with no removal path. So a summoner archetype at L10 gets ranks 1+2+3 from us and from WG, and ranks 2+3 from the book. No existing ruling settles it: work/rulings/DECISIONS.md and DECISIONS-round2.md contain no occurrence of \"slot\", \"bounded\", \"summoner\" or \"magus\"; work/owner-questions.json open[55] has no bounded/summoner/magus entry (the adjacent advanced-red-mantis-magic entry is a different archetype and a legacy per-feat slot table, and does not decide the bounded ladder); authorisedExceptions' \"the BOOK wins\" ruling of 2026-08-22 is scoped to six named records, none of them these. Per the file's own header (\"nothing in this file is authorisation to diverge\") and per the WG-vs-print reservation, this is the owner's call, not CONFIRMED. Fix spec needs correction on three counts (see correctedFix): the \"we currently match them\" claim is only half true, the sibling count is understated, and \"adopt-print\" is not a data-only option.",
  "correctedFix": "Queue in work/owner-questions.json \"open\" with the standard {id, batch, printed, theirs, ours, question} shape, id \"basic-summoner-spellcasting\", corrected on these points:\n\n1. OURS is NOT a straight match to theirs. We already hold PRINT on availability: public/core.json feats.basic-summoner-spellcasting is level 6 (aonId feat-2957), theirs is level 4. Only the SLOT LADDER matches. State \"ours\" as: no per-feat carrier at all (no spellSlotBonus in core.json, no hit in src/ for the id) — slots come from src/rules/casterArchetypes.ts:188 'summoner-dedication' -> mk() (:353) -> archetypeSlots (:438) -> RANK_UNLOCKS (:406-415) [1,4,'basic'],[2,6,'basic'],[3,8,'basic'], one slot each, never removed. Because our feat is level 6, the observable divergence from print starts at level 8 (we add a 3rd-rank slot the book does not give) and persists from 10 (we never replace the 1st-rank slot): L10+ we and WG give 3 leveled slots, the book gives 2.\n\n2. SIBLING COUNT: exactly ONE sibling basic feat, not \"magus?\" — grep of the mirror for feats citing Rules.aspx?ID=1485 returns exactly six, all magus/summoner: feat-2950/2951/2952 (Basic/Expert/Master Magus Spellcasting) and feat-2957/2959/2961 (Basic/Expert/Master Summoner Spellcasting). basic-magus-spellcasting (their id 22478, their level 4, print Feat 6) carries the byte-identical non-null ladder rank1@4-20, rank2@6-20, rank3@8-20 amt 1, and ours routes it through the same RANK_UNLOCKS via 'magus-dedication': mk('arcane','int',2,'magus') at casterArchetypes.ts:96. The defect also spans the EXPERT and MASTER tiers of both archetypes: their expert-summoner/magus-spellcasting give rank4@12, rank5@14, rank6@16 additively and master gives rank7@18, rank8@20, against print's \"replace your spell slots with two 4th-level and one 5th-level at 14, two 5th and one 6th at 16… replace your two 5th-level with two 7th-level at 20\" — book caps a bounded caster at four slots (two 7th + two 6th) at L20 where our ladder yields eight (ranks 1-8). So the ruling governs 2 archetypes / 6 feats, and the question should say so rather than implying 2 feats.\n\n3. \"adopt-print\" IS NOT A DATA-ONLY OPTION — say this in the question so the owner prices it. The existing schedule field, customUnlocks ({rank, level, featId}), cannot express either half of the bounded rule: archetypeSlots only ever does out[u.rank] = 1 and has no removal path (so no \"replace your 1st-level slot\"), and customUnlocks has no amount field (so no \"two 4th-level spell slots\"). Adopting print therefore requires an engine change to casterArchetypes.ts (an amt on the unlock plus a replaces/until-level), not a CASTER_ARCHETYPES row. Adopt-theirs is a genuine zero-diff status quo for the ladder — but note it would additionally mean dropping our level 6 back to their level 4, which contradicts feat-2957's printed \"Feat 6\", so offer that as a separately answerable third sub-question rather than folding it into \"status quo\".\n\n4. Also flag, one line, so the \"ours\" field is not misread: their basic feat carries adjValue SPELL_ATTACK=T / SPELL_DC=T, matching feat-2957's \"You become trained in spell attack rolls and spell DCs of that tradition\"; ours grants trained at the DEDICATION instead (archetypeProficiency, casterArchetypes.ts:443). That is a pre-existing separate divergence already recorded at scripts/audit/covered-batch1.json:2151 — do not re-open it here, just do not claim we match their T placement.\n\nNo overlay row and no effect-backfill.json entry is involved either way (I checked: the only effect-backfill hits for these ids are a grantsFeats list at :49031 and an aon-* edition row at :120323 — no collision). Separately out of scope but worth a spawned task: the comment at casterArchetypes.ts:187 says \"caps at Expert (no master-summoner-spellcasting feat exists)\" and test/caster-archetypes.test.ts:44 special-cases summoner on that basis, yet master-summoner-spellcasting exists in core.json (level 18, aonId feat-2961) and in WG (id 22555)."
 }
]