BATCH 037 — WG-COMPARISON LANE — family data-rows-3 (chunk 3 of 6 of "data-rows")
Agent: data-rows builder. Files written: work/.b037-rows-data-rows-3.json,
work/.b037-created-desc-data-rows-3.json, test/batch037-data-rows-3.test.ts.
No src/ or scripts/ file was edited; scripts/data/effect-backfill.json was NOT written.

DATA STILL NEEDED
- feats/lesser-augmentation, field effectChoices, whole value re-emitted with supersedes:true (an effect-backfill row of this key already exists, valLen 3097): the 'hardened-chassis' option's note changes from "Prose benefit - read this feat's Enhancement entry and apply it in play; the app does not compute it." to "Computed on your sheet: choose resistance 3 to all physical damage, or raise the damage type you chose on Hardened Chassis itself to half your level (minimum 3)." — a path row cannot reach it, because apply-backfill-paths.mjs resolves an array step only by `id=` and an effectChoices OPTION is keyed by `value`.
- feats/greater-augmentation, field effectChoices, same change, same supersedes:true (existing row valLen 5146).
- items/armor-in-earth-medium, create row re-emitted with supersedes:true, dropping only the sentence "It uses your HIGHEST armor proficiency — set that on the sheet if your best track is not medium." from `note` — to be authored in the SAME batch as the src armorFamiliarity entry below, never before it.
- items/armor-in-earth-heavy, create row re-emitted with supersedes:true, same note cleanup (its note carries no "set it yourself" sentence today, so this row is only needed if the medium one's wording is mirrored onto it).
- items/hardwood-armor-armor, create row re-emitted with supersedes:true, dropping "It is medium armor but uses your HIGHEST armor proficiency — set that on the sheet if your best track is not medium." from `note`, same ordering rule.
- FALLBACK ONLY: if the driver refuses my path row {category:'feats', id:'haunting-memories', path:['choice'], field:'note'} because a pathless whole-value row for feats/haunting-memories `choice` already exists (valLen 6830), re-emit that row whole with supersedes:true and change nothing but `note`, to the value my spec carries.

CROSS-FILE GAPS
- src/rules/types.ts (SkillRankGate, line 2817) — molten-wit#three-branches needs a MULTI-skill option gate (e.g. `requiresSkillRanks?: SkillRankGate[]`, honoured by qualifiesForOption at derive.ts:1771); both printed branch conditions name two skills at once and the single-skill gate cannot express either.
- src/rules/featFeatGrants.ts (CHOICE_FEAT_GRANTS, line 57) + src/rules/featGrantsLane.ts (line 46) — molten-wit#three-branches: the per-answer FEAT grant must stay here, because a FEAT's choice.options[].grant.grantsFeats is not read anywhere (build.ts:5406/5465 collect that lane for heritage and classFeature effectChoices only; the comment at build.ts:5400 states it).
- src/rules/featGrantsAuto.ts:474 — intuitive-crafting#middle-case: `redundantFallback: true` must become conditional on the character already owning specialty-crafting, so the middle case (trained in Crafting, no Specialty Crafting) grants the feat ONLY and the free skill stays on the both-already-held case. Needs a gate field on FeatGrant (src/rules/featGrants.ts:267) plus its reader at build.ts:5829.
- src/rules/featGrantsAuto.ts — armor-in-earth#armour-proficiency: add `'armor-in-earth': { armorFamiliarity: { armors: ['armor-in-earth-medium','armor-in-earth-heavy'], mirrorBest: true } }` and NO `armor: {...}` categories, so the kineticist borrows their best light/medium/heavy rank for these two items only and gains no medium/heavy training (which would let them wear real plate — WG's shortcut, refused by the owner).
- src/rules/featGrantsAuto.ts — hardwood-armor-armor#armour-proficiency: add `'hardwood-armor': { armorFamiliarity: { armors: ['hardwood-armor-armor'], mirrorBest: true } }`. ⚠ The finding says "(and its heavy version)" — there is none. feat-4283 prints one armour ("This hardwood armor is medium armor but uses your highest armor proficiency") and a shield; our data has exactly hardwood-armor-armor and hardwood-armor-shield. Only the armour takes the borrowing.
- src/rules/types.ts (grantsItems, line 1073) + src/rules/build.ts:7802 — armigers-protection#armor-pick: the item a record grants needs a per-answer gate (the `whenChoice` idiom InnateSpellGrant already uses at types.ts:2430) so the SUIT THE PLAYER PICKED lands in inventory. Today grantsItems is unconditional and EffectGrant has no grantsItems arm, so the feat still hands over a breastplate whichever of the three the player chooses. My row fixes the picker; this fixes the delivery.
- test/automaton-enhancement.test.ts — hardened-chassis#enhancement flips three ratchet values in a file I may not write: line ~262 `a.computed.sort()` gains 'feats/hardened-chassis' (6 → 7), line ~264 `a.proseOnly` 23 → 22, and line ~231 the "Computed on your sheet" option count on lesser-augmentation 6 → 7 (that last one only once the two augmentation-note rows above land). Each changed expect( needs `// batch 037: hardened-chassis#enhancement` in the 6 lines above it, and the enclosing describe/it title already carries no record id — the title must gain "hardened-chassis" or the flip audit refuses it.
- src/rules/build.ts:4475 + src/builder/Builder.tsx:367,387 — flexible-spellcaster#collection-shape: the whole collection shape is those two code sites (per-rank slots and a per-rank repertoire); archetype/flexible-spellcaster carries no field at all. A flat pool sized by the character's TOTAL daily class slots, with a one-1st-rank-spell floor, is engine work. No backfill row can reach it.

PER FINDING
1. unifying-emblem-shundar-quah#daily-spell — ROW items/unifying-emblem-shundar-quah field innateSpells = [{spellId:'thoughtful-gift', rank:1, usesPerDay:1}]. Reuses the existing item innate-spell lane (Item.innateSpells, read for every invested item at build.ts:8075), the shape the sibling tattoo enveloping-light already uses for the same "Activate; Frequency once per day; Effect ... casts <spell>" paragraph — deliberately NOT heldSpells, which types.ts:4074 reserves for the staff/wand charge system. Pinned by test/batch037-data-rows-3.test.ts "unifying-emblem-shundar-quah casts Thoughtful Gift once a day" (built fighter, emblem invested → the spell is on the sheet, usesPerDay 1).
2. unifying-emblem-sklar-quah#daily-spell — TWO ROWS on items/unifying-emblem-sklar-quah: innateSpells = [{spellId:'concordant-choir', rank:1, usesPerDay:1}] and activationCost = {type:'variable', min:1, max:3}. Print takes the activation from the spell and Concordant Choir casts in 1–3 actions; the record had no activation cost at all. Pinned by "unifying-emblem-sklar-quah casts Concordant Choir, and its activation costs what the spell costs".
3. unifying-emblem-tamiir-quah#daily-spell — ROW items/unifying-emblem-tamiir-quah field innateSpells = [{spellId:'tailwind', rank:1, usesPerDay:1}]. The 2-action activation the record already showed is Tailwind's own Cast, so nothing else moves. Pinned by "unifying-emblem-tamiir-quah casts Tailwind behind the two-action activation it already showed", plus a control ("an emblem left in the pack casts nothing") that passes today and must keep passing.
4. molten-wit#three-branches — NO ROW, code-only fix; see CROSS-FILE GAPS. I did not author a partial gating, because gating the two shipped options on their own skill being untrained EMPTIES the picker for a character trained in both — the failure narrowChoiceOptions' own doc calls worse than an over-wide list — and the feat half cannot be data at all. Nothing pinned; a test asserting today's behaviour would have to be deleted the moment the gap is built.
5. intuitive-crafting#middle-case — NO ROW, code-only fix; see CROSS-FILE GAPS. The wrong free skill is src/rules/featGrantsAuto.ts:474's `redundantFallback: true`, whose only reader is build.ts:5829; the record carries no choice and no skill lane, so a backfill row could only ADD a second carrier and double the grant.
6. armor-in-earth#armour-proficiency — NO ROW, code-only fix; see CROSS-FILE GAPS and DATA STILL NEEDED. The borrowing lane the proposal names (FeatGrant.armorFamiliarity, featGrants.ts:310) is the only writer of Proficiencies.armorOverrides (build.ts:5806/7082) and it reads a src registry; no item or record field feeds it. I deliberately did NOT drop the "set it yourself" note yet: removing it before the lane lands leaves the player with neither the number nor the instruction.
7. hardwood-armor-armor#armour-proficiency — NO ROW, same reason and same ordering rule. ⚠ the finding's "(and its heavy version)" names an item that does not exist — feat-4283 prints one armour and a shield.
8. hardened-chassis#enhancement — TWO ROWS on feats/hardened-chassis: `effectChoices` (one choice, id 'hardened-chassis-enhancement', four options — resistance 3 to all physical, and three flag-gated "raise your chosen resistance to max(3,floor(@actor.level/2))" branches reading the record's own hardenedChassisDamage answer through onlyWhenFlag) and `enhancement` = {choiceIds:['hardened-chassis-enhancement']}. Built on the EXISTING automaton enhancement lane (types.ts:3359 EnhancementDef; the gate at build.ts:6334-6345), which is exactly "switched on ONLY when an augmentation feat that names Hardened Chassis is held" — never WG's ask-every-automaton shape. Four options on purpose: build.ts:6347 warns a one-option gated picker is auto-applied by resolvePick even unanswered. Pinned by four tests: the chosen resistance reaching 4 on a 9th-level augmented automaton, the all-physical branch, the un-augmented control (passes today, must keep passing), and the option/flag shape.
9. armigers-protection#armor-pick — ROW feats/armigers-protection field `choice`: the three REAL Hellknight suits (breastplate / half plate / plate — all three item ids exist; the shipped 'hellknight-plates' does not), each with a description saying when it is a type you become trained in, plus a FeatChoiceDef.note carrying the printed light+medium condition because no per-option gate can express an ARMOUR proficiency (SkillRankGate names a skill). The flag stays 'hellknightDedication' — it is the only occurrence in core.json, so nothing is stranded. Half of the owner's option B (landing the chosen suit in inventory) is in CROSS-FILE GAPS. Pinned by "armigers-protection lists all three printed suits and every value is a real item".
10. flexible-spellcaster#collection-shape — NO ROW, engine work; see CROSS-FILE GAPS. The finding's own proposal says "new engine work on the spell-collection lane", and the record carries nothing but id/name/edition/aonId.
11. haunting-memories#skill-feat-pick — THREE ROWS: CREATE feats/haunting-memories-skill-feat (category 'bonus' — the established grant-only shape; featSlots.ts:140 keeps 'bonus' out of every picker) carrying the second daily question as choice {kind:'open', daily:true, from:{type:'feat', featCategory:'skill', grantFeat:true, excludeOwn:true, maxLevel:4, maxLevelByLevel: 10→5 … 20→10}}; feats/haunting-memories field grantsFeats = ['haunting-memories-skill-feat'] (build.ts:5410 puts the carrier in c.feats, which is the set ownedDailyChoiceRecords walks); and a path row clearing the old stand-in note on feats/haunting-memories choice. The question itself is the wizard lane the proposal names (types.ts:2202 grantFeat, Experimental Spellshaping), whose answer dailyChoiceGrants turns into a real day-long grantsFeats at derive.ts:1820. The cap is HALF YOUR LEVEL via maxLevelByLevel, not WG's flat 10th. The printed "minimum requirement of your new rank in the chosen skill" is prerequisite TEXT nothing can filter on, so it is the choice's note, which dailyChoicesFor already renders. Prose for the created record is in work/.b037-created-desc-data-rows-3.json (a create row never carries a description). Pinned by three tests: the carrier arriving and both morning questions appearing, the half-level cap at 8th and at 20th plus skill-feats-only, and the old note going away.

VERIFICATION RUN
- node scripts/vt.mjs run test/batch037-data-rows-3.test.ts → 10 failed / 2 passed, every failure the authored field not existing yet (empty innate list, undefined enhancements, the old two-option armiger picker, the missing carrier feat, the old note). The 2 that pass are the controls that must stay green after the rows land.
- npx tsc --noEmit → clean.
- node scripts/test-flip-audit.mjs --batch 037 → clean, every flip cited.
