########## orc-warmask [b3 MATCHES] verify=CONFIRMED
---- FIX [src-edit] reader: scripts/wg-values.mjs ourAssertions (:481) → compare loop :1243-1252; verified by `node scripts/wg-values.mjs --ids orc-warmask --verbose`
PREFERRED (fixes the measurement, not one row). scripts/wg-values.mjs ourAssertions (:481): beside the existing effectChoices block at :735-741, add the plain-`choice` twin — `for (const o of rec.choice?.options ?? []) for (const [k,v] of Object.entries(o?.grant?.skills ?? {})) put('skill|'+k, v);` (and the `grant.save` twin for symmetry). wg-diff.mjs already treats the two containers identically ("a container should not decide whether a grant counts", wg-diff.mjs:683-688) and ourSets in wg-values does too (:343) — ourAssertions is the one harvest that was never given the second home. Effect: orc-warmask reports 4/4 agree and 7 further records stop being at risk. NOTE the situational wildcard is still correct behaviour and should stay: it is what lets the +1 item ceremony bonus match a per-skill op.
---- FIX [settle] reader: SETTLED_VALUES read at scripts/wg-values.mjs:1244; carrier reader is src/rules/build.ts:5084-5088 → applyAlwaysOn build.ts:4983-4985
NARROW ALTERNATIVE if the harvest is not to be touched: SETTLED_VALUES in scripts/wg-values.mjs (:771) gets 'orc-warmask': ['skill|religion','skill|nature','skill|arcana','skill|occultism'], with the comment that the carrier is the record's own `choice` (flag 'warmaskTradition', kind 'array'), whose four options hold grant.skills 'trained' — the same four ranks their four `adjValue …:'T'` ops assert — and that the ours=1 shown is the situationalBonuses.ts:2765 ceremony bonus reaching the row through the skill wildcard (wg-values.mjs:502, :1248), not a rank. ⚠ This settles ONE record and leaves the other 7 choice-carried skill grants to re-open the same way, so it is second best.
---- CORRECTED FIX:
PREFERRED (src-edit, scripts/wg-values.mjs). ⚠ Line numbers in the finding are stale by +15; anchor by content, not number. In ourAssertions (now line 496), the effectChoices harvest is now at lines 750-758 and reads:

  for (const ch of Array.isArray(rec.effectChoices) ? rec.effectChoices : []) {
    for (const o of ch?.options ?? []) {
      for (const [k, v] of Object.entries(o?.grant?.speeds ?? {})) put(`speed|${k}`, typeof v === 'string' ? v : Number(v));
      for (const [k, v] of Object.entries(o?.grant?.skills ?? {})) put(`skill|${k}`, v);
      for (const [k, v] of Object.entries(o?.grant?.save ?? {})) put(`save|${k}`, v);
    }
  }

Change ONLY the loop header, giving the plain `choice` its home in the same walk — the identical idiom ourSets already uses at line 358 and wg-diff.mjs uses for kinds ("a container should not decide whether a grant counts"), but keeping this line's Array.isArray guard, which the :358 form omits:

  for (const ch of [{ options: rec.choice?.options ?? [] }, ...(Array.isArray(rec.effectChoices) ? rec.effectChoices : [])]) {

Body unchanged. Extend the existing comment with: "…and a plain `choice`'s options carry `grant` in the same shape. Orc Warmask's four warmask sources moved onto it (the picked source sets the trained skill AND names the tradition Mask of Power casts at), and reading only `effectChoices` reported four trained skills the character really gets as `ours=1` — the `skill` wildcard from the record's own +1-item ceremony bonus, which is the only thing left to answer with. Same rule as ourSets above and wg-diff's kinds walk."

Why this over the finding's standalone loop: same effect on orc-warmask (candidates for skill|religion becomes ['trained', 1]; String('trained') === String('trained') matches, all four rows clear, record goes clean and wg-batch-gate's VALUES check passes), but it also closes the identical hole for grant.speeds in one edit and adds no duplicated body. Verified no regression risk: the compare loop pushes a row only when candidates is empty or no candidate stringifies equal, so extra assertions can only remove rows.

DO NOT also narrow situationalBonuses.ts:2765 — the detail:'all' wildcard is what lets the +1 item ceremony bonus match a per-skill op, and removing it would turn the four rows from DIFFERENT into MISSING rather than fixing them.

The narrow SETTLED_VALUES alternative stays valid as a fallback (note its anchor is now line 786, not 771), but it settles one record and leaves the other 7 choice-carried skill grants — ageless-spirit, ancient-memories, endless-memories, ancestral-longevity, expert-longevity, haunting-memories and classFeatures/avenger, which I confirmed are the complete set in core.json — to re-open the same way the moment their side starts asserting a rank for them.

########## creative-prodigy [b4 MATCHES] verify=CONFIRMED
---- FIX [settle] reader: SETTLED_VALUES read at scripts/wg-values.mjs:1244; carriers' readers are src/rules/build.ts FEAT_GRANTS skill application (build.ts:4590-4605) for the rank, featFeatGrantsFor (build.ts:4407) for the bonus feat, and src/rules/explain.ts entriesFor for the situational
SETTLED_VALUES in scripts/wg-values.mjs (:771) gets 'creative-prodigy': ['skill|deception'], with the comment: their SKILL_DECEPTION comes from ability_block 23367, the GANZI Creative Prodigy (trait 3035, Ancestry Guide backport, no prerequisite), which wgRowsByBucket picks over the nephilim twin 38501 because its operations string is 10 chars longer (wg-parse.mjs:260-262). Our record is feat-7199 (nephilim, prereq Proteankin) and matches 38501 op-for-op: performance trained (featGrantsAuto.ts:233 + the record's own `skills` overlay row), Impressive Performance (featFeatGrants.ts:625), +1 circumstance to Make an Impression (situationalBonuses.ts:249). We do not carry the Ganzi feat, so there is nothing of ours for their row to be missing from.
---- FIX [src-edit] reader: scripts/lib/wg-parse.mjs wgRowsByBucket (:245-267), consumed by wg-values.mjs theirRowFor (:1152-1155) and by wg-diff.mjs
OPTIONAL HARDENING (the twin problem is structural, not per-record). scripts/lib/wg-parse.mjs wgRowsByBucket (:245-267) currently disambiguates same-name rows by operations LENGTH alone. Add a tie-break that prefers the row whose `prerequisites` or trait set is consistent with our record before falling back to length — e.g. when two rows share a normalised name, prefer the one whose prerequisites string is non-empty and matches our record's prerequisites, else the one whose content_source era matches the record's `edition`. Ship it with a printed warning listing every name that resolved to more than one row, so the next twin is visible rather than silent. ⚠ This changes pairing for every bucket, so it should be run as a full wg-values/wg-diff before-and-after and reviewed as its own change, not folded into this family.
---- CORRECTED FIX:
FIX 1 (apply now) — add to SETTLED_VALUES in scripts/wg-values.mjs, which is DECLARED at :786 and READ at :1270 / :1284 (the finding's ":771" and ":1244" are stale by ~32 lines):

  'creative-prodigy': ['skill|deception'],

with this comment above it:
  /*
   * CREATIVE PRODIGY — a NAME TWIN, not a gap. Their SKILL_DECEPTION comes from ability_block 23367,
   * the GANZI Creative Prodigy (trait 3035, content_source 18 = Ancestry Guide backport, no
   * prerequisite; AoN feat-2532, Ancestry Guide pg. 96), which grants Deception + Performance +
   * Art Lore. OUR record is the NEPHILIM one: AoN feat-7199, War of Immortals pg. 53, prereq
   * Proteankin = their ability_block 38501, and we match it op for op —
   *   adjValue SKILL_PERFORMANCE T   -> featGrantsAuto.ts:233 + the record's own `skills` overlay
   *   giveAbilityBlock feat 20241    -> featFeatGrants.ts:625 ['impressive-performance']
   *   addBonusToValue +1 circ.       -> situationalBonuses.ts:249 (Make an Impression)
   * wgRowsByBucket (lib/wg-parse.mjs:245-267) keeps ONE row per normalised name and breaks ties on
   * operations LENGTH alone (:259-262); the Ganzi row's encoding is 561 chars to the nephilim row's
   * 551, so the wrong twin wins by 10 characters. Neither WG row disagrees with its own printed
   * source, so there is nothing here to adjudicate against the book.
   * We do not carry feat-2532 at all — note we DO ship the ganzi versatile heritage
   * (heritages['aon-ganzi']) and two ganzi feats, so importing feat-2532 as its own record is a fair
   * COVERAGE question for the ganzi lane; it must not be answered by grafting Deception onto
   * feat-7199, which is the exact defect batch 4 removed (work/wg-batch-004-parity.json).
   * ⚠ If the wgRowsByBucket tie-break is ever hardened to pair on prerequisites/traits, this entry
   * goes stale by design — scripts/wg-settle-stale.mjs will flag it and it should then be DELETED.
   */

Verification of the key: `node scripts/wg-values.mjs --ids creative-prodigy --verbose` reports exactly one row, `MISSING skill|deception theirs=trained ours=(nothing)`, alongside `ours(all): skill|performance=trained,trained,1`. So 'skill|deception' is complete and minimal; no wider settle is warranted. No pre-existing 'creative-prodigy' key in SETTLED_VALUES, so no collision.

FIX 2 (do NOT fold into this family) — the wgRowsByBucket length-only tie-break at scripts/lib/wg-parse.mjs:259-262 is a genuine structural defect, but changing it repaints pairing for every bucket and would invalidate this settle. Spin it out as its own change: prefer the same-name row whose prerequisites/traits are consistent with our record before falling back to operations length, print a warning naming every name that resolved to more than one row, and gate it on a full wg-values/wg-diff before-and-after. When it lands, re-run scripts/wg-settle-stale.mjs and delete the creative-prodigy entry it flags.

FIX 3 (optional cleanup, same root cause) — scripts/aon-verify/worklist.json's creative-prodigy entry carries the GANZI prose as its `aonProse`, and scripts/aon-verify/progress.jsonl:82 plus RESUME.md:35 record the resulting bogus "missing Deception" conclusion. These are stale twin-contaminated inputs that will re-derive the same wrong answer if that pass is ever re-run; worth correcting to the feat-7199 text or noting as superseded.

########## oatia-skysage-dedication [b7 MATCHES] verify=CONFIRMED
---- FIX [src-edit] reader: scripts/wg-diff.mjs fieldToKinds (:361) built from OUR_KINDS (:240-330), consumed by ourKinds at :667-670 and :683-688; verified with `node scripts/wg-diff.mjs --list --bucket they-only`
PREFERRED. scripts/wg-diff.mjs OUR_KINDS.skill (:254): add the bare key 'skills' to the list, so fieldToKinds.get('skills') → ['skill'] and the two container walks that already exist (effectChoices option grants at :667-670, plain choice option grants at :683-688) credit the kind they were written to credit. Safety checked: 'skills' as a field name means a skill-rank map in every place ourKinds looks — record top level (1 record, creative-prodigy), option grants, whileActive entries, enhancement.grant — so nothing is mis-credited; `choice: {kind:'skills'}` is a VALUE and is handled separately at :702. Effect: oatia drops off the they-only list and the other 51 grant.skills records are protected. Re-run `node scripts/wg-diff.mjs --list --bucket they-only | grep 'missing=\[skill\]'` — it should go 11 → 10 with no new WE-ONLY drift.
---- FIX [settle] reader: VERIFIED_EQUIVALENT read at scripts/wg-diff.mjs:1851-1852; carrier reader is src/rules/build.ts:5030 resolvePick → applyAlwaysOn build.ts:4983-4985
NARROW ALTERNATIVE if OUR_KINDS is not to be touched: extend the existing VERIFIED_EQUIVALENT entry in scripts/wg-diff.mjs (registry at :884, entry at :1482) from 'oatia-skysage-dedication': ['spellcasting'] to ['spellcasting','skill'], with the comment that the skill half is carried by the record's own effectChoices 'skysage-training' — trained lore:astronomy on one branch, EXPERT occultism on the other, which is their two-option select exactly — after featGrantsLane.ts's slot was removed (see featGrantsLane.ts:54-70) because it could only offer Occultism at trained. ⚠ Second best: it hides a harvest blind spot that will re-open on the next record whose grant moves into a container.
---- CORRECTED FIX:
Apply the PREFERRED src-edit, with corrected line numbers, corrected safety facts, and a corrected verification step. The settle alternative stays second-best and unchanged.

EDIT (unchanged in substance): scripts/wg-diff.mjs line 255 (finding said :254), OUR_KINDS.skill — add the bare key 'skills' as the first element: skill: ['skills', 'trainedSkill', 'trainedSkillChoice', …]. Verified unique: 'skills' appears zero times as a bare key anywhere in the OUR_KINDS block, so there is no collision with another kind; `choice: {kind:'skills'}` is a VALUE and is handled separately at :706; the FeatGrant tables are harvested through FEATGRANT_KEY_KINDS (:566), which already maps skills -> ['skill'], so that path is untouched.

CORRECTED LINE NUMBERS (wg-diff.mjs is itself modified in the working tree, so the finding's numbers are stale): OUR_KINDS.skill :255 (not :254); fieldToKinds built at :365 (not :361); FEATGRANT_KEY_KINDS :566 (not :562-564); effectChoices option-grant walk :671-677 (not :667-670); plain choice option-grant walk :688-692 (not :683-688); VERIFIED_EQUIVALENT registry :888 (not :884), oatia entry at :1515 (not :1482), read at :1916 (not :1851). build.ts: resolvePick call :5035 (not :5030), applyAlwaysOn :4988 (not :4983-4985). featGrantsLane.ts REMOVED note :54-62 (not :54-70).

CORRECTED SAFETY FACT — the finding's "record top level (1 record, creative-prodigy)" is wrong as stated. Measured across all of core.json: 107 records carry a top-level `skills` key — feats 1, animalCompanions 98, companionSpecializations 8 — and the companion ones are ARRAYS of skill names, not rank maps. The safety CONCLUSION still holds, but for a different reason than the finding gives: animalCompanions and companionSpecializations are not in WG_PAIRING (scripts/lib/wg-parse.mjs:171-180 pairs only feats, classFeatures, heritages, backgrounds, ancestries, classes, items, actions), so ourKindsOf never sees them. Within the paired buckets the counts are: top-level `skills` 1 (feats/creative-prodigy), effectChoices option grant.skills 43 records, plain choice option grant.skills 8 records, union 51 (the finding said 52), whileActive `skills` 0, enhancement.grant `skills` 0. Note in passing that feats.creative-prodigy's value is DOUBLY NESTED — "skills": {"skills": {"performance": "trained"}, "redundantFallback": true} — an authoring shape worth a separate look; it does not change any bucket here.

CORRECTED VERIFICATION — do NOT use `--list --bucket they-only | grep 'missing=\[skill\]'`. That list is truncated at 400 of 642 records (wg-diff.mjs:1948, list.slice(0, 400)), which is exactly why the finding reports an exposure of "11" (today it prints 16) — the number is an artifact of the print cap, not a census. Use the by-kind summary block instead. Measured before/after by running the patched copy: THEY-ONLY 642 -> 624; WE-ONLY 2447 -> 2466; AGREE 1087 -> 1086; disagree 1740 -> 1758; and in the "THEY-ONLY, by the kind we are missing" table, skill 101 -> 82. Nineteen records lose the skill-missing tag, not one: fey-ascension, oatia-skysage-dedication, anvil-dwarf, artisan-android, laborer-android, shoreline-strix, polychromatic-anadi, frightful-goloma, surgewise-fleshwarp, old-blood-vishkanya, prismatic-vishkanya, battle-ready-orc, winter-orc, roaming-minotaur, shadow-rat, gandharva, healer-samsaran, wilderness-samsaran, waning-moon-sarangay. So the finding's "oatia is the only one of them whose carrier is a grant.skills" is false — eighteen heritages carry the same shape (e.g. heritages/anvil-dwarf effectChoices 'trained-skill' with grant.skills {crafting:'trained'} and its redundancy branches), and every one of them is read live through the heritage resolvePick at build.ts:5051.

CORRECTED EXPECTATION — the finding's "with no new WE-ONLY drift" is false and must not be used as the pass criterion. WE-ONLY grows by 19, and two records leave AGREE for WE-ONLY: lorekeeper-shisk and ancient-ash, each landing as theirKinds=[conditional,choice] / ourKinds=[choice,skill] / extra=[skill]. Both genuinely train a printed skill on our side (lorekeeper-shisk's Int-or-Wis skill, ancient-ash's chosen skill), so those two are truthful WE-ONLY entries of the kind the header at wg-diff.mjs:20-24 says is expected and never actioned blindly — but the reviewer must expect them rather than read them as a regression. Likewise the disagree count rising to 1758 is not 18 new value conflicts: it is 18 records where `skill` now exists on BOTH sides and therefore becomes eligible for value adjudication by wg-values.

Everything else in the finding stands: the carrier is live, the overlay rows are correct and non-removing, the featGrantsLane removal is right, and no engine field is needed.

########## astrolabe-of-falling-stars [b16 ASK-OWNER] verify=ASK-OWNER
---- FIX [none] reader: 
NO FIX PROPOSED — owner-parked. The record sits in work/owner-questions.json `open` (batch 16) with a previously REFUSED fix, and Rule 2 forbids settling a WG-vs-print contradiction from this seat. What the owner needs in order to rule is already written there; the only thing worth adding to the entry is the shape precedent, that six sibling astrolabe records encode the same printed sentence as {kind:'skill', detail:'all'} +1 item with the restriction in `when` (situationalBonuses.ts:2367, 2368, 2480, 2559, 2589, 2604), so a ruling of 'follow the book, keep our shape' has an exact template and a ruling of 'follow WG' would make this id the only narrow one. Until he rules, the record stays as it is.
---- EVIDENCE:
BATCH 16 (work/wg-batch-016.json). ⚠ THE OWNER HOLDS THIS ONE — do not fix. work/owner-questions.json `open` carries the entry id 'astrolabe-of-falling-stars', batch 16, with both sides already written up and the note that a fix was proposed and REFUSED on review: "REFUSED: the fix settles a WG-vs-printed-text contradiction by itself. That is the owner's call, not the finder's." It is not in any authorisedExceptions block, so it is still open. It is also absent from work/wg-batch-016-parity.json's `records` array — which is the 'never-compared' status the brief refers to: the batch-16 parity pass produced no verdict for it, and the owner question is the only adjudication that exists. I re-verified the premise rather than trusting it: the gap is still exactly as recorded today (record has no situational; zero hits for the id in situationalBonuses.ts; nothing in featGrants*/featFeatGrants/featPickGrants/featCantripGrants/companionGrants/counterMods/modes.ts/consumable-modes.json/toggle-modes.json), and their single op is unchanged. THE CONTRADICTION TO RULE ON, stated plainly: the book grants the +1 item bonus to the CHECKS to identify celestial bodies without naming a skill; WG grants it to SURVIVAL only; our six sibling records grant it to skill 'all' gated on the printed restriction. Adopting WG here would narrow the bonus below the printed text AND diverge from six of our own records; adopting our sibling shape would add a bonus this id has never carried. Either way it is a WG-vs-print call, which is Rule 2's ASK-OWNER, and the owner already has it.

########## animistic-practice [b6 MATCHES] verify=CONFIRMED
---- FIX [settle] reader: FEAT_SITUATIONAL["seer"] at src/rules/situationalBonuses.ts:1112 → characterSituationalIds() src/rules/explain.ts:239 → featSituationalLines() src/rules/explain.ts:727 (AC and saving-throw stat rows)
RESTORE the value settle in SETTLED_VALUES (const at scripts/wg-values.mjs:771), replacing the removal marker at scripts/wg-values.mjs:1006 and keeping the justification comment block above it intact: 'animistic-practice': ['save|fortitude', 'save|reflex', 'save|will', 'ac|']. Justification to keep/restate: their Seer ops are level-tiered (+1 base, +2 under LEVEL>=9, +3 under LEVEL>=17, every op carrying the haunts/spirits/incorporeal-undead text) and theirAssertions() keeps only the last write, so 'theirs=3' is the top of a ladder, not a flat grant; ours holds the identical ladder and the identical condition as situationalBonuses['seer'], on the practice record rather than the selector. Add a pointer that the entry has FOUR keys and that a stale-settle sweep must not drop it wholesale on a focus-pool reason. Print agrees with WG on every tier (AoN practice-3), so nothing is adopted and nothing is escalated.
---- FIX [settle] reader: buildCharacter() animist focus-pool branch, src/rules/build.ts:3852-3890 (the `build.subclassId === 'medium' && level >= 9` clause at 3880 and the Math.min(poolMax, 3) cap at 3885)
RESTORE the kinds settle in VERIFIED_EQUIVALENT (const at scripts/wg-diff.mjs:884), replacing the removal marker at scripts/wg-diff.mjs:1585 and keeping the justification comment above it: 'animistic-practice': ['focus']. Justification: their FOCUS_POINT_BONUS=1 sits on the Medium option and is their flat approximation of Dual Invocation; ours implements the printed "whichever is higher (maximum 3)" clause in build.ts and is more faithful, and a `focus` field on the core.json record would be read by nothing — the same reasoning as the sibling entry 'animist-apparition-spellcasting': ['focus'] a few lines above. The removal reason ("the Medium's Dual Invocation now raises the focus pool from 9th") was about a DIVERGENCE that is fixed; it was never a reason the KIND row would stop reporting, and the row is now the only missing=[focus] in the corpus.
---- FIX [src-edit] reader: scripts/drop-fixed-settles.mjs (the FIXED map and the removal loop) — the tool that maintains VERIFIED_EQUIVALENT / SETTLED_VALUES / SETTLED_IDENTITIES
ROOT-CAUSE FIX so this class of regression cannot recur: scripts/drop-fixed-settles.mjs keys FIXED by bare record id and deletes the entire settle line whose key matches, across all three registries. Settle values are ARRAYS of buckets and one id routinely settles several unrelated buckets for several unrelated reasons. Change FIXED to name the buckets each stale reason covers — e.g. 'animistic-practice': { file: 'scripts/wg-diff.mjs', buckets: ['focus'], why: "..." } — and have the loop REMOVE ONLY THOSE MEMBERS from the array, leaving the remaining members and the surviving justification comment in place; only delete the whole key when the array empties. Then re-audit the other seven ids in FIXED (elemental-wrath, spirit-warrior-dedication, ghostly-resistance, firework-technician-dedication, gates-threshold, ascended-dragonet-heritage, the-moon-weavers-art) for the same collateral loss — each was removed from all three registries on a single-bucket reason. NOTE for the owner: I am read-only and have proposed, not applied, this.
---- CORRECTED FIX:
Fixes stand as proposed. Corrections to the anchors and two factual details:

LINE NUMBERS (situationalBonuses.ts:1112, explain.ts:239 and explain.ts:727 were exact; the rest have drifted):
- wg-values removal marker: 1034, not 1006.
- SETTLED_VALUES const: wg-values.mjs:786, not 771.
- wg-diff removal marker: 1618, not 1585.
- VERIFIED_EQUIVALENT const: wg-diff.mjs:888, not 884.
- build.ts focus pool: `let poolMax` 3856, medium clause 3886, `Math.min(poolMax, 3)` 3890 — cite 3856-3891, not 3852-3890 / 3880 / 3885.
- Add the AC reader line explicitly: explain.ts:1144 (AC row) and explain.ts:1010 (save row) are the two call sites of featSituationalLines() that surface this entry.
Each marker line is unique within its file, so the edits are unambiguous regardless of drift.

FACTUAL CORRECTIONS to fold into the settle comments:
1. Their Medium op is NOT bare. It sits inside `conditional IF LEVEL >= 9 AND FEAT_NAMES NOT_INCLUDES "circle of spirits" THEN adjValue FOCUS_POINT_BONUS = 1`. State it that way — it strengthens the settle rather than weakening it: their level gate matches Dual Invocation's 9th level and their non-stacking guard mirrors our separate `focusSeen.has('circle-of-spirits')` branch in build.ts, while their flat +1 still cannot express the printed "equal to the number of focus spells you have or the number of primary apparitions… whichever is higher (maximum 3)" that build.ts:3886-3890 implements.
2. `wg-show` prints BOTH rows (legacy 28491 with Channeler/Sage, remaster 38685 with Liturgist/Medium/Seer/Shaman). It is wg-values that pairs to exactly one, via `theirByBucket[bucket].get(norm(name))` (wg-values.mjs:1183) — a Map, one row per bucket+name — and the reported value 3 proves the winner is 38685. Say "wg-values compares 38685", not "wg-show/wg-values own 38685".
3. The gate prints `ours=(nothing)`, not `ours(all): (none)`.

ONE ADDITION worth putting in the restored wg-values comment, because it cost real time to rediscover: wg-values defaults `ids` to work/wg-batch-003.json (wg-values.mjs:1174-1175), so a bare run shows only batch 3 and this record appears absent. The reproduction command is `node scripts/wg-values.mjs --ids animistic-practice` (add `--raw` to bypass the settle). Without this note the next reader will run the script bare, see no animistic-practice row, and wrongly conclude the settle is inert.

The third fix (bucket-scoping scripts/drop-fixed-settles.mjs and re-auditing the other seven FIXED ids) is correct and should be kept as proposed-not-applied; the two-registry strikes on gates-threshold and ascended-dragonet-heritage confirm the collateral pattern is not unique to this record.

########## instinct-ability [b16 ASK-OWNER] verify=ASK-OWNER
---- FIX [none] reader: work/owner-questions.json → open[] entry { id: 'instinct-ability', batch: 16 }; carriers unchanged (derive.ts:3211 for derivedGrant, derive.ts:791 for the HP)
DO NOT ACT — owner-parked. No data change, no overlay row, no settle written by me. Our side already delivers both halves correctly (the instinct ability via derivedGrant, the +3 via barbarian-resiliency.maxHpBonus.perArchetypeFeat), so nothing is broken while the question waits. FOR THE OWNER, when they rule: if they accept the reading that this is Barbarian Resiliency's HP relocated by WG's engine — the reading already accepted eight times, including for the sister feat barbarian-dedication — the follow-up is a single-line settle in SETTLED_VALUES (const at scripts/wg-values.mjs:771), placed beside its sibling at line 963: 'instinct-ability': ['hp|'], with a comment naming the carrier (feats/barbarian-resiliency.maxHpBonus = { perArchetypeFeat: 3, archetype: 'barbarian' }) and the reader (featHpBonus, src/rules/derive.ts:778, perArchetypeFeat branch at :791; builder mirror at src/rules/build.ts:6949-6950), and recording that instinct-ability's own archetype: 'barbarian' is what makes the multiplier count it. The open[] entry in work/owner-questions.json should be updated or closed at the same time. This settle should be decided together with the batch-17 family under separate investigation, since it is one ruling covering all of them.
---- CORRECTED FIX:
DO NOT ACT in this pass - unchanged from the finding. Corrections for the owner's decision packet: (a) The batch-17 family is no longer pending; it is already settled at scripts/wg-values.mjs:857-862 (advanced-devotion, advanced-glory, advanced-kata, advanced-defender, champions-reaction, guardians-intercept) with the comment "Their flat 3 hangs off each archetype feat; ours is maxHpBonus.perArchetypeFeat: 3 on the *-resiliency feat that PRINTS it ... Not adopted; recorded." So the ruling this finding wants deferred to that family has in fact been made, in favour of settling. (b) Strengthen the owner packet with the proof I obtained: their Barbarian Resiliency row itself is a FLAT MAX_HEALTH_BONUS +3 (their id 32099 remaster, 29492 legacy) and each sibling barbarian archetype feat carries the conditional +3, so their engine's total equals 3 x (barbarian archetype feats held) - exactly our perArchetypeFeat product. There is no numeric disagreement with print, only relocation. (c) If and when the owner rules the same way they ruled fourteen times already, the follow-up is one line in SETTLED_VALUES placed beside its sister feat at scripts/wg-values.mjs:976 (NOT :963): 'instinct-ability': ['hp|'], with a comment naming the carrier (feats/barbarian-resiliency.maxHpBonus = { perArchetypeFeat: 3, archetype: 'barbarian' }), the reader (featHpBonus, src/rules/derive.ts:778, perArchetypeFeat branch :791-792; builder mirror src/rules/build.ts:6970-6971; type src/rules/types.ts:2866), and the fact that instinct-ability's own archetype: 'barbarian' is what makes the multiplier count it. The work/owner-questions.json open[] entry for instinct-ability should be closed in the same change. Note that the settle quiets ONLY the hp value; the printed instinct grant stays where it is, on derivedGrant, and must not be touched.
---- EVIDENCE:
BATCH 16. OWNER-PARKED — RECORDED AND SKIPPED PER THE BRIEF. work/owner-questions.json → open[] already contains { id: 'instinct-ability', batch: 16, ... } asking the owner to decide between two readings: "their row is misfiled from a neighbouring record, or they deliberately model something the printed text does not state", noting "Their encoding delivers a Hit Point bonus the printed text never mentions, gated on a feat the printed text never mentions, and omits the one thing the printed text does say. I am not adopting either side." I did not decide it. WHY IT IS OPEN NOW: the gap always existed and no settle was ever written for this id — the hp| value row simply keeps reporting because the parked question was never answered. Confirmed absent from SETTLED_VALUES (grep for "'hp|'" in scripts/wg-values.mjs returns basic-kata:849, basic-fury:892, basic-devotion:893, devout-magic:894, barbarian-dedication:963, monk-dedication:1038, exemplar-dedication:1039, basic-glory:1137 — not instinct-ability). NEW MATERIAL THAT ANSWERS THE PARKED QUESTION, offered for the owner's decision: this is not a misfile and not a house rule. It is the SAME shape already read, verified and settled EIGHT times in this repo, and one of those siblings is the sister feat of this very archetype — scripts/wg-values.mjs:956-963 settles 'barbarian-dedication': ['hp|'] with word-for-word the same finding: "Their only `hp` op sits inside a conditional gated on FEAT_NAMES including 'barbarian resiliency' and adds +3 MAX_HEALTH_BONUS — Barbarian Resiliency's Hit Points parked on the dedication row, not the dedication's own. The printed Barbarian Dedication grants no HP, and we carry those Hit Points on `barbarian-resiliency` itself as `maxHpBonus: { perArchetypeFeat: 3, archetype: 'barbarian' }`." scripts/wg-values.mjs:840-856 records the same for basic-kata and its batch-13 siblings, adding "ours are authored there, on `barbarian-resiliency` and `champion-resiliency`, read by featHpBonus at src/rules/derive.ts:755. Verified on built characters by three refuters." This is also the identical mechanism behind the batch-17 family the brief flags as under separate investigation (advanced-devotion / advanced-glory / basic-kata / advanced-defender, champions-reaction, guardians-intercept — each hp theirs=3): every one is an archetype class feat carrying its archetype's Resiliency HP. WHAT THE +3 IS, WHAT GATES IT: it is the per-archetype-feat Hit Points of the archetype's Resiliency feat, gated on holding that Resiliency feat, distributed one +3 per qualifying archetype feat. IS IT SOMETHING PRINT GRANTS: yes — but on the Resiliency feat, and as a scaling per-feat count, not as a flat number on this feat. Their encoding does not contradict print; it relocates it. Carriers swept per rule 4: grep of src/ finds no instinct-ability reference; no featGrants.ts / featGrantsAuto.ts / featGrantsLane.ts / featFeatGrants.ts / featPickGrants.ts / featCantripGrants.ts / companionGrants.ts / situationalBonuses.ts / counterMods.ts / CATALOG_MODES / consumable-modes.json / toggle-modes.json entry; the single overlay row is scripts/data/effect-backfill.json {"category":"feats","id":"instinct-ability","field":"derivedGrant","value":{"fromFeat":"barbarian-dedication"}}, which is the printed-sentence carrier.

########## ghostly-resistance [b14 MISMATCH] verify=CONFIRMED
---- FIX [overlay] reader: src/rules/derive.ts:2453-2481 — the defenses aggregator: `res.set(r.type, Math.max(res.get(r.type) ?? 0, v))` at :2477 (type-keyed, highest-wins per derive.ts:2625) and `note('resistance:' + r.type, src.__from, v, r.condition ?? src.__cond)` at :2481, surfaced as CharacterDefenses.resistances + .sources in the IWR breakdown. Two distinct types produce two map keys, so both bands are listed and neither is marked applied:false.
scripts/data/effect-backfill.json — the existing row {category:"feats", id:"ghostly-resistance", field:"resistances"} keeps its formulas (both verified against print at all 20 levels) but its two entries must carry DISTINCT type strings so derive's type-keyed map stops collapsing them. Replace value with: [{"type":"all damage except force, ghost touch, and vitality","value":"max(1,floor((@actor.level+2)/6))"},{"type":"non-magical damage (except force, ghost touch, and vitality)","value":"min(5,max(2,floor((@actor.level+2)/6)*2))"}]. The `condition` strings are folded into `type` and dropped, matching modes/vengeful-remnant, which is the same printed clause already authored this way in our own data. This is a whole-array SET, not a removal, so the Rule-5 value:null hazard does not apply. AUTHOR IT IN THE OWNING SCRIPT, not by hand: scripts/backfill-parity-fixes.mjs:193-212 currently emits this row and would re-emit the colliding shape on the next `npm run data`.
---- FIX [registry] reader: scripts/wg-values.mjs:1256 — `if (!RAW_SETTLES && (SETTLED_VALUES[id] ?? []).includes('set|' + s.key)) continue;`
Do NOT re-add a SETTLED_VALUES settle for this record. scripts/drop-fixed-settles.mjs's own doctrine applies — "a settle outlives the reason it was written: it silences that difference in every future batch, so a REGRESSION on a fixed record would never be reported again". Once the two types differ, the word-subset matcher at scripts/wg-values.mjs:1196-1206 covers their "non-magical damage" member on its own and the row disappears on merit. If the owner instead prefers to keep our current colliding shape, the settle must be restored AND the over-grant accepted in writing — flag that as the trade, since the sheet currently shows resistance 2/4/5 to ALL damage where print gives 1/2/3.
---- CORRECTED FIX:
[{"kind":"overlay","detail":"scripts/data/effect-backfill.json -- keep the row {category:\"feats\", id:\"ghostly-resistance\", field:\"resistances\"} and both verified formulas, but give the two entries DISTINCT type strings so derive's type-keyed map stops collapsing them. New value: [{\"type\":\"all damage except force, ghost touch, and vitality\",\"value\":\"max(1,floor((@actor.level+2)/6))\"},{\"type\":\"non-magical damage (except force, ghost touch, and vitality)\",\"value\":\"min(5,max(2,floor((@actor.level+2)/6)*2))\"}]. The `condition` strings fold into `type` and are dropped, matching modes/vengeful-remnant AND items/silhouette-cloak, which already author this same printed clause this way. Whole-array SET, so the Rule-5 value:null removal hazard does not apply.","reader":"src/rules/derive.ts:2472-2488 -- `res.set(r.type, Math.max(res.get(r.type) ?? 0, v))` at :2483 (type-keyed, highest-wins per derive.ts:2634-2635) and `note('resistance:' + r.type, src.__from, v, r.condition ?? src.__cond)` at :2488, surfaced as CharacterDefenses.resistances/.sources and rendered by src/sheet/VitalsRail.tsx:773-780 + src/rules/explain.ts:1477-1497. Two distinct types produce two map keys and two React keys, so both bands list and neither is marked applied:false."},{"kind":"script","detail":"AUTHOR THE NEW SHAPE IN BOTH OWNING SCRIPTS, not just one -- this is the correction to the original fix. (a) scripts/backfill-parity-fixes.mjs:193-212 (the row the brief identified). (b) scripts/wg-batch14-residual2.mjs:42-55, MISSED by the brief: it writes the IDENTICAL overlay key {feats, ghostly-resistance, resistances} with the superseded single-band value [{type:'all-damage', value:'floor((@actor.level+2)/6)', condition:'except force, vitality, and damage from a ghost touch weapon (or anything acting as one)'}], replace-in-place at its lines 63-68, and it has NO --write gate -- writeBackfill(ROOT, rows) executes on every invocation. Running it after the fix reverts BOTH the non-magical band and the max(1,...) sub-4th correction, silently reopening the batch-14 gap. Either update its `edits` array to the same two-type shape, or delete its ghostly-resistance entry entirely and leave backfill-parity-fixes.mjs the sole owner (preferred: one row, one owner). Its header comment at lines 4-10 should also be corrected, since it currently argues `condition` is the right carrier for the exception -- the very assumption that produced the collision. Note scripts/apply-defense-lane.mjs:35-37 lists the record in EXCEPTION_UNMODELLABLE and skips it, so it is inert and needs no change; its stated reason ('filing it as \"all\" would over-grant') independently corroborates this finding.","reader":"scripts/lib/write-backfill.mjs -> scripts/data/effect-backfill.json, applied over public/core.json by scripts/import-core-v2.mjs (step 1 of `npm run data`); the overlay is what makes the field survive regen. Neither authoring script is itself in the `npm run data` chain."},{"kind":"registry","detail":"Do NOT re-add a SETTLED_VALUES settle. scripts/drop-fixed-settles.mjs's own doctrine holds -- a settle outlives its reason and would silence a future regression on this record. Verified by replicating the word-subset matcher at scripts/wg-values.mjs:1211-1218: their member 'non-magical damage' -> {non, magical, damage} is a subset of the proposed 'non-magical damage (except force, ghost touch, and vitality)', and their base member is a superset of ours, so both are covered and the SET-GAP disappears on merit. Leave the removal marker comment at scripts/wg-values.mjs:902 in place; siblings 'mortification' (:903) and 'hardened-chassis' (:904) are untouched. If the owner instead elects to keep the colliding shape, the settle must be restored AND the over-grant accepted in writing -- flag the trade explicitly: the sheet currently promises resistance 2/4/5 to ALL damage where print gives 1/2/3.","reader":"scripts/wg-values.mjs:1269 -- `if (!RAW_SETTLES && (SETTLED_VALUES[id] ?? []).includes(`set|${s.key}`)) continue;` (the brief cited :1256, which is the NUMERIC-key filter, not the set-key one)."},{"kind":"cosmetic-note","detail":"Non-blocking, pre-existing: src/sheet/VitalsRail.tsx:74-77 typeLabel does t.replace(/-/g,' '), so 'non-magical damage (...)' renders as 'Non magical damage (...)'. modes/vengeful-remnant and items/silhouette-cloak already display this way today, so the fix introduces no new behaviour -- but if the owner wants the hyphen preserved, that is a one-line typeLabel change affecting all three records at once, not a reason to keep the collision.","reader":"src/sheet/VitalsRail.tsx:74-77 and :773-780; src/rules/explain.ts:1483 applies the same replace for the popup title."}]

########## sea-touch-elixir-lesser [b16 MATCHES] verify=CONFIRMED
---- FIX [settle] reader: CARRIER: public/core.json modes/item-sea-touch-elixir-lesser.speeds.swim = 20 (source scripts/data/consumable-modes.json:4831-4841). READER: src/rules/derive.ts:4714-4724, `for (const m of c.activeModes ?? []) for (const [k, raw] of Object.entries(m.speeds ?? {})) ... speeds[key] = Math.max(speeds[key] ?? 0, v)`; activated by src/rules/play.ts:1444 `useConsumable` (`m.fromItemId === inv.itemId` → toggleMode) and surfaced by src/sheet/ItemDetail.tsx:192. SETTLE READER: scripts/wg-values.mjs:1242, `if (!RAW_SETTLES && (SETTLED_VALUES[id] ?? []).includes(key)) continue;`
SETTLED_VALUES in scripts/wg-values.mjs — add `'sea-touch-elixir-lesser': ['speed|swim'],` (and, when the sibling grades are gated, 'sea-touch-elixir-moderate' and 'sea-touch-elixir-greater' identically). Justification comment to sit with it: their item 7741 asserts a single flat `setValue SPEED_SWIM 20` with no duration; print gives the swim Speed for 10 minutes only, so under Ruling 'a Speed is a real number only when ALWAYS on' it cannot be a record field. Ours carries it on modes/item-sea-touch-elixir-lesser (`speeds: {swim: 20}`, duration '10 minutes'), sourced from scripts/data/consumable-modes.json:4831-4841 per scripts/add-sea-touch-modes.mjs. The value comparer's only mode reader (wg-values.mjs:314-321) harvests resistances and weaknesses, so a mode-carried Speed can never be seen by the scalar path.
---- FIX [src-edit] reader: scripts/wg-values.mjs:1239-1253 — `const ours = ourAssertions(id, rec); ... if (!candidates.length) rowsOut.push({ key, tv, ov: '(nothing)', kind: 'MISSING' })`. Feeding speed|swim into `ours` makes the candidate list non-empty and the values equal (20 === 20).
PREFERRED OVER THE SETTLE, and it retires the settle entirely. scripts/wg-values.mjs `ourValues` — mirror the mode loop that already exists in `ourSets` at :314-321, but for the scalar path: `for (const m of Object.values(core.modes ?? {})) { if (m.fromItemId !== id && !(m.feats ?? []).includes(id)) continue; for (const [k, v] of Object.entries(m.speeds ?? {})) put('speed|' + k, typeof v === 'string' ? v : Number(v)); }`. This closes the row on merit rather than silencing it, keeps a future regression (mode deleted by a regen, speed changed) reportable, and fixes the same blind spot for every other Speed-granting mode in the corpus — the ourSets comment at :315-317 already documents this exact class of false negative for resistances ("Blood Booster's printed resistances live on modes['item-blood-booster-lesser'], and reading only the feat gate called them missing on the item that carries them with the right numbers and duration"). If this lands, do NOT add the settle above.
---- CORRECTED FIX:
Take fix #2 (the wg-values.mjs edit) and DROP fix #1 (the settle) entirely, with three corrections:

1. FUNCTION NAME. There is no `ourValues` in scripts/wg-values.mjs (grep returns nothing). The scalar path is `ourAssertions(id, rec)` at :481 — the finding's own reader field cites it correctly, only the fix prose mislabels it. Insert the loop inside ourAssertions, immediately after the `rec.enhancement?.grant?.speeds` loop at :760 and before `situationalMagnitudes(...)`, i.e. beside the two container-speed loops it mirrors:

  /* …and a Speed carried by a MODE — the scalar twin of the mode loop in ourSets at :314. A Speed the
   * book grants for a DURATION cannot be a record field (docs/gold-set-review.md:196), so it lives on
   * modes/item-*; reading only the record reported Sea Touch's swim 20 as granted by nobody. */
  for (const m of Object.values(core.modes ?? {})) {
    if (m.fromItemId !== id && !(m.feats ?? []).includes(id)) continue;
    for (const [k, v] of Object.entries(m.speeds ?? {})) put(`speed|${k}`, typeof v === 'string' ? v : Number(v));
  }

2. SIBLING SCOPING. The finding says the moderate and greater grades would need the same treatment "when the sibling grades are gated". They are not gated — `--ids sea-touch-elixir-lesser,sea-touch-elixir-moderate,sea-touch-elixir-greater` shows sea-touch-elixir-greater ALREADY emitting "MISSING speed|swim theirs=20 ours=(nothing)" today, while moderate is uncomparable ("prose-only bonus"). One edit closes lesser and greater at once; nothing further is owed for moderate.

3. OPTIONAL, FORMULA MODES. The literal `typeof v === 'string' ? v : Number(v)` above is the right shape for this record and matches its neighbours at :737/:753, but 12 of the 27 speed-carrying modes hold formulas (e.g. item-winged fly "min(25,@actor.speed.land)"). If those are to close too, push the evaluated base ALONGSIDE the raw string rather than instead of it — `put` is explicitly multi-assertion (:490, "A match against ANY assertion is agreement"), so `if (typeof v === 'string') { const b = evalSpeedFormula(v); if (b !== null) put(\`speed|${k}\`, b); put(\`speed|${k}\`, v); }` loses nothing. Do NOT substitute the base for the string the way :510-514 does: evalSpeedFormula evaluates @actor.speed.land to 0, so min-shaped formulas collapse to 0 and would print a misleading `ours=0` DIFFERENT row. This step is independent of the Sea Touch record and can be deferred.

########## shining-hackle [b16 MATCHES] verify=CONFIRMED
---- FIX [none] reader: src/rules/derive.ts:266-282 passiveItemBonus / :296-312 passiveItemBonusDetail (item bonus lane), consumed for skills at src/rules/derive.ts:525; src/rules/situationalBonuses.ts:2116 for the Perception condition display.
No data change. public/core.json items/shining-hackle.passiveEffects = {"skills":{"stealth":-1},"perception":1} already agrees with the printed "+1 item bonus to Perception checks based on sight, but you also take a –1 item penalty to your Stealth checks" AND with their two addBonusToValue ops (PERCEPTION "1" type item text "base on sight"; SKILL_STEALTH "-1" type item). Do not touch the overlay row at scripts/data/effect-backfill.json:7089-7098 — flipping the sign to match the comparer's printed "theirs=1" would introduce a defect where none exists.
---- FIX [src-edit] reader: scripts/wg-values.mjs:1251 — `else if (!candidates.some((v) => String(v) === String(tv))) rowsOut.push({ key, tv, ov: candidates.join(' / '), kind: 'DIFFERENT' });` — the string compare that turns 1 vs -1 into a reported difference.
scripts/wg-values.mjs — make the magnitude normalisation SYMMETRIC so this false positive cannot recur on any item penalty. Their side is already reduced to magnitude at :260 (`const scalar = typeof val === 'number' ? Math.abs(val) : val;`); our numeric skill/save/perception carriers must be reduced the same way. Narrowest change: at :648 write `put('skill|' + k, typeof v === 'number' ? Math.abs(v) : v);` (and the same at :649 for pe.saves and :650 for pe.perception, plus :643 for the classFeatures passiveEffects.skills path), so both sides speak magnitudes. Preferred alternative if the owner would rather keep signs: drop the Math.abs at :260 and instead compare |theirs| to |ours| at the comparison site :1251, which keeps a genuine sign disagreement reportable instead of erasing the concept. Either way the existing comment at :257-259 needs updating — it currently claims ours is always an unsigned string, which stopped being true when item passiveEffects joined the reader.
---- FIX [src-edit] reader: src/rules/derive.ts:525 (`const itemBonus = Math.max(abpSkillBonus(...), mpSenseSkillItemBonus(...), passiveItemBonus(c, db, 'skill', key), dynamicItemSkillBonus(...))`) and :538-543 (`poolTypedMods([{ type: 'item', value: itemBonus }, ...])`) — the skill statistic assembly, whose output is the Stealth modifier the sheet prints and whose breakdown is fed by passiveItemBonusDetail.
THE REAL DEFECT THIS INVESTIGATION SURFACED — an item's unconditional skill PENALTY reaches no number. src/rules/derive.ts:266-282 `passiveItemBonus` cannot return a negative (`let best = 0;` then `best = Math.max(best, v)`), and its skill caller at :525 wraps it in another Math.max, so items/shining-hackle's stealth -1 and items/wandering-pipe's stealth -1 are the only two negative passiveEffects.skills in the corpus and neither is applied. Spec: add a companion `passiveItemPenalty(c, db, kind, skillKey)` that walks the same worn/invested inventory (`itemInUse(inv)`, `db.items[inv.itemId]?.passiveEffects`, plus `c.resolvedItemPassives?.[inv.itemId]`) and returns `Math.min(0, ...)` of the values; then at :534-543 add it as its own term OUTSIDE the item-bonus Math.max — `{ type: 'item', value: itemBonus }` must keep the best bonus, and the penalty must be added separately, because an item penalty and an item bonus are not competing for the same slot. Mirror it in `passiveItemBonusDetail` (:296-312) so the Stealth breakdown names the shining hackle as the source of the -1 rather than showing an unexplained number. Cross-check against wandering-pipe before landing: its -1 is CONDITIONAL in print ("on Stealth checks to Hide or Sneak") and is already carried as display at src/rules/situationalBonuses.ts:1955, so applying it as a flat penalty as well would double-count — its passiveEffects.skills.stealth row should be dropped from the overlay in the same pass, leaving the situational entry as its sole carrier. shining-hackle's is unconditional and should compute.
---- CORRECTED FIX:
Adopt the finding's three fixes with these corrections.

(A) FIX 1 STANDS UNCHANGED. No data change. Do not touch the shining-hackle overlay row in scripts/data/effect-backfill.json. Verified identical to HEAD and in agreement with print and with their two ops.

(B) LINE ANCHORS IN scripts/wg-values.mjs ARE STALE AND DRIFTING — CITE CODE, NOT NUMBERS. The file is untracked and was being edited concurrently during this verification: `const scalar = ... Math.abs(val) ...` moved from 263 to 278 and the `Object.entries(pe.skills ?? {})` reader from 648 to 663 within minutes of each other. Apply fix 2 by anchor: the `const scalar = typeof val === 'number' ? Math.abs(val) : val;` line in the their-side op loop, and the `const pe = rec.passiveEffects ?? {};` block in ourAssertions.

(C) FIX 2'S "PREFERRED ALTERNATIVE" RATIONALE IS FACTUALLY WRONG. "Drop the Math.abs and instead compare |theirs| to |ours| at the comparison site" does NOT "keep a genuine sign disagreement reportable" — comparing magnitudes at the compare site erases sign disagreement exactly as the upstream abs does. Two real options: (i) take the narrow fix (abs our numeric carriers) and rewrite the comment to say plainly that the comparer is magnitude-only by design; or (ii) if sign flips must be reportable, stop abs'ing theirs, and at the situational-string reader (`const mags = [...String(e.bonus ?? '').matchAll(/([+-]?\d+)/g)].map((m) => Math.abs(Number(m[1])))`) push BOTH the signed and the abs'd value as candidates, then compare signed-first. Also: the instruction to abs `pe.saves` is a no-op for items — ItemPassiveEffects.saves is `number` (src/rules/types.ts:2014) while the reader does `Object.entries(pe.saves ?? {})`, which yields nothing for a number; that line is already dead for the item category.

(D) FIX 3 MUST BE SKILL-LANE ONLY. Do NOT wire the new `passiveItemPenalty` into src/rules/derive.ts:211 (saves), :343 (perception), or :1740 (AC), and do not change `passiveItemBonus`'s `let best = 0` in place — those three sites all consume it and a signed return would silently drag saves/Perception/AC. Reason this is not hypothetical: the finding's corpus scan was scoped to passiveEffects.skills and missed a THIRD negative item passive. `items/day-goggles.passiveEffects.perception = -2`, and that penalty is CONDITIONAL in print (visual Perception checks only) and already carried as display at src/rules/situationalBonuses.ts:1283 ("on VISUAL Perception checks, while wearing the goggles over your eyes", bonus "-2 item (penalty)"). A kind-symmetric wiring of the helper would apply -2 flat to ALL Perception for day-goggles wearers — a new regression created by the fix. Restrict the new term to the `poolTypedMods([...])` array in deriveSkill (src/rules/derive.ts:540), leaving `{ type: 'item', value: itemBonus }` untouched.

(E) NO NEW STACKING CODE. src/rules/modes.ts:169 `poolTypedMods` already keeps `bonus[type]`/`penalty[type]` apart and returns `bonus.item - penalty.item`. Passing an extra `{ type: 'item', value: -1 }` into the existing array is sufficient and correct RAW (item bonus and item penalty both apply). Mirror in passiveItemBonusDetail so the Stealth breakdown names the shining hackle.

(F) EXISTING IDIOM WORTH THE OWNER'S CHOICE (gate 8). The engine already has an item passive PENALTY lane: src/rules/derive.ts:4634-4645 accumulates `passiveSpeedPenalty` from a dedicated positive-magnitude field `pe.speedPenalty` (src/rules/types.ts:2034) via `Math.abs`, in its own lane rather than as a negative inside a bonus field. If the owner prefers that established shape, the alternative is a `skillPenalty?: Partial<Record<ProficiencyKey, number>>` field; but that requires migrating the two existing negative rows, so the companion-function approach is the lower-risk path. Either way an existing way to SAY it exists — this is not a novel concept, only a missing reader.

(G) THE WANDERING-PIPE EDIT IS MANDATORY, CO-LANDING, AND UNDER-EVIDENCED. It must land in the same commit, not "in the same pass, optionally": once the skill-lane penalty is live, `items/wandering-pipe.passiveEffects.skills.stealth = -1` starts applying flat, and its penalty is conditional per src/rules/situationalBonuses.ts:1955. But its conditionality CANNOT be sourced the way shining-hackle's was: `node scripts/wg-item.mjs "Wandering Pipe"` returns "no item matched" (they do not carry it), and our core.json record has no description/text field at all (keys: id, name, level, price, bulk, traits, rarity, source, usage, frequency, counters, activationCost, itemType, edition, passiveEffects, aonId). The only evidence for "conditional" is the authored display string itself, which is circular. Read AoN equipment-3513 directly before dropping the row. And EDIT the overlay value to {"skills":{"deception":2}} — do not delete the row: the deception +2 is live, positive, and correct.

(H) OUT OF SCOPE, CONFIRMED AS FLAGGED. shining-hackle's `passiveEffects.perception: 1` is stored unconditionally where print says "based on sight", and is simultaneously surfaced as a computed flat +1 AND as a situational note at src/rules/situationalBonuses.ts:2116. It is at parity with their side (flat PERCEPTION +1 with the condition in `text: "base on sight"`), so the WG gate is clean, but the double-surfacing is a genuine display question for the owner. Note the asymmetry with day-goggles, whose analogous conditional value is stored flat AND displayed but is harmlessly inert only because it happens to be negative.

########## aeon-stone-repairing [b16 MATCHES] verify=CONFIRMED
---- FIX [src-edit] reader: scripts/wg-identity.mjs ourIdentities() — the comparer itself. The SHIPPED reader for the value is src/rules/build.ts:6477-6482 (invested item + `wayfinder-slotted` designation -> innateGrants), surfaced on the Spells tab innate card at src/sheet/SpellsTab.tsx:1792-1899.
scripts/wg-identity.mjs, ourIdentities(), immediately after line 200 (`for (const s of rec.innateSpells ?? []) addSpell(s.spellId ?? s);`): add `for (const s of rec.resonant?.innateSpells ?? []) addSpell(s.spellId ?? s);`, with a comment mirroring wg-diff.mjs:287-290 — an aeon stone's second spell is a real granted spell held one level down. Closes the `spells` bucket on this record and on 15 other stones at once.
---- FIX [src-edit] reader: scripts/wg-identity.mjs theirIdentities(). Shipped carrier: src/rules/types.ts:4547 ItemDesignation 'wayfinder-slotted'; offered at src/sheet/InventoryTab.tsx:706 ("Slotted in a wayfinder"), gated at src/sheet/ItemDetail.tsx:495, consumed at src/rules/build.ts:6477.
scripts/wg-identity.mjs, theirIdentities(), in the `else if (op?.type === 'select')` branch (lines 88-93): skip the select whose `d.title` is their resonant-investment question, because our counterpart control is the `wayfinder-slotted` designation rather than an option list — the equivalence wg-diff.mjs:616-627 already records. ⚠ SUPPRESS ON THEIR SIDE, do not add literal 'yes'/'no' to ours.options: `contains()` (wg-identity.mjs) does substring matching, so a bare 'no' in ours would satisfy 'nature' and silence aeon-stone-amplifying's genuine [arcana, nature, occult, religion] gap. Verified safe: amplifying's tradition options live in a SEPARATE nested select titled "Select the tradition of the last spell you enhanced with this aeon stone.", so it keeps reporting after this change. Closes the `options` bucket on all 22 affected stones.
---- FIX [settle] reader: SETTLED_IDENTITIES in scripts/wg-identity.mjs (consumed at wg-identity.mjs:903).
FALLBACK ONLY, if the owner prefers a registry to a comparer fix: SETTLED_IDENTITIES in scripts/wg-identity.mjs:426, `'aeon-stone-repairing': ['spells', 'options']`. Not recommended — it settles one id at a time where the defect is comparer-wide across 22 stones, and wg-diff already set the precedent of teaching the reader instead.
---- CORRECTED FIX:
Keep both src-edits to scripts/wg-identity.mjs as written (the ours-side `resonant.innateSpells` descent after line 200, and the their-side skip of the select titled "Is this granting the resonant power?" in the `select` branch at lines 88-93). Drop the "settle" fallback — it settles one id where the defect is comparer-wide across 30 of their rows. THREE CORRECTIONS:

(A) MISSING STEP, must ship in the same edit. Fix 2 strands the existing settle `'aeon-stone-western-star': ['options']` at scripts/wg-identity.mjs:877 (with its comment block at 869-876). Verified with `node scripts/wg-identity.mjs --ids aeon-stone-western-star --raw`: that stone's ONLY reported gap is the same `options theirs-not-ours=[no, yes]` resonant select. Once their side stops emitting it, the entry silences nothing — which is exactly the hazard scripts/wg-settle-stale.mjs exists to catch; it parses SETTLED_IDENTITIES (wg-settle-stale.mjs:44) and reports a settle that answers nothing for DELETION ("A settle that silences nothing is a trap waiting to spring… until the day the comparer reports a DIFFERENT difference of the same kind on that record and the entry silences that one too, unread and unadjudicated"). Delete lines 869-877 as part of fix 2.

(B) The ⚠ rationale on fix 2 is factually wrong and should not be repeated in the code comment. `contains()` (wg-identity.mjs:393) would NOT let a bare 'no' in ours satisfy their 'nature': `'nature'.includes('no')` is false, `'no'.includes('nature')` is false, and the word-set fallback compares {'no'} against {'nature'} with no overlap. So adding 'no'/'yes' to ours.options would not have silenced aeon-stone-amplifying. Their-side suppression is still the correct route — ours genuinely has no option list to add to, since the control is a per-item designation — but write the comment on that ground, not on a collision that does not exist.

(C) The provenance sentence is false and should be struck from the record. The `field:"resonant"` overlay rows have NEVER been committed: `git show <c>:scripts/data/effect-backfill.json | grep -c '"resonant"'` returns 0 at 061f099, b673684, 035a2a8 and HEAD, and the 37 occurrences exist only in the modified working tree, written by the untracked scripts/backfill-aeon-resonance.mjs. The operative claim ("present today, materialised in public/core.json") is true and the verdict stands on it, but the lane is not yet durable across a clean checkout — scripts/data/effect-backfill.json and scripts/backfill-aeon-resonance.mjs both need committing before "survived every regen since" is true of anything.

(D) Cosmetic line drift in the citations, for whoever navigates by them: ItemDesignation is types.ts:4573 (not 4547); the resonant reader is build.ts:6498 (not 6477); SETTLED_IDENTITIES is consumed at wg-identity.mjs:912 (not 903). The "22 stones" figure is 21 reported plus western-star already settled; 30 of their rows carry the select in total.

########## aeon-stone-formulating [b16 MATCHES] verify=CONFIRMED
---- FIX [src-edit] reader: scripts/wg-identity.mjs ourIdentities(). Shipped reader for the value: src/rules/build.ts:6477-6482, rendered at src/sheet/SpellsTab.tsx:1792-1899.
scripts/wg-identity.mjs, ourIdentities(), after line 200: add `for (const s of rec.resonant?.innateSpells ?? []) addSpell(s.spellId ?? s);`. Closes the `spells` bucket here and on every other stone whose resonant power is a spell.
---- FIX [src-edit] reader: scripts/wg-identity.mjs theirIdentities(). Shipped carrier: `wayfinder-slotted` ItemDesignation (types.ts:4547 / InventoryTab.tsx:706 / ItemDetail.tsx:495 / build.ts:6477).
scripts/wg-identity.mjs, theirIdentities() lines 88-93: skip the select titled with their resonant-investment question (suppress on THEIR side, never by adding 'yes'/'no' to ours.options — substring matching in `contains()` would then let 'no' satisfy 'nature'). Mirrors wg-diff.mjs:616-627.
---- FIX [settle] reader: SETTLED_IDENTITIES in scripts/wg-identity.mjs.
FALLBACK ONLY: SETTLED_IDENTITIES in scripts/wg-identity.mjs:426, `'aeon-stone-formulating': ['spells', 'options']`. Not recommended — per-id settle for a comparer-wide blind spot.

########## aeon-stone-clear-quartz-octagon [b16 MATCHES] verify=CONFIRMED
---- FIX [src-edit] reader: scripts/wg-identity.mjs ourIdentities(). Shipped reader for the value: src/rules/build.ts:6477-6482, rendered at src/sheet/SpellsTab.tsx:1792-1899.
scripts/wg-identity.mjs, ourIdentities(), after line 200: add `for (const s of rec.resonant?.innateSpells ?? []) addSpell(s.spellId ?? s);`.
---- FIX [src-edit] reader: scripts/wg-identity.mjs theirIdentities(). Shipped carrier: `wayfinder-slotted` ItemDesignation (types.ts:4547 / InventoryTab.tsx:706 / ItemDetail.tsx:495 / build.ts:6477).
scripts/wg-identity.mjs, theirIdentities() lines 88-93: skip the select titled with their resonant-investment question, mirroring wg-diff.mjs:616-627. Suppress on their side only.
---- FIX [none] reader: n/a — observation only.
activationCost {actions:1} vs the printed "automatically activates" — flagged for a separate look, deliberately not fixed here: it is outside this identity gate and their side encodes nothing to differ against.
---- CORRECTED FIX:
Apply the two src-edits as written, with one required addition and one scope note.

1. scripts/wg-identity.mjs, ourIdentities(), after line 200 (`for (const s of rec.innateSpells ?? []) addSpell(s.spellId ?? s);`) add, in the shape of its neighbour at line 209:

   /* …and an aeon stone's RESONANT power, one level down. wg-diff already descends into `resonant`
    * (wg-diff.mjs:616-627); this reader stopped at the top level, so 14 stones whose spell lives at
    * resonant.innateSpells read as granting nothing. Shipped reader: build.ts:6498-6501. */
   for (const s of rec.resonant?.innateSpells ?? []) addSpell(s.spellId ?? s);

2. scripts/wg-identity.mjs, theirIdentities(), in the `else if (op?.type === 'select')` branch at lines 89-93, skip THIS ONE SELECT — per-select, not per-record:

   } else if (op?.type === 'select') {
     /* "Is this granting the resonant power?" is their shape for a state ours holds as the
      * `wayfinder-slotted` designation (InventoryTab.tsx:706, field-gated at ItemDetail.tsx:491-495,
      * read at build.ts:6498). Same question, different control — the equivalence wg-diff.mjs:616-627
      * already records. The title occurs on 30 item rows, all aeon stones, and 0 ability_block rows.
      * Skipped per-SELECT so Amplifying's second select (its tradition picker) still compares; and
      * because flattenOps hoists nested ops as siblings, the Yes branch's giveSpell still reaches the
      * spells bucket, so this hides no grant. */
     if (/granting the resonant power/i.test(String(d.title ?? ''))) continue;
     for (const o of d.optionsPredefined ?? []) { … unchanged … }

   Note `continue` requires the loop body to be a plain for-of over the flattened ops (it is), and `d`
   is already bound at the top of the loop.

3. REQUIRED, and missing from the original fix list: delete the now-stale settle at scripts/wg-identity.mjs:876,
   `'aeon-stone-western-star': ['options'],` together with its comment block at lines 869-875. Step 2
   makes it match nothing, and scripts/wg-settle-stale.mjs exists precisely to report such an entry for
   deletion ("A SETTLE THAT SILENCES NOTHING IS A TRAP WAITING TO SPRING") — leaving it in place would
   silence the NEXT, unrelated options difference on that stone, unread. Follow the file's own convention
   for retired entries, e.g.:
   /* aeon-stone-western-star — settle REMOVED: the resonant-power select is now skipped for every aeon
    * stone in theirIdentities, so this per-record entry answers nothing. Re-report if it returns. */

4. Correct the drifted citations when recording this: build.ts:6498-6501 (not 6477-6482) and
   types.ts:4573 (not 4547). InventoryTab.tsx:706, ItemDetail.tsx:491-495 and SpellsTab.tsx:1792-1899 are right.

5. Unchanged from the original: the activationCost {actions:1} vs printed "automatically activates"
   question stays kind:'none' — out of this gate, and their side encodes nothing to differ against.

After applying, re-run `node scripts/wg-identity.mjs --ids <the 34 aeon-stone ids> --raw` and confirm the
surviving lines are the genuine gaps only (aeon-stone-mottled-ellipsoid grants=[voidhealing],
aeon-stone-agate-ellipsoid), then `node scripts/wg-settle-stale.mjs` to confirm no settle was stranded.

########## aeon-stone-preserving [b16 MATCHES] verify=CONFIRMED
---- FIX [src-edit] reader: scripts/wg-identity.mjs ourIdentities(). Shipped reader for the value: src/rules/build.ts:6477-6482, rendered at src/sheet/SpellsTab.tsx:1792-1899.
scripts/wg-identity.mjs, ourIdentities(), after line 200: add `for (const s of rec.resonant?.innateSpells ?? []) addSpell(s.spellId ?? s);`.
---- FIX [src-edit] reader: scripts/wg-identity.mjs theirIdentities(). Shipped carrier: `wayfinder-slotted` ItemDesignation (types.ts:4547 / InventoryTab.tsx:706 / ItemDetail.tsx:495 / build.ts:6477).
scripts/wg-identity.mjs, theirIdentities() lines 88-93: skip the select titled with their resonant-investment question, mirroring wg-diff.mjs:616-627. Suppress on their side only.
---- FIX [none] reader: n/a — observation only.
frequency {max:1, per:'hour'} has no basis in the printed clause and no counterpart in their row — flagged for a separate look, outside this identity gate.
---- CORRECTED FIX:
[{"kind":"src-edit","detail":"scripts/wg-identity.mjs, ourIdentities(), immediately after line 200 (`for (const s of rec.innateSpells ?? []) addSpell(s.spellId ?? s);`): add `for (const s of rec.resonant?.innateSpells ?? []) addSpell(s.spellId ?? s);` with a comment naming the carrier — an aeon stone's resonant power is a second innate grant that applies only while the stone is slotted in a wayfinder. UNCHANGED FROM THE FINDING; verified unique (resonant is unread in this file), scoped (33 records, all aeon stones, no other bucket) and matched to its neighbours (enhancement?.grant?.innateSpells, line 209).","reader":"scripts/wg-identity.mjs ourIdentities(). Shipped reader for the value: src/rules/build.ts:6498-6503 (gated on inv.designations.includes('wayfinder-slotted')); rank-0 routing at build.ts:6586; rendered at src/sheet/SpellsTab.tsx:1838-1841."},{"kind":"src-edit","detail":"scripts/wg-identity.mjs — settle the yes/no residue. The finding's version (skip their select in theirIdentities(), lines 88-93) is SAFE and I verified it: the title occurs on exactly 30 rows, all Aeon Stones, and flattenOps still yields the nested giveSpell and Amplifying's nested tradition select, so nothing real is hidden. PREFER THE MIRROR DIRECTION INSTEAD, because it silences nothing of theirs and matches the sibling comparer exactly: in ourIdentities(), beside the new resonant line, add `if (rec.resonant) { out.options.add(key('yes')); out.options.add(key('no')); }` with the comment that wg-diff.mjs:617-632 already credits the same toggle on our side (kinds.add('choice')) — their select is 'is this stone slotted in a wayfinder?', ours is the `wayfinder-slotted` per-item designation the player toggles in play. Note for whichever is chosen: the finding's citation 'wg-diff.mjs:616-627' is wrong on both the range (617-632) and the direction (wg-diff credits OUR side, it does not suppress theirs), and a their-side skip is invisible to scripts/settle-divergence-audit.mjs, which reads only the SETTLED_* registries.","reader":"scripts/wg-identity.mjs. Shipped carrier: ItemDesignation 'wayfinder-slotted' — src/rules/types.ts:4573, offered at src/sheet/InventoryTab.tsx:706, filtered to resonant-bearing records at src/sheet/ItemDetail.tsx:491-495, read at src/rules/build.ts:6498."},{"kind":"none","detail":"OUT OF GATE #1 — frequency {max:1, per:'hour'} on items/aeon-stone-preserving has no basis in the printed clause and no counterpart in their row. Stronger than the finding stated: src/rules/itemUses.ts counterDefs (lines 32-41) synthesises a live counter {id:'freq', label:'per hour', max:1, resetsOnRest:true} from that field, so the stone currently shows a phantom once-per-hour activation tracker on the inventory card.","reader":"src/rules/itemUses.ts:32-41 -> itemCounters()."},{"kind":"none","detail":"OUT OF GATE #2 — NEW, missed by the finding, which described it as 'ours twice over'. items/aeon-stone-preserving carries the same printed resistance on BOTH carriers with DIFFERENT type spellings: passiveEffects.resistances [{type:'persistent damage', value:3}] and top-level resistances [{type:'persistent', value:3}]. src/rules/derive.ts:2358-2377 pushes both the passiveEffects object and the top-level record as defense sources, and derive.ts:2484 does res.set(r.type, ...) on the raw string with no normalisation, so the two spellings are two map keys and the sheet renders TWO resistance rows for one printed resistance. Only two items in the corpus carry both carriers; the other (rebounding-breastplate) uses matching spellings and collapses correctly. Either drop one row or make the spellings agree.","reader":"src/rules/derive.ts:2358-2377 (both sources pushed) and derive.ts:2484 (res.set keyed by raw type); output at derive.ts:2653."}]

########## aeon-stone-agate-ellipsoid [b16 MISMATCH] verify=CONFIRMED
---- FIX [src-edit] reader: public/core-descriptions.json items['aeon-stone-agate-ellipsoid'].d, rendered as the item description through RichText in src/sheet/ItemDetail.tsx — the live player surface for this clause.
Add a row to the REPAIRS table in scripts/apply-import-damaged-text.mjs — the lane that owns import-deleted prose and the only writer that updates BOTH the durable overlay row and public/core-descriptions.json (its header: "the overlay row is the DURABLE record (it survives `npm run data`) and public/core-descriptions.json is written directly so the repair is live now. Both, or the fix is either invisible or temporary."). Row: { category: 'items', id: 'aeon-stone-agate-ellipsoid', find: 'always succeed at the to give an answer other than', replace: 'always succeed at the DC 6 flat check to give an answer other than' }. The `find` string occurs exactly once in the shipped description, which is that script's stated precondition. Wording taken from the AoN mirror (Paizo/ORC), NOT from their injectText.
---- FIX [src-edit] reader: scripts/backfill-aeon-resonance.mjs (lane owner) -> scripts/data/effect-backfill.json {"category":"items","id":"aeon-stone-agate-ellipsoid","field":"resonant"} -> public/core.json.
THEN re-run `node scripts/backfill-aeon-resonance.mjs --write` so the derived `resonant` overlay row picks the repaired clause back up. Required and order-dependent: that script reads the clause out of core-descriptions.json, so repairing the description without re-running it leaves the effect-backfill `resonant` row still carrying "succeed at the to give an answer". Do NOT hand-edit the `resonant` row in scripts/data/effect-backfill.json — it is derived, and hand-editing it would be overwritten on the next lane run.
---- FIX [src-edit] reader: scripts/wg-identity.mjs theirIdentities(). Shipped carrier: `wayfinder-slotted` ItemDesignation (types.ts:4547 / InventoryTab.tsx:706 / ItemDetail.tsx:495 / build.ts:6477).
scripts/wg-identity.mjs, theirIdentities() lines 88-93: skip the select titled with their resonant-investment question, mirroring wg-diff.mjs:616-627. Suppress on THEIR side only — do not add 'yes'/'no' to ours.options, because `contains()` substring-matches and a bare 'no' would satisfy 'nature', silencing aeon-stone-amplifying's genuine [arcana, nature, occult, religion] gap. This is the only comparer change agate needs; the `resonant.innateSpells` descent is irrelevant here because agate's augury is already top-level.
---- FIX [none] reader: n/a — cross-investigation note.
FAMILY-WIDE PATTERN, for reconciliation with the separate aeon-stone-western-star (batch 17) investigation — stated, not acted on. Running wg-identity across all 25 aeon-stone records reports the SAME two buckets on 22 of them, from the two comparer blind spots above and nothing else. The family splits in exactly two: stones whose resonant power IS a spell (repairing, formulating, clear-quartz-octagon, preserving, pale-lavender-ellipsoid, pink-rhomboid, black-pearl, cymophane-cabochon, delaying, envisioning, nourishing, smoothing, sprouting, crescent, mottled-ellipsoid) report BOTH `spells` and `options` and need both comparer fixes; stones whose resonant power is note-only (agate-ellipsoid, western-star, rainbow-prism, amber-sphere, vital-amplification, flickering, amplifying) report `options` ALONE and need only the select-suppression fix. WESTERN-STAR SITS IN THE SECOND GROUP: it reports `options theirs-not-ours=[no, yes]` and nothing else, because its illusory-disguise grant is at top-level `innateSpells` (at-will) where the collector already reads it, and its resonant power (rendering stones and wayfinder invisible) is correctly a note. So the select-suppression fix closes western-star's identity gate on its own, and neither investigation should propose a data change for it on this evidence. Two family residues are NOT explained by the comparer and stay open for their own batches: aeon-stone-mottled-ellipsoid (grants=[voidhealing], spells=[falsevitality]) and aeon-stone-amplifying (options=[arcana, nature, occult, religion] — the tradition picker its resonant +2 item bonus needs, in a separate nested select we do not model).
---- CORRECTED FIX:
Keep fixes #1 and #2 as written, with one addition, and REPLACE fix #3.

FIX 1 (unchanged, verified). Add to the REPAIRS table in scripts/apply-import-damaged-text.mjs:
{ category: 'items', id: 'aeon-stone-agate-ellipsoid', find: 'always succeed at the to give an answer other than', replace: 'always succeed at the DC 6 flat check to give an answer other than' }
Verified: exactly 1 occurrence in public/core-descriptions.json and 1 in public/core.json; desc.items['aeon-stone-agate-ellipsoid'].d is a string so the script's fail() guard passes; wording transcribed from the AoN mirror (Paizo/ORC), not from their injectText.

FIX 2 (extended). Run, in this order:
  node scripts/apply-import-damaged-text.mjs
  node scripts/backfill-aeon-resonance.mjs --write
  node scripts/apply-backfill-now.mjs
The third command is the addition the finding omitted. backfill-aeon-resonance --write only rewrites the row in scripts/data/effect-backfill.json; without replaying the overlay, shipped public/core.json keeps resonant.note = "succeed at the to give an answer". apply-backfill-now.mjs is idempotent, routes description rows to core-descriptions.json (which apply-import-damaged-text.mjs has already written identically), and is the documented in-loop substitute for npm run data. Do not hand-edit the resonant row in effect-backfill.json — it is derived.

FIX 3 (REPLACED — do NOT edit theirIdentities()). Add a per-record settle to SETTLED_IDENTITIES in scripts/wg-identity.mjs, immediately beside the existing 'aeon-stone-western-star': ['options'] entry at line 877, with a comment in the same form:
  /* AEON STONE (AGATE ELLIPSOID) — same shape as Western Star above: their two options are the
   * question "Is this granting the resonant power?" (Yes/No), and BOTH branches run the identical
   * `giveSpell 4406 INNATE rank 2 DIVINE casts 1`. The Yes branch adds one `injectText` and nothing
   * else. Ours asks the same question once per inventory row as the `wayfinder-slotted` designation,
   * offered only on records carrying `resonant` (ItemDetail.tsx:495) and read at build.ts:6498; the
   * spell itself is top-level `innateSpells: [augury, divine, rank 2, 1/day]`, which is why no
   * `spells` gap is reported. This stone's resonant power grants no extra spell, so `resonant.note`
   * holds the clause and the printed description ends with it. Two options, one mechanic. */
  'aeon-stone-agate-ellipsoid': ['options'],
Rationale for the replacement: the finding's structural suppression inside theirIdentities() would silence this select for every stone at once, which makes the already-shipped western-star settle match nothing — and scripts/wg-settle-stale.mjs:44 parses SETTLED_IDENTITIES specifically to report settles that answer nothing for deletion ("a settle that silences nothing is a trap"). A per-record settle stays auditable by that script, matches the neighbour exactly, and regresses nothing. It also sidesteps the finding's contains() concern entirely, since nothing is added to ours.options. (Note scripts/wg-identity.mjs is untracked — git status reports "?? scripts/wg-identity.mjs" — so this edit is to a working-tree-only file either way.)

FIX 4: unchanged, non-actionable, but correct one detail — aeon-stone-western-star does NOT currently report an options gap under the default gate (it prints "ok, 3 identities agree") because its settle already exists at wg-identity.mjs:877; it reports [no, yes] only under --raw. No data change for western-star on this evidence, as the finding says.

########## skysunder [b16 ASK-OWNER] verify=ASK-OWNER
---- FIX [none] reader: work/owner-questions.json open[]; read by scripts/wg-batch-gate.mjs stage 6 (PARITY)
Park it, do not resolve it. `node scripts/add-owner-question.mjs --id skysunder --batch 16 --printed "rules-1101 'this results in a relic gaining one gift for every 4 levels its bearer has' + Table 2-22 (gift 2 at 5th, 3 Major at 9th, 4 Major at 13th, 5 Grand at 17th); equipment-2660 'Minor Gifts deadly spark, divine retribution, word of faith … Major Gifts holy light, propelling winds'" --theirs "WG item 17353: five unconditional giveAbilityBlock ops (28836/28863/28865/23261/28847) on a level-4 item, no conditional and no select" --ours "items.skysunder carries builtInRunes only; no gift carrier exists and relicAspects has no reader" --question "WG grants all five gifts at once on a level-4 relic; print gates them at one per four levels and leaves the choice to the GM. Adopt WG, or model the printed pool and let the table pick? Note WG's Holy Light points at the Knights of Lastwall class feat (ability_block 23261), not relic-16." --write`. Record stays as-is until ruled. Same question covers sparkwarden, uniter-of-clans and adamantine-echo — one ruling settles all four.
---- FIX [overlay] reader: src/sheet/ItemDetail.tsx:652 <DescBody description={item.description} …>
INDEPENDENT OF THE RULING (print-sourced, grants nothing, does not close the IDENTITY gate). Row {category:'items', id:'skysunder', field:'description', value:'<existing sentence> + the printed Forming the Bond / Advancing the Bond / Minor Gifts / Major Gifts block, transcribed from C:/wonderers guide/aon-2e-archive/data/by-category/equipment/equipment-2660.json — NOT from WG'}. apply-backfill-now.mjs routes description rows to public/core-descriptions.json. Today the player cannot even read which gifts this relic may take.
---- FIX [none] reader: n/a — record-creation gap, no reader until the owner rules on corpus scope
Corpus-boundary question to raise alongside: Skysunder's second Major Gift, Holy Light (AoN relic-16, Gamemastery Guide p.98, Aspect Celestial), exists only in the legacy GMG set, which our corpus deliberately excludes (0 of 238 relic gifts come from Gamemastery Guide). If the owner wants the printed pool represented, this one needs an overlay create row — e.g. {category:'items', id:'holy-light-relic', create:true, value:{name:'Holy Light', level:0, bulk:0, itemType:'equipment', rarity:'common', traits:['evocation'], relicAspects:['Celestial'], source:{book:'Pathfinder Gamemastery Guide'}, aonId:'relic-16'}} — keyed 'holy-light-relic' because 'holy-light' is already taken by feats/holy-light and spells/holy-light. Its activation is a 30-foot emanation, so the effect itself is out of scope under Ruling M (auras positional).
---- EVIDENCE:
BATCH 16 (work/wg-batch-016.json, 100 records; parity verdict in work/wg-batch-016-parity.json was THEY-ENCODE-NOTHING-USEFUL, "All five of their ops are opaque ability-block grants").

WHY IT IS OPEN NOW — A NEW COMPARER, NOT A REGRESSION AND NOT A DROPPED FIELD. `git status --porcelain` reports `?? scripts/wg-identity.mjs` and `?? scripts/wg-batch-gate.mjs`: both are untracked, i.e. brand-new machinery, and `git log --follow -- scripts/wg-identity.mjs` has no history at all. wg-identity.mjs's own header states the case: wg-diff compares KINDS, wg-values compares NUMBERS and SETS, "Neither compares WHICH THING… it has been invisible." The gate's stage 3 (scripts/wg-batch-gate.mjs:101-106, "IDENTITY: N record(s) grant something we do not") is that new screen. On the two screens batch 16 actually ran, a `giveAbilityBlock` whose target ability_block has an EMPTY `operations` column flattens (parseOps → flattenOps) to an op with no variable, value or target — which is exactly why the batch-16 reader called it "opaque" and wrote THEY-ENCODE-NOTHING-USEFUL. The identity comparer resolves `abilityBlockId` → ability_block.name and the grant stops being opaque. The gap always existed. Corroboration that nothing regressed: the last five `git log -- public/core.json` commits (newest 035a2a8, 2026-08-18) touch subclass pages / display trees, none of these ids; and every batch-16 fix for this family is still present in core.json (skysunder builtInRunes, uniter-of-clans passiveEffects, adamantine-echo runes.actsAs, spirit-dwelling create row).

GATE OUTPUT (`node scripts/wg-identity.mjs --ids skysunder,… --verbose`): "skysunder (Skysunder) grants theirs-not-ours=[deadlyspark, divineretribution, wordoffaith, holylight, propellingwinds] ours=[(nothing)]". No SETTLED_IDENTITIES entry exists for any of the seven (wg-identity.mjs:426-500), so nothing is being masked.

WHAT THE FAMILY IS: relic seeds. All 25 gift names across the six relics resolve on our side (items/deadly-spark, items/word-of-faith, items/feral-claws … each carrying `relicAspects`), 238 such gift items ship. But `relicAspects` (src/rules/types.ts:3288-3295) has NO reader — `grep -rn relicAspects src/ scripts/` hits only the type declaration and scripts/backfill-relic-gift-aspects.mjs. THERE IS NO RELIC-GIFT LANE: nothing in src/ joins a relic to its gifts, docs/mechanic-lanes.md has no relic entry, and the only item-granted-action lane, `grantsActions` (types.ts:270-290, reader src/sheet/MainTab.tsx:265-270), resolves ids against `content.actions` — the gifts live in `content.items`, so it cannot carry them as they stand.

WHY ASK-OWNER (rule 2): WG hands a LEVEL-4 relic all five gifts unconditionally; print says a relic gains one gift per 4 levels and Table 2-22 puts gift 5 at 17th level. This is the identical contradiction the owner already holds a REFUSED-fix record for under `avernal-cape` in work/owner-questions.json (the reviewer there quotes rules-1101 and Table 2-22 verbatim). Not parked under this id.

TWO EXTRA FACTS SPECIFIC TO SKYSUNDER: (1) WG's Holy Light target is the WRONG RECORD — ability_block 23261 carries traits Champion (legacy)/Cleric/Concentrate/Divine/Light and its description is the Knights of Lastwall class feat (our feats/holy-light, aonId feat-3639), not the relic Major Gift, which is AoN relic-16 "Relic Major Gift, Aspect Celestial, Evocation/Good". Even if the owner rules in WG's favour on the grants, this target must not be adopted. (2) We hold NO record for relic-16: the archive name index shows "holy light → relic-16 [Gamemastery Guide]" with no remaster twin, and our corpus ships ZERO Gamemastery Guide relic gifts (census of the 238: GM Core 141, Treasure Vault (Remastered) 49, Secrets of Magic 22, Highhelm 12, Rage of Elements 10, AP #193 2, AP #200 2). So Skysunder's second Major Gift has no record on our side by corpus policy, not by defect.

PROSE: our shipped description (public/core-descriptions.json items.skysunder) is one sentence — the printed Forming the Bond / Advancing the Bond / Minor Gifts / Major Gifts block was stripped. Measured across every relic item whose AoN mirror has a "Minor Gifts" line: 23 relics, 12 keep the block, 11 lost it — adamantine-echo, skysunder, sparkwarden, uniter-of-clans, avernal-cape, kalmaugs-journal, monarch, bountys-light, lifebloom, reapers-sigh, woe.

########## sparkwarden [b16 ASK-OWNER] verify=ASK-OWNER
---- FIX [none] reader: work/owner-questions.json open[]; read by scripts/wg-batch-gate.mjs stage 6 (PARITY)
Park it. `node scripts/add-owner-question.mjs --id sparkwarden --batch 16 --printed "equipment-2661 'Minor Gifts create spark, obscure, shadow smith / Major Gifts dancing shadow, elements of creation'; rules-1101 'this results in a relic gaining one gift for every 4 levels its bearer has' + Table 2-22" --theirs "WG item 17354: five unconditional giveAbilityBlock ops (29720/29726/29727/29728/29685), no conditional and no select, on a level-5 item" --ours "items.sparkwarden models only the once-per-day reaction (situationalBonuses.ts:1805); no gift carrier exists" --question "Adopt WG's unconditional grant of all five gifts, or model the printed pool and leave the pick to the table?" --write`. Or fold it into one combined ruling with skysunder / uniter-of-clans / adamantine-echo, since the question is word-for-word the same.
---- FIX [overlay] reader: src/sheet/ItemDetail.tsx:652 <DescBody description={item.description} …>
INDEPENDENT OF THE RULING, grants nothing, does not close the IDENTITY gate: {category:'items', id:'sparkwarden', field:'description', value:'<existing text> + the printed Forming the Bond / Advancing the Bond / Minor Gifts / Major Gifts block transcribed from equipment-2661.json (print, not WG)'}. Routed to public/core-descriptions.json by scripts/apply-backfill-now.mjs.
---- EVIDENCE:
BATCH 16. Prior parity verdict THEY-ENCODE-NOTHING-USEFUL ("All five of their ops are opaque ability-block grants with no flattened stat, value or target").

WHY OPEN NOW: same cause as skysunder — the untracked new comparer scripts/wg-identity.mjs (git status `??`, no git history) added as stage 3 of the equally-untracked scripts/wg-batch-gate.mjs. The old verdict was written on the KINDS+VALUES screens, where a giveAbilityBlock whose target ability_block has an empty `operations` column flattens to nothing; the identity comparer resolves abilityBlockId → name. Pre-existing gap, no regression, no dropped field (the record's situationalBonuses entry and its counters/frequency are all still in place).

GATE OUTPUT: "sparkwarden (Sparkwarden) grants theirs-not-ours=[creativespark, obscure, shadowsmith, dancingshadow, elementsofcreation] ours=[(nothing)]". No SETTLED_IDENTITIES entry.

WHY ASK-OWNER (rule 2): WG grants all five gifts unconditionally on a level-5 relic; print gives it two gifts at 5th at most (Table 2-22) and reserves the choice for the GM. Identical to the contradiction the owner already holds under `avernal-cape`. NO SETTLE IS POSSIBLE HERE: I read the raw op tree on both sides and our side carries no carrier for the gift set at all — the sparkwarden situationalBonuses row is the item's own once-per-day reaction, not any of the five gifts.

SECONDARY, ALSO CONTESTED-ADJACENT (do not fix under this gate): their Obscure block encodes an addBonusToValue on SKILL_STEALTH with the `value` key ABSENT — a text-only rider, which is our star lane by Ruling on conditional values, and belongs to items/obscure rather than to sparkwarden.

PROSE: items.sparkwarden's shipped description keeps the Activate/Frequency/Trigger/Effect block but drops the Forming the Bond / Advancing the Bond / Minor Gifts / Major Gifts lines (one of the 11 of 23 relics measured to have lost it).

########## uniter-of-clans [b16 ASK-OWNER] verify=ASK-OWNER
---- FIX [none] reader: work/owner-questions.json open[]; read by scripts/wg-batch-gate.mjs stage 6 (PARITY)
Park it. `node scripts/add-owner-question.mjs --id uniter-of-clans --batch 16 --printed "equipment-2662 'Minor Gifts ancestors' call, linguistic nexus, repository of knowledge (Dwarf Lore, Genealogy Lore, Warfare Lore) / Major Gifts perception filter, or increase the diplomat's badge's item bonus to Diplomacy to +2'; rules-1101 Table 2-22 puts a relic at 2 gifts at 5th and its first Major at 9th" --theirs "WG item 17359: addBonusToValue SKILL_DIPLOMACY 1 (already matched) plus four unconditional giveAbilityBlock ops 29708/29709/29711/29712 on a level-5 item; the level gating in their tree is inside the gifts' own scaling, never on whether the relic has the gift; the either/or Major Gift is not modelled as a choice" --ours "items.uniter-of-clans carries passiveEffects.skills.diplomacy=1 and nothing that names a gift" --question "Adopt WG's unconditional grant of all four gifts, or model the printed pool (and the either/or Major Gift) and leave the pick to the table?" --write`.
---- FIX [overlay] reader: src/sheet/ItemDetail.tsx:652 <DescBody description={item.description} …>
INDEPENDENT OF THE RULING, grants nothing, does not close the IDENTITY gate: {category:'items', id:'uniter-of-clans', field:'description', value:'<existing text> + the printed Forming the Bond / Advancing the Bond / Minor Gifts / Major Gifts block from equipment-2662.json (print, not WG)'}. The either/or Major Gift is the part a player most needs to read and it is currently nowhere on our side.
---- EVIDENCE:
BATCH 16. Prior parity verdict FIXED — and that verdict is NOT what re-opened: the gate's VALUES screen still agrees on SKILL_DIPLOMACY = 1. What re-opened is the newly-added IDENTITY screen, on the four gift grants that no comparer had ever looked at.

WHY OPEN NOW: scripts/wg-identity.mjs and scripts/wg-batch-gate.mjs are both untracked (`git status --porcelain` → `??`), with no git history — new machinery, stage 3 of the gate. wg-identity's header: wg-diff compares KINDS, wg-values NUMBERS and SETS, "Neither compares WHICH THING… it has been invisible." Pre-existing gap; nothing regressed and nothing was dropped (the batch-16 passiveEffects fix is intact in both core.json and the overlay).

GATE OUTPUT: "uniter-of-clans (Uniter of Clans) grants theirs-not-ours=[ancestorscall, linguisticnexus, repositoryofknowledge, perceptionfilter] ours=[(nothing)]". No SETTLED_IDENTITIES entry.

WHY ASK-OWNER (rule 2): WG hands a level-5 relic all four named gifts unconditionally. Print's Table 2-22 allows two gifts at 5th and puts the first Major (perception filter) no earlier than 9th, and rules-1101 reserves the pick: "You decide what gifts a relic gains…". Same contradiction the owner already holds under `avernal-cape`.

NO SETTLE POSSIBLE: raw op trees read on both sides — theirs is four resolvable giveAbilityBlock ops, ours has no field anywhere that names a gift. Note also that on print the second Major Gift is an EITHER/OR ("perception filter, or increase the diplomat's badge's item bonus to Diplomacy to +2"), which WG does not model as a choice at all — their Diplomacy op is value 1, the base diplomat's badge bonus, not the gift's upgrade to +2.

PROSE: items.uniter-of-clans keeps the Activate/Frequency/Effect block but lost the Forming/Advancing/Minor Gifts/Major Gifts lines (one of the 11 of 23).

########## kalmaugs-journal [b16 ASK-OWNER] verify=ASK-OWNER
---- FIX [none] reader: work/owner-questions.json open[]; read by scripts/wg-batch-gate.mjs stage 6 (PARITY)
NO FIX. The owner holds an open record for this id (work/owner-questions.json open[], {id:'kalmaugs-journal', batch:16}) whose proposed fix was already refused on review. The gift-set half is the same parked WG-vs-print contradiction recorded under avernal-cape. When the owner is next asked, add one line to the existing kalmaugs-journal entry noting that the identity screen also raises the three gift grants (29505 Ease Burden / 28865 Word of Faith / 29509 Underground Bounty), so he rules on the whole record at once rather than twice.
---- EVIDENCE:
BATCH 16 — and one of the TWO records in that batch that were NEVER GIVEN A PARITY VERDICT: work/wg-batch-016.json holds 100 ids, work/wg-batch-016-parity.json holds 94 records, and kalmaugs-journal and avernal-cape are the absentees. That is the gate's stage-6 "PARITY: N of M record(s) never compared" (scripts/wg-batch-gate.mjs:300), firing on the same record as the new stage-3 IDENTITY screen.

⚠ THE OWNER HOLDS A RECORD FOR THIS ID — NO FIX PROPOSED. work/owner-questions.json `open[]` contains {id:'kalmaugs-journal', batch:16}, a fix that was PROPOSED AND REFUSED on review ("REFUTED on the LANE CHECK, not on the arithmetic"). Per the brief and rule 2, the record stays as it is.

RECONCILING THE TWO SUBJECTS, precisely: the PARKED question's subject is the heldSpells RANK — glowing-trail is a rank-1 spell (AoN spell-1428) filed under key "0", the cantrip rank. The IDENTITY gate's subject is the GIFT SET (easeburden, wordoffaith, undergroundbounty). They are different fields. But the gift-set question is not a new question either: it is word-for-word the WG-vs-print contradiction the owner already holds under `avernal-cape` (WG grants every named gift unconditionally; rules-1101 gates them at one per four levels). So both halves of this record are covered by a parked question and neither is mine to decide.

WHY OPEN NOW: same cause as the rest of the family — scripts/wg-identity.mjs and scripts/wg-batch-gate.mjs are untracked (`git status` → `??`) with no git history; the IDENTITY bucket is new and never ran against batch 16. Pre-existing gap, not a regeneration loss and not a regression: this id has no overlay row at all and no code registry touches it, so there was nothing that could have been dropped.

GATE OUTPUT: "kalmaugs-journal (Kalmaug's Journal) grants theirs-not-ours=[easeburden, wordoffaith, undergroundbounty] ours=[(nothing)]". No SETTLED_IDENTITIES entry.

PROSE NOTE (for the owner's file, not a fix): items.kalmaugs-journal's shipped description keeps the spell list and the daily-preparation clause but drops the Forming/Advancing/Minor Gifts/Major Gifts block — one of the 11 of 23 relics measured to have lost it.

########## adamantine-echo [b16 ASK-OWNER] verify=ASK-OWNER
---- FIX [none] reader: work/owner-questions.json open[]; read by scripts/wg-batch-gate.mjs stage 6 (PARITY)
Park it. `node scripts/add-owner-question.mjs --id adamantine-echo --batch 16 --printed "equipment-2659 'Minor Gifts beginner's luck, ease burden, shattered earth / Major Gifts raise ramparts, steal luck'; rules-1101 'this results in a relic gaining one gift for every 4 levels its bearer has' and Table 2-22 (1st/5th/9th/13th/17th)" --theirs "WG item 17349: five unconditional giveAbilityBlock ops 29695/29505/29507/29508/29699 on a level-5 item, no conditional and no select; the only level gating in their tree is inside Ease Burden's own BULK_LIMIT_BONUS scaling" --ours "runes.adamantine-echo carries the +1 potency (actsAs, read at derive.ts:1721-1729); nothing on either record names a gift" --question "Adopt WG's unconditional grant of all five gifts including both Major Gifts on a level-5 relic, or model the printed pool and leave the pick to the table?" --write`. One combined ruling with skysunder / sparkwarden / uniter-of-clans would settle all four.
---- FIX [overlay] reader: src/sheet/ItemDetail.tsx:652 <DescBody description={item.description} …>
INDEPENDENT OF THE RULING, grants nothing, does not close the IDENTITY gate: {category:'items', id:'adamantine-echo', field:'description', value:'<existing potency sentence> + the printed Forming the Bond / Advancing the Bond / Minor Gifts / Major Gifts block from equipment-2659.json (print, not WG)'}.
---- CORRECTED FIX:
Keep the park; drop the second fix entirely.

SOLE ACTION — one owner question, enriched with the level facts:

node scripts/add-owner-question.mjs --id adamantine-echo --batch 16 --printed "equipment-2659 (Pathfinder #193 p.83, 'Item 5'): 'Minor Gifts beginner's luck, ease burden, shattered earth / Major Gifts raise ramparts, steal luck'. Governing rule, AoN 'Advancing a Relic' (GM Core p.309, rules-1101): 'Generally speaking, this results in a relic gaining one gift for every 4 levels its bearer has'; Table 2-22 minimum levels 1st/5th/9th/13th/17th (gifts 3 and 4 are Major, at 9th and 13th); and 'You decide what gifts a relic gains, generally by either selecting a single gift or offering two paths for the relic to grow and allowing the player to choose' — the pick is explicitly GM-reserved. A level-5 bearer should therefore have 2 Minor gifts, not 5 including 2 Major." --theirs "WG item 17349 (level 5): five unconditional giveAbilityBlock ops 29695/29505/29507/29508/29699, no conditional and no select anywhere in the item's tree. BUT their ability blocks are themselves level-tagged in line with print: Beginner's Luck 29695 lvl 1, Ease Burden 29505 lvl 1, Shattered Earth 29507 lvl 1, Raise Ramparts 29508 lvl 9, Steal Luck 29699 lvl 9. The only operations inside any gift are Ease Burden's own five BULK_LIMIT_BONUS conditionals (2/4/6/8/10 at levels <6/6-9/10-13/14-17/18+). Same flat shape on skysunder 17353, sparkwarden 17354, uniter-of-clans 17359." --ours "runes.adamantine-echo carries the printed +1 potency via actsAs, read live at src/rules/derive.ts:1728-1736 (folded into acItem) — that clause MATCHES and is not in question. Nothing on either the item or the rune record names a gift: no grantsFeats/grantsActions/grantsItems/choice/effectChoices at any depth. The only src/ reference is derive.ts:1350 (BODY_RUNE_EXCLUDED). The one field that mentions relic gifts, relicAspects (src/rules/types.ts:3321), has ZERO readers in src/ across all 238 records carrying it — the relic-gift lane is unmodelled engine-wide, so there is no existing way to say this. All five gift records ship (items/beginners-luck relic-97, ease-burden relic-148, shattered-earth relic-150, raise-ramparts relic-151, steal-luck relic-101) but no relic reaches them." --question "Adopt WG's unconditional grant of all five gifts (including both Major Gifts) on a level-5 relic, or model the printed pool gated by Table 2-22 and leave the pick to the table? Note WG level-tags Raise Ramparts and Steal Luck at 9, so a third option is to grant all five but gate on the gift's own level. Whichever way it goes, the relic-gift lane needs a carrier with a reader — relicAspects has none. One ruling settles adamantine-echo, avernal-cape, skysunder, sparkwarden and uniter-of-clans together." --write

DO NOT apply the proposed description overlay {category:'items', id:'adamantine-echo', field:'description', …}. Its claimed reader is pre-empted: src/sheet/ItemDetail.tsx:652 passes astKey="items" astId={item.id}, and src/sheet/DescBody.tsx returns the AstRenderer branch at `if (ast && astId)` before `description` is read (`useRich = !astId || (!ast && !astLoading)`). adamantine-echo has an AST node, and the Forming the Bond / Advancing the Bond / Minor Gifts / Major Gifts paragraphs are not matched by AstRenderer's META regex nor by isMetaishBlock, so the user is ALREADY shown that block. The overlay would write a dead field and duplicate on-screen text. If the owner wants core-descriptions.json items.adamantine-echo.d backfilled for non-AST consumers, that is a separate corpus-wide question about the 11-of-23 stripped relics, not a per-record overlay.
---- EVIDENCE:
BATCH 16. Prior parity verdict MATCHES — and that verdict is NOT what re-opened. The +1 armour potency clause still matches on both the KINDS and VALUES screens; what re-opened is the newly-added IDENTITY screen, on the five gift grants no comparer had ever examined.

WHY OPEN NOW: scripts/wg-identity.mjs and scripts/wg-batch-gate.mjs are both untracked (`git status --porcelain` → `??`) and have no git history — new machinery. wg-identity's header states the bucket it added: "wg-diff.mjs compares KINDS… wg-values.mjs compares NUMBERS and SETS. Neither compares WHICH THING… it has been invisible." Pre-existing gap, not a regression and not a dropped field: both of this record's overlay rows are intact and derive.ts:1343 still names it.

GATE OUTPUT: "adamantine-echo (Adamantine Echo) grants theirs-not-ours=[beginnersluck, easeburden, shatteredearth, raiseramparts, stealluck] ours=[(nothing)]". No SETTLED_IDENTITIES entry.

WHY ASK-OWNER (rule 2): WG hands a level-5 relic all five gifts at once, including two Major Gifts. Print gates them — rules-1101 "Generally speaking, this results in a relic gaining one gift for every 4 levels its bearer has", Table 2-22 minimum levels 1st/5th/9th/13th/17th — and reserves the pick for the GM. Identical to the contradiction already parked under `avernal-cape`.

NO SETTLE POSSIBLE: raw op tree read on both sides. Theirs is five resolvable giveAbilityBlock ops; ours has no field on either the item or the rune record that names a gift, and `relicAspects` (the only field that even mentions relic gifts, types.ts:3288-3295) has no reader in src/ at all. The MATCHES verdict answered a different op (the printed potency clause, which WG encodes in the item's meta_data rather than in operations).

ADJACENT, NOT THE GATE'S FINDING: our `usage` is "etched-onto-armor" where both print and WG say "incorporated into armor" — the same wording drift the avernal-cape owner-question already notes.

PROSE: items.adamantine-echo's shipped description is the potency sentence only; the Forming/Advancing/Minor Gifts/Major Gifts block was stripped (one of the 11 of 23 relics measured).

########## avernal-cape [b16 ASK-OWNER] verify=ASK-OWNER
---- FIX [none] reader: work/owner-questions.json open[]; read by scripts/wg-batch-gate.mjs stage 6 (PARITY)
NO FIX. The owner holds an open record for this id (work/owner-questions.json open[], {id:'avernal-cape', batch:16}) covering precisely this contradiction; a fix was already proposed and refused on review. The IDENTITY gate is double-reporting it. If the gate must go quiet before the owner rules, the honest move is a stage-3 exemption keyed to the parked-question list — NOT a SETTLED_IDENTITIES entry, which would be a false settle (nothing on our side carries the mechanic) and would silence the next real difference on this record unread.
---- CORRECTED FIX:
No fix. Leave items.avernal-cape untouched and add nothing to SETTLED_IDENTITIES. If the gate must go quiet before the owner rules, the only sound route is a stage-3 IDENTITY exemption keyed to work/owner-questions.json open[] (so the suppression expires when the question is answered and still reports any NEW difference on the record), not a settle entry. Two corrections to the finding as written: (1) the batch-16 parity absentee list is six ids, not two — kalmaugs-journal, avernal-cape, astrolabe-of-falling-stars, advanced-red-mantis-magic, arcana-of-iron, instinct-ability — so the "one of the TWO records" line in EVIDENCE should be restated; (2) worth noting for the owner's file that ours has usage "etched-onto-armor" where WG and print AGREE on "incorporated into armor" — that sub-difference needs no owner ruling on its own, but it is already inside the scope of the parked record's re-verification text, so it should be resolved together with the ruling rather than split out as a separate fix.
---- EVIDENCE:
BATCH 16 — and, like kalmaugs-journal, one of the TWO records in the batch that were NEVER GIVEN A PARITY VERDICT (work/wg-batch-016.json = 100 ids, work/wg-batch-016-parity.json = 94 records; avernal-cape and kalmaugs-journal are the absentees). So the gate is raising it twice: stage 6 "PARITY: N of M record(s) never compared" (scripts/wg-batch-gate.mjs:300) and the new stage 3 IDENTITY.

⚠ THE OWNER HOLDS A RECORD FOR THIS ID — NO FIX PROPOSED, and here the parked question is EXACTLY the identity gate's subject, not an adjacent field. work/owner-questions.json `open[]` {id:'avernal-cape', batch:16} records a fix that was proposed and REFUSED, and the reviewer's stated first ground is the very contradiction the identity screen has now surfaced independently: "IT SETTLES A WG-vs-PRINT CONTRADICTION — the owner's call. WG item 17336 carries five unconditional `giveAbilityBlock` ops (28858/29523/29524/28859/29527) on a LEVEL 5 item. The printed relic rules say the opposite: … rules-1101 ('Advancing a Relic') states 'this results in a relic gaining one gift for every 4 levels its bearer has', and its Table 2-22 sets minimum levels — gift 1 at 1st, 2 at 5th, 3 (Major) at 9th, 4 (Major) at 13th, 5 (Grand) at 17th." So the IDENTITY gate is DOUBLE-REPORTING an already-parked question. Nothing to decide, nothing to author.

WHY OPEN NOW: the new comparer, same as the whole family — scripts/wg-identity.mjs and scripts/wg-batch-gate.mjs are untracked with no git history, and the IDENTITY bucket never ran against batch 16. Not a regeneration loss and not a regression: this id has no overlay row and no code registry hit, so there was nothing to drop, and the reviewer re-verified on 2026 that "items.avernal-cape in public/core.json still has no grant fields, usage is still 'etched-onto-armor', and grep over src/ returns zero hits for 'avernal'" — which is still true today.

GATE OUTPUT: "avernal-cape (Avernal Cape) grants theirs-not-ours=[feralclaws, flarebolt, heathaze, formoffury, searingwave] ours=[(nothing)]". No SETTLED_IDENTITIES entry.

PROSE NOTE (for the owner's file, not a fix): items.avernal-cape's shipped description keeps the dueling-cape sentence and its descRef but drops the Forming/Advancing/Minor Gifts/Major Gifts block — one of the 11 of 23 relics measured to have lost it.

########## spirit-dwelling [b16 MISMATCH] verify=CONFIRMED
---- FIX [overlay] reader: src/sheet/MainTab.tsx:265-270 (the item branch of `grantedActions`, which resolves each id against content.actions — collect-spirit-remnant IS in content.actions, so it resolves and reaches the encounter action list), plus src/rules/explain.ts:256
Row {category:'items', id:'spirit-dwelling', field:'grantsActions', value:['collect-spirit-remnant']} in scripts/data/effect-backfill.json. This is exact parity with their carrier: WG hangs the grant on item 13641, not on the dedication, and so does print (action-1160's Requirements is "You're holding your spirit dwelling"). It also closes the IDENTITY gate — wg-identity.mjs:187 does `for (const g of rec.grantsActions ?? []) addGrant(g)`, anyName() resolves 'collect-spirit-remnant' in the `actions` bucket to "Collect Spirit Remnant", which keys to collectspiritremnant and matches theirs exactly.
---- FIX [overlay] reader: src/rules/build.ts:7228 (`worn: g.worn ?? false`) feeding src/sheet/MainTab.tsx:265-268
REQUIRED SECOND ROW — without it the first is write-only. Row {category:'feats', id:'exorcist-dedication', field:'grantsItems', value:[{itemId:'spirit-dwelling', worn:true}]}, replacing the current [{itemId:'spirit-dwelling'}]. `worn` is a declared field on the grant (src/rules/types.ts:1025-1029, "Already worn when handed over… a few are not removable kit") read at src/rules/build.ts:7228, with the Hardshell Surki carapace as precedent. It makes `inv.worn` true on the granted instance so MainTab's `inv.equipped || inv.worn` gate passes. Data-only, no engine change. Note the semantic stretch: the dwelling is HELD, not worn, and our inventory model has no held flag for equipment-typed items.
---- FIX [src-edit] reader: src/sheet/MainTab.tsx:265-270; compare src/rules/derive.ts:241 itemInUse
ALTERNATIVE TO THE SECOND ROW, and the semantically honest one — pick this OR the worn:true row, not both. Widen the item source at src/sheet/MainTab.tsx:265-268 so an item's grantsActions is not gated on equipped/worn. The printed requirement is "You're holding your spirit dwelling", and our model has no held flag for equipment-typed items, so the equip filter is the wrong gate for this lane. Either reuse `itemInUse` (src/rules/derive.ts:241 = worn || invested || equipped || attachedTo) or drop the filter for items whose grantsActions is not equip-conditioned. The existing comment at MainTab.tsx:257-264 explains the filter was written for the RESONANT trait case ("Gated on held or worn, which is what 'while wielding' means"), so widening it needs that comment updated rather than contradicted.
---- CORRECTED FIX:
Apply TWO overlay rows in scripts/data/effect-backfill.json and DROP the proposed src-edit entirely.

ROW 1 — {"category":"items","id":"spirit-dwelling","field":"grantsActions","value":["collect-spirit-remnant"]}
Exact parity with their carrier: WG hangs the grant on item 13641, not on the dedication, and print agrees (action-1160 Requirements: "You're holding your spirit dwelling"). Precedented shape — items/wish-blade and items/wish-knife already carry grantsActions rows for conduct-energy. Closes the identity gate: wg-identity.mjs:187 does `for (const g of rec.grantsActions ?? []) addGrant(g)`, and anyName() resolves 'collect-spirit-remnant' via the `actions` bucket to "Collect Spirit Remnant" -> key collectspiritremnant, matching theirs.
PLACEMENT CONSTRAINT (not in the original brief): scripts/lib/apply-backfill.mjs:44 iterates the overlay array in strict order with no create-first pass, so this row must come AFTER the existing items/spirit-dwelling create row (currently ~line 133871). Appending at the end of the array satisfies this.
READER: src/sheet/MainTab.tsx:265-270 only. Do NOT cite src/rules/explain.ts:256 — that loop reads grantsActions from c.feats and ownedFeatureIds class features only, never from items.

ROW 2 — replace the single existing feats/exorcist-dedication grantsItems row value [{"itemId":"spirit-dwelling"}] with [{"itemId":"spirit-dwelling","quantity":1,"worn":true}]
Required, or row 1 is write-only. MainTab.tsx:265-268 gates the item source on `inv.equipped || inv.worn`; build.ts:7248 emits granted items with `worn: g.worn ?? false, equipped: false`; and no UI path can flip any flag by hand (isEquippable at InventoryTab:732-737 needs armor/weapon/shield or the invested trait, equipControl at :73-78 returns null, the Invest button at :194 needs the invested trait, and the drag handler short-circuits on `if (!isEquippable(item)) return;`). `worn` is a declared grant field (src/rules/types.ts grantsItems) with three precedents, two of them on feats (tattooed-historian-dedication, palatine-detective-dedication) plus hardshell-surki. Data-only, no engine change. Keep the brief's honest caveat in the row's rationale: the dwelling is HELD, not worn, and our inventory model has no held flag for equipment-typed items; worn is the closest existing flag and costs nothing else here, since the item has no situational bonuses, effects, activation or heldSpells — the flag only also moves it into InventoryTab's Equipped section (line 653).

DROP the third proposal (widening src/sheet/MainTab.tsx:265-268). Reusing itemInUse (derive.ts:241 = worn||invested||equipped||attachedTo) is INERT here — all four flags are false/undefined on the granted instance and unreachable by the player — so it would not surface the action at all. And dropping the filter has no declared field to key "not equip-conditioned" off, and would regress the resonant case the filter exists for: wish-blade/wish-knife would offer Conduct Energy from a backpack, contradicting "while wielding a resonant weapon" and the comment at MainTab.tsx:257-264.

After applying, re-run `node scripts/apply-backfill-now.mjs` then `node scripts/wg-identity.mjs --ids spirit-dwelling --verbose` to confirm the gate goes clean, and update the spirit-dwelling verdict in work/wg-batch-016-parity.json from THEY-ENCODE-NOTHING-USEFUL to MISMATCH-FIXED (its current evidence string, "Their only op is an opaque ability-block grant", is now known to be wrong).

Leave the trait gap (print: necromancy + the tradition trait; ours: traits []) out of this fix — it is a separate record-level correction, and adding the invested trait to make the item equippable would be unprinted and is not the right route.

########## speaker-in-training [b15 MATCHES] verify=CONFIRMED
---- FIX [settle] reader: src/rules/featCantripGrants.ts:113 -> src/rules/build.ts:6497-6513 (innate grant + per-option tradition) and src/builder/Builder.tsx:2254-2256 (the picker)
Add SETTLED_IDENTITIES['speaker-in-training'] = ['options'] in scripts/wg-identity.mjs (registry at line 426), with the reading: their option titles name the TRADITION ("Divine"/"Primal"), ours name the SPELL each tradition grants (Bless / Fleet Step) — one pick, two labels for it, and the traditions themselves are carried by traditionByOption. The comparer cannot see our options at all because wg-identity.mjs:307 credits featCantripGrants ids to out.spells, which is why the spells bucket already matches.
---- FIX [settle] reader: src/rules/build.ts:6497-6513
Add VERIFIED_EQUIVALENT['speaker-in-training'] = ['conditional','specialStat'] in scripts/wg-diff.mjs (registry at line 884). specialStat = createValue FAITHSPEAKER/GREENSPEAKER, a boolean marker written inside the select's own option — the exact case wg-diff.mjs:196-210 already settles for string markers, and the same reading as the existing 'power-suit' (INVENTOR_ARMOR), 'martial-experience' (MARTIAL_EXPERIENCE) and 'bone-magic' (BONE_MAGIC_TRADITION) entries. conditional = their heritage gate (menu-option filtering, Ruling 3) plus the two IF-marker dispatches that only exist because the answer is held in a variable rather than in the pick.
---- FIX [src-edit] reader: scripts/wg-diff.mjs kindOfTheirOp (differ only — no app data changes)
OPTIONAL, and it would remove the specialStat half of the settle above rather than paper over it: widen the option-marker guard at scripts/wg-diff.mjs:206 from `typeof op.data?.value === 'string' && !Number.isFinite(Number(op.data.value))` to also accept `typeof op.data?.value === 'boolean'`. A boolean written inside a select option is the same bookkeeping the guard's own comment describes; today only string markers are recognised.
---- FIX [src-edit] reader: scripts/duplicate-pick-check.mjs is the guard that would catch the regression; scripts/feat-doctor.mjs is what reports the orphan
OWNERSHIP, durability only: `npm run feat -- speaker-in-training` reports "no authoring script names this record — its overlay rows are orphans". The two `value:null` rows are the whole of the batch-15 fix and nothing regenerates them; if the overlay is ever rebuilt from its owning scripts the record's innateSpells/choice come back and the lane collision (two 1/day spells, Fleet Step as divine) returns. Re-home them in an authoring script that states the one-clause-one-picker reason, next to the featCantripGrants entry.
---- CORRECTED FIX:
[{"kind":"settle","detail":"Add SETTLED_IDENTITIES['speaker-in-training'] = ['options'] in scripts/wg-identity.mjs (registry opens at line 426; 'options' is an established bucket there — myriad-forms, fey-influence, sacred-spells, clan-lore and ~11 more). Verified live: `node scripts/wg-identity.mjs --ids speaker-in-training --verbose` reports `options theirs-not-ours=[divine, primal] / ours=[(nothing)]`, and reports the same under --raw, so the settle answers a real difference rather than matching nothing. Reading: their two option titles name the TRADITION (\"Divine\"/\"Primal\"); ours name the SPELL each tradition grants (Bless / Fleet Step), and the traditions themselves are carried by traditionByOption. Our options bucket reads EMPTY (not \"Bless/Fleet Step\") because wg-identity.mjs:308 credits featCantripGrants ids to out.spells only — which is also why the spells bucket already matches on both sides.","reader":"src/rules/featCantripGrants.ts:118 -> src/rules/build.ts:6525-6532 (per-option tradition; load-bearing because core.json spells['fleet-step'].traditions is [\"arcane\",\"primal\"], arcane first) and src/builder/Builder.tsx:~2256 (the picker)"},{"kind":"settle","detail":"Add VERIFIED_EQUIVALENT['speaker-in-training'] = ['conditional','specialStat'] in scripts/wg-diff.mjs (registry opens at line 888). Verified live: `node scripts/wg-diff.mjs --list --bucket they-only` reports `speaker-in-training lvl 5 missing=[conditional,specialStat] ours=[spell,choice]` — the value covers exactly the reported set. specialStat = createValue FAITHSPEAKER/GREENSPEAKER, a boolean marker written INSIDE the select's own option; wg-diff.mjs:206 already settles that shape for string markers and misses this one only because its test is `typeof value === 'string'`. Closest precedent is 'power-suit': ['specialStat'] (wg-diff.mjs:918), whose own comment describes `createValue INVENTOR_ARMOR = true` — also a boolean marker; 'bone-magic' (:1492) is the second. DO NOT cite 'martial-experience' as a neighbour: it is settled as ['unmapped'], not ['specialStat']. conditional = their heritage gate on op 1 (menu-option filtering, the owner's \"filtering the options in a picker is ours\" exception) plus the two IF-marker dispatches in ops 2/3, which exist only because their answer lives in a variable rather than in the pick. THE COMMENT MUST ALSO STATE THE UNENFORCED CLAUSE, because this settle is where a future reader will look for it: print says \"If you've already made this decision (such as due to being a budding speaker) you must select the same Speaker you previously chose\", and we do not enforce it — core.heritages['budding-speaker-centaur'] records only a divine-or-primal cantrip filter and no speaker identity, and feats['speakers-defense'] asks the same question again in its own effectChoices[speaker-kind]. Three records, three independent answers, where WG threads one variable. A shared choice.flag ('speakerPath') is the shape our vocabulary has for it if the owner ever wants it.","reader":"src/rules/build.ts:6525-6532; guarded by scripts/duplicate-pick-check.mjs, which passes today"},{"kind":"src-edit","detail":"OWNERSHIP, durability only — keep as proposed. `npm run feat -- speaker-in-training` prints, in its own words, \"⚠ no authoring script names this record. Its overlay rows are orphans\" and lists \"innateSpells = null (deletes), choice = null (deletes)\". Those two value:null rows in scripts/data/effect-backfill.json ARE the whole of the batch-15 fix and nothing regenerates them. If the overlay is ever rebuilt from its owning scripts, innateSpells/choice come back and the collision returns for real: build.ts pushes record innateSpells at :6505 BEFORE the pick lane at :6516, and the dedupe at :6549 is first-wins (`!seenInnate.has(g.spellId) && seenInnate.add(...)`), so the record lane would beat the player's pick. Re-home both rows in an authoring script that states the one-clause-one-picker reason, next to the featCantripGrants entry.","reader":"scripts/duplicate-pick-check.mjs is the guard that catches the regression; scripts/feat-doctor.mjs is what reports the orphan"},{"kind":"DROPPED","detail":"REMOVED from this finding: the optional src-edit widening the option-marker guard at scripts/wg-diff.mjs:206 from `typeof op.data?.value === 'string'` to also accept booleans. It is a GLOBAL change to the differ, and `wg-diff --list --bucket they-only` shows 7 other records currently reporting specialStat as missing (vigilant-benediction, reclaimant-plea, additional-servings, avenging-runelord-dedication, path-to-perfection, second-path-to-perfection, animal-order, cultivation-order). How many of their markers are boolean-in-option was not measured by the finding and cannot be measured without editing the differ. The registry header's own rule — \"Never a place to quiet a real gap\" — applies with more force to a guard than to a per-record settle. The settle above already resolves this id; raise the widening separately, with the blast-radius count attached."}]

########## the-moon-weavers-art [b15 MATCHES] verify=CONFIRMED
---- FIX [settle] reader: src/rules/build.ts:6640-6647 (the innate-casting entry's proficiency: maxRank(..., level >= 12 ? 'expert' : 'trained')); the pick itself is read by resolvePick at src/rules/build.ts:4861-4888
Restore VERIFIED_EQUIVALENT['the-moon-weavers-art'] = ['conditional','spellcasting'] in scripts/wg-diff.mjs, replacing the removal marker at line 1066 and sitting under the comment block at 1049-1065 that already documents it (and that already keeps its twin 'labyrinthine-echoes' at line 1064). spellcasting = their per-record adjValue SPELL_DC/SPELL_ATTACK = T; conditional = the LEVEL>=12 step to E. Both are one general rule on our side, applied to every innate entry rather than copied onto 300-odd records.
---- FIX [src-edit] reader: scripts/drop-fixed-settles.mjs:26-56 (differ tooling only — no app data changes)
OPTIONAL, differ hygiene, prevents the removal recurring: scripts/drop-fixed-settles.mjs keys removal on record id alone across all three registries, so a settle answering question A is deleted because divergence B was fixed. Make FIXED name the registry AND the kinds each removal is entitled to drop (e.g. { file:'scripts/wg-values.mjs', kinds:[...] }), and refuse to delete an entry whose kinds are disjoint from them.
---- CORRECTED FIX:
Fix 1 stands, with corrected anchors: in scripts/wg-diff.mjs REPLACE the removal marker line (currently line 1070, `/* the-moon-weavers-art — settle REMOVED: ... */`) with `  'the-moon-weavers-art': ['conditional', 'spellcasting'],`, immediately under the surviving `'labyrinthine-echoes': ['conditional', 'spellcasting'],` (line 1069) and inside the shared comment block at 1053-1069. Replacing rather than inserting below matters: wg-batch-gate.mjs parseSettles carries the preceding comment forward to consecutive keys, so a surviving marker line would become the entry's printed "evidence" at batch close. Justification as read from their raw ops: spellcasting = adjValue SPELL_DC/SPELL_ATTACK = T; conditional = the LEVEL>=12 step to E (their conditional here wraps the proficiency step, not a second giveSpell as the comment's `conditional` paragraph — written for labyrinthine-echoes — describes; consider a one-line note so the next reader is not misled). Both are one general rule on our side at src/rules/build.ts ~6635-6651. Fix 2 needs sharpening or the restore is undone: as written it redesigns FIXED to name registry + entitled kinds, but the entry `'the-moon-weavers-art'` at scripts/drop-fixed-settles.mjs:34 must ITSELF be scoped (to the kinds the picker fix actually closed, e.g. ['choice']) or deleted from FIXED — otherwise the next `node scripts/drop-fixed-settles.mjs --write` re-matches `/^\s*'the-moon-weavers-art':\s*\[/` and deletes the restored settle again. The minimal sufficient step is removing that one id from FIXED; the kinds-scoped redesign is the durable version.

########## ascended-dragonet-heritage [b15 MATCHES] verify=CONFIRMED
---- FIX [settle] reader: src/rules/openChoice.ts:136-140 (resolves the five dragonet heritages at read time) and src/rules/build.ts:438-445 secondHeritageIdOf -> the 15+ second-heritage sites in build.ts, incl. innateSpells at 6444-6449 and feat prerequisites at 6519
Restore SETTLED_IDENTITIES['ascended-dragonet-heritage'] = ['grants','options'] in scripts/wg-identity.mjs, replacing the removal marker at line 489 and sitting under the comment block at 481-488 that already states the reading. Both buckets, because their five giveAbilityBlock grants and their five option titles are the same five names arriving twice.
---- FIX [settle] reader: src/rules/build.ts:438-445 secondHeritageIdOf; src/rules/derive.ts:1298-1306 (hasHeritage)
Restore VERIFIED_EQUIVALENT['ascended-dragonet-heritage'] = ['grantsRecord'] in scripts/wg-diff.mjs, replacing the removal marker at line 1047 and sitting under the comment block at 1037-1046. Their giveAbilityBlock-per-option against our secondHeritage field: same mechanic, and ours additionally preserves heritage IDENTITY, which the copied effect block cannot.
---- CORRECTED FIX:
[{"kind":"settle","detail":"Restore SETTLED_IDENTITIES['ascended-dragonet-heritage'] = ['grants', 'options'] in scripts/wg-identity.mjs (registry opens at line 426), replacing the removal marker at line 489 and sitting directly under the surviving comment block at 481-488 that already states the reading. Both buckets, because their five giveAbilityBlock grants and their five option titles are the same five names arriving twice — verified live: `node scripts/wg-identity.mjs --ids ascended-dragonet-heritage --verbose` reports grants and options theirs-not-ours=[feydragonet, homingdrake, housedrake, pearldragonet, tidepooldragonet], ours=[(nothing)], 'nothing' because an open choice has no baked option list. Matches the untouched sibling entry 'awakened-yaoguai-heritage': ['grants','options'] at line 519, whose --raw report is the identical shape.","reader":"src/rules/openChoice.ts case 'heritage' (filters heritages by ancestryId==='dragonet' && !versatile — measured as exactly the same five) and src/rules/build.ts:437-445 secondHeritageIdOf, feeding the second-heritage sites in build.ts including the innateSpells loop at build.ts:6465 and feat-prerequisite evaluation; src/rules/derive.ts heritageRecords/hasHeritage"},{"kind":"settle","detail":"Restore VERIFIED_EQUIVALENT['ascended-dragonet-heritage'] = ['grantsRecord'] in scripts/wg-diff.mjs (registry opens at line 888), replacing the removal marker at line 1051 (NOT 1047) and sitting directly under the surviving comment block at 1041-1050 (NOT 1037-1046). Their giveAbilityBlock-per-option against our secondHeritage field: same mechanic, and ours additionally preserves heritage IDENTITY, which the copied effect block cannot. Verified live: `node scripts/wg-diff.mjs --bucket they-only --list` reports `ascended-dragonet-heritage lvl 5 missing=[grantsRecord] ours=[choice,specialStat]` — exactly the settle's scope. Matches the untouched sibling 'awakened-yaoguai-heritage': ['grantsRecord'] at line 1192.","reader":"src/rules/build.ts:437-445 secondHeritageIdOf; src/rules/build.ts:6465 both-heritages innateSpells loop; src/rules/derive.ts heritageRecords/hasHeritage"},{"kind":"note","detail":"Correct the surviving wg-diff comment's closing sentence while restoring it: it says 'Same settle as its two siblings awakened-yaoguai-heritage and late-awakener.' Verified false for late-awakener — it holds no settle in any of the three registries, because their side encodes nothing named for it and `node scripts/wg-identity.mjs --ids late-awakener --verbose` and the wg-diff they-only list both report it zero times. Only awakened-yaoguai-heritage is a settled sibling; late-awakener is a MODELLING sibling (same secondHeritage + open-choice shape in core.json) and should be described as such so the next reader does not go looking for a settle that was never there.","reader":"n/a — comment accuracy only; scripts/wg-diff.mjs:1049 and scripts/wg-identity.mjs:487"}]

########## gates-threshold [b15 MISMATCH] verify=CONFIRMED
---- FIX [src-edit] reader: src/rules/build.ts:1374-1382 effectChoiceOffered -> src/rules/build.ts:4861-4888 resolvePick (called for owned features at build.ts:5052) and src/builder/shared.tsx:4200
Gate the record's 'gate-junction' effectChoices on the Expand the Portal branch, matching print ("Expand the Portal: … You also gain a gate junction"; Fork the Path grants none) and their nesting. Concretely: extend EffectChoice with a build-state gate that effectChoiceOffered can evaluate (it today handles only `requiresFeature`, build.ts:1374-1382) and have the four threshold records' junction groups require `!build.gateForks?.[<thresholdLevel>]`. Because effectChoiceOffered is shared by the builder's renderer (src/builder/shared.tsx:4200) and by resolvePick, one change hides the question and stops the grant together — the file's own note says "a question the player never sees can never" be answered into a grant. Keep the level-5 group as the id 'gate-junction' so existing answers round-trip.
---- FIX [overlay] reader: src/rules/build.ts:6976 / the generic feature-choice renderer are what surface `choice` today; after removal the only branch carrier is build.gateForks / gateExpands / gateForkImpulses
Delete the three duplicate branch pickers: scripts/data/effect-backfill.json rows {category:'classFeatures', id:'second-gates-threshold'|'third-gates-threshold'|'fourth-gates-threshold', field:'choice'} (~lines 72134, 72170, 71979). Use value:null, which is exactly what an overlay removal row does. They ask the Expand/Fork question a second time at L9/13/17 — the builder already asks it once per threshold through gateForks (shared.tsx:3341-3435) — and their answers reach no reader (`grep -rn gateThreshold src/` is empty), so the player is asked twice and the record-level answer does nothing. The level-5 record has no such duplicate and must not gain one; this is the same one-clause-one-picker rule scripts/duplicate-pick-check.mjs enforces for spells.
---- FIX [settle] reader: src/rules/build.ts:171-176 + 2135 + 2638-2646 + 4115-4136 and src/builder/shared.tsx:3341-3435 for the branch; src/rules/derive.ts:4155 for the blast dice; pinned by test/batch15-parity.test.ts:73-121
AFTER the two fixes above land, restore both settles: SETTLED_IDENTITIES['gates-threshold'] = ['options'] in scripts/wg-identity.mjs (replacing the marker at line 497, under the surviving comment at 492-496) and VERIFIED_EQUIVALENT['gates-threshold'] = ['unmapped'] in scripts/wg-diff.mjs (replacing the marker at line 1101, under the surviving comment at 1081-1100). 'options': their two branch titles against our junction list is a comparison of two different questions — the branch is BuildState (gateForks/gateExpands/gateForkImpulses), the junction is the other half of Expand the Portal. 'unmapped': their adjValue KINETICIST_BLAST_DICE = 1, one per threshold record, is our derive.ts:4155 `1 + [5,9,13,17].filter(l => c.level >= l).length` — the same number read off the level at the same four levels, already settled on the values side in wg-values' NOT_A_SCALAR (wg-values.mjs:130).
---- CORRECTED FIX:
[{"kind":"src-edit","detail":"Gate the junction on the Expand branch, matching print and their nesting. (1) src/rules/types.ts — add to EffectChoice, next to `requiresFeature` (line 1873): `requiresNoGateFork?: number` (the Gate's Threshold level this group belongs to), documented in the same register as requiresFeature. It must carry the LEVEL because effectChoiceOffered's signature is `(ch, build, content)` — it never sees the recordId, and all four records use the same choice id 'gate-junction', so the level cannot be inferred from `ch` alone. (2) src/rules/build.ts:1374-1382 effectChoiceOffered — before the requiresFeature test, add `if (ch.requiresNoGateFork != null && build.gateForks?.[String(ch.requiresNoGateFork)]) return false;`. One predicate, so the picker (src/builder/shared.tsx:4202 EffectChoicesPicker) and the applier (resolvePick, build.ts:4866-4894, driven for owned features at build.ts:5057) hide the question and drop the stale answer together — a player who answers the junction then switches to Fork keeps neither the prompt nor the grant.","reader":"src/rules/build.ts:1374 effectChoiceOffered -> src/rules/build.ts:4866 resolvePick (called at build.ts:5057) and src/builder/shared.tsx:4202"},{"kind":"overlay","detail":"scripts/data/effect-backfill.json — set `requiresNoGateFork` on the junction group of each of the FOUR existing effectChoices rows {category:'classFeatures', id:'gates-threshold'|'second-'|'third-'|'fourth-gates-threshold', field:'effectChoices'} (array indices 1203-1206): 5, 9, 13, 17 respectively. Keep id 'gate-junction' on all four so saved answers under `<recordId>:gate-junction` round-trip. These are absolute assignments of the whole array, so edit the rows in place rather than adding path rows.","reader":"public/core.json classFeatures[*].effectChoices[0] -> effectChoiceOffered"},{"kind":"overlay","detail":"Delete the three duplicate branch pickers by setting value:null IN PLACE on the existing rows {category:'classFeatures', field:'choice'} for second-gates-threshold (index 3941), third-gates-threshold (3944), fourth-gates-threshold (3930). Verified: apply-backfill.mjs:63-64 treats value:null as `delete target[fix.field]`, and setting the existing row (rather than appending a new one) is order-independent. Verified dead and duplicated: flag 'gateThreshold' has no reader in src/, the options carry no `grant`/`grantsToken` so build.ts:5071-5078 and build.ts:6996 apply nothing, yet Builder.tsx:1636 renders a second Expand/Fork prompt beside the one shared.tsx:3341-3435 already asks through gateForks. Level-5 has no such row and must not gain one.","reader":"src/builder/Builder.tsx:1636 renderChoice (the surface today); after removal the only branch carrier is build.gateForks / gateExpands / gateForkImpulses"},{"kind":"test","detail":"NEW, and a precondition for the settle below: extend describe(\"batch 15 — Gate's Threshold really has both branches\") in test/batch15-parity.test.ts (line 73) with a case that MEASURES the gate. Build a kineticist 5 with extraChoices.element ['fire-gate','water-gate'] and effectChoices {'gates-threshold:gate-junction':'air-elemental-resistance'} twice — once plain, once with gateForks {'5':'air-gate'} — and assert the resistance lands on the Expand build and is ABSENT on the forked one; add the same pair for a skill junction ('air-skill-junction' -> stealth trained) so both grant shapes are covered. Without this, restoring the settle silences the only comparer that would catch the regression.","reader":"src/rules/build.ts:1374 effectChoiceOffered -> resolvePick -> character.resistances / proficiencies.skills"},{"kind":"settle","detail":"AFTER the three fixes and the test land, restore both settles: SETTLED_IDENTITIES['gates-threshold'] = ['options'] in scripts/wg-identity.mjs (replacing the marker at line 497, under the surviving comment at 491-496) and VERIFIED_EQUIVALENT['gates-threshold'] = ['unmapped'] in scripts/wg-diff.mjs (replacing the marker at line 1101, under the comment at 1082-1100). Amend BOTH comments to record the second fix, not just the impulse-grant one: the junction is now branch-gated (`requiresNoGateFork`), so our record and their nesting agree about WHEN it is asked and differ only in WHERE the branch is stored — ours in BuildState (gateForks/gateExpands/gateForkImpulses), theirs as a CUSTOM select. 'unmapped' covers their adjValue KINETICIST_BLAST_DICE = 1 per threshold, which is derive.ts:4163 `1 + [5, 9, 13, 17].filter((l) => c.level >= l).length`, already settled on the values side in wg-values.mjs NOT_A_SCALAR. Also drop 'gates-threshold' from the FIXED map in scripts/drop-fixed-settles.mjs:32 so the next run cannot strip the restored entries again.","reader":"src/rules/build.ts:172-177 + 2141 + 2643-2650 + 4120-4140 and src/builder/shared.tsx:3341-3435 for the branch; src/rules/derive.ts:4163 for the blast dice; pinned by test/batch15-parity.test.ts:73-121 plus the new case above"}]

########## advanced-red-mantis-magic [b16 MISMATCH] verify=ASK-OWNER
---- FIX [src-edit] reader: src/rules/casterArchetypes.ts:archetypeSlots() (customUnlocks branch, ~:433) and archetypeProficiency() (~:445); both are called from src/rules/build.ts via activeCasterArchetype().
src/rules/casterArchetypes.ts — give 'red-mantis-assassin-dedication' an explicit `customUnlocks` schedule so the legacy Advanced feat can contribute the one rank the standard ladder does not. ⚠ customUnlocks REPLACES RANK_UNLOCKS (archetypeSlots returns early at the customUnlocks branch, casterArchetypes.ts:433-437), so the whole ladder must be restated, and `archetypeProficiency` returns 'trained' under customUnlocks unless the two proficiency feats are named. Exact spec — keep basicId/expertId/masterId as they are and ADD:
  customUnlocks: [
    { rank: 1, level: 4,  featId: 'basic-red-mantis-magic' },
    { rank: 2, level: 6,  featId: 'basic-red-mantis-magic' },
    { rank: 3, level: 8,  featId: 'basic-red-mantis-magic' },
    { rank: 4, level: 10, featId: 'advanced-red-mantis-magic' },   // the printed 'At 10th level' clause
    { rank: 4, level: 12, featId: 'expert-red-mantis-magic' },
    { rank: 5, level: 14, featId: 'expert-red-mantis-magic' },
    { rank: 6, level: 16, featId: 'expert-red-mantis-magic' },
    { rank: 7, level: 18, featId: 'master-red-mantis-magic' },
    { rank: 8, level: 20, featId: 'master-red-mantis-magic' },
  ],
  profExpertFeat: 'expert-red-mantis-magic',
  profMasterFeat: 'master-red-mantis-magic',
The duplicated rank 4 is harmless — the loop writes `out[rank] = 1`, so whichever gate is satisfied first wins and the second is idempotent. Ranks 1-3 and 4-8 are byte-for-byte the levels RANK_UNLOCKS already produced, so no existing red-mantis character's slot table moves; the only behavioural delta is a 4th-rank slot appearing at level 10 instead of 12 for a character who also took Advanced.
---- FIX [overlay] reader: scripts/lib/apply-backfill.mjs applyBackfill() writes it into public/core.json; the builder's archetype feat list reads feats[].archetype.
Companion data row so the legacy feat is filed like its siblings and is reachable from the archetype's feat list: scripts/data/effect-backfill.json {"category":"feats","id":"advanced-red-mantis-magic","field":"archetype","value":"red-mantis-assassin"}. Every other Red Mantis feat we ship carries this (basic/expert/master/vernai-training/red-mantis-school-spell); this one alone does not, which is why nothing lists it under the archetype. Optional second row {"field":"prerequisites","value":["Basic Red Mantis Magic"]} to match the printed Prerequisites line of feat-897.
---- FIX [none] reader: src/rules/build.ts:2523 applyEditionFilter
ALTERNATIVE the owner may prefer instead of the two fixes above, stated so it is on the record and not silently discarded: mark the record superseded rather than wiring it in — {"category":"feats","id":"advanced-red-mantis-magic","field":"edition","value":"superseded"}. build.ts:2523 hides edition 'superseded' unconditionally (not just under the hideLegacy toggle). The argument for it: the Prey for Death remaster replaced the World Guide ladder with Basic/Expert/Master, we do not ship the legacy Basic this feat's own prerequisite names, and its two lower clauses are already delivered by the remaster Basic. The argument against: this is a scope call about legacy content, not a parity call, and the whole point of the customUnlocks fix is that WG and print agree on what the feat does. I did not choose between them.

########## armor-potency-1 [b16 MATCHES] verify=CONFIRMED
---- FIX [src-edit] reader: scripts/lib/wg-parse.mjs wgRecord()/wgRowsByBucket(); consumed by scripts/wg-diff.mjs, scripts/wg-values.mjs, scripts/wg-identity.mjs and therefore by scripts/wg-batch-gate.mjs.
PREFERRED, because it clears this record, `assisting`, and the other 157 runes at once instead of one settle row per rune. Teach the comparers to read the runes twin. In scripts/lib/wg-parse.mjs, wgRecord(core, id) currently returns the first bucket hit from WG_BUCKETS — for a rune id that is always the chassis in `items`. Add `runes` as a MERGE, not a new bucket: when core.items[id] and core.runes[id] both exist, the fields each comparer harvests should be taken from the union (their-side pairing stays `items` -> their `item` table, since their row is the same single rune row). Narrowest correct form: in the `ourFields`/`ourValues` walk of wg-diff.mjs and wg-values.mjs, walk `core.runes[id]` in addition to `core.items[id]` for any id present in both. Guard it exactly the way wgOwnsComparison() guards the actions/aon- twins so nothing else changes shape. Include the derived facts a RuneDef implies but does not spell as a field, or the ac row will survive the merge: `kind === 'potency'` + `value: N` IS an `ac` assertion of N (the rune record has no `ac` key), which is the same translation OUR_KINDS already does for nested paths.
---- FIX [settle] reader: scripts/wg-diff.mjs:1852 `const settled = RAW_SETTLES ? undefined : VERIFIED_EQUIVALENT[id]`; scripts/wg-values.mjs:1242 `(SETTLED_VALUES[id] ?? []).includes(key)`.
NARROW ALTERNATIVE if the comparer change is judged too broad to make now — two rows, both needed because two different gates fire:
  · scripts/wg-diff.mjs VERIFIED_EQUIVALENT: 'armor-potency-1': ['ac'], with the comment naming the carrier (core.runes/armor-potency-1 kind:'potency' value:1) and the reader chain (attachments.ts:111-116 planRune -> hostInv.runes.potency -> derive.ts:1729 acItem, ITEM bonus), plus the note that the comparers do not read the runes bucket so the items twin is expected to be empty.
  · scripts/wg-values.mjs SETTLED_VALUES: 'armor-potency-1': ['ac|'] (the key wg-values prints is `ac|` — see the report line `MISSING ac| theirs=1`).
Do NOT settle without also recording that the second printed clause (the property-rune slot) is ours-only via propertyRuneCapacity(), or a later reader will think the settle covers less than it does.
---- CORRECTED FIX:
Take the SETTLE option, not the comparer merge. Two rows, both needed because two different gates fire on this record right now (both reproduced live).

1) scripts/wg-diff.mjs, in VERIFIED_EQUIVALENT (alphabetical neighbourhood, same shape as 'unbreakable-expertise': ['ac'] at line 1015; consumed at wg-diff.mjs:1916):

  /* ARMOR POTENCY (+1) — the mechanic is in the RUNES bucket, which no comparer reads.
   * Printed (GM Core p.226, AoN equipment-2785-2570): "Increase the armor's item bonus to AC by 1.
   * The armor can be etched with one property rune." Their item 6719 encodes only the first clause,
   * as addBonusToValue AC_BONUS=1 type=item.
   * Ours carries BOTH. core.runes/armor-potency-1 {kind:'potency', value:1} is the carrier; the
   * items-bucket twin is the purchasable chassis and is EXPECTED to be empty, because
   * lib/wg-parse.mjs WG_BUCKETS has no `runes` entry and every one of the 159 runes is therefore
   * compared against its chassis. Reader chain: attachments.ts:87 content.runes[attachment.id] ->
   * attachments.ts:111-116 planRune writes hostInv.runes.potency -> derive.ts:1736 acItem, applied
   * as an ITEM bonus at derive.ts:1738. Same stat, same value, same bonus type as theirs.
   * SECOND CLAUSE, ours-only: derive.ts:2256 propertyRuneCapacity() sizes the property-rune slots off
   * the same potency value, enforced at attachments.ts:140. Asserted by test/armor-runes.test.ts:41. */
  'armor-potency-1': ['ac'],

2) scripts/wg-values.mjs, in SETTLED_VALUES (key format per the null-detail neighbours 'uncanny-awareness': ['perception|'] and 'basic-kata': ['hp|']; consumed at wg-values.mjs:1270). The key the report prints is `ac|` — confirmed by running `node scripts/wg-values.mjs --ids armor-potency-1 --verbose`:

  /* Same record, same reading as the wg-diff settle above: the +1 lives on core.runes/armor-potency-1
   * (kind:'potency', value:1) and reaches AC through attachments.ts:111-116 -> derive.ts:1736 as an
   * item bonus. wg-values reads the items-bucket chassis, which holds no `ac` key by design. */
  'armor-potency-1': ['ac|'],

Both comments are hedge-free and state WHY, so gate 6 (wg-settle-audit.mjs HEDGES) passes. Neither key exists today — verified unique by grepping armor-potency/weapon-potency across wg-diff.mjs, wg-values.mjs, wg-identity.mjs and lib/wg-parse.mjs — so there is no duplicate-object-key overwrite and no sibling regression. Note that after the wg-diff settle the record moves to `agree` (ours has no kinds, so `extra` is empty) and therefore appears in out.disagree, which is harmless: the gate's VALUES check is served by wg-values, which the second row settles.

Do NOT fold the runes-bucket comparer merge into this. Spin it off as its own scoped change, because it is not the one-line widening the finding describes: only 18 of 159 runes carry `value`, 24 `damage`, 1 `passiveEffects`, 1 `actsAs`, and the remaining 141 property runes are encoded in derive.ts by name, so the merge clears far fewer rows than claimed while requiring a whole new derived-fact translation layer (potency+value -> ac for armor and attack for weapons, striking -> damage dice, resilient -> save, reinforcing -> shield) in code that all four comparers and wg-batch-gate.mjs share. Any such change must be followed by a re-run of batches 1-15 plus `node scripts/wg-settle-stale.mjs`, since changing what the comparers harvest can strand existing settles — and a settle that matches nothing is the trap wg-values.mjs's own header warns about.

Sibling note for whoever picks this up: armor-potency-2 and armor-potency-3 print the identical `MISSING ac|` row (theirs=2 and theirs=3) from the same cause, and batch 16's `assisting missing=[carry]` is the same runes-bucket blindness with core.runes/assisting.passiveEffects.bulkLimitBonus as its carrier. Settle each in its own batch on its own reading; do not generalise this row to them.

########## assisting [b16 MATCHES] verify=CONFIRMED
---- FIX [src-edit] reader: scripts/lib/wg-parse.mjs wgRecord(); consumed by scripts/wg-diff.mjs (OUR_KINDS.carry), scripts/wg-values.mjs (:615 `put('bulk|', ...)`), scripts/wg-batch-gate.mjs.
SAME PREFERRED FIX AS armor-potency-1 — teach scripts/lib/wg-parse.mjs to merge core.runes[id] into the record the comparers harvest when an id exists in both `items` and `runes`. Assisting is the cleaner test case for it, because the rune record already spells the field with the exact name and nesting wg-diff.mjs:356 and wg-values.mjs:615 are looking for (`passiveEffects.bulkLimitBonus`): the moment the walk sees the runes record, both the KINDS row and the VALUES row clear with no per-record registry entry. Fixing it once here clears all 159 runes.
---- FIX [settle] reader: scripts/wg-diff.mjs:1852; scripts/wg-values.mjs:1242.
NARROW ALTERNATIVE, two rows for the two gates:
  · scripts/wg-diff.mjs VERIFIED_EQUIVALENT: 'assisting': ['carry'] — comment quoting the carrier core.runes/assisting.passiveEffects.bulkLimitBonus = 1 and the reader derive.ts:4862, and noting that the items twin is the purchase chassis because this is an etched armour property rune.
  · scripts/wg-values.mjs SETTLED_VALUES: 'assisting': ['bulk|'] — the key the report prints (`MISSING bulk| theirs=1`).
---- CORRECTED FIX:
Both proposed routes are sound; adopt them with these corrections. (A) PREFERRED src-edit, scripts/lib/wg-parse.mjs: in wgRecord()/wgAllRecords(), when an id resolves in `items` and core.runes[id] also exists, merge the rune record ITEMS-WINS, not runes-over-items — measured, all 159/159 rune ids also live in `items`, so nothing new appears, but `kolss-oath` and `trudds-strength` carry a `price` that differs between the two records and a runes-over-items merge would silently rewrite it. Merging only keys absent from the items record (or only the mechanical keys: passiveEffects, slot, kind, damage) leaves those two untouched and still delivers `passiveEffects.bulkLimitBonus: 1` to the exact path wg-diff.mjs carry and wg-values.mjs:624 already look for, clearing both rows with no registry entry. Do not add `runes` to WG_BUCKETS as an eighth walked bucket — WG_PAIRING has no `runes` table, so the 159 records would land in noMatch and the counters would move for a reason unrelated to parity. (B) NARROW alternative, correct anchors: add `'assisting': ['carry']` inside VERIFIED_EQUIVALENT at scripts/wg-diff.mjs:888 (consumed at :1916), and `'assisting': ['bulk|']` inside SETTLED_VALUES at scripts/wg-values.mjs:786 (consumed at :1270 as `(SETTLED_VALUES[id] ?? []).includes(key)`; the printed key is exactly `bulk|`). Neither registry currently holds an `assisting` key, so there is no collision. Comment each row with the carrier (core.runes/assisting.passiveEffects.bulkLimitBonus = 1), the live reader (derive.ts:4873 via propertyRuneDefs, thresholds assembled at derive.ts:4922), the existing coverage (test/armor-runes.test.ts:69-74), and the note that the items twin is the purchase chassis because this is an etched armour property rune. Note if (A) lands, these two rows become stale and wg-settle-stale.mjs will flag them — pick one route, not both.

########## bolkas-blessing [b16 THEY-ENCODE-NOTHING-USEFUL] verify=CONFIRMED
---- FIX [src-edit] reader: scripts/wg-diff.mjs kindOfTheirOp() (:180-214) -> the THEY-ONLY missing-kinds list -> scripts/wg-batch-gate.mjs KINDS gate.
PREFERRED: in scripts/wg-diff.mjs kindOfTheirOp(), before the `return 'unmapped'` fallback at :214, add a guard `if (!v) return null;` (v is the normalised data.variable) with a comment recording the measurement — 6 ops across 5 records corpus-wide, all empty-variable stubs, and the reasoning that an op naming no variable asserts nothing to disagree with. Sits naturally beside the existing `return null` guards for PRIMARY_SHEET_TABS/PAGE_*, BLACKLIST_/WHITELIST_ and _FEAT_COUNT, which are the same 'their bookkeeping, not a rule' judgement. Clears bolkas-blessing plus the four ability_block records named above, in whatever batch they land in.
---- FIX [settle] reader: scripts/wg-diff.mjs:1852.
NARROW ALTERNATIVE, precedented: scripts/wg-diff.mjs VERIFIED_EQUIVALENT: 'bolkas-blessing': ['unmapped'] — three ['unmapped'] settles already exist (untrained-improvisation:945, martial-experience:1078, unburdened-iron:1447). The comment must quote both of their ops verbatim (variable:"", text:"") so the next reader can see the settle is 'they encode nothing', NOT 'we cover it elsewhere', and name our two carriers: items.bolkas-blessing.passiveEffects.skills.diplomacy = 1, and situationalBonuses.ts:1989.
---- FIX [src-edit] reader: src/rules/derive.ts:272/:279 and :302/:309 (the passiveEffects skill accumulators); src/rules/derive.ts:1316 propertyRuneDefs().
SEPARATE FROM THE WG FINDING — the etched-rune defect above, offered as a spec so it is not lost. A one-line overlay CANNOT fix it: adding `passiveEffects.skills.diplomacy = 1` to core.runes/bolkas-blessing writes a field no reader consults. It needs BOTH: (a) the rune record to carry the passive, and (b) src/rules/derive.ts to fold property-rune skill passives into the same accumulator as item passives at :272/:302 — the natural place is the loop at :279/:309 that already reads `db.items[inv.itemId]?.passiveEffects`, extended with `for (const r of propertyRuneDefs(inv, db)) read(r.passiveEffects)` and the body-rune equivalent, mirroring exactly what :4862/:4865 already do for bulkLimitBonus. Then the ITEMS-record passive can stay as-is (it is correct for the unetched loose item) and both states deliver the printed +1. Worth confirming how many other property runes print a standing skill bonus before building it.
---- CORRECTED FIX:
Do NOT apply the finding's PREFERRED fix (blanket `if (!v) return null;` before wg-diff.mjs:214). It suppresses ability_block 57388 Instinctive Collaborator's `{"variable":"","value":"2","type":"circumstance","text":"to Aid"}` — a complete +2 circumstance bonus that is that record's only op — and 57671 Undying Resilience's Fortitude clause carried in `text`.

APPLY INSTEAD (either; the first is the smaller, already-precedented move):

(A) The narrow settle. In scripts/wg-diff.mjs, immediately before the closing `};` of VERIFIED_EQUIVALENT (currently line 1885, after `'stylish-tricks': ['choice'],` at :1884):

  /* Their two ops are UNFINISHED STUBS, quoted whole so the next reader does not have to fetch them:
   *   {"type":"addBonusToValue","data":{"variable":"","text":""}}   x2
   * No variable, no value, no bonus type. Only the COUNT is legible — two bonuses, which is how many
   * the printed text gives. Their GREATER grade (item 18178) encodes the same effect properly
   * (SKILL_DIPLOMACY value 2 type item; PERCEPTION value 2 type item "to Sense Motive"), which is what
   * establishes these blanks as stubs of those two bonuses rather than a different encoding.
   * This is "they encode nothing", NOT "we cover it elsewhere". We carry both printed halves:
   * items.bolkas-blessing.passiveEffects.skills.diplomacy = 1 (unconditional half, read by
   * derive.ts:279/:309) and src/rules/situationalBonuses.ts:1989 (the Sense Motive half, reached via
   * explain.ts:278). Empty `variable` falls through kindOfTheirOp's pattern chain to the named
   * 'unmapped' fallback at :214, which is why this reads like a lane we have not built. */
  'bolkas-blessing': ['unmapped'],

(B) If a general guard is still wanted, narrow the predicate so it fires only on ops that assert nothing AT ALL, and place it just before `return 'unmapped'` at :214:

  /* An op with no variable AND no value, bonus type or text asserts nothing there is anything to
   * disagree about. Measured corpus-wide across ability_block + item, flattening every nested branch:
   * 8,174 value-carrying ops, 6 with an empty `variable`, of which only THREE are wholly empty —
   * Bolka's Blessing item 18177 (x2) and Medium's Awareness 28411. The other three are deliberately
   * left reporting because they DO assert something without naming a variable: Instinctive
   * Collaborator 57388 {value:"2",type:"circumstance",text:"to Aid"}, Undying Resilience 57671
   * (a whole Fortitude clause in `text`), and Mystery (legacy) 26443 {value:false}. Sits with the
   * PRIMARY_SHEET_TABS/PAGE_*, BLACKLIST_/WHITELIST_ and _FEAT_COUNT guards above — their
   * bookkeeping, not a rule. */
  if (!v && op.data?.value === undefined && !String(op.data?.text ?? '') && !op.data?.type) return null;

(B) clears bolkas-blessing and mediums-awareness (missing=["unmapped"]) and nothing else; re-measure before trusting that, since the corpus can move. The rune-etching defect stays a separate item and must not be folded into this settle.

########## warriors-training-ring [b16 MATCHES] verify=CONFIRMED
---- FIX [settle] reader: scripts/wg-values.mjs:1228-1233 splits unmapped variables into `NOT_A_SCALAR[v]` (adjudicated) vs untaught; scripts/wg-batch-gate.mjs's "VALUES: N variable(s) nothing has ever compared" gate reads that split.
ONE registry row. scripts/wg-values.mjs NOT_A_SCALAR — add:
  ATTACK_ROLLS_BONUS: 'a rider on attack rolls with no number on their side (their op carries `text` and no `value`) — covered by FEAT_SITUATIONAL in src/rules/situationalBonuses.ts, and where the clause is "add your level" it is a proficiency FLOOR (`untrainedWeaponProficiency`), not a value on a track',
Place it beside MARTIAL_EXPERIENCE and UNTRAINED_IMPROVISATION, whose entries make the same argument for the same shape. The comment above it should record what I verified here: their op 14767 has no `value` key, our carrier is situationalBonuses.ts:2422 carrying BOTH printed clauses, and the reader is characterSituationalIds (explain.ts:239, itemInUse gate) -> featSituationalFor (situationalBonuses.ts:3486) -> explain.ts:740. Also worth noting in the comment that the record's batch-16 verdict was FIXED, so a future reader does not mistake the settle for a never-read skip.
---- FIX [none] reader: n/a
NO DATA CHANGE. The record is complete against print — both clauses carried, frequency counter and free-action activation cost present. Explicitly do NOT add a mode for the activation: rule 5 forbids authoring modes through the overlay, and the once-per-day +2/+4 is already a situational star with its trigger stated, which is the lane every comparable item activation in this file uses.
---- CORRECTED FIX:
ONE REGISTRY ROW, but in VAR — not NOT_A_SCALAR.

scripts/wg-values.mjs, in `VAR`, immediately after the AC_BONUS entry (~line 82), whose comment makes the identical argument for the identical shape:

  /* The attack twin of AC_BONUS, and the same reason: their side has no conditional verb, so it writes
   * a plain ATTACK_ROLLS_BONUS and parks the trigger in the op's own `text`. Numeric on eight of its ten
   * carriers — Acid Flask/Dread Ampoule (Major) 3 item, Weapon Potency (+2)/(+3), Hunter's Arrowhead,
   * Fated Rival, Courageous Anthem, Automatic Bonus Progression — and ours already holds that number as
   * `attackItemBonus`, put as `attack|` a few lines below. Warrior's Training Ring is the prose case:
   * item 14767's single op carries `text` and no `value`, so theirAssertions drops it and the record
   * lands in the existing, non-failing 'prose-only bonus (no number asserted)' bucket. Its data was
   * already FIXED in batch 16 — both printed clauses are on FEAT_SITUATIONAL['warriors-training-ring']
   * (situationalBonuses.ts:2422), read by characterSituationalIds (explain.ts:239) -> featSituationalFor
   * (situationalBonuses.ts:3486) -> explain.ts:740. This row is the comparer catching up, not a defect. */
  ATTACK_ROLLS_BONUS: ['attack', null],

Do NOT add ATTACK_ROLLS_BONUS to NOT_A_SCALAR. That lookup is by bare variable name, so it would silence acid-flask-major and dread-ampoule-major — both tripping this same gate today, both with attackItemBonus: 3 on our record matching their value 3 — behind the false claim that we hold no scalar for it.

DATA: unchanged, exactly as the finding says. Both clauses carried, frequency counter and free-action activation present, and no mode authored through the overlay.

ONE CONSEQUENCE TO CARRY, not blocking batch 16 (neither record is in it): weapon-potency-2 / weapon-potency-3 hold no attack number on our record — a potency rune's bonus lives in the rune engine, and types.ts:3536 marks `builtInRunes` as deliberately distinct from `attackItemBonus` — so once taught they will read `attack| theirs=2/3 ours=(nothing)` when they reach a batch. The AC_BONUS precedent is the template for handling that: the same pass that taught AC_BONUS also fed `passiveEffects.ac` and `unarmoredAc.acBonus` into ourAssertions so the teach would not "report a false gap on every magic item". Either feed the potency value into `attack|` the same way, or settle those two rune records by hand when they surface — do not pre-emptively suppress the variable to avoid them.

########## beastmasters-sigil [b16 MISMATCH] verify=CONFIRMED
---- FIX [overlay] reader: src/rules/build.ts:4820-4878 walks a record's `effectChoices` and reads the player's answer from `build.effectChoices['<recordId>:<choiceId>']`; the picker is rendered by src/builder/shared.tsx / Builder.tsx. Parity readers: scripts/wg-diff.mjs:305 (OUR_KINDS.choice) and scripts/wg-identity.mjs:258-264 (ourIdentities effectChoices loop, key() at :48).
THREE rows in scripts/data/effect-backfill.json, one per grade — the sigil, its greater and its major, because situationalBonuses.ts already carries all three and leaving two without the picker would just move the gap. Row for the base grade:
{"category":"items","id":"beastmasters-sigil","field":"effectChoices","value":[{"id":"affix","prompt":"What is the spellheart affixed to?","options":[{"value":"armor","label":"Armor","note":"You gain a +1 item bonus to saving throws against poison."},{"value":"melee-weapon","label":"Melee Weapon","note":"You gain a +1 item bonus to Athletics checks to Trip."},{"value":"ranged-weapon","label":"Ranged Weapon","note":"If you critically hit with the affixed weapon and the target is adjacent to a creature summoned with the sigil, the target takes 1d6 persistent bleed damage."}]}]}
Same row for "beastmasters-sigil-greater" with the bleed at 2d6 (item bonuses stay +1 per print). Same for "beastmasters-sigil-major" with the item bonuses at +2 and the bleed at 3d6 — which is what situationalBonuses.ts:1658 already pays. `id` MUST be "affix" to match every sibling spellheart, since the answer is stored under `<recordId>:<choiceId>`. Options are NOTE-ONLY — no `grant` key — because situationalBonuses.ts:1656-1658 already delivers the two bonuses; a grant here would double them. Verified the overlay can write this: applyBackfill() at scripts/lib/apply-backfill.mjs:56-66 does `target[fix.field] = fix.value` for any JSON value, and `effectChoices` is a recognised item field (src/data/index.ts:289 lists it among the mechanical fields that keep a record alive).
---- FIX [none] reader: src/rules/explain.ts:239 characterSituationalIds -> src/rules/situationalBonuses.ts:3486 featSituationalFor -> src/rules/explain.ts:740.
LEAVE situationalBonuses.ts:1656-1658 EXACTLY AS IT IS. Its "only while the sigil is affixed to your armor" / "...to a melee weapon" restriction text is what puts the star on the save and Athletics rows, and wg-values already reports the four values agreeing. The effectChoices row answers WHICH branch; the stars pay it. Do not migrate the bonuses onto the choice options.
---- FIX [none] reader: scripts/wg-settle-stale.mjs
NOT NEEDED and explicitly checked: no settle row. If the overlay above lands, both the wg-diff KINDS `choice` row and the wg-identity `options` row clear on their own merits, so a VERIFIED_EQUIVALENT / SETTLED_IDENTITIES entry would be a stale settle — and a settle that matches nothing is the trap the --raw / wg-settle-stale.mjs machinery exists to catch.
