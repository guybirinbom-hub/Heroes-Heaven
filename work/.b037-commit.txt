parity batch 037 — WG-comparison lane, 125 records read against print

WHAT WAS READ. 136 ids were cut; Wanderer's Guide encodes 125 of them and the read produced 144
CONFIRMED findings, 0 refuted, 0 deferred to the owner at read time. Fourteen build families worked
them (instruments-1..7, data-rows-1..6, situational) and eight gap families built the readers and
lanes the data families were blocked on.

WHAT CHANGED. 23 manifest specs applied 147 overlay rows into scripts/data/effect-backfill.json
(14,411 -> 14,413 rows; 2 new keys, 3 supersessions), every one replayed into public/core.json and
public/core-descriptions.json and read back from the shipped artefacts. Code lanes built this batch:
InnateSpellGrant.heightenWhenFlag (a grant gated on ANOTHER record's answer — Locate Lawbreakers
heightens only for the Order of the Gate), ItemBase.resonant.spellNotes and its reader (the three
aeon stones' resonant clauses now reach the spell entry), IwrEntry.whenArmorCategory (Guardian's
Armor's resistance is automatic and conditional on the worn armour instead of a hand toggle),
GrantedStrike.die/damageType optional plus onHit (owner ruling #145's damageless attack, which is
what puts the spider's Web on an animal-instinct barbarian), grantsItems[].whenChoice (Armiger's
Protection hands over the ONE suit the player picked), FeatChoiceDef.minLevel on the build-time path,
CasterArchetype.cantripOptions, and play.ts's rank-0 daily-grant route (a daily cantrip could reach
the sheet by no route at all before). Six records left FEAT_CANTRIP_GRANTS for their own daily
`choice`: all three kitsune and all three nagaji spell feats print "During your daily preparations,
choose …" and were asking the question twice, once in the builder and once at daily preparations,
with only the builder one granting.

WHAT THE CLOSER SETTLED. One EXPERIENCE red, feats/nagaji-spell-expertise: their `adjValue SPELL_DC
= E` / `SPELL_ATTACK = E` pair had no surface predicate, because ours delivers the pair centrally —
rules-2232, "At 12th level, these proficiencies increase to expert." — rather than per record. The
lane predicate learned the expert half (scripts/lib/wg-experience-lanes.mjs, with the host's level
passed through on the surface from scripts/wg-experience.mjs), mutation-proofed on both axes in
test/batch037-closer.test.ts. Two guards were taught one true thing about the overlay: a pathless
whole-value row followed by a `path` row into that same field is one intended two-step write that a
regeneration reproduces exactly, so the first row is not a stale bake
(scripts/regen-durability-check.mjs and test/overlay-durability.test.ts). Four spec rows were
corrected before they could ship: a description authored in our own voice for merchants-scale (print
has no rules body at all, so the entry is print's own line), a mirror doc id that resolved to nothing
for mask-of-the-mantis-major, a Wyrm Spindle grade DC written as a phrase print never forms, and a
Kitsune Spell Familiarity option granting Ghost Sound, which is superseded by Figment.

WHAT IS QUEUED FOR THE OWNER. Desk #151 screech-shooter-major-rune-grade: print states a rune grade
for the base and the greater screech shooter and is SILENT for the major, so the major's grade — and
whether builtInRunes may be authored for all three from that ruling — cannot be invented. Desk #152
empty-description-marker-line: ruling 2026-09-10 #17 asked for one plain line, clearly marked as
ours, on merchants-scale, and the apply pre-check refuses authored prose in a description row; the
question is whether that guard should learn a marker prefix, whether the line belongs on the sheet
instead of in the data, and whether it applies to all 244 items that ship an empty description for
the same reason. 44 gap lines are parked, most against desk #127 (the typed all-Speeds lane) and #85
(reborn-soul's restricted skill increases), both unruled since earlier batches.

WHAT IS DEFERRED, AND WHY THIS BATCH IS NOT CLOSED. The close stage refuses: twelve CONFIRMED
findings sit on eleven records that have no Wanderer's Guide packet at all (android, wild-lights,
armigers-protection, flexible-spellcaster, soul-well, mask-of-the-mantis-major, zealot-staff, both
screech-shooter grades, both wyrm-spindle grades), and wg-batch-close.mjs may not write a
WG-comparison verdict for a record whose id is not in work/wg-batch-037.json. Their rows ARE applied
and tested; it is the verdict file that cannot name them. A thirteenth refusal, swashbucklers-speed,
is the known desk-key defect the close script documents: its question is filed as
`speed-status-lane-031`, which `desk.has(<record id>)` does not resolve. Both need the orchestrator —
a batch-composition call and a --reverdict — so work/wg-batch-037-parity.json and -residual.json are
unwritten and the RESIDUAL and PARITY gates are red for that reason and no other.

STATE AT COMMIT. Full suite green (6,264 passed, 1 skipped). npx tsc --noEmit clean. The verify chain
is 40 of 41 clean; the one red, parity-residual-check, was already red when the batch was cut and is
the same missing parity file. The earlier-batch re-gate is green across all 36 batches — nothing this
batch changed made an older record fall silent. The EXPERIENCE gate is green: 125 records played on
the real builder, 111 OK. The flip audit is clean across 47 touched test files and 13 new settle keys.
