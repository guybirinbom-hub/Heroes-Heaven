BATCH 036 — chunk data-rows-2 (of 2) of the data-rows family
Spec: work/.b036-rows-data-rows-2.json   Tests: test/batch036-data-rows-2.test.ts
9 findings assigned, 9 specced as backfill rows, 0 dropped, 0 owner questions raised.
No src/ or scripts/ file was edited by this chunk.

DATA STILL NEEDED
(none — all nine findings are authored as backfill rows in work/.b036-rows-data-rows-2.json; the driver applies them.)

CROSS-FILE GAPS
(none — every finding resolved inside an existing data lane; no change was needed outside my own files.)

PER FINDING
- lesson-of-the-flock — TAUGHT by row {classFeatures, lesson-of-the-flock, grantedSpells, ["levitate"]}. AoN lesson-27: "You gain the Sheltering Wings hex, and your familiar learns Levitate." We shipped only focusSpells:['sheltering-wings']; the flat grantedSpells lane is the batch-035 lesson reader at src/rules/build.ts:4048 (lesson-of-vengeance's row), reached through the choice-owned-feature walk. spells/levitate is rank 3, matching WG's giveSpell rank. No collision — the record's existing overlay rows are focusSpells and edition. Pinned by test/batch036-data-rows-2.test.ts "classFeatures/lesson-of-the-flock grants Levitate to the familiar".

- wind-seeker — TAUGHT by row {feats, wind-seeker, spellListAdditions, {spells:["aerial-form"], as:"repertoire", entryId:"animist-apparition-casting"}}. AoN feat-7138: "You add aerial form to your apparition spell repertoire, allowing you to cast it with your apparition spellcasting." The record carried no carrier at all; the shape is byte-identical to its own prerequisite feats/walk-the-wilds and to monstrous-inclinations, and the reader at src/rules/build.ts:6618 walks taken feats and routes as:'repertoire' into grantedRepertoireAdds. Pinned by "feats/wind-seeker adds aerial form to the apparition repertoire", which also asserts the key set matches walk-the-wilds so the two cannot drift.

- wind-seeker#darkened-forest-form — TAUGHT by row {feats, wind-seeker, spellNotes, [{spellId:"darkened-forest-form", note:"Wind Seeker: if you are attuned to an apparition of darkened boughs, add the bat and bird forms in aerial form to your darkened forest form lists."}]}. AoN feat-7138 Special. Changes no number the sheet holds, which is exactly what spellNotes carries (writer src/rules/build.ts:6797, display via spellNotesFor in explain.ts); same shape as feats/scattered-fire's fireball note. Distinct FIELD from the row above, so the two findings on this record are two rows, not a merge. Pinned by "feats/wind-seeker carries its Special clause as a note on darkened forest form".

- deadly-mutation — TAUGHT by row {feats, deadly-mutation, modifiesGrant, [{from:"ostilli-host-dedication", actionRider:{actionId:"spit-ambient-magic", value:"d8 damage dice", note:"…d8s… 1d6 persistent bleed…"}}]}. AoN feat-5459. ⚠ CORRECTION TO THE FINDING'S PROPOSAL: it wrote from:"spit-ambient-magic", which would have shipped a silently dead row. The ownership gate at src/rules/build.ts:6551 tests `ownedIds.has(mod.from)` against FEAT and CLASS-FEATURE ids only — draconic-paragon works because feats/kobold-breath IS a feat, while spit-ambient-magic exists in the actions bucket alone (feats['spit-ambient-magic'] is undefined). `from` is therefore feats/ostilli-host-dedication, which carries grantsActions:["spit-ambient-magic",…] and is Deadly Mutation's printed prerequisite, so the gate can never wrongly withhold the rider; actionRider.actionId names the action row the mark lands on. Pinned by "feats/deadly-mutation rides on the granted Spit Ambient Magic action", which asserts the from/actionId split and that spit-ambient-magic is an action and not a feat — the exact fact the proposal's shape got wrong.

- skill-mastery-rogue#maxtakable — TAUGHT by row {feats, skill-mastery-rogue, maxTakable, 5}. AoN feat-5096: "**Special** You can select this feat up to five times." maxTakes() at src/rules/featGrants.ts:789 returns `feat.maxTakable ?? 1`, so four of the five takes were unreachable; sibling feats/skill-mastery (feat-6208) already carries 5. Pinned by "feats/skill-mastery-rogue can be taken five times", which asserts through maxTakes() itself, not just the field. No collision with the sibling chunk's skill-mastery-rogue#duplicate-skill finding — that one is a code fix to distinctSiblingPicks in build.ts and authors no row.

- golem-grafter-dedication — TAUGHT by row {feats, golem-grafter-dedication, resistances, [{type:"physical", value:"@actor.archetypeFeats.golem-grafter", condition:"except adamantine"}]} with supersedes: true. AoN feat-1227: "resistance to physical damage (except adamantine) equal to your number of class feats from the golem grafter archetype." SUPERSEDES the existing effect-backfill.json row for the same category/id/field ([{type:'physical', value:'@actor.archetypeFeats.golem-grafter'}]) — that value is carried whole into mine with only `condition` added. `condition` and not `against`: derive.ts counts the number and renders the clause beside it (r.condition ?? src.__cond), which is right here because the resistance is always on and only its scope is uncheckable. Exact shape of feats/stoney-skin. Pinned by "feats/golem-grafter-dedication resists physical except adamantine", which also asserts maxHpBonus {perLevel:1} survives the superseding row.

- unravel-mysteries — TAUGHT by row {feats, unravel-mysteries, recordMarks, [{on:"action", id:"decipher-writing", note:"…half as long… no -2 circumstance penalty…"}]}. AoN feat-1132. Neither clause reached the player anywhere; recordMarks on:'action' is our injectText-on-an-action equivalent (reader recordMarksFor at src/rules/explain.ts:477, which walks taken feats) and actions/decipher-writing exists. Pinned by "feats/unravel-mysteries marks the Decipher Writing action", asserting BOTH printed clauses are in the note.

- spirit-walk#resistance — TAUGHT by row {feats, spirit-walk, resistances, [{type:"all", value:"floor(@actor.level/2)", against:"damage dealt by haunts or spirits, during your first turn in an encounter (you and allies in the 30-foot aura)"}]}. AoN feat-7137. `against` and not `condition`: derive.ts:2744 skips res.set for an `against` entry and emits only the conditional IWR line, which is what keeps the headline number honest for a first-turn-only resistance. type 'all' + floor() are both already in service (modes/dampening-harmonics, modes/deitys-protection). No supersede — the record's only existing overlay row is a description repair. The three display entries at situationalBonuses.ts:1069 are untouched (different clause, and outside my files anyway). Pinned by "feats/spirit-walk carries the first-turn haunt/spirit resistance", which asserts `against` is present and `condition` is absent.

- eye-of-the-unseen#innate-spell — TAUGHT by row {items, eye-of-the-unseen, innateSpells, [{spellId:"see-the-unseen", tradition:"arcane", rank:2, usesPerDay:1}]}. AoN equipment-1367-1240: "**Frequency** once per day … The eye casts See the Unseen on you." The item had the 1/day counter and the 2-action activation cost but no grant, so the cast never reached the Spells page; the invested-item innateSpells walk at src/rules/build.ts:8013 is the only lane that can put it there, and items/hydration-cloak carries the identical 1/day shape. Print names it *see invisibility*, the legacy title of spells['see-the-unseen'] (rank 2, arcane/divine/occult), consistent with the record's legacy-era edition. Does not collide with the sibling chunk's eye-of-the-unseen finding, which deletes the passiveEffects field on the same record — different field. Pinned by "items/eye-of-the-unseen casts See the Unseen once per day".

VERIFICATION
- All 9 tests were run and fail exactly once, each on the missing authored field (`expected undefined to …`), i.e. for the right reason; they turn green when the driver applies the spec. Run with `node scripts/vt.mjs run test/batch036-data-rows-2.test.ts`.
- `npx tsc --noEmit` clean.
- Every field authored here has a confirmed live reader: grantedSpells build.ts:4048, spellListAdditions build.ts:6618, spellNotes build.ts:6797, modifiesGrant.actionRider build.ts:6555-6578, maxTakable featGrants.ts:789, resistances.condition/.against derive.ts:~2744-2752, recordMarks explain.ts:477, item innateSpells build.ts:8013.
