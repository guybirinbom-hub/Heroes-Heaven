BATCH 033 — WG-COMPARISON LANE — FAMILY: data-rows
spec: work/.b033-rows-data-rows.json   tests: test/batch033-data.test.ts   (12 findings, 10 rows, 9 tests)
All 9 tests fail today, each on exactly one assertion — the absent or contradicted field — with every harness half passing first.
npx tsc --noEmit clean; node scripts/test-flip-audit.mjs --batch 033 clean.

DATA STILL NEEDED
- classFeatures/empiricism-methodology aonId = "methodology-6" and edition = "remaster" (supersedes the existing edition row "legacy") — same defect as my alchemical-sciences finding, mirror methodology-2.remaster_id = ["methodology-6"]; not a finding in this batch so not authored here.
- classFeatures/forensic-medicine-methodology aonId = "methodology-7" and edition = "remaster" (supersedes "legacy") — mirror methodology-3.remaster_id = ["methodology-7"].
- classFeatures/interrogation-methodology aonId = "methodology-8" and edition = "remaster" (supersedes "legacy") — mirror methodology-4.remaster_id = ["methodology-8"].
- classFeatures/decay-instinct field `whileActive`, classFeatures/fury-instinct field `whileActive`, classFeatures/spirit-instinct field `whileActive`, classFeatures/elemental-instinct field `choice`, classFeatures/superstition-instinct field `choice` — whole-value re-emit of each field with the IWR key `note` renamed to `condition` on the entries inside it (33 entries; measured by walking every resistances/weaknesses array in core.json — 36 total, the 3 in my skin-hard-as-horn row are the rest). Same reason as skin-hard-as-horn: IwrEntry declares no `note`, so the clause is stored and never rendered.
- classFeatures/arcane-bond-school-of-unified-magical-theory, create:true, value {"id":"arcane-bond-school-of-unified-magical-theory","name":"Arcane Bond (Unified Magical Theory)","traits":["wizard"],"rarity":"common","level":1,"source":{"book":"Pathfinder Player Core","license":"ORC"},"aonId":"arcane-school-21","limitedUses":{"max":1,"per":"day","maxByLevel":{"3":2,"5":3,"7":4,"9":5,"11":6,"13":7,"15":8,"17":9,"19":10}}} — AUTHOR ONLY AFTER the featUses.ts gap below lands. Today the variant would draw a SECOND pip row beside arcane-bond's 1/day instead of replacing it, which is worse than the under-count it fixes.

CROSS-FILE GAPS
- src/data/index.ts loadDescriptions() (:566-575): it writes `.d`/`.r` from core-descriptions.json onto db[bucket][id] and never onto classes.<cls>.subclass.options[<same id>] / extraChoices[].options[], which is what Builder.tsx:1872 and shared.tsx:1382+3240 actually render. 30 subclass options are stale today. This is the whole of the-tangible-dream#granted-spell-2nd (core-descriptions is already correct there, so the sync alone fixes it) and the second wrong copy in angel-eidolon#hallowed-strikes-text.
- src/rules/featUses.ts effectiveUses() (:42-66): nothing prefers a `<featureId>-<subclassId>` variant's `limitedUses` over the generic record's, so the derive.ts:3428-3442 variant lane cannot retune a class feature's uses from a subclass. Blocks school-of-unified-magical-theory#drain-bonded-item-uses (print: one Drain Bonded Item per day per spell rank you can cast).
- src/rules/featGrantsAuto.ts:671 (and lines 228, 293, 453, 511, 533, 669, 763): delete the eight animist apparition entries whose skills the class option's `grants.lores` + `loreProgression` already grant at build.ts:3620. That IS steward-of-stone-and-fire#duplicate-lore-grant — there is no core.json row to author for it.
- src/rules/companionGrants.ts:423: the angel-eidolon note still reads "Hallowed Strikes: the eidolon's unarmed Strikes deal an extra 1 spirit (good) damage." — no holy trait, no target restriction, no nonlethal clause. Replace with the eidolon-1 text my description row now carries. Third copy of angel-eidolon#hallowed-strikes-text.
- src/rules/derive.ts:5224: a mode targeting 'speed' folds into `speeds.land` only. My curse-of-creeping-ashes-4 row delivers the printed -10 to the walking Speed; "all your Speeds" (WG emits SPEED_FLY / SPEED_CLIMB / SPEED_BURROW / SPEED_SWIM too) needs that one line widened to the other movement types.
- src/rules/types.ts SpellAccessGrant (:5265-5293) + the spell picker's narrowing: no trait filter, so my fey-eidolon row opens the whole arcane list where print opens "spells that have the illusion or mental traits that appear on the arcane spell list". Wants `traits?: string[]` on SpellAccessGrant, applied where spellListTraditions narrows the picker.
- src/rules/types.ts IwrEntry + the resistance loop at src/rules/derive.ts:2701-2729: no gate for VOID HEALING. deriveDefenses already computes `negativeHealing` in the same function (:2917) and build.ts computes the same fact at :6663-6667, so a `whenVoidHealing?: boolean` sibling of `whenCreatureTrait` is a one-line gate. Until it exists, otherworldly-protection's void→vitality swap rides a `condition` string instead of switching the type; once it lands, the row's void entry becomes {whenVoidHealing:false} and a {type:"vitality", value:"3+floor(@actor.level/2)", whenVoidHealing:true} entry joins it.
- src/builder + build.ts resolvePick (build.ts:6299/6304): `effectChoices` on an INVENTOR MODIFICATION record has no reader at all — resolvePick runs over ownedFeatureIds (the class feature list, build.ts:6149) and grantOptions (subclass / extra-choice picks), and c.inventor.modifications is in neither, though derive's ownedFeatureIds does include them. Not needed for my rows (I used sheet-fact gates instead), but any future finding that wants a visible pick on a modification hits this wall.

PER FINDING

1. skin-hard-as-horn — ROW: {classFeatures, skin-hard-as-horn, choice, <whole object, note→condition on all three resistance grants>, supersedes:true}.
   ikon-15: "This resistance doesn't apply against critical hits." The clause sat in a `note` key IwrEntry (src/rules/types.ts:479-527) does not declare and the resistance loop (derive.ts:2701-2729) never reads. `condition` is the field that loop renders beside the value; `against` was rejected because it WITHHOLDS the entry from the total, and a type whose only contributor is `against` renders on no screen at all — the resistance is real, only its limits are unknowable from the sheet.
   TEST: "skin-hard-as-horn keeps the immanence limits on the resistance it grants" — pins the field on all three options AND runs deriveDefenses on a level-8 exemplar holding the ikon with the morning's answer stored, asserting the resistance is still counted (bludgeoning 4) and that sources['resistance:bludgeoning'] carries the printed clause. Verified against a patched in-memory copy that the derive half passes once the row lands, then removed the probe.
   Not done: the other 33 unread `note` entries (five instinct records) — DATA STILL NEEDED, not findings here.

2. the-tangible-dream#granted-spell-2nd — NO ROW, and no test.
   The repaired text already ships: core-descriptions.json classFeatures/the-tangible-dream reads "- 2nd: Invisibility". The only stale copy is classes.psychic.subclass.options[the-tangible-dream].description, and no backfill row can reach it — a `description` row carrying a `path` is refused, and there is no existing classes/psychic subclass `options` row to supersede, so the alternative is re-emitting the whole 14-option conscious-mind array (~200 KB) to change one substring. Filed as the loadDescriptions() CROSS-FILE GAP, which fixes this record and 29 others in one line.

3. fey-eidolon#fey-gift-spells — ROW: {classFeatures, fey-eidolon, spellListAdditions, {traditions:["arcane"]}}.
   eidolon-8's Fey Gift Spells. The widening loop at build.ts:6536-6552 reads spellListAdditions off classFeatureIdsOwned(...), which includes the summoner's subclassId (fey-eidolon itself), and pushes a whole-tradition rule onto Character.spellListTraditions rather than expanding ~1,500 ids.
   TEST: "fey-eidolon delivers Fey Gift Spells" — harness half first (the built summoner's subclassId), then the field, then ch.spellListTraditions carrying arcane. Probed: it resolves to [{traditions:["arcane"], from:"Fey Eidolon"}].
   Not done: the illusion-or-mental narrowing — SpellAccessGrant has no trait filter (CROSS-FILE GAP). The row over-opens the arcane list, which is the proposal's accepted cost and still closer to print than today's zero access.

4. alchemical-sciences-methodology#edition-aonid — ROWS: {classFeatures, alchemical-sciences-methodology, aonId, "methodology-5"} and {classFeatures, …, edition, "remaster", supersedes:true}.
   We ship methodology-5's text verbatim (Player Core 2 pg. 103) while pointing at the APG's methodology-1, whose own remaster_id is ["methodology-5"], tagged edition 'legacy'. applyEditionFilter (build.ts:3131-3145) drops 'legacy' when hide-legacy is on, taking grantsActions ['quick-tincture'] and the investigator subclass control with it — a mechanical loss, not a hidden description.
   TEST: "alchemical-sciences-methodology is current Player Core 2 content, not legacy" — harness half pins source.book and the quick-tincture grant (what the filter would remove), then both fields.
   Not done: the three sibling methodologies — DATA STILL NEEDED with their exact remaster ids, since none of them is a finding in this batch.

5. light-mortar-innovation#duplicate-modification-choice — ROW: {classFeatures, light-mortar-innovation, choice, null, supersedes:true}.
   innovation-4 prints ONE pick. The record's `choice` (flag lightMortarModification, marked `inert`) rendered a second control beside inventorModificationOptions (shared.tsx:3585), and only the live picker writes build.inventorModifications (read at build.ts:2945 and derive c.inventor.modifications). WG encodes one modification lane too.
   TEST: "light-mortar-innovation asks for its modification exactly once" — harness half first (the three modification records still carry the tag the live picker reads), then choice === undefined.
   Not done: nothing. value:null deletes the field, so this row cannot literally carry the superseded object merged in — dropping it IS the fix, and the row is declared supersedes:true and says so in its note.

6. school-of-unified-magical-theory#drain-bonded-item-uses — NO ROW (deliberately), no test.
   arcane-school-21 gives a UMT wizard one Drain Bonded Item per day per castable rank; classFeatures/arcane-bond ships {max:1, per:'day'}. The `<featureId>-<subclassId>` variant lane would OWN a created record, but effectiveUses (featUses.ts:42-66) reads only the record's own maxByLevel plus `usesUpgrade` off db.feats — nothing prefers a variant — so the variant would draw a SECOND pip row rather than replacing the 1/day one. Reader gap filed as CROSS-FILE; the exact create row is specified verbatim under DATA STILL NEEDED, to be authored after it.

7. curse-of-creeping-ashes#speed-penalty — ROWS: {modes, curse-of-creeping-ashes-4, modifiers, [<the existing −2 ranged-attack modifier>, {value:-10, type:"status", target:"speed"}]} and {modes, …, note, <same note without the "apply that Speed penalty by hand, the mode can't" clause>}.
   mystery-20 Cursebound 4. The mode's own note claimed the mode could not carry the penalty; it can — 'speed' is a ModeTargetKind (types.ts:4578) read by modeNumberBonus at derive.ts:5224. scripts/data/toggle-modes.json holds the same record and is NOT edited (outside this family's files); effect-backfill applies over it, as modes/dampening-harmonics already demonstrates with a field-level override.
   TEST: "curse-of-creeping-ashes cursebound 4 moves the Speed number" — harness halves first (the existing −2 modifier survives the re-emit; the oracle has a Speed to reduce), then the new modifier, the repaired note, and deriveSpeeds land dropping by exactly 10. Probed: 25 → 15.
   Not done: fly/climb/burrow/swim ("all your Speeds") — derive folds a 'speed' mode into land only (CROSS-FILE GAP).

8. steward-of-stone-and-fire#duplicate-lore-grant — NO ROW, no test. Code-only, and outside this family's files: the duplicate is the featGrantsAuto.ts entry (CROSS-FILE GAP), nothing in core.json is wrong. Verified harmless today — build.ts:5739 writes maxRank(cur, at(r)), a raise-only write, so the flat 'trained' cannot pull the 8th/16th Lore ladder back down; it is a floor waiting for an ordering change.

9. otherworldly-protection#void-healing-swap — ROW (shared with finding 10): {classFeatures, otherworldly-protection, resistances, [void +condition, spirit, unholy whenCreatureTrait:holy, holy whenCreatureTrait:unholy], supersedes:true}.
   The record's own remaster description carries both clauses; we shipped a flat [void, spirit] pair. The void entry keeps counting and now carries the printed swap as a `condition` — the field derive renders beside the number — because no void-healing gate exists on IwrEntry yet (CROSS-FILE GAP, with the follow-up row spelled out there).
   TEST: "otherworldly-protection resists what print says" › "keeps the void resistance counted and names the vitality swap beside it" — harness half pins the unchanged formula, then the condition, then deriveDefenses on a level-8 armor inventor holding the modification (void 7).
   Not done: actually SWAPPING the type for a character with void healing. Needs the IwrEntry gate.

10. otherworldly-protection#sanctified-resistance — merged into the finding-9 row (same record + field; the applier hard-refuses two rows on one key). Contributes {type:"unholy", whenCreatureTrait:"holy"} and {type:"holy", whenCreatureTrait:"unholy"}, both at 3+floor(@actor.level/2).
   `whenCreatureTrait` is honoured by the resistance loop at derive.ts:2708-2709 through hasCreatureTrait → creatureTraitsOf, which reads c.chosenCreatureTraits — exactly where a deity's sanctification is recorded (build.ts:6294-6296). So the whole printed clause lands with NO code change. Deliberately NOT the proposal's `effectChoices`: an inventor modification is in neither of resolvePick's two owned sets, so an effectChoices entry here would have no reader; and WG's "Select a Sanctification" is their workaround for a fact our sheet holds, the same shape settled on battle-creed#control.
   TEST: same describe, second it — pins both gated entries, then deriveDefenses: unholy absent for an unsanctified inventor, unholy 7 once chosenCreatureTraits carries 'holy'. Probed both directions.
   Not done: nothing. WG omits the spirit resistance print grants — WG encoding LESS, so ours stays.

11. red-mantis-magic-school#curriculum-description — ROW: {classes, wizard, path:["subclass"], options, <whole 18-option array with this option's description replaced by core-descriptions.json classFeatures/red-mantis-magic-school.d>, supersedes:true}.
   arcane-school-23 prints all 19 curriculum spells; the option copy the Builder renders had six names deleted ("- **2nd** , Mist", "- **9th**") while descRefs still listed all 16 labels. The mangled string is shipped BY this effect-backfill row, so repairing it there removes the defect at its source; a `description` row carrying a path is refused, and the replacement text is otherwise byte-identical. Every other option and this option's id/name/descRefs/focusSpells/advancedFocusSpell carry over unchanged, and core.json's own `curriculum` field was already correct and is untouched.
   TEST: "red-mantis-magic-school shows the player its whole curriculum" — harness half (descRefs never lost its labels), then all six restored curriculum lines in the option's description.
   Not done: the other 29 stale subclass options — the systemic loadDescriptions() sync (CROSS-FILE GAP). I repaired only the record print names in this finding.

12. angel-eidolon#hallowed-strikes-text — ROW: {classFeatures, angel-eidolon, description, <shipped text with the one pre-remaster sentence replaced by eidolon-1's>}.
   eidolon-1: "unarmed Strikes gain the holy trait and deal 1 extra spirit damage to unholy creatures and creatures with weakness to holy." Our copy said "an extra 1 good damage… harms only evil creatures", contradicting print on trait, damage type and targets. One sentence replaced; descRefs untouched (no ref label changes) and the nonlethal clause preserved.
   TEST: "angel-eidolon prints the remaster Hallowed Strikes" — harness half asserts the one already-correct copy (option.eidolonAbilities[0].text), then the record description matches print, does not match the legacy sentence, and still carries the nonlethal clause.
   Not done: two of the three copies are outside my files. classes.summoner.subclass.options[angel-eidolon].description heals from this row once loadDescriptions() syncs the option (a whole-array `options` re-emit is not available here anyway — effect-backfill already carries per-option path:[…,"id=angel-eidolon"] `eidolonAbilities` rows a pathless whole-value row would collide with), and companionGrants.ts:423 is a src/ file. Both CROSS-FILE GAPS.
