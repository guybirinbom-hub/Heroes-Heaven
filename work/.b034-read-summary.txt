confirmed 42 | refuted 4 | askOwner 2
confirmed but NOT adversarially confirmed: 0

== FAMILIES
(routed from each finding's PROPOSAL by FAMILY_RULES in scripts/wg-batch-run.mjs — first rule wins, exactly one family per finding; a family with no findings is omitted)
engine (12): the-silent-whisper#sending-rank, spirit-instinct#ghost-touch, spirit-instinct#damage-type-choice, construct-innovation#modification-effects, construct-innovation#higher-tier-picks, construct-innovation#duplicate-picker, construct-innovation#overdrive, elemental-blast#damage-type-trait, elemental-instinct#rage-damage-type, psychopomp-eidolon#language, beast-eidolon#language-sylvan, improved-familiar-attunement#ability-budget
situational (7): the-silent-whisper#daze-range, the-silent-whisper#message-reach, curse-of-turbulent-moments, elemental-instinct#concealment, weapon-innovation#segmented-frame-duplicate, scar-of-the-survivor#diehard-ungated, victors-wreath#spell-attack
instruments (17): empiricism-methodology, precise-discipline, spirit-instinct#instrument, vanguard-of-roaring-waters#instrument, elemental-blast#instrument, emotional-acceptance#instrument, way-of-the-pistolero, elemental-instinct#instrument, monarch-of-the-fey-courts#instrument, inspired-stratagem#instrument, giant-instinct#instrument, scar-of-the-survivor#instrument-unsupported, victors-wreath#instrument, the-infinite-eye#organsight, echo-of-lost-moments#instrument, experimental-spellshaping#instrument, improved-familiar-attunement#instrument
data-rows (6): speed-boosters, elemental-blast#action-cost, way-of-the-triggerbrand#combination-weapon-access, beast-eidolon#stat-array-names, the-infinite-eye#clairvoyance-description, improved-familiar-attunement#drain-familiar

== CONFIRMED
- the-silent-whisper#sending-rank [player]: Sending is granted at the wrong rank: the conscious mind's ladder prints it as the 6th-rank granted spell, but our lane files every granted spell under the spell's OWN rank, so a Silent Whisper psychic gets two 5th-rank granted spells (Synaptic Pulse + Sending
    PROPOSAL: Let the ladder carry the granted RANK: add `rank` to the `{id, level}` entries of classFeatures/conscious-mind.grantedSpells (effect-backfill.json), i.e. add {"id":"sending","level":11,"rank":6} for the-silent-whisper, and in src/rules/build.ts (~line 4231) key the grant off the ladder's `rank` when present instead of `content.spells[id].rank`.
- the-silent-whisper#daze-range [player]: The conscious mind doubles daze's range and neither side carries it, so a Silent Whisper psychic reads 60 feet on the very row they cast from.
    PROPOSAL: Add to SPELL_MARKERS in src/rules/situationalBonuses.ts: "daze": [{ source: 'the-silent-whisper', when: "while you have The Silent Whisper conscious mind", bonus: "range 120 feet (instead of the spell's usual 60)" }] — the same lane and wording as the telekinetic-projectile entry.
- the-silent-whisper#message-reach [player]: The conscious mind's message modification (no line of effect or line of sight needed, and it can be cast purely telepathically, losing the auditory trait) is modelled by neither side, so the player never learns their message ignores walls and can drop auditory
    PROPOSAL: Add to SPELL_MARKERS in src/rules/situationalBonuses.ts: "message": [{ source: 'the-silent-whisper', when: "while you have The Silent Whisper conscious mind", bonus: "no line of effect or line of sight needed (any unblocked path of 120 feet or less), and it can be cast purely telepathically, losing the auditory trait" }].
- empiricism-methodology [internal]: The baseline digest's MISSING skill|arcana / skill|crafting / skill|occultism rows are an instrument misread — the trained-skill choice is carried by the investigator CLASS's subclass option, not by the class-feature record the comparer read.
    PROPOSAL: instrument: the comparer looked for the skill grant on classFeatures/empiricism-methodology and did not sweep classes/investigator.subclass.options[...].skillChoice / skillChoiceLore, which is where every methodology's skill grant lives (alchemical-sciences -> grants.skills [crafting], forensic-medicine -> [medicine], interrogation -> [diplomacy]).
- precise-discipline [internal]: The classDc / attribute / spellcasting / UNSUPPORTED flags are an instrument misread — every one of those is carried by the psychic CLASS record plus its subconscious-mind choice option, not by the class-feature record.
    PROPOSAL: instrument: the comparer read classFeatures/precise-discipline alone and did not sweep the owning class's extraChoices option (keyAbility) or classes/psychic.spellcasting, so a key-attribute-setting choice option reads as an unsupported class-DC/attribute/spellcasting gap.
- speed-boosters [player]: The always-on +5-foot status bonus to Speed is delivered by nothing on our side — only the Overdrive rider is carried, as a star — so an inventor with Speed Boosters shows their unmodified Speed.
    PROPOSAL: Add `landSpeedBonus: 5` to classFeatures/speed-boosters (effect-backfill.json), matching hyper-boosters' existing landSpeedBonus: 10 — and because derive.ts:5070-5072 SUMS landSpeedBonus across owned features while both bonuses are status bonuses to Speed and Hyper Boosters prints "You must have the speed boosters modification to select this modification", the pair must not stack: either drop hyper-boosters to the +5
- curse-of-turbulent-moments [player]: The curse's status penalty to AC and to saves is carried only as a prose mark on the cursebound condition row; no AC or save carrier applies it, so an oracle at cursebound 1-4 shows an unpenalised AC and unpenalised Fortitude/Reflex/Will.
    PROPOSAL: Add FEAT_SITUATIONAL['curse-of-turbulent-moments'] in src/rules/situationalBonuses.ts: [{ targets: [{ kind: 'ac' }], when: "against attacks made against you from reactions or free actions, while cursebound", bonus: "−(your cursebound value) status" }, { targets: [{ kind: 'save', detail: 'all' }], when: "against effects that would make you fatigued or slowed, while cursebound", bonus: "−(your cursebound value) status"
- spirit-instinct#ghost-touch [player]: Neither side models the Ghost Touch effect Spirit Rage gives the weapon, so a spirit barbarian striking an incorporeal creature sees no sign that the resistance to all damage does not apply.
    PROPOSAL: src/rules/derive.ts: add a `ghostTouch?: boolean` to the RAGE_DAMAGE entry shape, set it on 'spirit-instinct', and append ', ghost touch vs incorporeal' to the note rageStrikeRider() returns when it is set and the type override applies.
- spirit-instinct#damage-type-choice [player]: Spirit Rage is a per-Rage choice; ours applies 3 spirit damage unconditionally, so a spirit barbarian who rages without taking the spirit option still sees 3 spirit instead of 2 of the weapon's own damage type.
    PROPOSAL: Adopt their label at minimum: add `typeOptional: true` to RAGE_DAMAGE['spirit-instinct'] in src/rules/derive.ts and have rageStrikeRider() print '3 spirit or 2 <weapon type> (choose each Rage)' for such instincts; a full fix is an exclusive-group mode ('Spirit Rage') gating the override the way cat-cursebound-N gates the oracle curses.
- spirit-instinct#instrument [internal]: The SET-GAP resistances flag is an instrument misread — both sides carry the same two Raging Resistance entries at the same value; only the label string differs.
    PROPOSAL: instrument: the comparer diffed the resistance TYPE strings verbatim and never read the sibling `condition` field, so our short label 'all damage from undead' failed to match their sentence-long label for the identical resistance. Compare type+condition together (or normalise) before reporting a resistances SET-GAP.
- construct-innovation#modification-effects [player]: The chosen initial construct modification is a label only — none of the seven modifications' mechanics (Speed, swim Speed, size, senses, the ranged unarmed attack, the three trained skills) reach the construct companion on either side.
    PROPOSAL: Add the seven modifications as classFeatures tagged otherTags: ['construct-innovation-modification'] carrying real fields (speed / swim speed, size, senses, grantedStrikes for Projectile Launcher, grants.skills for Wonder Gears), then apply the picked one to the construct companion from src/rules/companionGrants.ts ('construct-innovation'). WG models nothing here either (their Accelerated Mobility ability block, id 2
- construct-innovation#higher-tier-picks [player]: A construct inventor never gets a modification picker at 7th or 15th level (nor the archetype's Basic Modification pick), while WG offers a modification select at each of those gates.
    PROPOSAL: Once the construct-innovation-modification records exist, delete the `if (type === 'construct')` early return in src/builder/shared.tsx:3550 so the existing tiers loop (initial / breakthrough / revolutionary, plus the viaDedication + basic-modification gate) renders for construct like it already does for armor, weapon and light-mortar.
- construct-innovation#duplicate-picker [player]: Two of our carriers speak for the same initial-modification pick and contradict each other: the record's own choice renders a 7-option picker while the Builder's innovation card tells the same player the modifications are text only.
    PROPOSAL: Drop the placeholder SubCard in src/builder/shared.tsx:3550-3555 (or replace it with the real tier picker per construct-innovation#higher-tier-picks) so one carrier answers the pick.
- construct-innovation#overdrive [player]: The construct companion's share of Overdrive is modelled by neither side, so an Overdriven inventor's construct shows none of the extra damage it is owed (and none of the fire damage it risks).
    PROPOSAL: Extend the note on src/rules/companionGrants.ts:104 ('construct-innovation') to carry the Overdrive rider — "While you are Overdriven your construct gains the same Overdrive benefit, and takes the same fire damage on a critical failure" — so it renders on the companion card beside its Strikes.
- vanguard-of-roaring-waters#instrument [internal]: The UNSUPPORTED flag is an instrument misread — every operation WG carries for this apparition is already delivered by our animist apparition option of the same id, exactly.
    PROPOSAL: instrument: the comparer read only classFeatures/vanguard-of-roaring-waters and never the class-side carrier — classes.animist.extraChoices['apparition'].options[<same id>] — so a record with full parity (lores + 8/16 ladder + 10 spells + vessel focus spell) reported UNSUPPORTED. Teach the carrier sweep to follow extraChoices options by id, the same way it already follows subclass options (which is why cobyslarni rea
- elemental-blast#action-cost [player]: Elemental Blast prints as a 1- OR 2-action activity, but our record fixes its action cost at 1 action, so the 2-action blast (the one the sheet's own strike line actually renders, Con status bonus included) has no badge anywhere.
    PROPOSAL: Set classFeatures/elemental-blast actionCost to {"type":"variable","min":1,"max":2} via a scripts/data/effect-backfill.json field entry ({"category":"classFeatures","id":"elemental-blast","field":"actionCost","value":{...}}); ActionCost already has the 'variable' shape and 19 records use it (e.g. feats/black-powder-boost {type:variable,min:1,max:2}).
- elemental-blast#damage-type-trait [player]: The printed rule that a non-physical damage type adds its trait to the blast is not applied: our blast strike's trait list never gains electricity/fire/cold/vitality.
    PROPOSAL: In src/rules/derive.ts deriveBlastStrikes, push `type` into the per-element traits array when it is not a physical type (bludgeoning/piercing/slashing) — i.e. traits: ['attack','impulse','kineticist', el, ...(PHYSICAL.has(type) ? [] : [type])] — and use the same list for the blastTraits handed to mapStepFor/mapNotesFor.
- elemental-blast#instrument [internal]: The 'specialStat' flag on Elemental Blast is a comparer misread: WG's KINETICIST_BLAST_DICE variable has an exact carrier on our side, so it is not an unmodelled special stat.
    PROPOSAL: instrument: the comparer looked for the blast-dice counter on the classFeatures/elemental-blast record; it lives in derive.ts (class table) plus the feats' blastDiceBonus field. No gap — settle 'specialStat' as carried.
- way-of-the-triggerbrand#combination-weapon-access [player]: Our stored text for the way replaces the printed access clause with a paraphrase, so the printed grant of triggerbrand access disappears — a mechanic neither side models and the player can no longer read.
    PROPOSAL: Restore the printed sentence in the description via a scripts/data/effect-backfill.json entry ({"category":"classFeatures","id":"way-of-the-triggerbrand","field":"description", value = the AoN way-6 text) so the triggerbrand access is at least readable; no engine change is needed while item rarity is not gated.
- emotional-acceptance#instrument [internal]: The classDc / attribute / spellcasting flags on Emotional Acceptance are comparer misreads: all three of WG's ops have carriers on our side, on the psychic class record rather than on the class-feature record.
    PROPOSAL: instrument: the comparer searched the classFeatures/emotional-acceptance record for keyAbility / classDc / spellcasting carriers; they live on classes.psychic (extraChoices option keyAbility 'cha' + the class spellcasting block). No gap — settle classDc, attribute and spellcasting as carried.
- way-of-the-pistolero [internal]: All three instrument flags on this record are comparer misreads: the way's deeds and its Deception-or-Intimidation choice are carried on the CLASS subclass option, not on the classFeatures record the comparer read.
    PROPOSAL: instrument: the comparer scoped the record to core.json classFeatures.way-of-the-pistolero and never looked at classes.gunslinger.subclass.options[way-of-the-pistolero], where skillChoice and featureIds live; it also compared deed slugs with the possessive apostrophe stripped differently on each side (pistolersretort vs pistolerosretort). Teach the option/grant comparer to fold a class's subclass.options[].skillChoic
- elemental-instinct#rage-damage-type [player]: The chosen element's damage type never reaches the Strike: our Rage rider hard-codes the literal string 'energy' for elemental-instinct, so a barbarian who chose Air (slashing) or Earth (bludgeoning) sees "4 energy" instead of the physical type they picked.
    PROPOSAL: src/rules/derive.ts, rageStrikeRider: for elemental-instinct take the damage type from the answered `instinctElement` choice (option value's suffix — 'air-electricity' -> 'electricity') instead of the literal type:'energy' in RAGE_DAMAGE; the answer must be carried onto the Character in src/rules/build.ts (it is already read there as build.featChoices[`feature:${fid}`] in the choiceTokens pass, build.ts:8508), keepin
- elemental-instinct#concealment [player]: Elemental Rage's concealment against ranged attacks is prose on both sides — no carrier puts it on the sheet while the Rage toggle is on, so the player never sees that ranged attackers owe a DC 5 flat check.
    PROPOSAL: src/rules/situationalBonuses.ts: add "elemental-instinct": [{ targets: [{ kind: 'ac' }], when: "while raging, against ranged attacks", bonus: "you are concealed (attacker must succeed at a DC 5 flat check); this concealment can't be used to Hide or Sneak" }] — same shape as the existing superstition-instinct entry.
- elemental-instinct#instrument [internal]: The baseline digest's SET-GAP for this record is a comparer misread on both halves: it read ours=(nothing) for resistances and read WG's kinetic-element ability blocks as grants we lack.
    PROPOSAL: instrument: the comparer flattened only record-level fields, so it missed choice.options[].grant.whileActive[].resistances and mistook WG's kineticelement* ability blocks for a grant lane; no resistance should be added (doing so would double-count the two rows we already deliver).
- monarch-of-the-fey-courts#instrument [internal]: The record's UNSUPPORTED flag is a carrier misread — our mechanics for this apparition are on the animist class record, not on the classFeatures row the comparer read.
    PROPOSAL: instrument: the comparer looked only at classFeatures/<id> and reported UNSUPPORTED; for animist apparitions (and every extraChoices group) it must read classes.<class>.extraChoices[].options[]. Only the Avatar mode is a genuine gap here.
- inspired-stratagem#instrument [internal]: The grantsRecord flag on classFeatures/inspired-stratagem is a pairing misread: our level-1 record IS the granted reaction (WG's own level-1 row 19732 carries no operations), and the giveAbilityBlock the comparer matched belongs to the level-8 feat, which we a
    PROPOSAL: instrument: the comparer paired our level-1 reaction record with WG's level-8 feat row; compare feats/inspired-stratagem against WG 20263 and the level-1 record against WG 19732 (both empty). Nothing to build. Optional hygiene: classFeatures/inspired-stratagem duplicates actions/inspired-stratagem (same aonId, nothing owns it) — one of the 5 such feat-aonId class-feature stubs (the-bigger-they-are, primal-howl, inspi
- psychopomp-eidolon#language [player]: The psychopomp eidolon's fixed Language line (Requian) is delivered by neither the summoner subclass option nor its COMPANION_MODS row, so the eidolon block shows no Languages entry at all — while WG grants the language.
    PROPOSAL: Add `languages: ["requian"]` to COMPANION_MODS['psychopomp-eidolon'] in src/rules/companionGrants.ts (content.languages already has the id 'requian', so deriveEidolon resolves it to the printed name).
- weapon-innovation#segmented-frame-duplicate [player]: FEAT_SITUATIONAL['weapon-innovation'] restates Segmented Frame's +2 circumstance Stealth bonus that FEAT_SITUATIONAL['segmented-frame'] already delivers, so a weapon inventor who picks Segmented Frame sees the same star twice — and one who does not pick it see
    PROPOSAL: Delete the "weapon-innovation" entry from FEAT_SITUATIONAL in src/rules/situationalBonuses.ts:3680; the 'segmented-frame' entry at :1406 is keyed on the actual pick and already delivers it.
- giant-instinct#instrument [internal]: The baseline digest's SET-GAP 'set|resistances theirs=bludgeoning,cold,electricity,fire ours=(nothing); ours(all): (none)' is wrong — our Raging Resistance is carried, and at the same value as WG's.
    PROPOSAL: instrument: the resistances comparer reads only flat record fields (resistances/defenses) and misses choice.options[].grant.whileActive[].resistances — the carrier used by every barbarian instinct's Raging Resistance. Settle giant-instinct's SET-GAP as an instrument misread, not a gap.
- scar-of-the-survivor#diehard-ungated [player]: The Diehard half of the Immanence sentence is granted permanently while print gives it only while the divine spark is in this ikon — the +1 Fortitude half of the SAME sentence is correctly gated, so the record delivers one clause conditionally and the other un
    PROPOSAL: Drop `grantsFeats: ["diehard"]` from classFeatures/scar-of-the-survivor (author the removal in work/effect-backfill.json, the only home that survives `npm run data`) and carry the clause in the existing src/rules/situationalBonuses.ts:1404 entry the way leap-the-falls does — append a second target line `{ targets: [{ kind: 'save', detail: 'fortitude' }], when: "while your divine spark is in this ikon (immanence activ
- scar-of-the-survivor#instrument-unsupported [internal]: The UNSUPPORTED flag is a harness host limitation, not a delivery gap: the ikon is reachable in the app and its carriers fire on a real exemplar build.
    PROPOSAL: instrument: classFeatureHost has no host strategy for an extraChoices-group option (exemplar ikons/epithets, thaumaturge implements, kineticist elements, psychic minds) — the flag means 'not played', not 'not delivered'. Settle scar-of-the-survivor's UNSUPPORTED as an instrument limitation; a host that answers the owning class's extraChoices group would play it.
- beast-eidolon#language-sylvan [player]: The eidolon's printed fixed Language line is not delivered — our companion mod for beast-eidolon carries no `languages`, so the eidolon block shows no language, while WG grants it.
    PROPOSAL: Add `languages: ["Sylvan"]` to the 'beast-eidolon' entry in src/rules/companionGrants.ts, in the same printed-name shape as 'fey-eidolon' (our language table is remaster-only, so the printed name is the right form here, not an id).
- beast-eidolon#stat-array-names [player]: Our shipped description labels the Beast eidolon's two ability arrays with the Anger Phantom eidolon's names — a player picking an array sees two headings that do not exist on the Beast's page.
    PROPOSAL: Correct the two headings to "Brutal Beast" and "Fleet Beast" in the beast-eidolon description (classFeatures/beast-eidolon in core-descriptions.json and the summoner subclass-option copy), authored as a description override in work/effect-backfill.json so it survives `npm run data`.
- victors-wreath#spell-attack [player]: The immanence aura is written as a bonus to Strike attack rolls only, so a spellcasting exemplar (or a spellcasting ally in the emanation) never sees the +1 status bonus on their spell attack row, although print says "attack rolls" without qualification.
    PROPOSAL: Add { kind: 'spell', detail: 'attack' } to the targets of FEAT_SITUATIONAL['victors-wreath'] in src/rules/situationalBonuses.ts:1436, matching pennant-of-victory (line 534), the project's existing "you and allies" attack-roll aura, which carries both { kind: 'strikeAttack' } and { kind: 'spell', detail: 'attack' } for the same printed phrase.
- victors-wreath#instrument [internal]: The UNSUPPORTED flag is a harness blind spot, not a data gap: the harness's classFeatureHost only searches class.features and class.subclass.options, and exemplar ikons are options of classes.exemplar.extraChoices.ikon, so no host is ever found for any of the 
    PROPOSAL: instrument: classFeatureHost (test/wg-experience.harness.test.tsx:493-509, reason string at :509 'no class or subclass grants this feature at level 20') walks only db.classes[classId] features and db.classes[classId].subclass.options — extend it to walk db.classes[classId].extraChoices[].options and seat the pick in build.extraChoices, so ikon / apparition / epithet / thesis options are judged instead of skipped.
- the-infinite-eye#clairvoyance-description [player]: The conscious-mind option the player reads in the Builder lists its granted-spell ladder with the 4th-rank spell name missing ("- 4th:" with nothing after it), so a psychic choosing The Infinite Eye is shown a blank 4th rank; only this one option of the six is
    PROPOSAL: Restore "- 4th: Clairvoyance" and the { label: 'Clairvoyance', key: 'spells' } descRef on the the-infinite-eye option of classes.psychic.subclass via a scripts/data/effect-backfill.json entry { category: 'classes', id: 'psychic', field: 'subclass', ... } (the gunslinger 'subclass' entry is the existing precedent for that field). Mechanics need no change — grantedSpells already lists clairvoyance.
- the-infinite-eye#organsight [internal]: The batch's NO-SHEET-EFFECT verdict rests on a single undelivered op — WG's 3rd-rank grant of the LEGACY spell Organsight — where the remastered print grants Locate, which we do carry; there is nothing missing on our side.
    PROPOSAL: instrument: the lone undelivered op is WG carrying the pre-remaster ladder entry — AoN conscious-mind-2 (Dark Archive, legacy) prints "- **3rd** Organsight" while conscious-mind-8 (Dark Archives Remastered, our aonId) prints "- **3rd** Locate". Settle the-infinite-eye's NO-SHEET-EFFECT as the known legacy-vs-remaster false positive; no change to our data.
- echo-of-lost-moments#instrument [internal]: The UNSUPPORTED flag is the same harness blind spot: animist apparitions are options of classes.animist.extraChoices.apparition, which classFeatureHost never searches, so all 14 apparitions are skipped rather than judged; the record's printed mechanics are in 
    PROPOSAL: instrument: extend classFeatureHost (test/wg-experience.harness.test.tsx:493-509) to walk db.classes[classId].extraChoices[].options and seat the pick in build.extraChoices, so apparition options are judged instead of reported UNSUPPORTED.
- experimental-spellshaping#instrument [internal]: The UNSUPPORTED flag is the same harness blind spot: wizard arcane theses are options of classes.wizard.extraChoices.thesis (5 options), which classFeatureHost never searches; both printed clauses of this thesis are in fact carried, so there is no gap behind t
    PROPOSAL: instrument: extend classFeatureHost (test/wg-experience.harness.test.tsx:493-509) to walk db.classes[classId].extraChoices[].options; note that the daily half is a ruling-Q23 daily choice and must keep being reported, not failed, once the host exists.
- improved-familiar-attunement#ability-budget [player]: The familiar's ability budget is a flat 4 at every level, so a 1st-level wizard gets one ability more than print allows and an 18th-level wizard gets two fewer.
    PROPOSAL: Set abilityBudget: 3 on FEAT_COMPANION_GRANTS['improved-familiar-attunement'] and generalise the growth step in familiarAbilityBudget (src/rules/companions.ts:663) from the hard-coded 'familiar-witch' test to a growthLevels?: number[] field on the grant, set to [6, 12, 18] for both familiar-witch and improved-familiar-attunement; update the note to 'Choose 3 familiar abilities — 4 at 6th, 5 at 12th, 6 at 18th.' This 
- improved-familiar-attunement#drain-familiar [player]: The thesis replaces Drain Bonded Item with the Drain Familiar free action; WG hands the action over and we hand over nothing, so the sheet still shows only Drain Bonded Item on a wizard who by this thesis has no bonded item.
    PROPOSAL: Add a 'drain-familiar' action record (free action, arcane/wizard traits, limitedUses 1/day, text as printed above) and give classFeatures/improved-familiar-attunement grantsActions: ['drain-familiar'] via scripts/data/effect-backfill.json (arcane-bond's grantsActions ['drain-bonded-item'] is the shape); the record has no AoN page of its own, so its text comes from arcane-thesis-7. Optionally suppress drain-bonded-ite
- improved-familiar-attunement#instrument [internal]: The UNSUPPORTED flag is the same harness blind spot (classes.wizard.extraChoices.thesis), so this record's two real defects above were never surfaced by the gate at all.
    PROPOSAL: instrument: extend classFeatureHost (test/wg-experience.harness.test.tsx:493-509) to walk db.classes[classId].extraChoices[].options so the five wizard theses are judged; with a host present this record would have read NO-SHEET-EFFECT on the Drain Familiar grant.

== ASK OWNER
- skybearers-belt#wg-immanence (CONFIRMED): WG's injected text replaces the ikon's printed Immanence with a different mechanic — a 15-foot ally Bulk aura — and drops the printed +1 circumstance bonus entirely, contradicting print.
- echo-of-lost-moments#illusory-disguise (CONFIRMED): WG's 5th-rank apparition spell for this apparition is Illusory Disguise (encoded with rank 1, between the rank-4 and rank-6 grants), where print gives Illusory Scene at 5th; ours matches print.

== REFUTED
- silence-in-snow: The Builder-facing copy of the patron's description in classes.witch.subclass.options has the burst size deleted — it reads "ice to form in a centered on a squa || The stale string is real in the static file (core.json classes.witch.subclass.options[silence-in-snow] reads "ice to form in a centered", while core-descriptions.classFeatures['silence-in-snow'].d and
- monarch-of-the-fey-courts#avatar: The apparition's Avatar form (Queen of the Winter Court) is prose only for us — WG hands it over as a mode with real Speeds and two Strikes, and no apparition i || The reader read `giveAbilityBlock type=mode abilityBlockId=39041` without opening the block. `node scripts/wg-show.mjs "Monarch of the Fey Courts - Primary" --raw` shows mode 39041 contains exactly th
- giant-instinct#free-larger-weapon: Titan Mauler's free personal larger weapon — a per-character pick constrained to a common melee or ranged weapon of 9 gp or less — is asked and granted by neith || The factual half checks out — the app really does show the remaster clause (the barbarian subclass option and the packet's `printed` both carry PC2/instinct-11 wording, though classFeatures['giant-ins
- beast-eidolon#instrument-language: The digest's "language; OK" for beast-eidolon is a misread — the language lane is NOT delivered (see beast-eidolon#language-sylvan); the OK came from the differ || The stated mechanism is contradicted by the actual run: work/wg-batch-034-experience.json shows beast-eidolon with sheetDiffCount 0 (not >0), deliveredBy "surface", delivery {delivered:3, undelivered:
