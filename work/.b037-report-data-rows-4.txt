BATCH 037 — WG-COMPARISON LANE — family/chunk: data-rows-4 (data-rows, chunk 4 of 6)
Spec:   work/.b037-rows-data-rows-4.json      (11 findings, 11 backfill rows; 3 findings correctly carry none)
Tests:  test/batch037-data-rows-4.test.ts     (19 its: 12 fail until the driver applies, 7 are checks that pass as shipped)
Also:   test/diehard-threshold.test.ts, test/batch031-data.test.ts — two existing assertions my rows flip, re-cited to batch 037
No src/ or scripts/ file was touched. scripts/data/effect-backfill.json was not written.

DATA STILL NEEDED
- classFeatures/animal-instinct field `grantedStrikes` (supersedes the existing overlay row of the same key): re-emit the shipped 31-entry array UNCHANGED and append the printed Web row — {"name":"Web","damageType":null,"die":null,"traits":["unarmed"],"group":"brawling","range":15,"choiceValue":"spider"} (instinct-8's table prints "| Spider | Fangs | 1d8 P | Grapple, unarmed, venomous | / | Web | Special* | Range increment 15 feet |", and the footnote "*The spider's web attack deals no damage, but the target takes a -10-foot circumstance penalty to its Speeds for 1 round on a hit"). BLOCKED until the two CROSS-FILE GAPS below land: the lane's type declares die/damageType required and build.ts copies both onto the NaturalAttack, so this row shipped today renders a Strike with no damage dice instead of a damageless attack. Exact shape of the null/omitted damage fields is the lane change's call, not mine.

CROSS-FILE GAPS
- animal-instinct#spider-web — src/rules/types.ts `GrantedStrike` declares `die: string` and `damageType: string` as REQUIRED, and src/rules/build.ts:2860 (`collectGrantedNaturals`'s `push`) copies both onto the NaturalAttack after running `die` through `stepDie`. Owner #145 option A is a LANE change first: make a damageless granted attack expressible and make the Strikes readout print "no damage" for one. Both files are outside this chunk.
- animal-instinct#spider-web — the Web's rider ("the target takes a -10-foot circumstance penalty to its Speeds for 1 round on a hit") has no carrier on a granted attack: `GrantedStrike` has no on-hit effect field and `StrikeDamageRider` carries damage only. Needs either a rider lane or a recordMarks entry in src/rules/situationalBonuses.ts beside the new attack row.
- scar-of-the-survivor#dying-toggle AND soul-well#dying-toggle — nothing can move the death threshold from a TOGGLE. `ModeDef` (src/rules/types.ts) has no dying-threshold field, and src/rules/build.ts:6958-6961 sums `dyingThresholdBonus` off `content.feats[...]` only, unconditionally, into `Character.dyingThreshold`. Needs `ModeDef.dyingThresholdBonus` plus a read of the ACTIVE modes in src/rules/play.ts's applyPlayState (the same place borrowed spells and daily languages are overlaid). Until then both modes carry the clause in their `note` and the player raises the number by hand.
- nagaji-spell-familiarity#daily-control — src/rules/featCantripGrants.ts:104 still carries `'nagaji-spell-familiarity': { prompt: "Choose a cantrip", tradition: 'occult', options: ['daze','detect-magic','telekinetic-hand'] }`, which is the BUILD-TIME picker print does not have ("During your daily preparations, choose…", feat-3984). Delete that one entry — the same deletion batch 031 made for dream-magic, colugos-traversal, empathic-calm and merge-with-the-source, pinned by test/batch031-gap-data-rows.test.ts:63, which should grow this id. NOTE the file's own warning: scripts/aon-verify/apply-clear.ts rewrites featCantripGrants.ts whole through a serialiser.
- nagaji-spell-familiarity#daily-control — src/rules/play.ts:513 DROPS the grant my row authors: the daily-choice overlay reads `const rank = content.spells[b.spellId].rank ?? 0; if (rank <= 0) continue;`, so a rank-0 cantrip answered at daily preparations reaches no spellcasting entry at all (that block only ever had to serve Loaner Spell's ranked borrow). Route rank 0 into the innate entry's `cantrips` list, and honour `atWill` instead of forcing `uses: s.usesPerDay ?? 1`. Until this lands the only live control is the build-time picker in the gap above, so the two gaps must land together or the feat grants nothing.

FINDINGS

dream-magic#second-taking — DONE (2 rows).
  Rows: feats/dream-magic `effectChoices` -> null (supersedes the batch-031 row, whose whole value is carried into the sibling row) and feats/dream-magic `choice` -> {flag:"dreamMagicSpell", kind:"array", distinctAcrossTakes:true, options:[dream-message, sleep]} with each option's grant.innateSpells at rank 4, occult, usesPerDay 1.
  Why this shape: an `effectChoices` answer is stored per RECORD (build.effectChoices keyed by record id), so both takings of this maxTakable:2 feat shared one answer. A record `choice` answer is stored per feat ENTRY and src/rules/build.ts:6452's `for (const fc of feats)` loop applies `opt.grant` once per entry, so two takings grant two spells. `choice.distinctAcrossTakes` is the shipped lane for the Special clause (src/rules/build.ts:2036 `answersInOtherTakes`; greyed with a reason per ruling Q27, which leaves exactly one selectable option on the second picker). No new field and no new reader.
  Tests: test/batch037-data-rows-4.test.ts — "dream-magic carries its pick on `choice`, not on the shared effectChoices lane", "dream-magic forces the second taking onto the spell the first did not choose", "dream-magic still grants a 4th-rank occult innate spell once per day on each taking". The batch-031 assertion that read `effectChoices` for the same rank was re-pointed at `choice` in test/batch031-data.test.ts:89-104 and re-cited to this finding.
  Not done: saved characters holding the old effectChoices answer lose it (the answer moves carrier). No migration lane exists for a choice-carrier move and none is in this chunk's files.

scar-of-the-survivor#dying-toggle — PARTLY DONE (1 row); the dying number is blocked, see CROSS-FILE GAPS.
  Row: creates modes/scar-of-the-survivor — gated `feats: ["scar-of-the-survivor"]` (modeGateIds resolves an exemplar's chosen ikons through ownedFeatureIds -> Character.classChoices), duration "while your divine spark is in the scar", one modifier +1 status to save/fortitude, and the Diehard clause in `note`.
  Why this shape: it is field-for-field the three immanence toggles already shipped (modes/aura-fetching-bangles, aura-mirrored-aegis, aura-victors-wreath). aura-mirrored-aegis is the precedent for a mode AND a display star on the same sentence, so the existing star at src/rules/situationalBonuses.ts:1472 is left exactly as batch 034 wrote it.
  Tests: test/batch037-data-rows-4.test.ts — "scar-of-the-survivor ships a mode gated on the ikon, with the +1 status Fortitude bonus", "scar-of-the-survivor names the Diehard clause on the toggle that gates it".
  Not done: "you gain the benefits of the Diehard feat" moves no number — no toggle-driven death threshold exists (CROSS-FILE GAPS).

soul-well#dying-toggle — PARTLY DONE (2 rows); the dying number is blocked, see CROSS-FILE GAPS.
  Rows: feats/soul-well `dyingThresholdBonus` -> null (supersedes the existing overlay row whose value was 1) and creates modes/soul-well — gated `feats: ["soul-well"]`, duration "1 minute", no modifiers, the printed area effect in `note`.
  Why this shape: print (feat-7707) makes it a two-action activity lasting one minute, and it is an AREA effect on living creatures, not a personal permanent one — but build.ts:6960 sums `dyingThresholdBonus` unconditionally into Character.dyingThreshold, so the shipped field gave every Soul Well owner a permanent death threshold of 5. Same toggle shape as scar-of-the-survivor, per owner #140's "also fixes Soul Well".
  Tests: test/batch037-data-rows-4.test.ts — "soul-well no longer carries the permanent dyingThresholdBonus field", "soul-well ships a one-minute toggle carrying the printed area effect". The existing assertion at test/diehard-threshold.test.ts:52 that pinned the field as present was flipped and re-cited to this finding (its it title now names soul-well).
  Not done: the dying value itself still does not move while the toggle is on (CROSS-FILE GAPS).

animal-instinct#spider-web — NOT DONE, deliberately, backfillRows: [].
  A row written today would ship a half-broken Strike: `GrantedStrike` requires die and damageType and build.ts copies both onto the attack. Owner #145 option A asks for the LANE first, and the lane lives in src/rules/types.ts and src/rules/build.ts, outside this chunk. The exact row is in DATA STILL NEEDED; the blocker is in CROSS-FILE GAPS.
  Test: test/batch037-data-rows-4.test.ts — "animal-instinct — and every other granted-attack row — carries a real die, never a blank one" pins that the spider still grants Fangs only AND that no record anywhere has been given a blank-die granted attack in the meantime. It passes today and fails the moment someone half-ships the row without the lane.

merchants-scale#no-rules — DONE (1 row).
  Row: items/merchants-scale `description` -> "**Heroes Heaven note (not printed text)** - the book prints no rules for this item: the whole entry is its Price, Hands and Bulk, so it grants no mechanical benefit."
  Why this shape: the record shipped `description: ""` and has no entry at all in public/core-descriptions.json, so the sheet opened a blank popup. Owner #17: one plain line, clearly marked as ours; no mechanical effect; never WG's invented +1 item bonus to seven skills; no flavour text of our own invented into a printed record. apply-backfill-now.mjs routes a pathless `description` row to core-descriptions.json and creates the missing items entry.
  Test: test/batch037-data-rows-4.test.ts — "merchants-scale carries one line, marked as ours, and no invented bonus" (also asserts passiveEffects is absent and FEAT_SITUATIONAL has no entry, so WG's bonus cannot creep in later).

mask-of-the-mantis-major#printed-contents — CHECK: 3 parts pass as shipped, 1 part fixed (1 row).
  PASSES, nothing changed: both Enhance Vision spells stay at the base 2nd rank (heldSpells {"2":["darkvision","see-the-unseen"]}) — the Major block never restates the Greater's 5th-rank heightening and the owner ruled to follow the archive literally; the once-per-day Locate Target activation is present (heldSpells {"8":["pinpoint"]}, pinpoint's printed rank, with its own `freq2` per-day counter); the +3 item bonus to Perception is present as passiveEffects.perception 3.
  Row: creates modes/item-mask-of-the-mantis-major — the Vernai's Ire activation, fromItemId mask-of-the-mantis-major, +3 item bonus to skill/intimidation, the Vernai-membership requirement and the candle-light clause in `note`.
  Why this shape: the Intimidation half of "The item bonus to Perception and Intimidation is +3" is switched on and off by an activation and gated on Vernai membership, so a passive would hand it to a wearer who is neither. `fromItemId` is the shipped item-mode lane that src/sheet/ItemDetail.tsx:193 renders as the item's own Activate control and src/rules/play.ts:toggleItemMode switches; ~72 other item modes use it.
  Tests: test/batch037-data-rows-4.test.ts — "mask-of-the-mantis-major keeps Enhance Vision at rank 2 and Locate Target once per day" (the check half, also pinning that the Greater's rank 5 did not move) and "mask-of-the-mantis-major carries Vernai's Ire as an item mode worth +3 item to Intimidation".

zealot-staff#free-action-limit — DONE (2 rows).
  Rows: items/zealot-staff `frequency` -> null, and items/zealot-staff `counters` -> the whole field re-emitted with only the `pool` charge entry (the `freq` "per minute" entry dropped).
  Why this shape: equipment-2263 prints "Activate Free Action (concentrate) Trigger You hit with a Strike using the staff" and no Frequency line at all, so the once-per-minute limit and its pip tracked a cadence no source prints. Foundry's once-per-10-minutes is not our source. The resulting shape is exactly items/celestial-staff's and items/staff-of-power's — level-17 staves with a charge pool and no frequency.
  Test: test/batch037-data-rows-4.test.ts — "zealot-staff carries no frequency and no per-minute counter, only its charge pool".

zealot-staff#printed-contents — CHECK: 3 parts pass as shipped, 1 part fixed (1 row).
  PASSES, nothing changed: the spell list matches the page at every printed rank (cantrip Divine Lance; 1st Bane; 2nd Augury; 3rd Warding Aggression; 4th Divine Wrath; 5th Crisis of Faith + Divine Wrath; 6th Spiritual Armament + Zealous Conviction; 7th Crisis of Faith + Divine Decree). The drained-1-unless-you-worship-its-deity clause stays printed text — it fires at daily preparations and no character field records whom the wielder worships against the staff's deity. "Add the cleric spells granted by your deity to the list for their rank and each higher rank" stays printed text too — heldSpells is a static map and no lane widens a staff's list from the wielder's deity.
  Row: items/zealot-staff `builtInRunes` -> {potency: 3, striking: "greater"}.
  Why this shape: "Used as a weapon, the staff is a +3 greater striking staff" (equipment-2263), and the record shipped a bare 1d4 bludgeoning club with no runes, so the Strike carried neither the +3 nor greater striking's extra dice. `builtInRunes` is the shipped lane — 374 items, 26 of them staves; items/celestial-staff, the other level-17 staff, ships the same field.
  Tests: test/batch037-data-rows-4.test.ts — "zealot-staff is a +3 greater striking staff" and "zealot-staff holds the printed spell list at the printed ranks" (the check half).

skybearers-belt#scoped-stars — CHECK: PASSES as shipped, no row.
  The three +1 stars at src/rules/situationalBonuses.ts:2861 are already scoped, not blanket: Athletics "to Disarm, Grapple, Shove or Trip, while your divine spark is in this ikon", Fortitude "to resist a Grapple or a Shove…", Reflex "to resist a Disarm or a Trip…". The save split is per-action rather than per-save-kind because Grapple and Shove are rolled against the defender's Fortitude DC and Disarm and Trip against their Reflex DC, so between them the two save stars cover exactly "your saving throws to resist these actions" and nothing wider. The two-sizes-larger permission reaches the player as the first paragraph of the classFeatures/skybearers-belt description. The transcendence stays text (grantsClassFeatures: ["bear-allies-burdens"]).
  Tests: test/batch037-data-rows-4.test.ts — "skybearers-belt stars name the four actions and never sit blanket on a stat" and "skybearers-belt shows the two-sizes-larger permission to the player". Both pass today and pin the check so the next pass does not re-read it from scratch.
  The WG side of this record (their 15-foot 4-Bulk aura) is skybearers-belt#bulk-aura, an instruments-family settle — not mine and not touched.

spirit-walk#printed-pieces — CHECK: PASSES as shipped, no row.
  All three printed pieces are present. (1) +2 status to Recall Knowledge about spirits, haunts and undead across the 30-foot aura — src/rules/situationalBonuses.ts:1078, first entry. (2) The exploration clause is two further entries there, one on `ac` and one on `save`/all, both scoped "while Searching or Detecting Magic in exploration mode, against reactions triggered from haunts and spirits". (3) The first-turn resistance is feats/spirit-walk.resistances at floor(@actor.level/2), built in batch 036. Print names no sense clause on this feat, so nothing sense-shaped was added — apparition sight stays on Apparition Sense where print puts it.
  Tests: test/batch037-data-rows-4.test.ts — "spirit-walk carries the Recall Knowledge star and the exploration AC/save stars" and "spirit-walk keeps the first-turn haunt/spirit resistance at half level". Both pass today.
  The WG side (their playtest row's precise 30-foot sense) is spirit-walk#playtest-row, an instruments-family settle — not mine and not touched.

nagaji-spell-familiarity#daily-control — PARTLY DONE (1 row); the second control and the cantrip reader are blocked, see CROSS-FILE GAPS.
  Row: feats/nagaji-spell-familiarity `choice` -> the whole shipped value re-emitted (same flag, prompt, kind, daily:true and the same three options in the same order) with an `{spellId, tradition:"occult", atWill:true}` innate-spell grant added to each option. Supersedes the existing overlay row of the same key.
  Why this shape: print asks the question at daily preparations (feat-3984), and the record already asked it there — but its options carried no `grant`, and src/rules/derive.ts:1791 dailyChoiceGrants only pushes `opt.grant`, so the morning's answer was recorded and changed nothing. `atWill` with no rank is the innate-cantrip shape: an innate cantrip is cast at will and auto-heightens.
  Test: test/batch037-data-rows-4.test.ts — "nagaji-spell-familiarity grants an at-will occult innate cantrip from the daily answer".
  Not done: deleting the live build-time picker (src/rules/featCantripGrants.ts) and teaching src/rules/play.ts to stop dropping rank-0 cantrips from a daily-choice grant. Both are src/ files outside this chunk — CROSS-FILE GAPS. Until they land the row adds a correct answer and takes nothing away from a player: the build-time picker still grants today.
