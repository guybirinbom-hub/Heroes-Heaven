[
 {
  "id": "orc-warmask",
  "verdict": "CONFIRMED",
  "reason": "Re-read both sides independently. THEIRS (wg-show --raw, id 23174): one select, four options, each adjValue SKILL_RELIGION|NATURE|ARCANA|OCCULTISM = {\"value\":\"T\"} plus giveAbilityBlock feat 20087 plus giveItem — the giveItem ids are 13062/13064/13065/13066, four distinct masks, not 13062 four times as the finding transcribed (immaterial to the skill rows; already settled as wg-identity 'orc-warmask': ['items']). PRINTED (public/ast/feats.json.gz, feat-2415): \"You become trained in the associated skill, and you gain the Dubious Knowledge skill feat as a bonus feat… you gain a +1 item bonus to the associated skill whenever you wear the warmask\", with the four sources. Print, WG and ours all say trained, so this is not a WG-vs-print disagreement and rule (4) does not fire. OURS: core.json feats['orc-warmask'].choice = flag 'warmaskTradition', kind 'array', four options whose grant.skills hold the STRING 'trained'; effect-backfill rows confirmed (choice, grantsItems, effectChoices:null — a REMOVAL); no numeric rank exists in the record, the overlay row or any registry, so the sheet is not broken. Carriers verified by grep: featFeatGrants.ts:506 dubious-knowledge; featCantripGrants.ts:93 traditionFromChoiceFlag:'warmaskTradition'; situationalBonuses.ts:2765 targets [{kind:'skill',detail:'all'}] bonus '+1 item'; git show HEAD:src/rules/featGrantsAuto.ts:252 still has the skillChoices slot, the worktree replaced it with the MOVED comment — both carriers wg-values could read are gone. MEASUREMENT ARTEFACT CONFIRMED: `node scripts/wg-values.mjs --ids orc-warmask --verbose` reproduces DIFFERENT skill|religion/nature/arcana/occultism theirs=trained ours=1 with `ours(all): skill|all=1`; situationalMagnitudes puts skill|all=1 with anyDetail, which adds 'skill' to __wildcards, and the compare loop offers that wildcard as the candidate for each per-skill row. The blind spot is unique and provable: `grep -n \"rec\\.choice\" scripts/wg-values.mjs` returns EXACTLY ONE hit, inside ourSets — ourAssertions reads only rec.effectChoices for grant.skills. LIVE READER CONFIRMED: build.ts finds the picked option on content.feats[fc.featId].choice and calls applyAlwaysOn, which does proficiencies.skills[k] = maxRank(cur ?? 'untrained', r); the askedAtDailyPrep guard (derive.ts:1147) requires def.daily, which this choice does not set, so the reader is not skipped. Sibling precedent verified verbatim: wg-diff.mjs has the plain-choice twin with the \"a container should not decide whether a grant counts\" rationale, ourSets has it too, and test/bespoke-tail.test.ts asserts the kind:'array' + per-option grant.skills shape for ancient-memories. Rule-4 sweep clean: only three orc-warmask hits in all of src/. No sibling regression is possible from the fix — the compare loop only emits a row when candidates is empty or nothing matches, so adding assertions can only remove rows, never create them; and it cannot manufacture a false agreement because the added assertion is exactly what the live reader applies. TWO CORRECTIONS, neither fatal: (1) every cited line number has drifted +15 (the file was rewritten at 18:02 today) — ourAssertions is now 496, the effectChoices block 750-758, situationalMagnitudes 457, SETTLED_VALUES 786, the compare candidates 1274, ourSets' choice loop 358; (2) the \"7 further records at risk\" is LATENT, not active — all 8 records exist as claimed (I scanned core.json and got exactly that list), but running the comparer on them reports 6 as \"prose-only bonus (no number asserted)\" and avenger is absent from their dump, so their side asserts nothing comparable today. The generalization is still right; it just does not unblock 7 rows now. Fix improved to the merged-container form (see correctedFix): the standalone loop as written works, but the neighbour idiom in ourSets merges both containers in the loop HEADER, which also covers grant.speeds in the same edit (zero records today, same latent hole) with no duplicated body. I also verified the corpus has no choice.options[].grant.save and no choice.options[].grant.speeds today, so the finding's \"grant.save twin for symmetry\" is harmless-but-speculative; the merged form gets it for free. The situational wildcard is correct behaviour and stays — narrowing it would only turn the four DIFFERENT rows into MISSING, so the harvest fix is needed either way. The narrow SETTLED_VALUES alternative is correctly ranked second-best.",
  "correctedFix": "PREFERRED (src-edit, scripts/wg-values.mjs). ⚠ Line numbers in the finding are stale by +15; anchor by content, not number. In ourAssertions (now line 496), the effectChoices harvest is now at lines 750-758 and reads:\n\n  for (const ch of Array.isArray(rec.effectChoices) ? rec.effectChoices : []) {\n    for (const o of ch?.options ?? []) {\n      for (const [k, v] of Object.entries(o?.grant?.speeds ?? {})) put(`speed|${k}`, typeof v === 'string' ? v : Number(v));\n      for (const [k, v] of Object.entries(o?.grant?.skills ?? {})) put(`skill|${k}`, v);\n      for (const [k, v] of Object.entries(o?.grant?.save ?? {})) put(`save|${k}`, v);\n    }\n  }\n\nChange ONLY the loop header, giving the plain `choice` its home in the same walk — the identical idiom ourSets already uses at line 358 and wg-diff.mjs uses for kinds (\"a container should not decide whether a grant counts\"), but keeping this line's Array.isArray guard, which the :358 form omits:\n\n  for (const ch of [{ options: rec.choice?.options ?? [] }, ...(Array.isArray(rec.effectChoices) ? rec.effectChoices : [])]) {\n\nBody unchanged. Extend the existing comment with: \"…and a plain `choice`'s options carry `grant` in the same shape. Orc Warmask's four warmask sources moved onto it (the picked source sets the trained skill AND names the tradition Mask of Power casts at), and reading only `effectChoices` reported four trained skills the character really gets as `ours=1` — the `skill` wildcard from the record's own +1-item ceremony bonus, which is the only thing left to answer with. Same rule as ourSets above and wg-diff's kinds walk.\"\n\nWhy this over the finding's standalone loop: same effect on orc-warmask (candidates for skill|religion becomes ['trained', 1]; String('trained') === String('trained') matches, all four rows clear, record goes clean and wg-batch-gate's VALUES check passes), but it also closes the identical hole for grant.speeds in one edit and adds no duplicated body. Verified no regression risk: the compare loop pushes a row only when candidates is empty or no candidate stringifies equal, so extra assertions can only remove rows.\n\nDO NOT also narrow situationalBonuses.ts:2765 — the detail:'all' wildcard is what lets the +1 item ceremony bonus match a per-skill op, and removing it would turn the four rows from DIFFERENT into MISSING rather than fixing them.\n\nThe narrow SETTLED_VALUES alternative stays valid as a fallback (note its anchor is now line 786, not 771), but it settles one record and leaves the other 7 choice-carried skill grants — ageless-spirit, ancient-memories, endless-memories, ancestral-longevity, expert-longevity, haunting-memories and classFeatures/avenger, which I confirmed are the complete set in core.json — to re-open the same way the moment their side starts asserting a rank for them."
 },
 {
  "id": "creative-prodigy",
  "verdict": "CONFIRMED",
  "reason": "Independently reproduced end-to-end. THEIR SIDE: `wg-show.mjs creative-prodigy --raw` returns two rows. 38501 — traits {1451}=Nephilim, content_source 400=War of Immortals, prerequisites {Proteankin}, ops len 551 — encodes exactly `adjValue SKILL_PERFORMANCE {value:T}` + `giveAbilityBlock {type:feat, abilityBlockId:20241}` + `addBonusToValue SKILL_PERFORMANCE value:1 type:circumstance text:\"to Make an Impression\"`. 23367 — traits {3035}=Ganzi, content_source 18=Ancestry Guide (backport), prerequisites {}, ops len 561 — encodes SKILL_DECEPTION + SKILL_PERFORMANCE + createValue SKILL_LORE_ART. Trait and content_source names resolved from their own tables, not assumed. PRINT: the AoN mirror at C:/wonderers guide/aon-2e-archive/data/by-category/feat/ holds these as TWO DISTINCT FEATS — feat-7199.json (War of Immortals pg. 53, trait Nephilim, prereq Proteankin) whose text grants Performance, the Impressive Performance skill feat, and +1 circumstance to Make an Impression with no Deception anywhere; and feat-2532.json (Ancestry Guide pg. 96, trait Ganzi, no prereq) whose text grants Deception, Performance and Art Lore. So neither WG row disagrees with its own printed source — this is not a WG-vs-print conflict and does not route to ASK-OWNER; it is a comparer identity error. PAIRING PROVEN: wgRowsByBucket (wg-parse.mjs:245-267) keys one row per normalised name and its only tie-break is `ops.length > prev.operations.length` (:259-262), so 561 beats 551 and the Ganzi twin displaces the nephilim one by 10 characters, exactly as claimed. OUR RECORD is unambiguously feat-7199: core.json feats['creative-prodigy'] carries aonId feat-7199, traits ['nephilim'], prerequisites ['Proteankin'], source \"Pathfinder War of Immortals\", and core-descriptions.json holds the nephilim prose. All three of 38501's ops have a live home, each grep-verified verbatim: featGrantsAuto.ts:233 `'creative-prodigy': { skills: { performance: 'trained' }, redundantFallback: true }` (read via featGrants.ts:24 FEAT_SKILL_GRANTS), featFeatGrants.ts:625 `'creative-prodigy': ['impressive-performance']` (read via build.ts:58/4412 featFeatGrantsFor), situationalBonuses.ts:249 targets skill|performance when \"when you use Performance to Make an Impression\" bonus \"+1 circumstance\" (read via explain.ts:78 and CompanionsTab.tsx:49). REPRODUCED: `node scripts/wg-values.mjs --ids creative-prodigy --verbose` emits exactly one row, `MISSING skill|deception theirs=trained ours=(nothing)`, with `ours(all): skill|performance=trained,trained,1` — so ['skill|deception'] is the complete and minimal settle key. No 'creative-prodigy' key exists in SETTLED_VALUES today, so no collision, and the suppression path is real (wg-values.mjs:1270 `if (!RAW_SETTLES && (SETTLED_VALUES[id] ?? []).includes(key)) continue`). TWO CORRECTIONS, neither load-bearing. (a) Every cited line number is stale by roughly +32: SETTLED_VALUES is DECLARED at :786 (not :771) and READ at :1270 and :1284 (not :1244); theirRowFor is :1183-1186 (not :1152-1155); the wg-parse tie-break is :259-262. (b) The aside \"no ganzi ancestry\" is factually wrong — we DO ship heritages['aon-ganzi'], a fully modelled versatile heritage with chaos-resistance effectChoices, plus two ganzi-traited feats (vestigial-wings feat-2536, mischievous-tail feat-2544). What is true, and what the settle actually turns on, is that the Ganzi Creative Prodigy (feat-2532) is absent from our corpus, so their SKILL_DECEPTION has no record of ours to be missing from. That absence is a separate corpus-coverage question for the ganzi lane, not a value gap on feat-7199, and it must not be answered by grafting Deception onto the nephilim record — which is precisely the defect batch 4 fixed (work/wg-batch-004-parity.json, verdict FIXED). Also note scripts/aon-verify/worklist.json and progress.jsonl carry the SAME name-twin error independently: the worklist entry for slug creative-prodigy has the GANZI prose pasted as its aonProse, and RESUME.md:35 records \"creative-prodigy -> missing Deception\". That is corroboration of a systemic twin problem, not evidence against the fix. FIX #2 (the wg-parse hardening) is a real structural defect but must ship separately: it repaints pairing for every bucket, and if it lands this very settle becomes stale — scripts/wg-settle-stale.mjs already runs and would correctly flag it for deletion at that point, which is a self-correcting interaction worth stating rather than a reason to hold the settle.",
  "correctedFix": "FIX 1 (apply now) — add to SETTLED_VALUES in scripts/wg-values.mjs, which is DECLARED at :786 and READ at :1270 / :1284 (the finding's \":771\" and \":1244\" are stale by ~32 lines):\n\n  'creative-prodigy': ['skill|deception'],\n\nwith this comment above it:\n  /*\n   * CREATIVE PRODIGY — a NAME TWIN, not a gap. Their SKILL_DECEPTION comes from ability_block 23367,\n   * the GANZI Creative Prodigy (trait 3035, content_source 18 = Ancestry Guide backport, no\n   * prerequisite; AoN feat-2532, Ancestry Guide pg. 96), which grants Deception + Performance +\n   * Art Lore. OUR record is the NEPHILIM one: AoN feat-7199, War of Immortals pg. 53, prereq\n   * Proteankin = their ability_block 38501, and we match it op for op —\n   *   adjValue SKILL_PERFORMANCE T   -> featGrantsAuto.ts:233 + the record's own `skills` overlay\n   *   giveAbilityBlock feat 20241    -> featFeatGrants.ts:625 ['impressive-performance']\n   *   addBonusToValue +1 circ.       -> situationalBonuses.ts:249 (Make an Impression)\n   * wgRowsByBucket (lib/wg-parse.mjs:245-267) keeps ONE row per normalised name and breaks ties on\n   * operations LENGTH alone (:259-262); the Ganzi row's encoding is 561 chars to the nephilim row's\n   * 551, so the wrong twin wins by 10 characters. Neither WG row disagrees with its own printed\n   * source, so there is nothing here to adjudicate against the book.\n   * We do not carry feat-2532 at all — note we DO ship the ganzi versatile heritage\n   * (heritages['aon-ganzi']) and two ganzi feats, so importing feat-2532 as its own record is a fair\n   * COVERAGE question for the ganzi lane; it must not be answered by grafting Deception onto\n   * feat-7199, which is the exact defect batch 4 removed (work/wg-batch-004-parity.json).\n   * ⚠ If the wgRowsByBucket tie-break is ever hardened to pair on prerequisites/traits, this entry\n   * goes stale by design — scripts/wg-settle-stale.mjs will flag it and it should then be DELETED.\n   */\n\nVerification of the key: `node scripts/wg-values.mjs --ids creative-prodigy --verbose` reports exactly one row, `MISSING skill|deception theirs=trained ours=(nothing)`, alongside `ours(all): skill|performance=trained,trained,1`. So 'skill|deception' is complete and minimal; no wider settle is warranted. No pre-existing 'creative-prodigy' key in SETTLED_VALUES, so no collision.\n\nFIX 2 (do NOT fold into this family) — the wgRowsByBucket length-only tie-break at scripts/lib/wg-parse.mjs:259-262 is a genuine structural defect, but changing it repaints pairing for every bucket and would invalidate this settle. Spin it out as its own change: prefer the same-name row whose prerequisites/traits are consistent with our record before falling back to operations length, print a warning naming every name that resolved to more than one row, and gate it on a full wg-values/wg-diff before-and-after. When it lands, re-run scripts/wg-settle-stale.mjs and delete the creative-prodigy entry it flags.\n\nFIX 3 (optional cleanup, same root cause) — scripts/aon-verify/worklist.json's creative-prodigy entry carries the GANZI prose as its `aonProse`, and scripts/aon-verify/progress.jsonl:82 plus RESUME.md:35 record the resulting bogus \"missing Deception\" conclusion. These are stale twin-contaminated inputs that will re-derive the same wrong answer if that pass is ever re-run; worth correcting to the feat-7199 text or noting as superseded."
 },
 {
  "id": "oatia-skysage-dedication",
  "verdict": "CONFIRMED",
  "reason": "Mechanism verified end to end, independently. (1) THEIR OPS re-read raw: one CUSTOM select with two branches only — createValue SKILL_LORE_ASTRONOMY {value:'T',attribute:ATTRIBUTE_INT} type=prof, and adjValue SKILL_OCCULTISM {value:'E'} — plus SPELL_ATTACK 'T', SPELL_DC 'T', defineCastingSource OATIA_SKYSAGE:::SPONTANEOUS-REPERTOIRE:::OCCULT:::ATTRIBUTE_INT, two SPELL selects. (2) PRINT re-read from the mirror (public/core-descriptions.json .feats.oatia-skysage-dedication): \"You either become trained in Astronomy Lore or an expert in Occultism\", two cantrips from Detect Magic/Guidance/Know the Way/Read Aura, trained occult attack+DC, Int key. Print, WG and ours agree on both ranks and on there being ONE question, so this is not a WG-vs-print disagreement and ASK-OWNER does not apply. (3) OURS: public/core.json feats.oatia-skysage-dedication carries effectChoices[0].id 'skysage-training' with grant.skills {'lore:astronomy':'trained'} / {occultism:'expert'} plus spellcastingGrant {occult,int,trained}; both are overlay rows in scripts/data/effect-backfill.json (field 'effectChoices' and field 'spellcastingGrant', values non-null so neither is a removal), and casterArchetypes.ts:289 holds the spontaneous occult Int repertoire with cantrips:2. (4) LIVE READER CONFIRMED: build.ts:5035 resolvePick(fc.featId, choices, applyAlwaysOn, …) -> applyAlwaysOn at build.ts:4988 does `for (const [k,r] of Object.entries(g.skills ?? {})) … maxRank(...)`, which is the exact key the record uses; resolvePick also runs for heritages (:5051), deities (:5055), backgrounds (:5056) and class features (:5057, :5062), so this carrier is live in every paired bucket. (5) BLIND SPOT CONFIRMED: OUR_KINDS.skill at wg-diff.mjs:255 contains zero bare 'skills' (measured: 0 occurrences of 'skills' in the whole OUR_KINDS block; only 'passiveEffects.skills'), while the effectChoices-option walk at :671-677 and the plain-choice walk at :688-692 both hand the literal key name to fieldToKinds (:365). So the walk runs, finds 'skills', maps to nothing, and the record reports missing=[skill]. Reproduced: `node scripts/wg-diff.mjs --list --bucket they-only` prints \"oatia-skysage-dedication lvl 2 missing=[skill] ours=[spellcasting,choice,spell,spellSlot]\". (6) RE-OPENING CAUSE CONFIRMED: `git show HEAD:src/rules/featGrantsLane.ts` line 29 still carries 'oatia-skysage-dedication': {\"skillChoices\":[{\"options\":[\"lore:astronomy\",\"occultism\"],\"rank\":\"trained\"}]}; the working tree (uncommitted) replaces it with the REMOVED note at featGrantsLane.ts:54-62, and the removal is correct — that slot could only offer Occultism at trained where the book says EXPERT, and asked the one printed question twice. The entry-by-entry harvest at wg-diff.mjs:576-585 matches such entries by id and FEATGRANT_KEY_KINDS (:566-567) maps skillChoices -> ['skill','choice'], which is precisely what used to supply the credit. (7) I RAN THE PROPOSED FIX (patched copy piped through `node --input-type=module`; nothing written to disk) and it works — oatia leaves they-only. VERDICT CONFIRMED, but the finding's EXPOSURE numbers and its verification line are wrong and would mislead whoever applies it, so see correctedFix.",
  "correctedFix": "Apply the PREFERRED src-edit, with corrected line numbers, corrected safety facts, and a corrected verification step. The settle alternative stays second-best and unchanged.\n\nEDIT (unchanged in substance): scripts/wg-diff.mjs line 255 (finding said :254), OUR_KINDS.skill — add the bare key 'skills' as the first element: skill: ['skills', 'trainedSkill', 'trainedSkillChoice', …]. Verified unique: 'skills' appears zero times as a bare key anywhere in the OUR_KINDS block, so there is no collision with another kind; `choice: {kind:'skills'}` is a VALUE and is handled separately at :706; the FeatGrant tables are harvested through FEATGRANT_KEY_KINDS (:566), which already maps skills -> ['skill'], so that path is untouched.\n\nCORRECTED LINE NUMBERS (wg-diff.mjs is itself modified in the working tree, so the finding's numbers are stale): OUR_KINDS.skill :255 (not :254); fieldToKinds built at :365 (not :361); FEATGRANT_KEY_KINDS :566 (not :562-564); effectChoices option-grant walk :671-677 (not :667-670); plain choice option-grant walk :688-692 (not :683-688); VERIFIED_EQUIVALENT registry :888 (not :884), oatia entry at :1515 (not :1482), read at :1916 (not :1851). build.ts: resolvePick call :5035 (not :5030), applyAlwaysOn :4988 (not :4983-4985). featGrantsLane.ts REMOVED note :54-62 (not :54-70).\n\nCORRECTED SAFETY FACT — the finding's \"record top level (1 record, creative-prodigy)\" is wrong as stated. Measured across all of core.json: 107 records carry a top-level `skills` key — feats 1, animalCompanions 98, companionSpecializations 8 — and the companion ones are ARRAYS of skill names, not rank maps. The safety CONCLUSION still holds, but for a different reason than the finding gives: animalCompanions and companionSpecializations are not in WG_PAIRING (scripts/lib/wg-parse.mjs:171-180 pairs only feats, classFeatures, heritages, backgrounds, ancestries, classes, items, actions), so ourKindsOf never sees them. Within the paired buckets the counts are: top-level `skills` 1 (feats/creative-prodigy), effectChoices option grant.skills 43 records, plain choice option grant.skills 8 records, union 51 (the finding said 52), whileActive `skills` 0, enhancement.grant `skills` 0. Note in passing that feats.creative-prodigy's value is DOUBLY NESTED — \"skills\": {\"skills\": {\"performance\": \"trained\"}, \"redundantFallback\": true} — an authoring shape worth a separate look; it does not change any bucket here.\n\nCORRECTED VERIFICATION — do NOT use `--list --bucket they-only | grep 'missing=\\[skill\\]'`. That list is truncated at 400 of 642 records (wg-diff.mjs:1948, list.slice(0, 400)), which is exactly why the finding reports an exposure of \"11\" (today it prints 16) — the number is an artifact of the print cap, not a census. Use the by-kind summary block instead. Measured before/after by running the patched copy: THEY-ONLY 642 -> 624; WE-ONLY 2447 -> 2466; AGREE 1087 -> 1086; disagree 1740 -> 1758; and in the \"THEY-ONLY, by the kind we are missing\" table, skill 101 -> 82. Nineteen records lose the skill-missing tag, not one: fey-ascension, oatia-skysage-dedication, anvil-dwarf, artisan-android, laborer-android, shoreline-strix, polychromatic-anadi, frightful-goloma, surgewise-fleshwarp, old-blood-vishkanya, prismatic-vishkanya, battle-ready-orc, winter-orc, roaming-minotaur, shadow-rat, gandharva, healer-samsaran, wilderness-samsaran, waning-moon-sarangay. So the finding's \"oatia is the only one of them whose carrier is a grant.skills\" is false — eighteen heritages carry the same shape (e.g. heritages/anvil-dwarf effectChoices 'trained-skill' with grant.skills {crafting:'trained'} and its redundancy branches), and every one of them is read live through the heritage resolvePick at build.ts:5051.\n\nCORRECTED EXPECTATION — the finding's \"with no new WE-ONLY drift\" is false and must not be used as the pass criterion. WE-ONLY grows by 19, and two records leave AGREE for WE-ONLY: lorekeeper-shisk and ancient-ash, each landing as theirKinds=[conditional,choice] / ourKinds=[choice,skill] / extra=[skill]. Both genuinely train a printed skill on our side (lorekeeper-shisk's Int-or-Wis skill, ancient-ash's chosen skill), so those two are truthful WE-ONLY entries of the kind the header at wg-diff.mjs:20-24 says is expected and never actioned blindly — but the reviewer must expect them rather than read them as a regression. Likewise the disagree count rising to 1758 is not 18 new value conflicts: it is 18 records where `skill` now exists on BOTH sides and therefore becomes eligible for value adjudication by wg-values.\n\nEverything else in the finding stands: the carrier is live, the overlay rows are correct and non-removing, the featGrantsLane removal is right, and no engine field is needed."
 },
 {
  "id": "astrolabe-of-falling-stars",
  "verdict": "ASK-OWNER",
  "reason": "Every load-bearing fact re-verified independently; the finding's ASK-OWNER stands.\n\nTHEIRS (raw, re-read): scripts/wg-show.mjs only reads ability_block, so I parsed the `item` COPY block myself with scripts/lib/wg-parse.mjs over work/wg/wg-data.sql (6244 rows, 0 failed). Item 19819 \"Astrolabe of Falling Stars\" (level 5, UNCOMMON, 150 gp) has exactly one op: addBonusToValue {variable:\"SKILL_SURVIVAL\", value:\"1\", type:\"item\", text:\"to identify celestial bodies.\"}. Siblings 15077 (Mariner's) and 15076 (Standard) are byte-identical in shape. Confirms the finding.\n\nPRINTED (re-read from the repo's AoN mirror, public/core-descriptions.json): items.astrolabe carries \"An astrolabe also grants a +1 item bonus to checks to identify celestial bodies\", preceded by \"To use an astrolabe, the holder must be trained in Survival\" — the training requirement gates USE, the bonus sentence names the checks and no skill. items.astrolabe-of-falling-stars (equipment-4028) does NOT contain that sentence; it reaches it only via \"An astrolabe of falling stars can be used as a mariner's astrolabe.\" The inheritance chain the finding describes is real. (Minor: the finding's \"two actions\" for Starfall comes from WG's cost=\"TWO-ACTIONS\"; the AoN mirror omits the symbol and the twin record's mirror shows 1 action. Not load-bearing — frequency once/day is all we encode and both agree.)\n\nOURS: public/core.json items.astrolabe-of-falling-stars = frequency {max:1,per:'day'} and nothing else; situational undefined, no passiveEffects. Only row for the id in scripts/data/effect-backfill.json is the frequency row (line 76017). Zero keys for the id anywhere in src/rules/situationalBonuses.ts (FEAT_SITUATIONAL and the marks table both). Only src/ hit is itemAliases.ts:16, which keys the DIFFERENT id aon-astrolabe-of-falling-stars. Gap is real today.\n\nCARRIER IS LIVE: characterSituationalIds (src/rules/explain.ts:437+) pushes inv.itemId when itemInUse(inv), and entriesFor reads FEAT_SITUATIONAL[id], so an item id written into that table does fire. A fix would work; that is not the obstacle.\n\nSTATUS: work/owner-questions.json `open` (55 entries) contains id \"astrolabe-of-falling-stars\", batch 16, with the prior REFUSED-on-review note. Not in authorisedExceptions. work/wg-batch-016-parity.json `records` (94) contains zero astrolabe rows — the never-compared status is confirmed. work/wg-batch-016.json carries the worklist entry with ourFields {} and ourRegistries [].\n\nTWO CORRECTIONS TO THE FINDING (neither fatal). (1) All six cited line numbers are stale: actual are situationalBonuses.ts:2376 astrolabe-standard, :2377 astrolabe-mariners, :2504 astrolabe, :2583 astrolabe-of-the-falling-stars, :2613 mariners-astrolabe, :2628 standard-astrolabe. The cited 2367/2368/2480/2559/2589/2604 are verbatim the numbers already in owner-questions.json, so they were copied, not re-grepped — contradicting \"I re-verified the premise rather than trusting it.\" (2) The finding's only proposed action — add the shape precedent to the owner entry — is a NO-OP: the entry's `ours` field already states all six carriers as {kind:'skill', detail:'all'} +1 item, with those same stale numbers.\n\nNEW MATERIAL THE FINDING MISSED, which sharpens the owner's question rather than answering it: line 2583 is NOT a sibling, it is a DUPLICATE RECORD OF THIS VERY ITEM. core.json holds both astrolabe-of-falling-stars (aonId equipment-4028, 150 gp, no usage) and astrolabe-of-the-falling-stars (no aonId, 50 gp, usage held-in-two-hands, activationCost 1 action, same Gatewalkers p.232 Starfall text in core-descriptions). itemAliases.ts:16 aliases the removed aon- stub to the twin, calling it the canonical Foundry twin. The twin's entry sits inside the \"rulings B + C\" block written by scripts/apply-rulings-bc.mjs, headed \"C: an action-named bonus stars EVERY skill that can perform that action, not just the obvious one. 55 records / 65 bonuses, adversarially verified.\" So one of two records for the same printed item carries the star and the other does not — and the one that carries it is the WORSE record (50 gp and 1 action against the printed 150 gp).\n\nWHY STILL ASK-OWNER AND NOT CONFIRMED: I pressed the strongest counter — that ruling C plus the twin at 2583 make a copy-the-twin fix mechanical rather than adjudicative. It does not survive. Ruling C is a house display rule about WHICH skills a known bonus stars; it is not an adjudication of WG's SKILL_SURVIVAL encoding. Writing any entry for this id necessarily picks print (skill 'all', restriction in `when`) over WG (Survival only), or declines the bonus entirely — three live options, all of them the owner's call under Rule 2, and a reviewer already refused this exact fix on that ground. Correctly parked; no fix from this seat. If the owner wants the entry amended, the useful addition is the twin-record fact above, not the shape precedent already recorded."
 },
 {
  "id": "animistic-practice",
  "verdict": "CONFIRMED",
  "reason": "Independently reproduced on both sides; every load-bearing claim survives.\n\nTHEIR RAW OPS (read directly, `node scripts/wg-show.mjs animistic-practice --raw`). Two rows share the name. Row 38685's Seer option is verbatim as described: four unconditional `addBonusToValue` at value \"1\", type=status, on SAVE_FORT/SAVE_REFLEX/SAVE_WILL/AC_BONUS, each carrying text \"against the effects of haunts and the abilities of spirits and incorporeal undead.\"; then a `conditional LEVEL >= 9` holding `adjValue RESISTANCES = \"spirit, {level/2}\"` + `\"void, {level/2}\"` and the same four vars at \"2\"; then a `conditional LEVEL >= 17` holding `adjValue SENSES_PRECISE = \"apparition sight,30\"` and the same four vars at \"3\". The ladder claim is exact, and `theirAssertions()` (wg-values.mjs:239) is a flat `out.set(key, scalar)` Map, so the last write wins and \"3\" is the top of a ladder, not a flat grant — confirmed.\n\nPRINT. WG's own Seer option description reproduces the AoN practice-3 text (Invocation of Sight 1st / Protection 9th / Resilience 17th) word for word, and our `classes.animist.subclass.options[seer].description` in public/core.json carries the same three invocations with the same +1 / +2 / +3 ladder. PRINT AND WG AGREE on every tier, so rule 4 is not triggered — nothing to escalate, nothing adopted.\n\nGATE STATE. A bare `node scripts/wg-values.mjs` does NOT show this record — `ids` defaults to work/wg-batch-003.json (wg-values.mjs:1174-1175). Scoped, it reports exactly the four claimed rows: `node scripts/wg-values.mjs --ids animistic-practice` → MISSING save|fortitude / save|reflex / save|will / ac|, all `theirs=3  ours=(nothing)`. Identical with `--raw`, i.e. the settle really is gone. `node scripts/wg-diff.mjs --list --bucket they-only` → `animistic-practice lvl 1 missing=[focus] ours=[choice,grantsRecord,skill,sense,defense,conditional,save,ac]`, and it is the only row in the corpus with `focus` inside `missing=[...]` (0 others). save and ac kinds already agree; only the value bucket and the lone focus kind are open. Confirmed.\n\nCARRIERS (all live, each re-read). (1) src/rules/situationalBonuses.ts:1112 `\"seer\"` — exact line, both entries verbatim as quoted, save|all + ac, same condition, \"+1 status (+2 at 9th, +3 at 17th)\". (2) Reader chain verified end to end: characterSituationalIds() at explain.ts:239 pulls ownedFeatureIds at :244; ownedFeatureIds (derive.ts:3101) adds the chosen subclass id itself — `if (subId && db.classFeatures[subId]) out.add(subId)` — and `db.classFeatures['seer']` exists; featSituationalLines() at explain.ts:727 is called on the save row (explain.ts:1010) and the AC row (explain.ts:1144). (3) core.json classFeatures/seer.resistances = [{spirit, floor(@actor.level/2), minLevel 9}, {void, same}], exactly as claimed, and test/batch2-8-parity.test.ts:142-160 exercises it through `build('animist', N, {subclassId:'seer'})` → deriveDefenses; I ran it (`npx vitest run test/batch2-8-parity.test.ts -t seer`) and both cases pass, which independently proves the subclassId→ownedFeatureIds path is live at runtime. (4) The Medium pool in build.ts: `let poolMax` at 3856, `if (build.subclassId === 'medium' && level >= 9) poolMax = Math.max(poolMax, 2, pointCostingKnown.length)` at 3886, `poolMax = Math.min(poolMax, 3)` at 3890 — the printed \"whichever is higher (maximum 3)\" exactly, plus a separate circle-of-spirits branch.\n\nREGRESSION ROOT CAUSE. scripts/drop-fixed-settles.mjs exists and is exactly as described: `FIXED` keyed by bare record id with `'animistic-practice': \"the Medium's Dual Invocation now raises the focus pool from 9th\"`, matched by `/^\\s*'([a-z0-9-]+)':\\s*\\[/` with no bucket scoping, applied across wg-diff/wg-values/wg-identity, replacing the whole line with the marker. Both markers exist and are unique in their files. The surviving justification comment two lines above the wg-values marker predicted this exact failure (\"⚠ Extended IN PLACE — a second 'animistic-practice' key later in this object would win and silently drop the three save settles\"). Collateral loss is generalized, not hypothetical: gates-threshold and ascended-dragonet-heritage were each struck from TWO registries on one single-bucket reason. So a focus-pool reason deleting four value buckets is a tooling regression, and the data gap never existed.\n\nSETTLE SANITY. Key formats match the readers: wg-values.mjs:1255 tests `SETTLED_VALUES[id].includes(key)` against exactly `save|fortitude`/`ac|`; wg-diff.mjs:1916-1917 filters `missing` by `VERIFIED_EQUIVALENT[id]`, so `['focus']` is right and per-kind — a new bucket would still report, and `--raw` still exposes both. Neighbours match in shape ('draconic-resistance': ['set|resistances'] above the wg-values slot; 'animist-apparition-spellcasting': ['focus'] at wg-diff.mjs:1612, whose comment gives the identical reasoning). No duplicate 'animistic-practice' key exists in any of the three registries. No overlay rows and no new engine fields are proposed, so rules 5 and 8 do not apply.\n\nOnly defects found are citation drift and two small imprecisions, none of which changes the verdict — corrected below.",
  "correctedFix": "Fixes stand as proposed. Corrections to the anchors and two factual details:\n\nLINE NUMBERS (situationalBonuses.ts:1112, explain.ts:239 and explain.ts:727 were exact; the rest have drifted):\n- wg-values removal marker: 1034, not 1006.\n- SETTLED_VALUES const: wg-values.mjs:786, not 771.\n- wg-diff removal marker: 1618, not 1585.\n- VERIFIED_EQUIVALENT const: wg-diff.mjs:888, not 884.\n- build.ts focus pool: `let poolMax` 3856, medium clause 3886, `Math.min(poolMax, 3)` 3890 — cite 3856-3891, not 3852-3890 / 3880 / 3885.\n- Add the AC reader line explicitly: explain.ts:1144 (AC row) and explain.ts:1010 (save row) are the two call sites of featSituationalLines() that surface this entry.\nEach marker line is unique within its file, so the edits are unambiguous regardless of drift.\n\nFACTUAL CORRECTIONS to fold into the settle comments:\n1. Their Medium op is NOT bare. It sits inside `conditional IF LEVEL >= 9 AND FEAT_NAMES NOT_INCLUDES \"circle of spirits\" THEN adjValue FOCUS_POINT_BONUS = 1`. State it that way — it strengthens the settle rather than weakening it: their level gate matches Dual Invocation's 9th level and their non-stacking guard mirrors our separate `focusSeen.has('circle-of-spirits')` branch in build.ts, while their flat +1 still cannot express the printed \"equal to the number of focus spells you have or the number of primary apparitions… whichever is higher (maximum 3)\" that build.ts:3886-3890 implements.\n2. `wg-show` prints BOTH rows (legacy 28491 with Channeler/Sage, remaster 38685 with Liturgist/Medium/Seer/Shaman). It is wg-values that pairs to exactly one, via `theirByBucket[bucket].get(norm(name))` (wg-values.mjs:1183) — a Map, one row per bucket+name — and the reported value 3 proves the winner is 38685. Say \"wg-values compares 38685\", not \"wg-show/wg-values own 38685\".\n3. The gate prints `ours=(nothing)`, not `ours(all): (none)`.\n\nONE ADDITION worth putting in the restored wg-values comment, because it cost real time to rediscover: wg-values defaults `ids` to work/wg-batch-003.json (wg-values.mjs:1174-1175), so a bare run shows only batch 3 and this record appears absent. The reproduction command is `node scripts/wg-values.mjs --ids animistic-practice` (add `--raw` to bypass the settle). Without this note the next reader will run the script bare, see no animistic-practice row, and wrongly conclude the settle is inert.\n\nThe third fix (bucket-scoping scripts/drop-fixed-settles.mjs and re-auditing the other seven FIXED ids) is correct and should be kept as proposed-not-applied; the two-registry strikes on gates-threshold and ascended-dragonet-heritage confirm the collateral pattern is not unique to this record."
 },
 {
  "id": "instinct-ability",
  "verdict": "ASK-OWNER",
  "reason": "Every factual claim re-verified independently and holds. THEIR RAW OPS (wg-show --raw): remaster row 31306 has exactly one operation, conditional {FEAT_NAMES INCLUDES \"barbarian resiliency\" -> addBonusToValue MAX_HEALTH_BONUS +3}, with no instinct grant of any kind; legacy row 29498 carries the five ARCHETYPE_BARBARIAN_INSTINCT EQUALS animal/dragon/fury/giant/spirit -> giveAbilityBlock conditionals PLUS the same HP conditional. PRINT (AoN mirror C:/wonderers guide/aon-2e-archive/data): feat-6194 Instinct Ability, Feat 6, PC2 p.176, prereq Barbarian Dedication, whole benefit = \"You gain the instinct ability for the instinct you chose for Barbarian Dedication\" - no Hit Points; feat-6191 Barbarian Resiliency, Feat 4, PC2 p.176 = \"You gain 3 additional Hit Points for each barbarian archetype class feat you have.\" CARRIERS: public/core.json feats/instinct-ability.derivedGrant={fromFeat:'barbarian-dedication'} with archetype:'barbarian'; feats/barbarian-resiliency.maxHpBonus={perArchetypeFeat:3,archetype:'barbarian'}; readers live at src/rules/derive.ts:778 featHpBonus with the perArchetypeFeat branch at :791-792, mirrored at src/rules/build.ts:6970-6971, typed at src/rules/types.ts:2866, and the derivedGrant reader in choiceOwnedFeatureIds at src/rules/derive.ts:~3211. I additionally proved the derivedGrant actually resolves: all ten Barbarian Dedication choice values (animal-instinct ... superstition-instinct) are live db.classFeatures ids, so our side really does deliver the printed sentence and nothing is broken while the question waits. Sweep clean: zero src/ references to instinct-ability, no featGrants/featGrantsAuto/featGrantsLane/featFeatGrants/situationalBonuses/counterMods entry, single overlay row scripts/data/effect-backfill.json:56291 with no collision. STILL OPEN: work/owner-questions.json open[] (55 entries) still holds the batch-16 instinct-ability question verbatim, instinct-ability is genuinely absent from SETTLED_VALUES, and `node scripts/wg-values.mjs --ids instinct-ability` still prints MISSING hp| theirs=3 ours=(nothing). NEW DECISIVE EVIDENCE the investigator did not have: `wg-show \"Barbarian Resiliency\"` shows THEIR Barbarian Resiliency row (32099 remaster / 29492 legacy) is a FLAT MAX_HEALTH_BONUS +3 covering itself, while every sibling barbarian archetype feat carries the conditional +3 gated on holding it - so their total is exactly 3 x (barbarian archetype feats held), numerically identical to our perArchetypeFeat formula. The distributed-accounting reading is now proven rather than inferred, and there is no net WG-vs-print numeric disagreement, only a placement difference. TWO STALE CLAIMS (do not change the verdict, but correct the record): (1) the batch-17 family the finding calls \"under separate investigation\" has ALREADY LANDED as settles at scripts/wg-values.mjs:857-862 - advanced-devotion, advanced-glory, advanced-kata, advanced-defender, champions-reaction, guardians-intercept - with the same reasoning, so the shape now has fourteen 'hp|' settles, not eight, and the owner has effectively already ruled on it; (2) cited line numbers have drifted - barbarian-dedication settle is wg-values.mjs:976 not :963, basic-fury/basic-devotion/devout-magic are :905-907 not :892-894, monk-dedication/exemplar-dedication :1051-1052, basic-glory :1150, build.ts :6970-6971 not :6949-6950, types.ts :2866 not :2840 (basic-kata :849 is correct). VERDICT RATIONALE: the fix is kind:\"none\" - no data change, no overlay row, no settle - which is safe and correct, and the underlying matter is a WG-encoding-vs-printed-placement question the investigator explicitly parked with the owner and did not decide. Per rule (4) a fix that would settle a WG-vs-print disagreement is ASK-OWNER and never CONFIRMED, and this is that: the owner must be the one to say the +3 on this row is Barbarian Resiliency's Hit Points relocated by WG's engine rather than a claim about Instinct Ability. Not REFUTED, because nothing in the finding is wrong on the merits.",
  "correctedFix": "DO NOT ACT in this pass - unchanged from the finding. Corrections for the owner's decision packet: (a) The batch-17 family is no longer pending; it is already settled at scripts/wg-values.mjs:857-862 (advanced-devotion, advanced-glory, advanced-kata, advanced-defender, champions-reaction, guardians-intercept) with the comment \"Their flat 3 hangs off each archetype feat; ours is maxHpBonus.perArchetypeFeat: 3 on the *-resiliency feat that PRINTS it ... Not adopted; recorded.\" So the ruling this finding wants deferred to that family has in fact been made, in favour of settling. (b) Strengthen the owner packet with the proof I obtained: their Barbarian Resiliency row itself is a FLAT MAX_HEALTH_BONUS +3 (their id 32099 remaster, 29492 legacy) and each sibling barbarian archetype feat carries the conditional +3, so their engine's total equals 3 x (barbarian archetype feats held) - exactly our perArchetypeFeat product. There is no numeric disagreement with print, only relocation. (c) If and when the owner rules the same way they ruled fourteen times already, the follow-up is one line in SETTLED_VALUES placed beside its sister feat at scripts/wg-values.mjs:976 (NOT :963): 'instinct-ability': ['hp|'], with a comment naming the carrier (feats/barbarian-resiliency.maxHpBonus = { perArchetypeFeat: 3, archetype: 'barbarian' }), the reader (featHpBonus, src/rules/derive.ts:778, perArchetypeFeat branch :791-792; builder mirror src/rules/build.ts:6970-6971; type src/rules/types.ts:2866), and the fact that instinct-ability's own archetype: 'barbarian' is what makes the multiplier count it. The work/owner-questions.json open[] entry for instinct-ability should be closed in the same change. Note that the settle quiets ONLY the hp value; the printed instinct grant stays where it is, on derivedGrant, and must not be touched."
 },
 {
  "id": "ghostly-resistance",
  "verdict": "CONFIRMED",
  "reason": "Defect verified end-to-end. Print (public/ast/feats.json.gz -> ghostly-resistance, Book of the Dead p.47) and WG raw ops (scripts/wg-show.mjs, their id 28468) AGREE: two bands, all-damage 1/2/3 and non-magical 2/4/5, with two distinct type strings on their side. Our overlay row in scripts/data/effect-backfill.json carries both bands with the SAME type \"all-damage\". The live reader src/rules/derive.ts:2483 is `res.set(r.type, Math.max(res.get(r.type) ?? 0, v))` -- type-keyed, highest-wins -- and `condition` enters neither that key nor the attribution key at :2481/:2488, so the two bands collapse and the NON-MAGICAL number is applied to ALL damage: L4 sheet shows 2 where print gives 1, L10 shows 4 where print gives 2, L16/L20 show 5 where print gives 3. The correct band is then flagged applied:false by derive.ts:2635-2641 and rendered \"superseded -- does not stack\" via explain.ts:1494. Real over-grant, not a labelling nit. Formulas hand-checked L4-20: zero mismatches against print, so only the type strings need changing. SET-GAP reproduced verbatim (`node scripts/wg-values.mjs --ids ghostly-resistance --verbose` -> \"SET-GAP set|resistances theirs=non-magical damage ours=all-damage\"); replicating the word-subset matcher (wg-values.mjs:1211-1218) shows the proposed two-type shape covers both of their members, so the row closes on merit and no settle is warranted. Precedent is STRONGER than the brief claimed: TWO records already author this exact ghost clause with distinct free-text types -- modes/vengeful-remnant and items/silhouette-cloak (\"all damage except force, damage from light-trait effects, and ghost touch Strikes\" / \"non-magical damage (except ...)\"). A corpus scan finds 83 distinct resistance type strings (brief said 47), overwhelmingly free-text; no type enum or validator exists, VitalsRail typeLabel just title-cases, and no test pins this record. Not ASK-OWNER: WG and print agree; ours is the only side that diverges. NOT REFUTED, but the fix instruction has one real defect requiring correction: it names scripts/backfill-parity-fixes.mjs:193-212 as THE owning script, while scripts/wg-batch14-residual2.mjs:42-55 writes the IDENTICAL overlay row (same category/id/field, replace-in-place) with the OLD single-band value `floor((@actor.level+2)/6)` and NO --write gate (writeBackfill runs unconditionally at line 68) -- a rerun silently reverts both the non-magical band and the sub-4th max(1,...) fix. scripts/apply-defense-lane.mjs:37 also names the record but HOLDS IT BACK, so it is inert. Two further inaccuracies corrected: neither script is in `npm run data` (chain is import-core-v2 ... edition-drift-check; the overlay survives regen via import-core-v2), so the stated regen rationale is wrong though authoring in the owning scripts is still right; and the set-settle filter is scripts/wg-values.mjs:1269, not :1256. Rule 5 clear -- whole-array SET, no value:null. src/ carrier grep clean (zero hits) as claimed.",
  "correctedFix": "[{\"kind\":\"overlay\",\"detail\":\"scripts/data/effect-backfill.json -- keep the row {category:\\\"feats\\\", id:\\\"ghostly-resistance\\\", field:\\\"resistances\\\"} and both verified formulas, but give the two entries DISTINCT type strings so derive's type-keyed map stops collapsing them. New value: [{\\\"type\\\":\\\"all damage except force, ghost touch, and vitality\\\",\\\"value\\\":\\\"max(1,floor((@actor.level+2)/6))\\\"},{\\\"type\\\":\\\"non-magical damage (except force, ghost touch, and vitality)\\\",\\\"value\\\":\\\"min(5,max(2,floor((@actor.level+2)/6)*2))\\\"}]. The `condition` strings fold into `type` and are dropped, matching modes/vengeful-remnant AND items/silhouette-cloak, which already author this same printed clause this way. Whole-array SET, so the Rule-5 value:null removal hazard does not apply.\",\"reader\":\"src/rules/derive.ts:2472-2488 -- `res.set(r.type, Math.max(res.get(r.type) ?? 0, v))` at :2483 (type-keyed, highest-wins per derive.ts:2634-2635) and `note('resistance:' + r.type, src.__from, v, r.condition ?? src.__cond)` at :2488, surfaced as CharacterDefenses.resistances/.sources and rendered by src/sheet/VitalsRail.tsx:773-780 + src/rules/explain.ts:1477-1497. Two distinct types produce two map keys and two React keys, so both bands list and neither is marked applied:false.\"},{\"kind\":\"script\",\"detail\":\"AUTHOR THE NEW SHAPE IN BOTH OWNING SCRIPTS, not just one -- this is the correction to the original fix. (a) scripts/backfill-parity-fixes.mjs:193-212 (the row the brief identified). (b) scripts/wg-batch14-residual2.mjs:42-55, MISSED by the brief: it writes the IDENTICAL overlay key {feats, ghostly-resistance, resistances} with the superseded single-band value [{type:'all-damage', value:'floor((@actor.level+2)/6)', condition:'except force, vitality, and damage from a ghost touch weapon (or anything acting as one)'}], replace-in-place at its lines 63-68, and it has NO --write gate -- writeBackfill(ROOT, rows) executes on every invocation. Running it after the fix reverts BOTH the non-magical band and the max(1,...) sub-4th correction, silently reopening the batch-14 gap. Either update its `edits` array to the same two-type shape, or delete its ghostly-resistance entry entirely and leave backfill-parity-fixes.mjs the sole owner (preferred: one row, one owner). Its header comment at lines 4-10 should also be corrected, since it currently argues `condition` is the right carrier for the exception -- the very assumption that produced the collision. Note scripts/apply-defense-lane.mjs:35-37 lists the record in EXCEPTION_UNMODELLABLE and skips it, so it is inert and needs no change; its stated reason ('filing it as \\\"all\\\" would over-grant') independently corroborates this finding.\",\"reader\":\"scripts/lib/write-backfill.mjs -> scripts/data/effect-backfill.json, applied over public/core.json by scripts/import-core-v2.mjs (step 1 of `npm run data`); the overlay is what makes the field survive regen. Neither authoring script is itself in the `npm run data` chain.\"},{\"kind\":\"registry\",\"detail\":\"Do NOT re-add a SETTLED_VALUES settle. scripts/drop-fixed-settles.mjs's own doctrine holds -- a settle outlives its reason and would silence a future regression on this record. Verified by replicating the word-subset matcher at scripts/wg-values.mjs:1211-1218: their member 'non-magical damage' -> {non, magical, damage} is a subset of the proposed 'non-magical damage (except force, ghost touch, and vitality)', and their base member is a superset of ours, so both are covered and the SET-GAP disappears on merit. Leave the removal marker comment at scripts/wg-values.mjs:902 in place; siblings 'mortification' (:903) and 'hardened-chassis' (:904) are untouched. If the owner instead elects to keep the colliding shape, the settle must be restored AND the over-grant accepted in writing -- flag the trade explicitly: the sheet currently promises resistance 2/4/5 to ALL damage where print gives 1/2/3.\",\"reader\":\"scripts/wg-values.mjs:1269 -- `if (!RAW_SETTLES && (SETTLED_VALUES[id] ?? []).includes(`set|${s.key}`)) continue;` (the brief cited :1256, which is the NUMERIC-key filter, not the set-key one).\"},{\"kind\":\"cosmetic-note\",\"detail\":\"Non-blocking, pre-existing: src/sheet/VitalsRail.tsx:74-77 typeLabel does t.replace(/-/g,' '), so 'non-magical damage (...)' renders as 'Non magical damage (...)'. modes/vengeful-remnant and items/silhouette-cloak already display this way today, so the fix introduces no new behaviour -- but if the owner wants the hyphen preserved, that is a one-line typeLabel change affecting all three records at once, not a reason to keep the collision.\",\"reader\":\"src/sheet/VitalsRail.tsx:74-77 and :773-780; src/rules/explain.ts:1483 applies the same replace for the popup title.\"}]"
 },
 {
  "id": "sea-touch-elixir-lesser",
  "verdict": "CONFIRMED",
  "reason": "Re-read both sides myself; every load-bearing claim holds, and the WG-vs-print escape hatch does not apply.\n\nTHEIRS (read directly, not taken on trust). `node scripts/wg-show.mjs sea-touch-elixir-lesser --raw` → \"No ability_block row\"; `node scripts/wg-item.mjs \"Sea Touch Elixir (Lesser)\"` → item id 7741, level 5, price {\"gp\":22}, bulk 0.1, usage \"held in 1 hand\", and exactly ONE operation: {\"type\":\"setValue\",\"data\":{\"variable\":\"SPEED_SWIM\",\"value\":20}}. Nothing else in the op tree. Crucially — and this STRENGTHENS the finding beyond what it claimed — their own `text` column reproduces the print verbatim: \"...granting you a swim Speed of 20 feet for 10 minutes.\" So WG's prose agrees with print; only their operation encoding has no duration lane. There is therefore NO WG-vs-print disagreement to settle (adversarial check 4 does not fire, so ASK-OWNER is not owed); the divergence is purely a limitation of their setValue format.\n\nPRINT corroborated: our item carries aonId \"equipment-3311-3202\", level 5, price 22 gp, traits alchemical/consumable/elixir/polymorph, activationCost 1 action — matching their row and the cited AoN entry. No local AoN text mirror exists for this item (scripts/aon-verify holds only worklists/chunks), but their own text field is independent corroboration of the printed wording.\n\nOURS. public/core.json items/sea-touch-elixir-lesser has NO `speeds` field (verified: `'speeds' in it` → false). The mechanic is on modes/item-sea-touch-elixir-lesser = duration \"10 minutes\", speeds {swim: 20}, fromItemId sea-touch-elixir-lesser. Source of truth confirmed at scripts/data/consumable-modes.json:4832-4841 (all three grades at :4832/:4843/:4854).\n\nRULE 3 upheld. The ruling is real, not invented: docs/gold-set-review.md:196 — \"must be present permanently on the sheet — always on, not a castable spell entry and not a limited-duration effect the player has to remember to switch on.\" Print grants the swim Speed for 10 minutes, so a record-level speeds.swim would be an over-grant; theirs is exactly that over-grant. No adoption owed.\n\nRULE 4, every carrier grepped myself. scripts/data/effect-backfill.json → 0 hits for sea-touch (the Rule-5 violation the authoring script describes was genuinely removed). scripts/data/toggle-modes.json → 0 hits. grep -rn 'sea-touch-elixir' src/ → 0 hits (nothing in featGrants*/companionGrants/situationalBonuses/counterMods). consumable-modes.json is the sole carrier and is the correct one. Working-tree novelty confirmed: `git show HEAD:scripts/data/consumable-modes.json | grep -c sea-touch` → 0; `git diff --stat` → +397 lines. scripts/add-sea-touch-modes.mjs header quotes the Rule-5 lesson verbatim, as claimed.\n\nREADER CHAIN, all four sites read. src/rules/play.ts:1440-1449 useConsumable finds the mode by `m.fromItemId === inv.itemId` and toggles it; src/rules/derive.ts:4726-4735 iterates c.activeModes and, for a non-land key, does `speeds[key] = Math.max(speeds[key] ?? 0, v)` — swim 20 lands; src/sheet/ItemDetail.tsx:192 surfaces it; src/sheet/ModesPanel.tsx:87 filters fromItemId modes out of the generic list. Live end to end.\n\nTHE BLIND SPOT, verified structurally. `grep -n \"core\\.modes\" scripts/wg-values.mjs` returns exactly ONE line — :314, inside ourSets — and that loop harvests only `m.resistances` and `m.weaknesses`. The scalar path never touches core.modes, so a mode-carried Speed is unreachable by it. Rerun reproduces: `--ids sea-touch-elixir-lesser --verbose` → \"MISSING speed|swim theirs=20 ours=(nothing)\", \"ours(all): (none)\".\n\nFIX SANITY (check 6). `core` is module scope (:45), so the proposed loop compiles where placed. The comparer iterates ONLY theirs' keys (:1252 `for (const [key, tv] of theirs)`), so adding ours-side assertions is strictly additive — it can never manufacture a new failure, only turn MISSING into a match or into a more informative DIFFERENT. It matches its neighbours: the identical shape already exists at :737 and :753 for grant.speeds. Blast radius measured: 27 modes carry speeds; running the comparer over a sample of their owning records shows only sea-touch lesser and greater currently emit an open speed row, both of which the edit closes on merit (20 === 20). The settle option is correctly identified as the inferior alternative and should be dropped.\n\nThree small inaccuracies, none load-bearing, corrected below.",
  "correctedFix": "Take fix #2 (the wg-values.mjs edit) and DROP fix #1 (the settle) entirely, with three corrections:\n\n1. FUNCTION NAME. There is no `ourValues` in scripts/wg-values.mjs (grep returns nothing). The scalar path is `ourAssertions(id, rec)` at :481 — the finding's own reader field cites it correctly, only the fix prose mislabels it. Insert the loop inside ourAssertions, immediately after the `rec.enhancement?.grant?.speeds` loop at :760 and before `situationalMagnitudes(...)`, i.e. beside the two container-speed loops it mirrors:\n\n  /* …and a Speed carried by a MODE — the scalar twin of the mode loop in ourSets at :314. A Speed the\n   * book grants for a DURATION cannot be a record field (docs/gold-set-review.md:196), so it lives on\n   * modes/item-*; reading only the record reported Sea Touch's swim 20 as granted by nobody. */\n  for (const m of Object.values(core.modes ?? {})) {\n    if (m.fromItemId !== id && !(m.feats ?? []).includes(id)) continue;\n    for (const [k, v] of Object.entries(m.speeds ?? {})) put(`speed|${k}`, typeof v === 'string' ? v : Number(v));\n  }\n\n2. SIBLING SCOPING. The finding says the moderate and greater grades would need the same treatment \"when the sibling grades are gated\". They are not gated — `--ids sea-touch-elixir-lesser,sea-touch-elixir-moderate,sea-touch-elixir-greater` shows sea-touch-elixir-greater ALREADY emitting \"MISSING speed|swim theirs=20 ours=(nothing)\" today, while moderate is uncomparable (\"prose-only bonus\"). One edit closes lesser and greater at once; nothing further is owed for moderate.\n\n3. OPTIONAL, FORMULA MODES. The literal `typeof v === 'string' ? v : Number(v)` above is the right shape for this record and matches its neighbours at :737/:753, but 12 of the 27 speed-carrying modes hold formulas (e.g. item-winged fly \"min(25,@actor.speed.land)\"). If those are to close too, push the evaluated base ALONGSIDE the raw string rather than instead of it — `put` is explicitly multi-assertion (:490, \"A match against ANY assertion is agreement\"), so `if (typeof v === 'string') { const b = evalSpeedFormula(v); if (b !== null) put(\\`speed|${k}\\`, b); put(\\`speed|${k}\\`, v); }` loses nothing. Do NOT substitute the base for the string the way :510-514 does: evalSpeedFormula evaluates @actor.speed.land to 0, so min-shaped formulas collapse to 0 and would print a misleading `ours=0` DIFFERENT row. This step is independent of the Sea Touch record and can be deferred."
 },
 {
  "id": "shining-hackle",
  "verdict": "CONFIRMED",
  "reason": "Every load-bearing claim reproduced independently; nothing refutes it, and the \"theirs=+1\" premise is indeed a comparer artefact.\n\nTHEIRS (raw, re-read myself). `node scripts/wg-show.mjs shining-hackle --raw` → \"No ability_block row whose name slugs to shining-hackle.\" `node scripts/wg-item.mjs \"Shining Hackle\"` → their item id 18419, level 4, price {\"gp\":80}, usage \"worn headwear\", bulk 0.1, traits {1527,1504}, and exactly two operations: addBonusToValue {\"variable\":\"PERCEPTION\",\"value\":\"1\",\"type\":\"item\",\"text\":\"base on sight\"} and addBonusToValue {\"variable\":\"SKILL_STEALTH\",\"value\":\"-1\",\"type\":\"item\",\"text\":\"\"}. Their value is the string \"-1\". The finding's characterisation is exact.\n\nPRINT. Their own record carries the printed text verbatim: \"While wearing the shining hackle, you gain a +1 item bonus to Perception checks based on sight, but you also take a –1 item penalty to your Stealth checks.\" A MINUS ONE PENALTY. Print, theirs, and ours all agree; there is no WG-vs-print disagreement, so gate (4) does not fire.\n\nOURS. public/core.json items['shining-hackle'].passiveEffects = {\"skills\":{\"stealth\":-1},\"perception\":1}. Overlay row at scripts/data/effect-backfill.json (id shining-hackle, field passiveEffects) is byte-identical to HEAD — `git show HEAD:scripts/data/effect-backfill.json` returns the same value, so no regeneration dropped it. Unique row, no collision, not value:null.\n\nFALSE POSITIVE REPRODUCED. `node scripts/wg-values.mjs --ids shining-hackle --verbose` → \"DIFFERENT skill|stealth theirs=1 ours=-1 / ours(all): skill|stealth=-1 perception|=1,1\". Their side is abs'd (`const scalar = typeof val === 'number' ? Math.abs(val) : val;`, under the comment \"A PENALTY is the same rule as its magnitude\"); our item passiveEffects.skills reader passes the signed value straight through. Asymmetry confirmed. scripts/wg-values.mjs is untracked (`?? scripts/wg-values.mjs`), i.e. a new comparer.\n\nRULE 4 CARRIERS. `grep -rn shining-hackle src/` returns exactly ONE hit — src/rules/situationalBonuses.ts:2116, the Perception-condition display row. Zero hits in featGrants.ts, featGrantsAuto.ts, featGrantsLane.ts, featFeatGrants.ts, featPickGrants.ts, featCantripGrants.ts, companionGrants.ts, counterMods.ts, src/rules/modes.ts, scripts/data/consumable-modes.json, scripts/data/toggle-modes.json, and no other scripts/data/*.json overlay. situationalBonuses.ts's header does say \"It is DISPLAY-ONLY: nothing here changes a computed number.\"\n\nTHE ENGINE DEFECT IS REAL. src/rules/derive.ts:262-283 `passiveItemBonus` has `let best = 0;` (269) then `best = Math.max(best, v);` (275) — a negative can never win. Its skill caller at :525 wraps it in another Math.max. All pe.skills consumers enumerated: only passiveItemBonus (:262) and passiveItemBonusDetail (:292, same max-from-0 shape, consumed by explain.ts:938/996/1048/1118) plus ItemEditorModal.tsx UI; build.ts/companions.ts touch only languages, recoveryDcReduction, speeds. So the -1 is genuinely write-only. Grep for itemPenalty/skillPenalty/passivePenalt in src/rules/*.ts returns nothing.\n\nSTRENGTHENED, not weakened: src/rules/modes.ts:169 `poolTypedMods` ALREADY implements the right stacking — it keeps `bonus[type]` and `penalty[type]` separately and returns `bonus.item - penalty.item`. So the finding's \"add it as its own term OUTSIDE the item-bonus Math.max\" is exactly correct and needs no new pooling logic.\n\nCorrections and hazards are in correctedFix — chiefly a THIRD negative item passive the finding did not surface (day-goggles perception -2) that makes a kind-symmetric wiring of the proposed helper a live regression, plus stale line anchors.",
  "correctedFix": "Adopt the finding's three fixes with these corrections.\n\n(A) FIX 1 STANDS UNCHANGED. No data change. Do not touch the shining-hackle overlay row in scripts/data/effect-backfill.json. Verified identical to HEAD and in agreement with print and with their two ops.\n\n(B) LINE ANCHORS IN scripts/wg-values.mjs ARE STALE AND DRIFTING — CITE CODE, NOT NUMBERS. The file is untracked and was being edited concurrently during this verification: `const scalar = ... Math.abs(val) ...` moved from 263 to 278 and the `Object.entries(pe.skills ?? {})` reader from 648 to 663 within minutes of each other. Apply fix 2 by anchor: the `const scalar = typeof val === 'number' ? Math.abs(val) : val;` line in the their-side op loop, and the `const pe = rec.passiveEffects ?? {};` block in ourAssertions.\n\n(C) FIX 2'S \"PREFERRED ALTERNATIVE\" RATIONALE IS FACTUALLY WRONG. \"Drop the Math.abs and instead compare |theirs| to |ours| at the comparison site\" does NOT \"keep a genuine sign disagreement reportable\" — comparing magnitudes at the compare site erases sign disagreement exactly as the upstream abs does. Two real options: (i) take the narrow fix (abs our numeric carriers) and rewrite the comment to say plainly that the comparer is magnitude-only by design; or (ii) if sign flips must be reportable, stop abs'ing theirs, and at the situational-string reader (`const mags = [...String(e.bonus ?? '').matchAll(/([+-]?\\d+)/g)].map((m) => Math.abs(Number(m[1])))`) push BOTH the signed and the abs'd value as candidates, then compare signed-first. Also: the instruction to abs `pe.saves` is a no-op for items — ItemPassiveEffects.saves is `number` (src/rules/types.ts:2014) while the reader does `Object.entries(pe.saves ?? {})`, which yields nothing for a number; that line is already dead for the item category.\n\n(D) FIX 3 MUST BE SKILL-LANE ONLY. Do NOT wire the new `passiveItemPenalty` into src/rules/derive.ts:211 (saves), :343 (perception), or :1740 (AC), and do not change `passiveItemBonus`'s `let best = 0` in place — those three sites all consume it and a signed return would silently drag saves/Perception/AC. Reason this is not hypothetical: the finding's corpus scan was scoped to passiveEffects.skills and missed a THIRD negative item passive. `items/day-goggles.passiveEffects.perception = -2`, and that penalty is CONDITIONAL in print (visual Perception checks only) and already carried as display at src/rules/situationalBonuses.ts:1283 (\"on VISUAL Perception checks, while wearing the goggles over your eyes\", bonus \"-2 item (penalty)\"). A kind-symmetric wiring of the helper would apply -2 flat to ALL Perception for day-goggles wearers — a new regression created by the fix. Restrict the new term to the `poolTypedMods([...])` array in deriveSkill (src/rules/derive.ts:540), leaving `{ type: 'item', value: itemBonus }` untouched.\n\n(E) NO NEW STACKING CODE. src/rules/modes.ts:169 `poolTypedMods` already keeps `bonus[type]`/`penalty[type]` apart and returns `bonus.item - penalty.item`. Passing an extra `{ type: 'item', value: -1 }` into the existing array is sufficient and correct RAW (item bonus and item penalty both apply). Mirror in passiveItemBonusDetail so the Stealth breakdown names the shining hackle.\n\n(F) EXISTING IDIOM WORTH THE OWNER'S CHOICE (gate 8). The engine already has an item passive PENALTY lane: src/rules/derive.ts:4634-4645 accumulates `passiveSpeedPenalty` from a dedicated positive-magnitude field `pe.speedPenalty` (src/rules/types.ts:2034) via `Math.abs`, in its own lane rather than as a negative inside a bonus field. If the owner prefers that established shape, the alternative is a `skillPenalty?: Partial<Record<ProficiencyKey, number>>` field; but that requires migrating the two existing negative rows, so the companion-function approach is the lower-risk path. Either way an existing way to SAY it exists — this is not a novel concept, only a missing reader.\n\n(G) THE WANDERING-PIPE EDIT IS MANDATORY, CO-LANDING, AND UNDER-EVIDENCED. It must land in the same commit, not \"in the same pass, optionally\": once the skill-lane penalty is live, `items/wandering-pipe.passiveEffects.skills.stealth = -1` starts applying flat, and its penalty is conditional per src/rules/situationalBonuses.ts:1955. But its conditionality CANNOT be sourced the way shining-hackle's was: `node scripts/wg-item.mjs \"Wandering Pipe\"` returns \"no item matched\" (they do not carry it), and our core.json record has no description/text field at all (keys: id, name, level, price, bulk, traits, rarity, source, usage, frequency, counters, activationCost, itemType, edition, passiveEffects, aonId). The only evidence for \"conditional\" is the authored display string itself, which is circular. Read AoN equipment-3513 directly before dropping the row. And EDIT the overlay value to {\"skills\":{\"deception\":2}} — do not delete the row: the deception +2 is live, positive, and correct.\n\n(H) OUT OF SCOPE, CONFIRMED AS FLAGGED. shining-hackle's `passiveEffects.perception: 1` is stored unconditionally where print says \"based on sight\", and is simultaneously surfaced as a computed flat +1 AND as a situational note at src/rules/situationalBonuses.ts:2116. It is at parity with their side (flat PERCEPTION +1 with the condition in `text: \"base on sight\"`), so the WG gate is clean, but the double-surfacing is a genuine display question for the owner. Note the asymmetry with day-goggles, whose analogous conditional value is stored flat AND displayed but is harmlessly inert only because it happens to be negative."
 },
 {
  "id": "aeon-stone-repairing",
  "verdict": "CONFIRMED",
  "reason": "Attacked on every axis and it holds; the reported gap is a comparer artefact, not a data gap.\n\n(1) THEIR RAW OPS, read myself. `node scripts/wg-item.mjs 19528 --raw` (wg-show.mjs only parses ability_block, so it correctly answers \"no such row\" — aeon stones are items). Item 19528 \"Aeon Stone (Repairing)\", level 4, 75gp, carries exactly one top-level op: a `select` titled \"Is this granting the resonant power?\" with No/Yes; BOTH branches run `addBonusToValue SKILL_CRAFTING +1 Item` and `injectText {type:action, id:19756}` with the +5/+10 HP repair sentence; Yes adds `giveSpell {spellId:4719, type:INNATE, rank:1, casts:1}` and nothing else. Their spell 4719 = Mending (traditions {arcane,divine,occult,primal}); their giveSpell carries NO tradition field. Quoted ops match the finding verbatim.\n\n(2) PRINT, re-read from the AoN mirror. Equipment.aspx?ID=3055 → Aeon Stone (Repairing), level 4, 75 gp, Pathfinder #213: Thirst for Blood pg. 82; effect \"+1 item bonus to skill checks made to Craft… when Repairing an item, you restore an additional 5 Hit Points (10 on a critical success)\"; resonant \"cast mending as an arcane innate spell once per day.\" Ours (public/core.json items/aeon-stone-repairing.resonant) is mending / arcane / rank 1 / usesPerDay 1 — matches print on every field. Their own item text says \"arcane\" too; only their ops omit it. So the fix settles NO WG-vs-print disagreement — it changes no data at all — and ASK-OWNER does not apply.\n\n(3) CARRIERS, all re-grepped and live. Overlay row present and clean: scripts/data/effect-backfill.json holds exactly two rows for this id, `field:\"frequency\"` and `field:\"resonant\"` — different fields, no collision, neither value null. Crafting bonus present in situationalBonuses.ts alongside its aeon-stone-formulating twin. ItemDesignation 'wayfinder-slotted' exists (types.ts:4573), is offered at InventoryTab.tsx:706, gated to records with `resonant` at ItemDetail.tsx:495, and consumed at build.ts:6498 → innateGrants → the innate card at SpellsTab.tsx 1792-1899. wg-diff.mjs:290 ('resonant.innateSpells' in the spell kind list) and wg-diff.mjs:616-627 (kinds.add('choice') for any `resonant`) are verbatim as quoted.\n\n(4) REPRODUCED. `node scripts/wg-identity.mjs --ids aeon-stone-repairing` → spells theirs-not-ours=[mending], options theirs-not-ours=[no, yes], ours=[(nothing)] on both. wg-identity.mjs is untracked (`?? scripts/wg-identity.mjs`) while wg-diff.mjs is tracked, and the word `resonant` appears in wg-identity.mjs only inside a settle comment — never as a field descent. Line 200 is the ours spell collector; line 209 (`rec.enhancement?.grant?.innateSpells`) is an existing one-level-down descent, so the proposed edit matches its neighbours exactly and the find is unique.\n\n(5) FIX SAFETY, both directions checked. Fix 1 cannot regress a sibling: the comparer reports one direction only (`[...theirs[bucket]].filter(n => !contains(ours[bucket], n))`, wg-identity.mjs:913), so enlarging ours.spells can only remove reports, never add. Fix 2 is precisely scoped: the title \"Is this granting the resonant power?\" occurs on exactly 30 rows in the dump, ALL of them aeon stone items — no non-aeon record loses anything. And flattenOps (scripts/lib/wg-parse.mjs:54-60) recurses into optionsPredefined, so amplifying's tradition picker is emitted as its own `select` titled \"Select the tradition of the last spell you enhanced with this aeon stone.\" and keeps reporting [arcana, nature, occult, religion] after the skip. Family-wide run across all 25 comparable aeon ids: 21 report (western-star suppressed by an existing settle), 15 of them with the spells gap — matching wg-diff's own \"15 stones spell-less\" comment.\n\nCONFIRMED, but the fix list is incomplete and three of its supporting claims are wrong — see correctedFix. The load-bearing omission is that fix 2 strands an existing settle that wg-settle-stale.mjs is purpose-built to condemn.",
  "correctedFix": "Keep both src-edits to scripts/wg-identity.mjs as written (the ours-side `resonant.innateSpells` descent after line 200, and the their-side skip of the select titled \"Is this granting the resonant power?\" in the `select` branch at lines 88-93). Drop the \"settle\" fallback — it settles one id where the defect is comparer-wide across 30 of their rows. THREE CORRECTIONS:\n\n(A) MISSING STEP, must ship in the same edit. Fix 2 strands the existing settle `'aeon-stone-western-star': ['options']` at scripts/wg-identity.mjs:877 (with its comment block at 869-876). Verified with `node scripts/wg-identity.mjs --ids aeon-stone-western-star --raw`: that stone's ONLY reported gap is the same `options theirs-not-ours=[no, yes]` resonant select. Once their side stops emitting it, the entry silences nothing — which is exactly the hazard scripts/wg-settle-stale.mjs exists to catch; it parses SETTLED_IDENTITIES (wg-settle-stale.mjs:44) and reports a settle that answers nothing for DELETION (\"A settle that silences nothing is a trap waiting to spring… until the day the comparer reports a DIFFERENT difference of the same kind on that record and the entry silences that one too, unread and unadjudicated\"). Delete lines 869-877 as part of fix 2.\n\n(B) The ⚠ rationale on fix 2 is factually wrong and should not be repeated in the code comment. `contains()` (wg-identity.mjs:393) would NOT let a bare 'no' in ours satisfy their 'nature': `'nature'.includes('no')` is false, `'no'.includes('nature')` is false, and the word-set fallback compares {'no'} against {'nature'} with no overlap. So adding 'no'/'yes' to ours.options would not have silenced aeon-stone-amplifying. Their-side suppression is still the correct route — ours genuinely has no option list to add to, since the control is a per-item designation — but write the comment on that ground, not on a collision that does not exist.\n\n(C) The provenance sentence is false and should be struck from the record. The `field:\"resonant\"` overlay rows have NEVER been committed: `git show <c>:scripts/data/effect-backfill.json | grep -c '\"resonant\"'` returns 0 at 061f099, b673684, 035a2a8 and HEAD, and the 37 occurrences exist only in the modified working tree, written by the untracked scripts/backfill-aeon-resonance.mjs. The operative claim (\"present today, materialised in public/core.json\") is true and the verdict stands on it, but the lane is not yet durable across a clean checkout — scripts/data/effect-backfill.json and scripts/backfill-aeon-resonance.mjs both need committing before \"survived every regen since\" is true of anything.\n\n(D) Cosmetic line drift in the citations, for whoever navigates by them: ItemDesignation is types.ts:4573 (not 4547); the resonant reader is build.ts:6498 (not 6477); SETTLED_IDENTITIES is consumed at wg-identity.mjs:912 (not 903). The \"22 stones\" figure is 21 reported plus western-star already settled; 30 of their rows carry the select in total."
 },
 {
  "id": "aeon-stone-formulating",
  "verdict": "CONFIRMED",
  "reason": "Every load-bearing claim re-derived from source, not taken on trust.\n\n(1) THEIR RAW OPS — read myself via `node scripts/wg-item.mjs \"aeon stone (formulating)\" --raw` (wg-show.mjs correctly reports no ability_block row; the record is an `item`). Item id 19433, level 4, UNCOMMON, price {\"gp\":75}, usage worn. Operations are verbatim as quoted: one `select` titled \"Is this granting the resonant power?\" with predefined options \"No\" (one `addBonusToValue` SKILL_CRAFTING = \"1\" type Item, text = the formula-knowledge sentence) and \"Yes\" (the identical addBonusToValue plus `{\"type\":\"giveSpell\",\"data\":{\"spellId\":4764,\"type\":\"INNATE\",\"rank\":1,\"casts\":1}}`). No tradition key on the giveSpell — confirmed by eye on the raw JSON, not inferred. Their spell row 4764 = \"Phantasmal Minion\", rank 1, traditions {arcane,occult}, so the spell identity is right and the tradition is simply unencoded on the grant.\n\n(2) PRINTED TEXT — flattened public/ast/items.json.gz['aeon-stone-formulating'] myself. Pathfinder #213: Thirst for Blood pg. 81, Item 4, 75 gp: \"...This counts as having the formula for the purposes of Crafting checks... You also gain a +1 item bonus to skill checks made to Craft while the stone is invested. The resonant power allows you to cast phantasmal minion as an arcane innate spell once per day.\" Print states arcane; WG omits tradition. Not a WG-vs-print disagreement — omission is not a clash — so gate (4) does not trigger ASK-OWNER. Corroborated by scripts/verify-aeon-parity.mjs, whose `clash()` encodes exactly that rule; I ran it and formulating is not among its 1 DIFFER (polished-pebble) / 3 THEY-ONLY / 2 WE-ONLY, i.e. it is in the 15-strong AGREE set as claimed.\n\n(3) OURS — public/core.json items['aeon-stone-formulating'].resonant = {note: \"...arcane innate spell once per day.\", innateSpells:[{spellId:\"phantasmal-minion\",tradition:\"arcane\",rank:1,usesPerDay:1}]}. Rank 1 = rank 1, usesPerDay 1 = casts 1. The overlay row exists verbatim in scripts/data/effect-backfill.json: {\"category\":\"items\",\"id\":\"aeon-stone-formulating\",\"field\":\"resonant\",\"value\":{...}} (plus a separate `frequency` row) — a real value, not null, so no REMOVE semantics in play. Data is intact and unregenerated.\n\n(4) CARRIERS RE-GREPPED — all live, though several cited line numbers have drifted: the resonant reader is src/rules/build.ts:6498-6501 (`if (item?.resonant && inv.designations?.includes('wayfinder-slotted'))` → pushes into innateGrants + noteSrc `${item?.name} (resonant)`), NOT 6477-6482; `ItemDesignation` union carrying 'wayfinder-slotted' is src/rules/types.ts:4573, NOT 4547. InventoryTab.tsx:706 and ItemDetail.tsx:491-495 (which filters the designation to items that actually have `resonant`) are exact. The render range src/sheet/SpellsTab.tsx:1792-1899 is correct (itemEntries → isInnate branch, per-spell uses/cadence pips and the tradition footer). Drift is in prose only; every carrier resolves.\n\n(5) THE BLIND SPOT — reproduced. `node scripts/wg-identity.mjs --ids aeon-stone-formulating --verbose` prints exactly `spells theirs-not-ours=[phantasmalminion] / ours=[(nothing)]` and `options theirs-not-ours=[no, yes] / ours=[(nothing)]`. `grep -n resonant scripts/wg-identity.mjs` returns ZERO hits; wg-diff.mjs carries both descents at line 290 (`spell:` kind list includes 'resonant.innateSpells') and 619-629 (ourKindsOf descends `resonant` and `kinds.add('choice')`). wg-identity.mjs is `??` untracked in git status. Systemic, not per-id: running the comparer over delaying/envisioning/nourishing/preserving/smoothing/sprouting/formulating/repairing/crescent reports the identical two-bucket miss on all nine, and core.json has 33 items with `resonant`, 18 of them with `innateSpells`.\n\n(6) FIX 1 VALIDATED — wg-identity.mjs:200 is `for (const s of rec.innateSpells ?? []) addSpell(s.spellId ?? s);`, so the insertion point and shape match the neighbours exactly (identical to the `enhancement?.grant?.innateSpells` line a few rows down). Additive only, cannot manufacture a false MATCH on any other bucket. Name join verified end-to-end, not assumed: `resonant` has only two subfields across all 33 records (`note`, `innateSpells`), so one line covers the field completely; anyName('phantasmal-minion') resolves through core.spells to \"Phantasmal Minion\"; key() lowercases, strips a leading article and strips non-alphanumerics → 'phantasmalminion', the exact key wg-identity reports as theirs-not-ours. Same holds for the other 8 stones' ids I spot-checked.\n\n(7) FIX 2 VALIDATED — theirIdentities' select branch is wg-identity.mjs:89-93 (cited 88-93; off by one). The title \"Is this granting the resonant power?\" is the ONLY distinct such title in the dump and appears on 30 rows, so a title-exact skip is precise and scoped to aeon stones. Critically, it does not over-suppress: flattenOps emits the select AND its nested ops as separate entries (visible in the wg-item output), so skipping the select still collects the nested giveSpell into `spells` — the fix closes `options` only. The stated direction (suppress on THEIR side rather than adding 'yes'/'no' to ours) is the right one.\n\n(8) NO NEW ENGINE FIELD NEEDED — `resonant.innateSpells` and the `wayfinder-slotted` designation already exist and already ship; nothing is being invented.\n\nOne inaccuracy, in rationale not in the fix: the finding justifies fix 2 by saying contains() substring matching \"would then let 'no' satisfy 'nature'\". contains() (wg-identity.mjs:394-396) does do substring matching both directions, but 'nature' does not contain the substring 'no' — the specific pair is wrong. The underlying hazard is nonetheless real (`name.includes(mine)` with mine='no' would falsely match any their-option keying to e.g. 'gnome', 'knowledge', 'nomad'), so the recommendation stands unchanged. Not enough to disturb the verdict.\n\nFix 3 (per-id SETTLED_IDENTITIES entry at wg-identity.mjs:426) is correctly self-labelled as not recommended — settling one id against a comparer-wide blind spot affecting 18 records would silence the next 17 unread, which is precisely what that registry's own warning comment forbids."
 },
 {
  "id": "aeon-stone-clear-quartz-octagon",
  "verdict": "CONFIRMED",
  "reason": "Every load-bearing claim survives adversarial re-reading. THEIR RAW OPS (node scripts/wg-item.mjs 19529): item 19529 carries exactly one operation, select title \"Is this granting the resonant power?\" with No -> operations:[] and Yes -> giveSpell {spellId:4719, type:INNATE, rank:1, tradition:DIVINE, casts:1}; their spell row 4719 resolves to Mending, rank 1. OUR RECORD: public/core.json items/aeon-stone-clear-quartz-octagon.resonant = {note, innateSpells:[{spellId:'mending', tradition:'divine', rank:1, usesPerDay:1}]} — agrees field-for-field (divine=DIVINE, rank 1=rank 1, usesPerDay 1=casts 1). PRINT: our shipped public/core-descriptions.json mirrors the AoN text and its resonant clause is identical to both sides, so NO WG-vs-print disagreement is being settled (gate 4 clears; not ASK-OWNER). OVERLAY (gate 5): the row exists in scripts/data/effect-backfill.json with exact field 'resonant', non-null value (so it writes, not removes), and the only other row for this id is 'frequency' — no collision; git log -S confirms commit b673684, so not a regeneration drop. LIVE READER: build.ts:6498-6501 reads item.resonant.innateSpells gated on inv.designations.includes('wayfinder-slotted'), rendered at SpellsTab.tsx:1792-1899; the designation is offered at InventoryTab.tsx:706 and field-gated at ItemDetail.tsx:491-495, typed at types.ts:4573. ROOT CAUSE CONFIRMED: wg-identity.mjs:200 collects only rec.innateSpells (and enhancement.grant / heldSpells at 209/218) — nothing reads rec.resonant.innateSpells, while wg-diff.mjs:616-627 already descends into `resonant` and already treats it as a 'choice'. Running the comparer reproduces the reported \"spells theirs-not-ours=[mending] / ours=[(nothing)]\" plus an \"options theirs-not-ours=[no, yes]\" line. GATE 6 (no sibling regression): the select title is unique to 30 aeon-stone item rows and 0 ability_block rows; fix #1's insertion point at line 200 is unique and matches its neighbour at 209 exactly; because flattenOps hoists nested option operations as siblings, skipping the select at lines 88-93 removes only the no/yes labels and leaves the giveSpell reaching the spells bucket, so the comparison is not blunted. GATE 8: no new engine field is proposed — resonant.innateSpells already exists and already ships. TWO CORRECTIONS, neither fatal, folded into correctedFix. (a) The finding under-sells its own scope: wg-identity --raw over all 34 aeon stones shows ~22 records with the same false spells/options pair, and fix #1 clears the spells line on 14 of them while the genuine gaps (mottled-ellipsoid grants=[voidhealing], agate-ellipsoid) correctly survive — so nothing real is masked. (b) The finding misses that fix #2 strands a stale settle: wg-identity.mjs:876 already carries 'aeon-stone-western-star': ['options'] settling this very mismatch per-record, and once the select is skipped globally that entry silences nothing — exactly the hazard scripts/wg-settle-stale.mjs exists to report for DELETION. It must be removed in the same edit. I also verified fix #2's per-select shape is strictly better than the per-record settle it generalises: Aeon Stone (Amplifying) (item 6677) carries a SECOND select, \"Select the tradition of the last spell you enhanced with this aeon stone.\" (Arcana/Nature/Occult/Religion), which per-select skipping preserves and a per-record ['options'] settle would have over-silenced. Cited line numbers drifted slightly (build.ts 6477-6482 is really 6498-6501; types.ts 4547 is really 4573) — cosmetic, the carriers are all live. The activationCost {actions:1} vs printed \"automatically activates\" note is correctly scoped as an out-of-gate observation with kind 'none' and is not part of this verdict.",
  "correctedFix": "Apply the two src-edits as written, with one required addition and one scope note.\n\n1. scripts/wg-identity.mjs, ourIdentities(), after line 200 (`for (const s of rec.innateSpells ?? []) addSpell(s.spellId ?? s);`) add, in the shape of its neighbour at line 209:\n\n   /* …and an aeon stone's RESONANT power, one level down. wg-diff already descends into `resonant`\n    * (wg-diff.mjs:616-627); this reader stopped at the top level, so 14 stones whose spell lives at\n    * resonant.innateSpells read as granting nothing. Shipped reader: build.ts:6498-6501. */\n   for (const s of rec.resonant?.innateSpells ?? []) addSpell(s.spellId ?? s);\n\n2. scripts/wg-identity.mjs, theirIdentities(), in the `else if (op?.type === 'select')` branch at lines 89-93, skip THIS ONE SELECT — per-select, not per-record:\n\n   } else if (op?.type === 'select') {\n     /* \"Is this granting the resonant power?\" is their shape for a state ours holds as the\n      * `wayfinder-slotted` designation (InventoryTab.tsx:706, field-gated at ItemDetail.tsx:491-495,\n      * read at build.ts:6498). Same question, different control — the equivalence wg-diff.mjs:616-627\n      * already records. The title occurs on 30 item rows, all aeon stones, and 0 ability_block rows.\n      * Skipped per-SELECT so Amplifying's second select (its tradition picker) still compares; and\n      * because flattenOps hoists nested ops as siblings, the Yes branch's giveSpell still reaches the\n      * spells bucket, so this hides no grant. */\n     if (/granting the resonant power/i.test(String(d.title ?? ''))) continue;\n     for (const o of d.optionsPredefined ?? []) { … unchanged … }\n\n   Note `continue` requires the loop body to be a plain for-of over the flattened ops (it is), and `d`\n   is already bound at the top of the loop.\n\n3. REQUIRED, and missing from the original fix list: delete the now-stale settle at scripts/wg-identity.mjs:876,\n   `'aeon-stone-western-star': ['options'],` together with its comment block at lines 869-875. Step 2\n   makes it match nothing, and scripts/wg-settle-stale.mjs exists precisely to report such an entry for\n   deletion (\"A SETTLE THAT SILENCES NOTHING IS A TRAP WAITING TO SPRING\") — leaving it in place would\n   silence the NEXT, unrelated options difference on that stone, unread. Follow the file's own convention\n   for retired entries, e.g.:\n   /* aeon-stone-western-star — settle REMOVED: the resonant-power select is now skipped for every aeon\n    * stone in theirIdentities, so this per-record entry answers nothing. Re-report if it returns. */\n\n4. Correct the drifted citations when recording this: build.ts:6498-6501 (not 6477-6482) and\n   types.ts:4573 (not 4547). InventoryTab.tsx:706, ItemDetail.tsx:491-495 and SpellsTab.tsx:1792-1899 are right.\n\n5. Unchanged from the original: the activationCost {actions:1} vs printed \"automatically activates\"\n   question stays kind:'none' — out of this gate, and their side encodes nothing to differ against.\n\nAfter applying, re-run `node scripts/wg-identity.mjs --ids <the 34 aeon-stone ids> --raw` and confirm the\nsurviving lines are the genuine gaps only (aeon-stone-mottled-ellipsoid grants=[voidhealing],\naeon-stone-agate-ellipsoid), then `node scripts/wg-settle-stale.mjs` to confirm no settle was stranded."
 },
 {
  "id": "aeon-stone-preserving",
  "verdict": "CONFIRMED",
  "reason": "RAW OPS RE-READ ON BOTH SIDES, independently. Theirs (node scripts/wg-item.mjs 6683 — wg-show.mjs only indexes ability_block, so it returns \"no row\" for this item and is the wrong tool here): one select titled \"Is this granting the resonant power?\"; No = adjValue RESISTANCES \"persistent damage, 3\"; Yes = the smeared adjValue plus giveSpell {spellId:4857,type:INNATE,rank:0,tradition:PRIMAL} with NO casts key. I verified spell 4857 = Stabilize, rank 0, and verified independently that `casts` IS their per-day key: every limited innate grant in the dump carries casts:N and every rank-0 innate (Ancestral Cantrip rows) carries none. So their encoding is an unlimited primal cantrip. The smear is exactly as described (giveSpell carries the sibling's variable/value; the adjValue carries the spell keys) and changes no readable spell field.\n\nPRINTED TEXT re-read from public/core-descriptions.json items/aeon-stone-preserving (the AoN-sourced prose feat-doctor prints): matches the quoted clause word for word, including \"as a primal innate cantrip\", and states no frequency.\n\nOURS: resonant.innateSpells [{spellId:'stabilize', tradition:'primal', rank:0}] with neither atWill nor usesPerDay. Verified this is the cantrip shape end to end, not just by type: types.ts:2115-2123 (rank ignored for cantrip/at-will), build.ts:6586 `innate.filter(g => castRank(g) === 0)` -> innateCantrips, rendered at SpellsTab.tsx:1838-1841 as \"at will . heightened to Nth\". Both sides therefore say primal, rank 0, no cast cap. verify-aeon-parity.mjs run: 15 AGREE / 1 DIFFER / 3 they-only / 2 we-only, and this record is in AGREE.\n\nCARRIERS RE-GREPPED, ALL LIVE (line numbers drift ~20-25 from the finding): build.ts:6498-6503 reads item.resonant.innateSpells gated on designations.includes('wayfinder-slotted'); types.ts:4573 ItemDesignation; InventoryTab.tsx:706 offers \"Slotted in a wayfinder\"; ItemDetail.tsx:491-495 filters that designation to records that HAVE a resonant power. One citation is wrong but not load-bearing: SpellsTab.tsx:461/487 is the learn-a-spell picker's \"Cantrip\" label, not the innate render; the innate render is at 1838-1841, inside the separately cited 1792-1899 range.\n\nTHE MISS IS REAL AND LANE-WIDE, not a one-off: wg-identity --ids over every aeon stone reports ~21 records with theirs-not-ours spells and/or options [no, yes], and wg-batch-gate.mjs:102-105 fails a batch on any identity miss.\n\nFIX 1 (ourIdentities + resonant.innateSpells) VERIFIED: `resonant` is unread anywhere in wg-identity.mjs today (unique find); it exists on 33 records, all aeon stones, and on no record in any other bucket (no sibling regression); the added line matches its immediate neighbours (rec.innateSpells at 200, enhancement?.grant?.innateSpells at 209, both `s.spellId ?? s`). anyName('stabilize') -> \"Stabilize\" -> key \"stabilize\", which is exactly their token.\n\nFIX 2 (theirIdentities skip of that select) VERIFIED SAFE, and I attacked it specifically for gap-hiding: the title appears on exactly 30 rows, and I listed all 30 - every one is an \"Aeon Stone (...)\", so the skip cannot leak to an unrelated record. flattenOps (lib/wg-parse.mjs:54-60) descends into optionsPredefined[].operations, so skipping the select op suppresses ONLY its two labels: the nested giveSpell is still visited as its own op (Stabilize still compares), and Aeon Stone (Amplifying)'s nested \"Select the tradition...\" select still contributes arcana/nature/occult/religion. The three stones where they encode a resonant spell and we encode none (agate-ellipsoid, dusty-rose-prism, western-star) still report after both fixes.\n\nNOT AN OWNER QUESTION: their row and the printed clause agree with each other and with ours, so nothing here settles a WG-vs-print disagreement. The only thing being changed is the comparer's reading, and no data is touched.\n\nTHREE THINGS THE FINDING GOT WRONG OR UNDERSTATED (none of which unseat the fixes):\n(a) \"mirroring wg-diff.mjs:616-627\" is inaccurate. The real block is wg-diff.mjs:617-632 and it runs the OPPOSITE direction: it credits OUR side (kinds.add('choice') when rec.resonant exists) and suppresses nothing of theirs. A true mirror would credit our two answers in ourIdentities rather than skip their select. Both clear the miss; I am not blocking on it, but note that a code-level their-side skip is invisible to scripts/settle-divergence-audit.mjs (FILES at 35-39 reads only the SETTLED_* registries), which is in `npm run verify` - so this silencing will never be re-audited, whereas the ours-side credit silences nothing.\n(b) \"Their No-branch resistance is ours twice over\" is presented as parity-positive; it is a defect. derive.ts:2358-2377 pushes BOTH db.items[id].passiveEffects AND the top-level record as defense sources, and derive.ts:2484 does res.set(r.type, ...) keyed by the RAW type string with no normalisation - so \"persistent damage\" and \"persistent\" are two different keys and the sheet shows TWO resistance rows for one printed resistance. aeon-stone-preserving is one of only two items in the whole corpus carrying both carriers, and the other (rebounding-breastplate) uses matching spellings so it collapses harmlessly. This belongs on the out-of-gate list beside frequency.\n(c) The frequency observation is correct (no printed basis, no counterpart in their row) but understates the effect: itemUses.ts counterDefs (lines 32-41) synthesises a live counter {id:'freq', label:'per hour', max:1, resetsOnRest:true} from that field, so the stone shows a phantom once-per-hour activation tracker on the inventory card.\n\nIndependently re-ran and reproduced: node scripts/wg-item.mjs 6683, node scripts/wg-identity.mjs --ids aeon-stone-preserving (and across all aeon stones, plain and --raw), node scripts/verify-aeon-parity.mjs, node scripts/feat-doctor.mjs aeon-stone-preserving --no-evidence (AUTHORED FIELDS lists resonant; overlay rows passiveEffects/resistances/resonant survive `npm run data`; \"no authoring script names this record\" reproduced verbatim even though scripts/backfill-aeon-resonance.mjs demonstrably writes the row via readBackfill/writeBackfill).",
  "correctedFix": "[{\"kind\":\"src-edit\",\"detail\":\"scripts/wg-identity.mjs, ourIdentities(), immediately after line 200 (`for (const s of rec.innateSpells ?? []) addSpell(s.spellId ?? s);`): add `for (const s of rec.resonant?.innateSpells ?? []) addSpell(s.spellId ?? s);` with a comment naming the carrier — an aeon stone's resonant power is a second innate grant that applies only while the stone is slotted in a wayfinder. UNCHANGED FROM THE FINDING; verified unique (resonant is unread in this file), scoped (33 records, all aeon stones, no other bucket) and matched to its neighbours (enhancement?.grant?.innateSpells, line 209).\",\"reader\":\"scripts/wg-identity.mjs ourIdentities(). Shipped reader for the value: src/rules/build.ts:6498-6503 (gated on inv.designations.includes('wayfinder-slotted')); rank-0 routing at build.ts:6586; rendered at src/sheet/SpellsTab.tsx:1838-1841.\"},{\"kind\":\"src-edit\",\"detail\":\"scripts/wg-identity.mjs — settle the yes/no residue. The finding's version (skip their select in theirIdentities(), lines 88-93) is SAFE and I verified it: the title occurs on exactly 30 rows, all Aeon Stones, and flattenOps still yields the nested giveSpell and Amplifying's nested tradition select, so nothing real is hidden. PREFER THE MIRROR DIRECTION INSTEAD, because it silences nothing of theirs and matches the sibling comparer exactly: in ourIdentities(), beside the new resonant line, add `if (rec.resonant) { out.options.add(key('yes')); out.options.add(key('no')); }` with the comment that wg-diff.mjs:617-632 already credits the same toggle on our side (kinds.add('choice')) — their select is 'is this stone slotted in a wayfinder?', ours is the `wayfinder-slotted` per-item designation the player toggles in play. Note for whichever is chosen: the finding's citation 'wg-diff.mjs:616-627' is wrong on both the range (617-632) and the direction (wg-diff credits OUR side, it does not suppress theirs), and a their-side skip is invisible to scripts/settle-divergence-audit.mjs, which reads only the SETTLED_* registries.\",\"reader\":\"scripts/wg-identity.mjs. Shipped carrier: ItemDesignation 'wayfinder-slotted' — src/rules/types.ts:4573, offered at src/sheet/InventoryTab.tsx:706, filtered to resonant-bearing records at src/sheet/ItemDetail.tsx:491-495, read at src/rules/build.ts:6498.\"},{\"kind\":\"none\",\"detail\":\"OUT OF GATE #1 — frequency {max:1, per:'hour'} on items/aeon-stone-preserving has no basis in the printed clause and no counterpart in their row. Stronger than the finding stated: src/rules/itemUses.ts counterDefs (lines 32-41) synthesises a live counter {id:'freq', label:'per hour', max:1, resetsOnRest:true} from that field, so the stone currently shows a phantom once-per-hour activation tracker on the inventory card.\",\"reader\":\"src/rules/itemUses.ts:32-41 -> itemCounters().\"},{\"kind\":\"none\",\"detail\":\"OUT OF GATE #2 — NEW, missed by the finding, which described it as 'ours twice over'. items/aeon-stone-preserving carries the same printed resistance on BOTH carriers with DIFFERENT type spellings: passiveEffects.resistances [{type:'persistent damage', value:3}] and top-level resistances [{type:'persistent', value:3}]. src/rules/derive.ts:2358-2377 pushes both the passiveEffects object and the top-level record as defense sources, and derive.ts:2484 does res.set(r.type, ...) on the raw string with no normalisation, so the two spellings are two map keys and the sheet renders TWO resistance rows for one printed resistance. Only two items in the corpus carry both carriers; the other (rebounding-breastplate) uses matching spellings and collapses correctly. Either drop one row or make the spellings agree.\",\"reader\":\"src/rules/derive.ts:2358-2377 (both sources pushed) and derive.ts:2484 (res.set keyed by raw type); output at derive.ts:2653.\"}]"
 },
 {
  "id": "aeon-stone-agate-ellipsoid",
  "verdict": "CONFIRMED",
  "reason": "The core defect is real and I reproduced it end to end. Raw ops read on BOTH sides: node scripts/wg-item.mjs 15071 shows one select \"Is this granting the resonant power?\" whose Yes and No branches both run an identical giveSpell 4406 INNATE rank 2 DIVINE casts 1, with Yes adding only an injectText — so their spell is the BASE power and the resonant power is prose, exactly as reported. Print read independently from the AoN mirror (C:/wonderers guide/aon-2e-archive/data/by-category/equipment/equipment-407-868.json, the Agate Ellipsoid sub-entry): \"always succeed at the DC 6 flat check to give an answer other than \"nothing.\"\". Ours ships \"succeed at the to give an answer\" in BOTH public/core.json items/aeon-stone-agate-ellipsoid.resonant.note AND public/core-descriptions.json items['aeon-stone-agate-ellipsoid'].d. All three sources agree on the content, so this is import damage, not a WG-vs-print disagreement — not ASK-OWNER. Carrier claims re-checked by grep and all hold: resonant.note has NO reader anywhere in src/ (only presence at ItemDetail.tsx:495 and .innateSpells at build.ts:6498-6499), the live player surface is DescBody description={item.description} at ItemDetail.tsx:652 hydrated from core-descriptions.json at src/data/index.ts:530, so the repair must land in the description. The lane is right and independently corroborated: node scripts/scan-damaged-descriptions.mjs prints its own footer directing repairs into scripts/apply-import-damaged-text.mjs because that script alone writes both the durable overlay row and the live descriptions file. The find string occurs exactly once in each public JSON (that script's stated precondition), no overlay row collides (no existing items/aeon-stone-agate-ellipsoid description row; 453 items-category description rows already exist, so the shape matches neighbours), no value:null is involved, and the order-dependence is real — scripts/backfill-aeon-resonance.mjs derives the note from descs.items[id].d (line ~113), and the repaired clause still parses note-only (no \"innate spell\" in it) so no spurious resonant.innateSpells appears. test/aeon-resonance.test.ts:78,97 survives. Family scan reproduced: of 33 resonant notes, agate is the only damaged one. Two corrections, neither touching the core defect. (1) Fix #3 is wrong-shaped and would regress a sibling. scripts/wg-identity.mjs:869-877 ALREADY carries 'aeon-stone-western-star': ['options'] in SETTLED_IDENTITIES, settled on the identical reasoning (same select title, both branches the same giveSpell, wayfinder-slotted as our carrier) — that is this file's established remedy for this exact gap, and I confirmed it by running the gate with and without --raw. Structurally suppressing the select inside theirIdentities() would make that settle match nothing, and scripts/wg-settle-stale.mjs:44 parses SETTLED_IDENTITIES precisely to report settles that answer nothing for DELETION. The finding's stated hazard is also false: 'nature' does not contain the substring 'no', so a bare 'no' would not silence aeon-stone-amplifying's [arcana, nature, occult, religion] gap. (2) Fix #2 stops one step short — re-running backfill-aeon-resonance --write updates only scripts/data/effect-backfill.json; shipped public/core.json keeps the damaged resonant.note until the overlay is replayed. Minor immaterial inaccuracy noted, not corrected: the finding says the phrase \"was a rules link that the importer stripped\" — the mirror markdown has \"DC 6 flat check\" as PLAIN text, not a link, so the mechanism is the importer's inline-value stripping (the same shape scan-damaged-descriptions.mjs catalogues) rather than link removal. The fix does not depend on the mechanism.",
  "correctedFix": "Keep fixes #1 and #2 as written, with one addition, and REPLACE fix #3.\n\nFIX 1 (unchanged, verified). Add to the REPAIRS table in scripts/apply-import-damaged-text.mjs:\n{ category: 'items', id: 'aeon-stone-agate-ellipsoid', find: 'always succeed at the to give an answer other than', replace: 'always succeed at the DC 6 flat check to give an answer other than' }\nVerified: exactly 1 occurrence in public/core-descriptions.json and 1 in public/core.json; desc.items['aeon-stone-agate-ellipsoid'].d is a string so the script's fail() guard passes; wording transcribed from the AoN mirror (Paizo/ORC), not from their injectText.\n\nFIX 2 (extended). Run, in this order:\n  node scripts/apply-import-damaged-text.mjs\n  node scripts/backfill-aeon-resonance.mjs --write\n  node scripts/apply-backfill-now.mjs\nThe third command is the addition the finding omitted. backfill-aeon-resonance --write only rewrites the row in scripts/data/effect-backfill.json; without replaying the overlay, shipped public/core.json keeps resonant.note = \"succeed at the to give an answer\". apply-backfill-now.mjs is idempotent, routes description rows to core-descriptions.json (which apply-import-damaged-text.mjs has already written identically), and is the documented in-loop substitute for npm run data. Do not hand-edit the resonant row in effect-backfill.json — it is derived.\n\nFIX 3 (REPLACED — do NOT edit theirIdentities()). Add a per-record settle to SETTLED_IDENTITIES in scripts/wg-identity.mjs, immediately beside the existing 'aeon-stone-western-star': ['options'] entry at line 877, with a comment in the same form:\n  /* AEON STONE (AGATE ELLIPSOID) — same shape as Western Star above: their two options are the\n   * question \"Is this granting the resonant power?\" (Yes/No), and BOTH branches run the identical\n   * `giveSpell 4406 INNATE rank 2 DIVINE casts 1`. The Yes branch adds one `injectText` and nothing\n   * else. Ours asks the same question once per inventory row as the `wayfinder-slotted` designation,\n   * offered only on records carrying `resonant` (ItemDetail.tsx:495) and read at build.ts:6498; the\n   * spell itself is top-level `innateSpells: [augury, divine, rank 2, 1/day]`, which is why no\n   * `spells` gap is reported. This stone's resonant power grants no extra spell, so `resonant.note`\n   * holds the clause and the printed description ends with it. Two options, one mechanic. */\n  'aeon-stone-agate-ellipsoid': ['options'],\nRationale for the replacement: the finding's structural suppression inside theirIdentities() would silence this select for every stone at once, which makes the already-shipped western-star settle match nothing — and scripts/wg-settle-stale.mjs:44 parses SETTLED_IDENTITIES specifically to report settles that answer nothing for deletion (\"a settle that silences nothing is a trap\"). A per-record settle stays auditable by that script, matches the neighbour exactly, and regresses nothing. It also sidesteps the finding's contains() concern entirely, since nothing is added to ours.options. (Note scripts/wg-identity.mjs is untracked — git status reports \"?? scripts/wg-identity.mjs\" — so this edit is to a working-tree-only file either way.)\n\nFIX 4: unchanged, non-actionable, but correct one detail — aeon-stone-western-star does NOT currently report an options gap under the default gate (it prints \"ok, 3 identities agree\") because its settle already exists at wg-identity.mjs:877; it reports [no, yes] only under --raw. No data change for western-star on this evidence, as the finding says."
 },
 {
  "id": "skysunder",
  "verdict": "ASK-OWNER",
  "reason": "Re-verified from source on both sides; every load-bearing claim holds, and the fix turns on a WG-vs-print disagreement (rule 4).\n\nRAW OPS, READ MYSELF (`node scripts/wg-item.mjs \"Skysunder\"`): WG item 17353, level 4, rarity UNIQUE, exactly five ops, all `giveAbilityBlock` type feat, ids 28836/28863/28865/23261/28847. No conditional, no select, no level gate in the item's tree. runes {\"striking\":1,\"potency\":1} on base_item clan-dagger. Matches the finding exactly.\n\nPRINT, READ MYSELF (aon-2e-archive/.../equipment-2660.json): \"**Minor Gifts** [deadly spark](/Relics.aspx?ID=1), [divine retribution](/Relics.aspx?ID=13),[ word of faith](/Relics.aspx?ID=15); … **Major Gifts** [holy light](/Relics.aspx?ID=16), [propelling winds](/Relics.aspx?ID=5)\" — Item 4, AP #193 p.84. rules-1101 verbatim: \"Generally speaking, this results in a relic gaining one gift for every 4 levels its bearer has\" and \"You decide what gifts a relic gains, generally by either selecting a single gift or offering two paths … allowing the player to choose\"; Table 2-22 = 1/1st Minor, 2/5th Minor, 3/9th Major, 4/13th Major, 5/17th Grand. So WG hands a LEVEL-4 item all five gifts including one the table puts at 17th. Real contradiction, owner's call.\n\nOURS: items.skysunder carries builtInRunes {potency:1,striking:'striking'} and nothing else — no grants of any kind. `skysunder` returns zero hits in src/. Its only effect-backfill row is the batch-16 builtInRunes fix, still present.\n\nNO LANE — VERIFIED HARDER THAN CLAIMED: of the 37 relic-trait items in core.json, ZERO carry grantsActions/grantsFeats/grantsItems/effectChoices/choice or any gift field. docs/mechanic-lanes.md has no relic entry. grantsActions resolves `content.actions[id]` (src/sheet/MainTab.tsx:270) while the gifts live in content.items, so it cannot carry them.\n\nNOT A REGRESSION: scripts/wg-identity.mjs and scripts/wg-batch-gate.mjs are both `??` untracked with no git history — genuinely new machinery. Gate output reproduced verbatim: \"grants theirs-not-ours=[deadlyspark, divineretribution, wordoffaith, holylight, propellingwinds] ours=[(nothing)]\". No SETTLED_IDENTITIES entry masks it.\n\nPRECEDENT CONFIRMED: work/owner-questions.json open[] holds 55 entries incl. `avernal-cape`, whose text is explicitly a REFUSED fix quoting rules-1101 and Table 2-22 for the identical contradiction. `skysunder`, `sparkwarden`, `uniter-of-clans`, `adamantine-echo` are NOT among them — genuinely unparked. add-owner-question.mjs accepts exactly the proposed flags.\n\nHOLY LIGHT MIS-TARGET — CONFIRMED, AND DECISIVE. Resolved all five ability_blocks against work/wg/wg-data.sql. 28836/28863/28865 each open \"**Gift Type** Minor\" and carry traits 3460 (Relic) + 3479 (Minor Gift); 28847 opens \"**Gift Type** Major\" with 3460 + 3480. But 23261 has traits {2535 Champion(legacy),1342 Cleric,1432 Concentrate,1475 Divine,1517 Light} — no Relic, no Major Gift — and its text is \"a beacon of holy light … 30-foot-radius, 100-foot-high cylinder … Unholy creatures\", i.e. the class feat, not relic-16 (a 30-foot emanation, Evil creatures frightened 1). WG's target is the wrong record and must not be adopted regardless of the ruling.\n\nCORPUS CLAIM CONFIRMED: exactly 238 gift items; by source GM Core 141, Treasure Vault 49, Secrets of Magic 22, Highhelm 12, Rage of Elements 10, AP#193 2, AP#200 2 — zero from legacy Gamemastery Guide. Our four gifts are relic-124/136/138/128 (GM Core remaster twins); relic-16 Holy Light has no twin and is absent from items, so that gap is corpus policy, not defect.\n\nOVERLAY IS SOUND AND INDEPENDENT: shipped description is the single sentence \"This *+1 striking clan dagger* bears the face of a stern dwarven god…\" — gift block stripped. Field `description` has a live reader (src/sheet/ItemDetail.tsx:652 DescBody), apply-backfill-now.mjs:65 routes string description rows to core-descriptions.json, its guard only touches records already in core.json (skysunder qualifies), 453 items description rows already exist, and skysunder has no existing description row — no collision, value is a string not null.\n\nTHREE IMMATERIAL CORRECTIONS: (1) relicAspects is declared at src/rules/types.ts:3321, not 3288-3295, and also appears in test/relic-aspects.test.ts — a data-integrity test, not a runtime reader, so \"no reader\" still holds for the engine. (2) effect-backfill.json now has 12,224 rows, not 12,193. (3) My stricter regex census gave 20/12/8; the finding's 23/12/11 is the correct count — bountys-light, lifebloom and reaper's-sigh bold the gift line differently but do print it and did lose it.\n\nONE FACT THE FINDING MISSED, worth adding to the question text: WG's gift ability_blocks carry their own `level` column — Deadly Spark 1, Divine Retribution 1, Word of Faith 1, Propelling Winds 9, Holy Light 10 — and their descriptions state \"**Gift Type** Minor/Major\" and \"**Aspect** air/celestial\". So WG is not level-blind about gifts; it simply does not gate the item's grants on data it already holds, and that per-gift level/aspect data would be the natural gating key if the owner rules for modelling the printed pool.\n\nPark it as proposed. Do not resolve. One ruling settles skysunder, sparkwarden, uniter-of-clans and adamantine-echo."
 },
 {
  "id": "sparkwarden",
  "verdict": "ASK-OWNER",
  "reason": "Every load-bearing claim reproduced independently. THEIRS: wg-item.mjs shows WG item 17354 (level 5) with exactly five ops, all unconditional `giveAbilityBlock {type:'feat'}` -> 29720/29726/29727/29728/29685, no conditional and no select; resolving the ability_block rows myself gives Creative Spark (ops {}), Obscure (one addBonusToValue on SKILL_STEALTH with the `value` key absent), Shadow Smith (ops {}), Dancing Shadow (ops {}, level 9, prerequisites {\"The relic is a weapon\"}), Elements of Creation (level 9, injectText into 29720). PRINT: equipment-2661.json carries \"Minor Gifts create spark, obscure, shadow smith / Major Gifts dancing shadow, elements of creation\"; rules-1101.json carries verbatim \"this results in a relic gaining one gift for every 4 levels its bearer has\", Table 2-22 (gift 2 at 5th Minor, 3 at 9th Major, 4 at 13th Major, 5 at 17th Grand) and \"You decide what gifts a relic gains\". OURS: items.sparkwarden is flat with no grantsFeats/grantsActions/grantsItems/choice/effectChoices at any depth; \"sparkwarden\" occurs twice in public/core.json (the key and its own id) and once in all of src/ (situationalBonuses.ts:1805, the item's own once-per-day reaction); zero hits in featGrants.ts, featGrantsAuto.ts, featGrantsLane.ts, featFeatGrants.ts, featPickGrants.ts, featCantripGrants.ts, companionGrants.ts, modes.ts, counterMods.ts, effect-backfill.json, consumable-modes.json, toggle-modes.json; the field union over all 37 relic-trait items contains no gift-pool field of any kind, and relicAspects (present on all five shipped gifts) has no reader in src/. So the investigator's \"no carrier exists\" is correct and no settle is available. GATE: `node scripts/wg-identity.mjs --ids sparkwarden --verbose` reproduces the quoted line verbatim; wg-identity.mjs and wg-batch-gate.mjs are both `??` untracked, matching the stated cause. RULE 4 CONTROLS: the only fix that would close the IDENTITY gap picks a side in a WG-vs-print contradiction - WG grants five gifts (two of them Major, one with its own unmet prerequisite) unconditionally on a level-5 tattoo, while print caps a 5th-level relic at two Minor gifts and hands the pick to the GM. Word-for-word the contradiction already parked under avernal-cape (work/owner-questions.json open[], batch 16); add-owner-question.mjs accepts every flag in the proposed command, and no sparkwarden entry exists yet, so the park is authorable as written. Two refinements, neither changing the verdict: (1) the owner question should record that WG's Dancing Shadow block itself requires \"The relic is a weapon\" while Sparkwarden is a tattoo, which strengthens rather than weakens the print side; (2) the PROSE claim needs correcting - our shipped description's Activate/Frequency/Trigger/Effect block is the WARDING TATTOO's (equipment-1000, verbatim, and our 140 gp price is that item's), not sparkwarden's, since equipment-2661 prints no activation at all; the record is effectively \"warding tattoo base, zero relic content\". The prose overlay itself checks out mechanically: apply-backfill-now.mjs routes field:'description' rows to public/core-descriptions.json setting only entry.d and preserving the existing r:[{label:\"Warding Tattoo\",key:\"items\"}] descRef, the reader at src/sheet/ItemDetail.tsx:652 is live, and no sparkwarden row exists in effect-backfill.json to collide with - but it grants nothing and does not close the IDENTITY gate, so it cannot stand in for the ruling. Verdict is the owner's: adopt WG's unconditional five-gift grant, or model the printed pool and leave the pick to the table."
 },
 {
  "id": "uniter-of-clans",
  "verdict": "ASK-OWNER",
  "reason": "I tried to refute this and could not. Every claim reproduces.\n\n1. THEIRS — RE-READ RAW, NOT TAKEN ON TRUST. `node scripts/wg-item.mjs \"Uniter of Clans\"` -> their item 17359, level 5, rarity UNIQUE, usage worn, traits {1527,1504,3460}, and exactly five ops: `addBonusToValue {variable:\"SKILL_DIPLOMACY\", value:\"1\", type:\"item\", text:\"\"}` then four UNCONDITIONAL `giveAbilityBlock {type:\"feat\"}` for 29708/29709/29711/29712 — no wrapper, no conditional, no level test on the item side. Resolved all four out of the ability_block table myself: 29708 Ancestors' Call (level 1, ONE-ACTION, traits {3460 Relic, 3497 Mind, 3479 Minor Gift, 1448, 1432}), 29709 Linguistic Nexus (level 1, 3479 Minor Gift), 29711 Repository of Knowledge (level 1, 3479 Minor Gift), 29712 Perception Filter (level 9, TWO-ACTIONS, 3480 MAJOR Gift). `wg-show --raw` on each: 29708 and 29712 carry no operations at all; 29709 is one bare LANGUAGE select plus nine LEVEL-gated conditionals (>=4/6/8/10/12/14/16/18, EQUALS 20) each wrapping a FILTERED LANGUAGE select; 29711 is three LEVEL conditionals (<9, >=9 AND <17, >=17) each wrapping three ADD-LORE ADJ_VALUE selects. The investigator's structural point is exactly right and is the crux: every level test in their tree is INSIDE a gift's own scaling; nothing anywhere gates WHETHER the level-5 relic has the gift. Their own Perception Filter block is level 9 and the level-5 item hands it over regardless.\n\n2. PRINT — RE-READ FROM THE MIRROR. C:/wonderers guide/aon-2e-archive/data/by-category/equipment/equipment-2662.json, field `markdown`: level 5, \"**Minor Gifts** ancestors' call, linguistic nexus, repository of knowledge (Dwarf Lore, Genealogy Lore, Warfare Lore)<br /> **Major Gifts** perception filter, or increase the diplomat's badge's item bonus to Diplomacy to +2.\" Verbatim as quoted. Governing rule re-read at .../rules/rules-1101.json (\"Advancing a Relic\"): Table 2-22 is 1 gift/1st Minor, 2/5th Minor, 3/9th MAJOR, 4/13th Major, 5/17th Grand, and the prose says \"this results in a relic gaining one gift for every 4 levels its bearer has\" and \"You decide what gifts a relic gains, generally by either selecting a single gift or offering two paths for the relic to grow and allowing the player to choose\". So print gives a POOL to be drawn from at a pace, WG grants the whole pool at once. The disagreement is real, and it is not a vocabulary difference.\n\n3. OURS — RE-CHECKED EVERY CARRIER CLAIM BY GREP/PARSE, NOT BY REPEATING THE FINDING. public/core.json items.uniter-of-clans printed in full: level 5, price {gp:125}, traits [enchantment,invested,magical,relic], usage worn, frequency 1/day, counters[freq], activationCost 1 action, aonId equipment-2662, and `passiveEffects.skills.diplomacy = 1` — the batch-16 fix IS still in place. Serialised the record and tested for grantsFeats / grantsFeat / grantsActions / grantsItems / choice / effectChoices / relicAspects / gifts at any depth: all false. Grep for \"uniter-of-clans\" over src/ returns NO MATCHES (the wider repo grep hit only .claude/worktrees copies of core.json — data, not readers). Parsed scripts/data/effect-backfill.json (12,224 rows): exactly ONE uniter row, {items, uniter-of-clans, passiveEffects, {skills:{diplomacy:1}}} — so no collision for a second row on a different field. All four gift records do ship: items.ancestors-call (relic-123, and it does carry its own `choice`), linguistic-nexus (relic-172), repository-of-knowledge (relic-174), perception-filter (relic-175). Nothing reaches them: 37 items carry the `relic` trait and ZERO of them carry grantsItems, grantsFeats or choice. `relicAspects` appears in src/ exactly once — types.ts:3321, a declaration with no reader. So the identity gap is real and there is genuinely no existing field on our side that names a gift.\n\n4. THE GATE OUTPUT IS REAL. `node scripts/wg-identity.mjs --ids uniter-of-clans --verbose` reproduces character-for-character: \"grants theirs-not-ours=[ancestorscall, linguisticnexus, repositoryofknowledge, perceptionfilter] ours=[(nothing)]\", \"checked 1 records that grant a NAMED thing; 0 match on every one\". No SETTLED_IDENTITIES entry for this id (the map is at wg-identity.mjs:426; grep for \"uniter\" across wg-identity.mjs and wg-batch-gate.mjs returns nothing), so nothing is being silenced. `git status --porcelain` confirms scripts/wg-identity.mjs, wg-batch-gate.mjs, wg-item.mjs and wg-show.mjs are all `??` — new machinery, so \"pre-existing gap, nothing regressed\" is right.\n\n5. WHY NOT CONFIRMED (rule 4). Any fix that grants gifts picks a side. Adopting WG verbatim would hand a 5th-level character four gifts including a MAJOR one that print (and WG's own ability_block) puts at 9th, and would erase the printed either/or. Modelling the printed pool instead would deliberately not match WG, against the owner's standing rule quoted in scripts/add-owner-question.mjs's own header. That is the owner's call, so ASK-OWNER, never CONFIRMED.\n\n6. WHY NOT REFUTED as a duplicate of avernal-cape. work/owner-questions.json open[] holds 57 entries including {id:\"avernal-cape\", batch:16} with the same shape and the same rules-1101 citation, so the precedent is genuine — but uniter carries a wrinkle avernal-cape does not: its Major Gift is an EITHER/OR (\"perception filter, or increase the diplomat's badge's item bonus to Diplomacy to +2\") that WG does not model as a choice at all — their Diplomacy op is value 1, the plain diplomat's badge bonus, never the gift's upgrade to +2. Avernal's Major Gifts line is a plain list. Separate record, separate question; add-owner-question.mjs is keyed per --id and reports \"adding uniter-of-clans\" (no existing entry).\n\n7. FIX MECHANICS BOTH CHECK OUT. (a) The park fix: add-owner-question.mjs takes exactly --id/--batch/--printed/--theirs/--ours/--question and refuses a half-written entry, --write persists to work/owner-questions.json. (b) The prose overlay row: field 'description' on an existing record IS a live lane — scripts/apply-backfill-now.mjs skips description in core.json (skipFields, line 45) and ROUTES it to public/core-descriptions.json (lines 65-73), guarded on `db[fix.category][fix.id]` existing, which uniter-of-clans does. I confirmed the prose gap directly: core-descriptions.json items['uniter-of-clans'].d holds only the diplomat's-badge base text plus the Activate/Frequency/Effect block, and contains no Forming the Bond / Advancing the Bond / Minor Gifts / Major Gifts lines. Reader confirmed at the cited line — src/sheet/ItemDetail.tsx:652 is literally `<DescBody description={item.description} descRefs={item.descRefs} … astKey=\"items\" astId={item.id} />`. The batch-16 numeric reader is also still exact: src/rules/derive.ts:525 is the `Math.max(abpSkillBonus…, passiveItemBonus(c, db, 'skill', key), …)` line.\n\nTwo documentation nits, neither of which changes the verdict or the fixes: the PARITY stage is labelled 5b in scripts/wg-batch-gate.mjs (not \"stage 6\"), and it reads the batch's own -parity.json verdict string — work/owner-questions.json is named only in that stage's comment, so parking the question is not itself gate-enforced. That matches precedent exactly: avernal-cape carries batch 16 in owner-questions.json yet does not appear in work/wg-batch-016-parity.json's 94 records at all, and that file currently holds 76 MATCHES / 9 FIXED / 9 THEY-ENCODE-NOTHING-USEFUL and zero OWNER-RULED. Also worth flagging for whoever applies fix (b): CLAUDE.md carries a stale warning that description overlay rows \"do NOT work\"; that warning is about split-descriptions.mjs / import-siege-and-gaps, and is superseded for this path by apply-backfill-now.mjs's routing.\n\nPark it as written, and apply the prose row independently of the ruling."
 },
 {
  "id": "kalmaugs-journal",
  "verdict": "ASK-OWNER",
  "reason": "MECHANICS RE-READ, ALL CONFIRMED (nothing taken from the summary).\n\nTHEIRS — `node scripts/wg-item.mjs \"Kalmaug's Journal\"`: item id 17343, level 0, rarity UNIQUE, bulk 1, traits {2855,1527,1504,3460}, no spell list, no bonus. Exactly three operations, all `giveAbilityBlock {type:'feat'}` with abilityBlockId 29505 / 28865 / 29509 — unconditional, no `conditional` and no `select` wrapping them. Resolved by name: `wg-show.mjs \"Ease Burden\"` → their id 29505, type=feat, level=1, five LEVEL-gated conditionals each wrapping `addBonusToValue variable=BULK_LIMIT_BONUS` at 2 (<6) / 4 (>=6,<10) / 6 (>=10,<14) / 8 (>=14,<18) / 10 (>=18) — matches the finding exactly. `\"Word of Faith\"` → id 28865, level 1, \"(no operations)\". `\"Underground Bounty\"` → id 29509, level 9, \"(no operations)\". `wg-show.mjs kalmaugs-journal --raw` does answer \"No ability_block row\" — the investigator is right that it is the wrong instrument for an item.\n\nPRINTED — C:/wonderers guide/aon-2e-archive/data/by-category/equipment/equipment-2668.json: level 5, \"Item 5\", Pathfinder #194 pg. 78, and the block ends \"Forming the Bond … Advancing the Bond … Minor Gifts ease burden, word of faith Major Gifts underground bounty\". rules-1101 Table 2–22 verbatim: \"2 5th Minor 160 gp / 3 9th Major 700 gp\", plus \"this results in a relic gaining one gift for every 4 levels\" and the explicit statement that the GM decides which gifts a relic gains. So a 5th-level relic has two gifts, both Minor; the Major is not available before 9th. WG hands all three to any bearer at any level. That is a genuine WG-vs-print contradiction.\n\nOURS — public/core.json items.kalmaugs-journal = {level 5, bulk 1, traits [divination,grimoire,invested,magical,relic], usage held-in-one-hand, heldSpells {\"0\":[\"glowing-trail\"],\"1\":[\"know-location\"],\"2\":[\"allfood\"],\"3\":[\"deep-sight\",\"fates-travels\",\"wanderers-guide\"],\"4\":[\"stonesense\"]}, aonId equipment-2668} — no grantsFeats/grantsActions/grantsItems/choice/effectChoices at any depth. `grep -rn -i kalmaug src/` → zero (which covers featGrants.ts, featGrantsAuto.ts, featGrantsLane.ts, featFeatGrants.ts, featPickGrants.ts, featCantripGrants.ts, companionGrants.ts, situationalBonuses.ts, modes.ts and counterMods.ts — I confirmed all ten live under src/rules/). scripts/data/effect-backfill.json: 12,224 rows, zero \"kalmaug\" hits. All three gift records ship as items (ease-burden relic-148, word-of-faith relic-138, underground-bounty relic-152). `node scripts/wg-identity.mjs --ids kalmaugs-journal --verbose` reproduces the gate line verbatim: \"grants theirs-not-ours=[easeburden, wordoffaith, undergroundbounty] ours=[(nothing)]\", and there is no SETTLED_IDENTITIES entry for the id (scripts/wg-identity.mjs:426 block greps clean).\n\nWHY ASK-OWNER, NOT CONFIRMED. Closing this gap requires choosing between WG's three unconditional gift grants on a level-0 item and print's Table 2–22 gating (two Minor at 5th, first Major at 9th, GM-assigned pacing). Rule 4 applies: a fix that settles a WG-vs-print disagreement is the owner's call. And he already holds it — work/owner-questions.json open[] contains {id:'kalmaugs-journal', batch:16} and {id:'avernal-cape', batch:16}, both recorded as \"a fix was proposed and REFUSED on review\". The finding proposes no repo change, so there is nothing to CONFIRM as a fix and nothing false at the mechanical core to REFUTE.\n\nTWO EVIDENCE CLAIMS ARE WRONG — correct them before this reaches the owner's file.\n(1) \"one of the TWO records in that batch that were NEVER GIVEN A PARITY VERDICT … kalmaugs-journal and avernal-cape are the absentees\" is FALSE. Applying the gate's own filter (scripts/wg-batch-gate.mjs:~296, `Object.values(batch).filter(r => (r.theirOps ?? []).length)`), all 100 records in work/wg-batch-016.json are encoded and work/wg-batch-016-parity.json holds 94, so stage 6 fires \"PARITY: 6 of 100 record(s) never compared\": kalmaugs-journal, avernal-cape, astrolabe-of-falling-stars, advanced-red-mantis-magic, arcana-of-iron, instinct-ability. All six carry open owner questions, which is why they have no verdict — the number is 6, not 2, and the conclusion is unaffected.\n(2) The PROSE NOTE is half true. public/core-descriptions.json does drop the Forming/Advancing/Minor Gifts/Major Gifts block (it ends at \"…when Learning the Spell.\"), but public/ast/items.json.gz KEEPS the whole block, gift links included (refs items:ease-burden→relic-25, items:word-of-faith→relic-15, items:underground-bounty→relic-29 — the same three Relics.aspx IDs the finding quotes). So this is not a relic that \"lost\" the block; the two shipped description stores disagree with each other. Whatever the \"11 of 23\" measurement counted, it did not count the AST bundle, and that inconsistency is a separate, ours-only question worth a line of its own.\nMinor: the parked entry cites effect-backfill.json at 12,163 rows; it is 12,224 today. Zero kalmaug hits either way.\n\nON THE RECOMMENDED ACTION. Adding a line to the existing kalmaugs-journal entry is near-redundant — that entry's `theirs` field already names all three ability blocks (\"giveAbilityBlock feat 29505 / 28865 / 29509 … Minor Gifts ease burden, word of faith; Major Gift underground bounty\"). What is genuinely new is only that the identity screen now fires on this id and that the gift-set half is posed to the owner nowhere except under avernal-cape. If a line is added, it should say that and correct the two errors above, not restate the gift ids."
 },
 {
  "id": "adamantine-echo",
  "verdict": "ASK-OWNER",
  "reason": "Every load-bearing claim re-verified from raw sources; the finding's own ASK-OWNER call is correct, and check 4 is decisive.\n\nTHEIRS (raw op tree re-read via `node scripts/wg-item.mjs \"Adamantine Echo\"`): item 17349, level 5, rarity UNIQUE, usage \"incorporated into armor\". Exactly five operations, all flat `giveAbilityBlock {type:'feat'}` — 29695/29505/29507/29508/29699. No `conditional`, no `select` on the item at any depth. Their ability blocks re-read via wg-show: Beginner's Luck 29695 (no ops), Shattered Earth 29507 (no ops), Raise Ramparts 29508 (no ops), Steal Luck 29699 (no ops); Ease Burden 29505 carries five `conditional` blocks of `addBonusToValue BULK_LIMIT_BONUS` 2/4/6/8/10 — the gift's own internal scaling, exactly as the finding says.\n\nPRINT (re-read independently from BOTH mirrors — `C:/wonderers guide/aon-2e-archive/data/by-category/equipment/equipment-2659.json` and the shipped AST in public/ast/items.json.gz): \"Item 5\", Usage \"incorporated into armor\", source Pathfinder #193 p.83, then verbatim \"Forming the Bond … Advancing the Bond … Minor Gifts beginner's luck, ease burden, shattered earth Major Gifts raise ramparts, steal luck\". Governing rule confirmed verbatim in the AST rules bucket under key `advancing-a-relic` (GM Core p.309): \"Generally speaking, this results in a relic gaining one gift for every 4 levels its bearer has\", Table 2-22 = 1/1st/Minor, 2/5th/Minor, 3/9th/Major, 4/13th/Major, 5/17th/Grand, and — the part that settles check 4 — \"You decide what gifts a relic gains, generally by either selecting a single gift or offering two paths for the relic to grow and allowing the player to choose\". The pick is explicitly GM-reserved. So a level-5 bearer should have 2 Minor gifts, not 5 gifts including 2 Major.\n\nOURS (re-checked by grep, not taken on trust): items.adamantine-echo has only id/name/level/price/bulk/traits/rarity/source/usage/itemType/edition/aonId — no grantsFeats/grantsActions/grantsItems/choice/effectChoices at any depth, and no `relicAspects` key at all. runes.adamantine-echo carries {actsAs:{kind:'potency',value:1}}, and that reader is live: derive.ts computes `actsAsPotency` as the max over property runes with actsAs?.kind==='potency' and folds it into `acItem` (line 1728-1736, not 1721-1729 — harmless drift). `grep -rn \"adamantine-echo\" src/` returns exactly one hit, derive.ts:1350 (not 1343 — same drift), the BODY_RUNE_EXCLUDED entry. So the batch-16 MATCHES verdict on the potency clause still holds and is NOT what re-opened. `relicAspects` is declared at types.ts:3321 (not 3288-3295) and has ZERO readers anywhere in src/ across 238 records — the whole relic-gift lane is unmodelled, confirming there is no existing field that could carry this.\n\nGATE reproduced byte-for-byte: `node scripts/wg-identity.mjs --ids adamantine-echo --verbose` prints \"grants theirs-not-ours=[beginnersluck, easeburden, shatteredearth, raiseramparts, stealluck] ours=[(nothing)]\". No SETTLED_IDENTITIES entry (grep of scripts/wg-identity.mjs and wg-batch-gate.mjs for \"adamantine\" is empty). Both scripts confirmed `??` in git status with no git log — new machinery, pre-existing gap, not a regression. All five gift records ship with the aonIds claimed (relic-97/148/150/151/101).\n\nCHECK 4 IS DECISIVE: any fix that closes this gate must either adopt WG's unconditional grant of all five gifts (contradicting Table 2-22 and the GM-reserved pick) or author a print-shaped gated pool (contradicting WG). That is the owner's call, never CONFIRMED. The finding's primary fix is kind:\"none\" — park it — which is escalation, not settlement, and is the right action. Precedent verified live: work/owner-questions.json open[] (57 entries) already holds `avernal-cape` batch 16 for the identical shape (their item 17336, five unconditional giveAbilityBlock ops on a level-5 relic, same rules-1101 citation) and `hardened-chassis` batch 14. No existing adamantine-echo entry, so no collision. Sibling relics re-checked: skysunder 17353, sparkwarden 17354, uniter-of-clans 17359 all carry the same flat giveAbilityBlock shape — a combined ruling is sound.\n\nTWO CORRECTIONS TO THE FINDING (neither changes the verdict):\n\n(1) The SECONDARY description overlay must be DROPPED — it fails rule 5's live-reader test. ItemDetail.tsx:652 is `<DescBody description={item.description} descRefs={item.descRefs} onExit={onClose} astKey=\"items\" astId={item.id} />`. DescBody returns at `if (ast && astId) { … <AstRenderer node={ast} bodyOnly hideMeta … /> }` BEFORE `description` is ever consumed; `useRich` is literally `!astId || (!ast && !astLoading)`. adamantine-echo resolves in ast-index.json to bucket \"items\" and public/ast/items.json.gz holds the full node. Worse for the fix: the block is NOT hidden — `META = /^(source|traditions?|…|craft requirements)/i` does not match \"Forming the Bond\", \"Advancing the Bond\", \"Minor Gifts\" or \"Major Gifts\", and `isMetaishBlock` is scoped to row/column layout nodes, so AstRenderer already renders that entire printed block to the user today. The overlay would write to a field that is dead for this record and duplicate text already on screen. ItemDetail.tsx:652 is also the ONLY DescBody call site passing astKey=\"items\". The finding's PROSE claim is true only of core-descriptions.json items.adamantine-echo.d (verified: potency sentence only), which is not what the sheet reads here.\n\n(2) The owner brief should carry a fact the finding omits: WG's own Raise Ramparts (29508) and Steal Luck (29699) ability blocks are LEVEL 9, and the three Minor gifts are level 1. WG's data is therefore level-tagged in line with Table 2-22 even though the item's grant ops are unconditional — the owner should know their engine may gate on the block level, which materially softens \"WG hands a level-5 relic all five gifts at once\" and may point at a cheaper reconciliation.",
  "correctedFix": "Keep the park; drop the second fix entirely.\n\nSOLE ACTION — one owner question, enriched with the level facts:\n\nnode scripts/add-owner-question.mjs --id adamantine-echo --batch 16 --printed \"equipment-2659 (Pathfinder #193 p.83, 'Item 5'): 'Minor Gifts beginner's luck, ease burden, shattered earth / Major Gifts raise ramparts, steal luck'. Governing rule, AoN 'Advancing a Relic' (GM Core p.309, rules-1101): 'Generally speaking, this results in a relic gaining one gift for every 4 levels its bearer has'; Table 2-22 minimum levels 1st/5th/9th/13th/17th (gifts 3 and 4 are Major, at 9th and 13th); and 'You decide what gifts a relic gains, generally by either selecting a single gift or offering two paths for the relic to grow and allowing the player to choose' — the pick is explicitly GM-reserved. A level-5 bearer should therefore have 2 Minor gifts, not 5 including 2 Major.\" --theirs \"WG item 17349 (level 5): five unconditional giveAbilityBlock ops 29695/29505/29507/29508/29699, no conditional and no select anywhere in the item's tree. BUT their ability blocks are themselves level-tagged in line with print: Beginner's Luck 29695 lvl 1, Ease Burden 29505 lvl 1, Shattered Earth 29507 lvl 1, Raise Ramparts 29508 lvl 9, Steal Luck 29699 lvl 9. The only operations inside any gift are Ease Burden's own five BULK_LIMIT_BONUS conditionals (2/4/6/8/10 at levels <6/6-9/10-13/14-17/18+). Same flat shape on skysunder 17353, sparkwarden 17354, uniter-of-clans 17359.\" --ours \"runes.adamantine-echo carries the printed +1 potency via actsAs, read live at src/rules/derive.ts:1728-1736 (folded into acItem) — that clause MATCHES and is not in question. Nothing on either the item or the rune record names a gift: no grantsFeats/grantsActions/grantsItems/choice/effectChoices at any depth. The only src/ reference is derive.ts:1350 (BODY_RUNE_EXCLUDED). The one field that mentions relic gifts, relicAspects (src/rules/types.ts:3321), has ZERO readers in src/ across all 238 records carrying it — the relic-gift lane is unmodelled engine-wide, so there is no existing way to say this. All five gift records ship (items/beginners-luck relic-97, ease-burden relic-148, shattered-earth relic-150, raise-ramparts relic-151, steal-luck relic-101) but no relic reaches them.\" --question \"Adopt WG's unconditional grant of all five gifts (including both Major Gifts) on a level-5 relic, or model the printed pool gated by Table 2-22 and leave the pick to the table? Note WG level-tags Raise Ramparts and Steal Luck at 9, so a third option is to grant all five but gate on the gift's own level. Whichever way it goes, the relic-gift lane needs a carrier with a reader — relicAspects has none. One ruling settles adamantine-echo, avernal-cape, skysunder, sparkwarden and uniter-of-clans together.\" --write\n\nDO NOT apply the proposed description overlay {category:'items', id:'adamantine-echo', field:'description', …}. Its claimed reader is pre-empted: src/sheet/ItemDetail.tsx:652 passes astKey=\"items\" astId={item.id}, and src/sheet/DescBody.tsx returns the AstRenderer branch at `if (ast && astId)` before `description` is read (`useRich = !astId || (!ast && !astLoading)`). adamantine-echo has an AST node, and the Forming the Bond / Advancing the Bond / Minor Gifts / Major Gifts paragraphs are not matched by AstRenderer's META regex nor by isMetaishBlock, so the user is ALREADY shown that block. The overlay would write a dead field and duplicate on-screen text. If the owner wants core-descriptions.json items.adamantine-echo.d backfilled for non-AST consumers, that is a separate corpus-wide question about the 11-of-23 stripped relics, not a per-record overlay."
 },
 {
  "id": "avernal-cape",
  "verdict": "ASK-OWNER",
  "reason": "Independently verified; the divergence is real and it is squarely a WG-vs-print disagreement, so it cannot be CONFIRMED (rule 4) and it is not REFUTED.\n\nTHEIRS (re-read raw, not taken on trust): `node scripts/wg-item.mjs \"Avernal Cape\"` shows item 17336, level 5, rarity UNIQUE, usage \"incorporated into armor\", traits {1523,1527,1504,3460}, and exactly five operations, all `{\"type\":\"giveAbilityBlock\",\"data\":{\"type\":\"feat\",\"abilityBlockId\":N}}` for N = 28858, 29523, 29524, 28859, 29527 — unconditional, no level gate, no prerequisite. `node scripts/wg-show.mjs avernal-cape --raw` correctly reports no ability_block row (the record lives in the item table), so the investigator's two-tool description is accurate rather than contradictory.\n\nPRINT (re-read from the mirror): aon-2e-archive/data/by-category/equipment/equipment-2667.json is Item 5, usage \"incorporated into armor\", and the text carries the dueling-cape sentence, the scaling fire resistance (\"twice the relic's number of gifts\"), Forming/Advancing the Bond, Minor Gifts feral claws/flare bolt/heat haze, Major Gifts form of fury (snake form)/searing wave. rules-1101.json (\"Advancing a Relic\") contains the quoted governing sentence verbatim: gifts arise \"one gift for every 4 levels its bearer has\". So WG hands a 5th-level item all five gifts at once while the printed rule ties gift count to bearer level — a genuine contradiction only the owner can settle, and the exact contradiction already parked.\n\nOURS (every carrier claim re-checked by grep, none taken on trust): public/core.json items.avernal-cape has exactly id,name,level,price,bulk,traits,rarity,source,usage,itemType,edition,aonId — no grantsItems/grantsFeats/grantsActions/situational/passiveEffects/resistances at any depth; usage is \"etched-onto-armor\" (differs from both WG and print). `grep -rn \"avernal\" src/` returns zero hits; `grep -rn \"avernal\" scripts/data/effect-backfill.json` returns zero; `grep -n \"avernal\" scripts/wg-identity.mjs` returns zero, so there is no SETTLED_IDENTITIES entry. All five gift records do ship (items.feral-claws relic-132, flare-bolt relic-160, heat-haze relic-161, form-of-fury relic-133, searing-wave relic-164) with nothing joining them to the relic.\n\nGATE: reproduced exactly. `node scripts/wg-identity.mjs --batch work/wg-batch-016.json` prints \"--- avernal-cape  (Avernal Cape) / grants theirs-not-ours=[feralclaws, flarebolt, heathaze, formoffury, searingwave] / ours=[(nothing)]\". Both scripts/wg-identity.mjs and scripts/wg-batch-gate.mjs are untracked (`git status --porcelain` → `??`), confirming \"new comparer, never ran against batch 16\" rather than a regression.\n\nPARKED QUESTION: work/owner-questions.json open[] (55 entries) does hold {id:'avernal-cape', batch:16}, and its `question` field states the refusal ground as exactly this level-gating contradiction, citing rules-1101 and Table 2-22. So the \"no fix proposed\" stance is right: proposing the grants would adopt WG over print on the owner's behalf, and a SETTLED_IDENTITIES entry would be a false settle (nothing on our side carries the mechanic) that would silence the next real difference on this record. The suggested stage-3 exemption keyed to the parked-question list is the honest alternative and is correctly left as a suggestion, not a fix.\n\nONE EVIDENCE CORRECTION (does not change the verdict): the finding asserts avernal-cape and kalmaugs-journal are \"the TWO records in the batch that were NEVER GIVEN A PARITY VERDICT\". That is wrong. Applying the gate's own filter (scripts/wg-batch-gate.mjs:~296, `Object.values(batch).filter(r => (r.theirOps ?? []).length)`), all 100 records in work/wg-batch-016.json carry theirOps and work/wg-batch-016-parity.json has 94 records, so there are SIX absentees: kalmaugs-journal, avernal-cape, astrolabe-of-falling-stars, advanced-red-mantis-magic, arcana-of-iron, instinct-ability. The stage-6 \"PARITY: N of M record(s) never compared\" double-report of this id still holds; only the count of companions is overstated.\n\nVERDICT ASK-OWNER: the mismatch is confirmed on the facts, but the only fixes available (adopt WG's five unconditional gifts, or write a settle) each decide a WG-vs-print disagreement, which is the owner's call and is already on his desk.",
  "correctedFix": "No fix. Leave items.avernal-cape untouched and add nothing to SETTLED_IDENTITIES. If the gate must go quiet before the owner rules, the only sound route is a stage-3 IDENTITY exemption keyed to work/owner-questions.json open[] (so the suppression expires when the question is answered and still reports any NEW difference on the record), not a settle entry. Two corrections to the finding as written: (1) the batch-16 parity absentee list is six ids, not two — kalmaugs-journal, avernal-cape, astrolabe-of-falling-stars, advanced-red-mantis-magic, arcana-of-iron, instinct-ability — so the \"one of the TWO records\" line in EVIDENCE should be restated; (2) worth noting for the owner's file that ours has usage \"etched-onto-armor\" where WG and print AGREE on \"incorporated into armor\" — that sub-difference needs no owner ruling on its own, but it is already inside the scope of the parked record's re-verification text, so it should be resolved together with the ruling rather than split out as a separate fix."
 },
 {
  "id": "spirit-dwelling",
  "verdict": "CONFIRMED",
  "reason": "Verified independently on raw sources both sides. THEIRS (read myself from work/wg/wg-data.sql): item 13641 has exactly one op, giveAbilityBlock->25715; raw ability_block 25715 is name=\"Collect Spirit Remnant\", operations={}, actions=THREE-ACTIONS, traits={1432 Concentrate,1433 Manipulate}, requirements=\"You're holding your spirit dwelling\", meta_data={\"unselectable\":true} — the unselectable flag independently corroborates that the ITEM, not a feat picker, is the intended carrier. So the op is not opaque: it names a real record, and WHICH action the item hands you is the only mechanic this item has. The batch-16 verdict THEY-ENCODE-NOTHING-USEFUL was true only about the VALUES screen (the target's empty operations column yields no number to compare) and wrong as a conclusion. PRINT (AoN mirror public/ast/sidebar.json.gz key spirit-dwellings-and-remnants, and actions.json.gz key collect-spirit-remnant): \"You can use the Collect Spirit Remnant activity.\" — unconditional, no level or feat gate — and the action's Requirements is \"You're holding your spirit dwelling.\" Print and WG AGREE on the carrier, so rule 4 does not fire and this is not ASK-OWNER. OURS: items.spirit-dwelling is an authored stub (fields exactly id,name,level,itemType,rarity,bulk,hands,traits,aonOrigin — no grantsActions); actions.collect-spirit-remnant ships (aonId action-1160, 3 actions, concentrate+manipulate) and is unowned. Carrier absence re-checked by grep: zero hits for collect-spirit-remnant anywhere in src/, and scripts/data/ holds only the three spirit-dwelling lines. GATE reproduced verbatim: `node scripts/wg-identity.mjs --ids spirit-dwelling --verbose` prints \"grants theirs-not-ours=[collectspiritremnant] ours=[(nothing)]\" (a bare run reports 0 only because it defaults to a 42-record scope). FREE TO FIX confirmed: work/wg-batch-016-parity.json has 94 rows and does carry a spirit-dwelling verdict, and owner-questions.json open[] (57 entries) contains avernal-cape and kalmaugs-journal but no spirit-dwelling. OVERLAY hygiene confirmed: no existing grantsActions row for items/spirit-dwelling (no collision), exactly one grantsItems row for feats/exorcist-dedication (unique find for replacement), neither value is null so nothing is removed. PRECEDENT confirmed: items+grantsActions rows already exist (wish-blade, wish-knife -> conduct-energy) and worn:true grants already exist on three records, two of them category feats (tattooed-historian-dedication, palatine-detective-dedication) plus hardshell-surki. READERS confirmed live: MainTab.tsx:265-268 is verbatim `.filter((inv) => inv.equipped || inv.worn)` then maps to content.items and filters grantsActions. SECOND ROW IS GENUINELY REQUIRED — I checked every equip path: isEquippable (InventoryTab:732-737) requires armor/weapon/shield or the invested trait; equipControl (InventoryTab:73-78) returns null for equipment; the Invest button is gated at InventoryTab:194 on item.traits.includes('invested'); and the drag handler short-circuits on `if (!isEquippable(item)) return;` before its invest branch. items.spirit-dwelling is itemType equipment with traits [], so NO UI path can set worn, equipped or invested. Reachability then completes end-to-end: isActionCost accepts {type:'actions',value:3} and \"Collect Spirit Remnant\" is absent from ACTIVITIES, so activityNames suppression does not fire. No regression from worn:true — the item has no situational bonuses, effects, activation or heldSpells, so the flag only moves it into InventoryTab's Equipped section (line 653). THREE DEFECTS require the corrected fix. (1) FIX 3 IS DEFECTIVE AND MUST BE DROPPED, not merely deprioritised. Its itemInUse variant is INERT: derive.ts:241 is worn||invested||equipped||attachedTo, and per the equip-path audit above all four are false/undefined on the granted instance and unreachable by the player, so widening to itemInUse surfaces nothing. Its \"drop the filter for items whose grantsActions is not equip-conditioned\" variant has no declared field to key off, and dropping the filter outright regresses the exact case it was written for — wish-blade/wish-knife would offer Conduct Energy from inside a backpack, contradicting \"while wielding a resonant weapon\". Rows 1+2 stand alone. (2) The reader citation on fix 1 wrongly names src/rules/explain.ts:256: that loop iterates only c.feats and ownedFeatureIds class features, never items, so it is not a reader for an item's grantsActions. MainTab.tsx:265-270 is the sole live reader. (3) Line drift (code is as described, only the anchors are wrong): grantedItems is collected at build.ts:6281-6285 not 6262, and `worn: g.worn ?? false` sits at ~7248 inside the `...grantedItems` spread that begins at 7241, not 7228. Also unmentioned but load-bearing: scripts/lib/apply-backfill.mjs:44 iterates the overlay in strict array order with no create-first pass, so the new row must sit AFTER the items/spirit-dwelling create row (~line 133871); appending at the end satisfies this. The adjacent trait note is accurate — print gives the necromancy trait plus the tradition trait, WG's item carries {1475 Divine,1514 Occult,1504 Magical}, ours ships traits:[] — and is correctly held out of scope.",
  "correctedFix": "Apply TWO overlay rows in scripts/data/effect-backfill.json and DROP the proposed src-edit entirely.\n\nROW 1 — {\"category\":\"items\",\"id\":\"spirit-dwelling\",\"field\":\"grantsActions\",\"value\":[\"collect-spirit-remnant\"]}\nExact parity with their carrier: WG hangs the grant on item 13641, not on the dedication, and print agrees (action-1160 Requirements: \"You're holding your spirit dwelling\"). Precedented shape — items/wish-blade and items/wish-knife already carry grantsActions rows for conduct-energy. Closes the identity gate: wg-identity.mjs:187 does `for (const g of rec.grantsActions ?? []) addGrant(g)`, and anyName() resolves 'collect-spirit-remnant' via the `actions` bucket to \"Collect Spirit Remnant\" -> key collectspiritremnant, matching theirs.\nPLACEMENT CONSTRAINT (not in the original brief): scripts/lib/apply-backfill.mjs:44 iterates the overlay array in strict order with no create-first pass, so this row must come AFTER the existing items/spirit-dwelling create row (currently ~line 133871). Appending at the end of the array satisfies this.\nREADER: src/sheet/MainTab.tsx:265-270 only. Do NOT cite src/rules/explain.ts:256 — that loop reads grantsActions from c.feats and ownedFeatureIds class features only, never from items.\n\nROW 2 — replace the single existing feats/exorcist-dedication grantsItems row value [{\"itemId\":\"spirit-dwelling\"}] with [{\"itemId\":\"spirit-dwelling\",\"quantity\":1,\"worn\":true}]\nRequired, or row 1 is write-only. MainTab.tsx:265-268 gates the item source on `inv.equipped || inv.worn`; build.ts:7248 emits granted items with `worn: g.worn ?? false, equipped: false`; and no UI path can flip any flag by hand (isEquippable at InventoryTab:732-737 needs armor/weapon/shield or the invested trait, equipControl at :73-78 returns null, the Invest button at :194 needs the invested trait, and the drag handler short-circuits on `if (!isEquippable(item)) return;`). `worn` is a declared grant field (src/rules/types.ts grantsItems) with three precedents, two of them on feats (tattooed-historian-dedication, palatine-detective-dedication) plus hardshell-surki. Data-only, no engine change. Keep the brief's honest caveat in the row's rationale: the dwelling is HELD, not worn, and our inventory model has no held flag for equipment-typed items; worn is the closest existing flag and costs nothing else here, since the item has no situational bonuses, effects, activation or heldSpells — the flag only also moves it into InventoryTab's Equipped section (line 653).\n\nDROP the third proposal (widening src/sheet/MainTab.tsx:265-268). Reusing itemInUse (derive.ts:241 = worn||invested||equipped||attachedTo) is INERT here — all four flags are false/undefined on the granted instance and unreachable by the player — so it would not surface the action at all. And dropping the filter has no declared field to key \"not equip-conditioned\" off, and would regress the resonant case the filter exists for: wish-blade/wish-knife would offer Conduct Energy from a backpack, contradicting \"while wielding a resonant weapon\" and the comment at MainTab.tsx:257-264.\n\nAfter applying, re-run `node scripts/apply-backfill-now.mjs` then `node scripts/wg-identity.mjs --ids spirit-dwelling --verbose` to confirm the gate goes clean, and update the spirit-dwelling verdict in work/wg-batch-016-parity.json from THEY-ENCODE-NOTHING-USEFUL to MISMATCH-FIXED (its current evidence string, \"Their only op is an opaque ability-block grant\", is now known to be wrong).\n\nLeave the trait gap (print: necromancy + the tradition trait; ours: traits []) out of this fix — it is a separate record-level correction, and adding the invested trait to make the item equippable would be unprinted and is not the right route."
 },
 {
  "id": "speaker-in-training",
  "verdict": "CONFIRMED",
  "reason": "EVERY SUBSTANTIVE CLAIM REPRODUCED INDEPENDENTLY.\n\nTHEIR RAW OPS — ran `node scripts/wg-show.mjs speaker-in-training --raw` myself. Byte-for-byte as described: 3 top-level conditionals, 10 flattened. Op 1 gates on HERITAGE_NAMES INCLUDES \"budding speaker centaur\" with trueOperations EMPTY and falseOperations = one select (title \"Select Divine or Primal\", modeType PREDEFINED, optionType CUSTOM); option \"Divine\" -> giveSpell 4415 INNATE rank 1 DIVINE casts 1 + createValue FAITHSPEAKER value **true** type bool; option \"Primal\" -> giveSpell 4625 INNATE rank 1 PRIMAL casts 1 + createValue GREENSPEAKER true bool. Ops 2/3 are IF FAITHSPEAKER/GREENSPEAKER EQUALS TRUE -> the same giveSpell. The dump-wide grep returns exactly 4 lines carrying FAITHSPEAKER|GREENSPEAKER, and `wg-show budding-speaker-centaur` shows the heritage is where the variable is first written (a select whose two options each set the variable AND open a SPELL select). So the investigator's reading of ops 2/3 as the cross-record carrier is right.\n\nPRINTED TEXT — read C:/wonderers guide/aon-2e-archive/data/by-category/feat/feat-5330.json directly. Verbatim match with the quote, Howl of the Wild pg. 32, level 5, centaur.\n\nWG-vs-PRINT: THEY AGREE. One pick, one spell per branch, correct tradition per branch, once per day. Nothing here forces ASK-OWNER.\n\nOUR SIDE — core.json feats['speaker-in-training'] keys are exactly [id,name,level,category,traits,rarity,source,prerequisites,actionCost,edition,aonId]: no innateSpells, no choice, no effectChoices. `npm run feat -- speaker-in-training` prints the overlay state in its own words: \"overlay rows (survive `npm run data`): innateSpells = null (deletes), choice = null (deletes)\" and \"⚠ no authoring script names this record. Its overlay rows are orphans\". Both rows located in scripts/data/effect-backfill.json (ids at lines 141838 and 155184). `grep -rn speaker-in-training src/` returns exactly ONE hit — featCantripGrants.ts (line 118, not 113); scripts/ adds only the overlay and migration/out artefacts. Rule-4 sweep clean.\n\nTHE READER CHAIN IS LIVE AND THE COLLISION REASONING HOLDS — build.ts:6505-6534 pushes record innateSpells FIRST, then the FEAT_CANTRIP_GRANTS pick, and the dedupe at :6549 is `!seenInnate.has(g.spellId) && seenInnate.add(...)`, i.e. first-wins, so a live record lane would indeed have beaten the picker. build.ts:6525-6532 even names this record in its comment and resolves `spec.traditionByOption?.[chosen]`. That is load-bearing, not decorative: core.json spells['fleet-step'].traditions = [\"arcane\",\"primal\"] — first entry ARCANE — so without traditionByOption the Greenspeaker branch resolves arcane. Uses: `(g.usesPerDay ?? 1)` at build.ts:6609 with both spells at rank 1 and neither cantrip -> 1/day at base rank, exactly as printed. Builder.tsx renders the picker in the FEAT_CANTRIP_GRANTS block (~2256). `node scripts/duplicate-pick-check.mjs` passes: \"no record asks the same question in two lanes\".\n\nBOTH SETTLES ANSWER A DIFFERENCE THAT IS LIVE RIGHT NOW, AND BOTH ARE UNIQUE.\n· `node scripts/wg-diff.mjs --list --bucket they-only | grep speaker` -> `speaker-in-training lvl 5 missing=[conditional,specialStat] ours=[spell,choice]`. The proposed VERIFIED_EQUIVALENT value ['conditional','specialStat'] covers precisely the reported set, no more.\n· `node scripts/wg-identity.mjs --ids speaker-in-training --verbose` -> `options theirs-not-ours=[divine, primal] / ours=[(nothing)]`, and it reports IDENTICALLY under `--raw`, so this is not a settle that matches nothing.\n· `grep -n speaker scripts/wg-diff.mjs scripts/wg-identity.mjs` returns ZERO — neither registry already holds this id, so neither insert collides.\n· Anchors verified: SETTLED_IDENTITIES at wg-identity.mjs:426; 'options' is a first-class bucket there with ~15 existing users (myriad-forms, fey-influence, sacred-spells, clan-lore…). VERIFIED_EQUIVALENT at wg-diff.mjs:888. specialStat maps to ['specialStatistic','passiveEffects.specialStatBonus','choice','dedicationGate'] at wg-diff.mjs:332. The option-marker guard is wg-diff.mjs:206 and does require `typeof op.data?.value === 'string'`, so a boolean marker escapes it exactly as claimed. wg-identity.mjs:308 is `for (const sid of registryIds(cantripText, id)) addSpell(sid)` — featCantripGrants is credited to out.spells ONLY, which is why our options bucket is empty and the spells bucket already matches.\n\nFOUR CITATION ERRORS, none of which changes the outcome (the fixes are justified by tool output I re-ran, not by the narrative):\n1. THE PROVENANCE STORY IS WRONG. work/.stale-diff.json does NOT record `ourKinds [choice,specialStat,spell] / missing []` for this id. I read it: it records theirKinds [conditional,choice,spell,specialStat] / ourKinds [spell,choice] / missing [conditional,specialStat] — byte-identical to work/.gate-diff.json, and its mtime (18:05) is LATER than gate-diff's (17:46). So the \"a recent fix regressed the gate / the gap did not exist at gate time\" framing is unsupported by the files cited. The gap is nonetheless real and live today.\n2. featCantripGrants.ts entry is at line 118, not 113.\n3. VERIFIED_EQUIVALENT is at wg-diff.mjs:888, not 884; the specialStat map is at :332, not :328.\n4. 'martial-experience' is settled as ['unmapped'], NOT ['specialStat'] — it is not a specialStat neighbour. The genuine neighbours are 'power-suit': ['specialStat'] (wg-diff.mjs:918, whose own comment reads \"createValue INVENTOR_ARMOR = true is a MARKER their engine tests elsewhere\" — a BOOLEAN marker, the closest precedent there is) and 'bone-magic': ['specialStat'] (:1492).\n\nONE PROPOSED FIX SHOULD NOT SHIP AS WRITTEN. Fix 3 widens the wg-diff.mjs:206 guard to accept booleans GLOBALLY. `wg-diff --list --bucket they-only` shows 7 other records currently reporting specialStat as missing (vigilant-benediction, reclaimant-plea, additional-servings, avenging-runelord-dedication, path-to-perfection, second-path-to-perfection, animal-order, cultivation-order). I could not measure how many of those markers are boolean-in-option without editing the differ, which is out of bounds here. The registry's own header says a settle is \"Never a place to quiet a real gap\"; a one-line guard change that silences an unmeasured set of other records is the same hazard one level up. It is explicitly OPTIONAL and the per-record settle already covers this id, so it should be dropped from this finding and raised separately with that measurement attached.\n\nONE THING THE SETTLE MUST NOT BURY, and the investigator did disclose it: the printed sentence \"If you've already made this decision (such as due to being a budding speaker) you must select the same Speaker you previously chose\" is UNENFORCED on our side. I confirmed core.heritages['budding-speaker-centaur'] carries only effectChoices[speaker-cantrip] with spellFilter.traditions ['divine','primal'] and records no speaker identity, and core.feats['speakers-defense'] asks the Faithspeaker/Greenspeaker question again in its own effectChoices[speaker-kind]. Three records, three independent answers. That is an OURS-vs-print gap, not a WG-vs-print disagreement, so it does not make this ASK-OWNER — but the 'conditional' settle is the exact place a future reader would look for it, so the settle comment has to say it out loud rather than describing the heritage gate as pure menu filtering.",
  "correctedFix": "[{\"kind\":\"settle\",\"detail\":\"Add SETTLED_IDENTITIES['speaker-in-training'] = ['options'] in scripts/wg-identity.mjs (registry opens at line 426; 'options' is an established bucket there — myriad-forms, fey-influence, sacred-spells, clan-lore and ~11 more). Verified live: `node scripts/wg-identity.mjs --ids speaker-in-training --verbose` reports `options theirs-not-ours=[divine, primal] / ours=[(nothing)]`, and reports the same under --raw, so the settle answers a real difference rather than matching nothing. Reading: their two option titles name the TRADITION (\\\"Divine\\\"/\\\"Primal\\\"); ours name the SPELL each tradition grants (Bless / Fleet Step), and the traditions themselves are carried by traditionByOption. Our options bucket reads EMPTY (not \\\"Bless/Fleet Step\\\") because wg-identity.mjs:308 credits featCantripGrants ids to out.spells only — which is also why the spells bucket already matches on both sides.\",\"reader\":\"src/rules/featCantripGrants.ts:118 -> src/rules/build.ts:6525-6532 (per-option tradition; load-bearing because core.json spells['fleet-step'].traditions is [\\\"arcane\\\",\\\"primal\\\"], arcane first) and src/builder/Builder.tsx:~2256 (the picker)\"},{\"kind\":\"settle\",\"detail\":\"Add VERIFIED_EQUIVALENT['speaker-in-training'] = ['conditional','specialStat'] in scripts/wg-diff.mjs (registry opens at line 888). Verified live: `node scripts/wg-diff.mjs --list --bucket they-only` reports `speaker-in-training lvl 5 missing=[conditional,specialStat] ours=[spell,choice]` — the value covers exactly the reported set. specialStat = createValue FAITHSPEAKER/GREENSPEAKER, a boolean marker written INSIDE the select's own option; wg-diff.mjs:206 already settles that shape for string markers and misses this one only because its test is `typeof value === 'string'`. Closest precedent is 'power-suit': ['specialStat'] (wg-diff.mjs:918), whose own comment describes `createValue INVENTOR_ARMOR = true` — also a boolean marker; 'bone-magic' (:1492) is the second. DO NOT cite 'martial-experience' as a neighbour: it is settled as ['unmapped'], not ['specialStat']. conditional = their heritage gate on op 1 (menu-option filtering, the owner's \\\"filtering the options in a picker is ours\\\" exception) plus the two IF-marker dispatches in ops 2/3, which exist only because their answer lives in a variable rather than in the pick. THE COMMENT MUST ALSO STATE THE UNENFORCED CLAUSE, because this settle is where a future reader will look for it: print says \\\"If you've already made this decision (such as due to being a budding speaker) you must select the same Speaker you previously chose\\\", and we do not enforce it — core.heritages['budding-speaker-centaur'] records only a divine-or-primal cantrip filter and no speaker identity, and feats['speakers-defense'] asks the same question again in its own effectChoices[speaker-kind]. Three records, three independent answers, where WG threads one variable. A shared choice.flag ('speakerPath') is the shape our vocabulary has for it if the owner ever wants it.\",\"reader\":\"src/rules/build.ts:6525-6532; guarded by scripts/duplicate-pick-check.mjs, which passes today\"},{\"kind\":\"src-edit\",\"detail\":\"OWNERSHIP, durability only — keep as proposed. `npm run feat -- speaker-in-training` prints, in its own words, \\\"⚠ no authoring script names this record. Its overlay rows are orphans\\\" and lists \\\"innateSpells = null (deletes), choice = null (deletes)\\\". Those two value:null rows in scripts/data/effect-backfill.json ARE the whole of the batch-15 fix and nothing regenerates them. If the overlay is ever rebuilt from its owning scripts, innateSpells/choice come back and the collision returns for real: build.ts pushes record innateSpells at :6505 BEFORE the pick lane at :6516, and the dedupe at :6549 is first-wins (`!seenInnate.has(g.spellId) && seenInnate.add(...)`), so the record lane would beat the player's pick. Re-home both rows in an authoring script that states the one-clause-one-picker reason, next to the featCantripGrants entry.\",\"reader\":\"scripts/duplicate-pick-check.mjs is the guard that catches the regression; scripts/feat-doctor.mjs is what reports the orphan\"},{\"kind\":\"DROPPED\",\"detail\":\"REMOVED from this finding: the optional src-edit widening the option-marker guard at scripts/wg-diff.mjs:206 from `typeof op.data?.value === 'string'` to also accept booleans. It is a GLOBAL change to the differ, and `wg-diff --list --bucket they-only` shows 7 other records currently reporting specialStat as missing (vigilant-benediction, reclaimant-plea, additional-servings, avenging-runelord-dedication, path-to-perfection, second-path-to-perfection, animal-order, cultivation-order). How many of their markers are boolean-in-option was not measured by the finding and cannot be measured without editing the differ. The registry header's own rule — \\\"Never a place to quiet a real gap\\\" — applies with more force to a guard than to a per-record settle. The settle above already resolves this id; raise the widening separately, with the blast-radius count attached.\"}]"
 },
 {
  "id": "the-moon-weavers-art",
  "verdict": "CONFIRMED",
  "reason": "Re-read their raw ops myself (wg-show worked by slug, so the finding's \"No ability_block row\" process note is wrong — immaterial): select/SPELL with two singular giveSpell operations (4678/6752), adjValue SPELL_DC=T, adjValue SPELL_ATTACK=T, and conditional IF LEVEL>=12 -> both to E. Re-read the printed text from the mirror (public/core-descriptions.json): verbatim as quoted, with NO proficiency clause on the feat — so their SPELL_DC/SPELL_ATTACK ops are the general Player Core p.298 innate rule copied onto the record, and there is no WG-vs-print disagreement to escalate. wg-diff now reports exactly `the-moon-weavers-art lvl 5 missing=[spellcasting,conditional] ours=[choice,spell]` — precisely the two kinds the deleted settle covered — while the twin labyrinthine-echoes is absent because its identical ['conditional','spellcasting'] entry survives at wg-diff.mjs:1069 under the shared comment block (1053-1069) that documents both, with the removal marker at 1070. drop-fixed-settles.mjs:34 justifies the deletion as \"the duplicate registry picker is gone; one clause, one prompt\" — a src fix in featCantripGrants.ts (the REMOVAL comment is live at line 134, not 129), which is a different divergence from the innate-proficiency vocabulary the settle answered; its loop (44-56) matches on the bare id, so the mismatch is structural, not a judgement call. Engine coverage verified end to end rather than taken on trust: effectChoices -> resolvePick (build.ts ~4866) -> applyAlwaysOn pushes g.innateSpells into chosenInnateGrants (4993) -> innateGrants (6512) -> the pooled 'innate-casting' entry whose proficiency is maxRank(maxRank(caster, profile), level >= 12 ? 'expert' : 'trained') (~6648) — a floor, matching their T-then-E. Carrier sweep confirms a single owner (backfill-batch15.mjs:260) and no second lane; duplicate-pick-check passes; NOT_A_SCALAR in wg-values holds SPELL_DC/SPELL_ATTACK; flattenOps (wg-parse.mjs:58) walks `operations` plural only and wg-identity reads o.title ?? o.name ?? o.operation?.data?.variable, so the comparer blind spot is real. No test or gate asserts the marker: wg-backlog-citations.test.ts does not apply (the comment cites no backlog) and wg-batch-gate only PRINTS settles for owner veto. Note that wg-batch-gate's parseSettles shares the preceding comment across consecutive keys, so replacing the marker line — rather than adding the key beneath it — is load-bearing and the fix says exactly that. Line numbers throughout the finding are ~4 lines stale but every content anchor is correct.",
  "correctedFix": "Fix 1 stands, with corrected anchors: in scripts/wg-diff.mjs REPLACE the removal marker line (currently line 1070, `/* the-moon-weavers-art — settle REMOVED: ... */`) with `  'the-moon-weavers-art': ['conditional', 'spellcasting'],`, immediately under the surviving `'labyrinthine-echoes': ['conditional', 'spellcasting'],` (line 1069) and inside the shared comment block at 1053-1069. Replacing rather than inserting below matters: wg-batch-gate.mjs parseSettles carries the preceding comment forward to consecutive keys, so a surviving marker line would become the entry's printed \"evidence\" at batch close. Justification as read from their raw ops: spellcasting = adjValue SPELL_DC/SPELL_ATTACK = T; conditional = the LEVEL>=12 step to E (their conditional here wraps the proficiency step, not a second giveSpell as the comment's `conditional` paragraph — written for labyrinthine-echoes — describes; consider a one-line note so the next reader is not misled). Both are one general rule on our side at src/rules/build.ts ~6635-6651. Fix 2 needs sharpening or the restore is undone: as written it redesigns FIXED to name registry + entitled kinds, but the entry `'the-moon-weavers-art'` at scripts/drop-fixed-settles.mjs:34 must ITSELF be scoped (to the kinds the picker fix actually closed, e.g. ['choice']) or deleted from FIXED — otherwise the next `node scripts/drop-fixed-settles.mjs --write` re-matches `/^\\s*'the-moon-weavers-art':\\s*\\[/` and deletes the restored settle again. The minimal sufficient step is removing that one id from FIXED; the kinds-scoped redesign is the durable version."
 },
 {
  "id": "ascended-dragonet-heritage",
  "verdict": "CONFIRMED",
  "reason": "Verified independently on both sides. THEIRS (re-read raw): feat 51410 has one select \"Select a Heritage\" (PREDEFINED/CUSTOM) with five CUSTOM options — Fey Dragonet, Homing Drake, House Drake, Pearl Dragonet, Tidepool Dragonet — each a single giveAbilityBlock{type:'heritage', abilityBlockId 51271/51272/51274/51275/51276} plus that heritage's effect prose as the description. OURS: public/core.json feats.ascended-dragonet-heritage carries choice{flag:'firstHeritage', prompt:'The dragonet heritage you selected at 1st level', kind:'open', from:{type:'heritage', ancestry:'dragonet'}} and secondHeritage:true, both overlay rows owned by scripts/backfill-batch15.mjs:46 and :55. CARRIERS ARE LIVE AND MEASURED: src/rules/openChoice.ts case 'heritage' filters heritages by ancestryId==='dragonet' && !versatile — enumerated against core.json that is exactly five (fey-dragonet, homing-drake, house-drake, pearl-dragonet, tidepool-dragonet), none versatile: their five and no more. secondHeritageIdOf is at src/rules/build.ts:437-445 and reads build.featChoices[slotKey] for any feat with secondHeritage; the same shape is already used by the two precedent records late-awakener (ancestry awakened-animal) and awakened-yaoguai-heritage (ancestry yaoguai), which share the identical flag 'firstHeritage' and kind 'open' — so no collision and exact neighbour match. derive.ts heritageRecords/hasHeritage dereference secondHeritageId. THE FIX IS REAL AND IN THE TREE: build.ts:6465 now loops `for (const hid of [build.heritageId, secondHeritageIdOf(build, content)])` for innateSpells, with the comment naming Homing Drake; homing-drake is verified the only one of the five carrying innateSpells (know-the-way, arcane, atWill, heightenHalfLevel), so it is the case the fix was measured on. THE REGRESSION IS REAL: scripts/drop-fixed-settles.mjs:33 lists this id with reason \"the second heritage's innate spells now reach the character\" and its loop deleted the entry from every registry, leaving markers at scripts/wg-diff.mjs:1051 and scripts/wg-identity.mjs:489. Both justification comment blocks SURVIVE immediately above their markers and both describe the CARRIER question, not the innate-spell bug — and the innate-spell bug was a defect in our reader that no comparer bucket ever expressed (their ops produce only grantsRecord/grants/options), so removing these settles did not un-silence the thing that was fixed; it un-silenced a still-live, still-correctly-reasoned carrier difference. MEASURED LIVE: `node scripts/wg-identity.mjs --ids ascended-dragonet-heritage --verbose` prints grants AND options theirs-not-ours=[feydragonet, homingdrake, housedrake, pearldragonet, tidepooldragonet], ours=[(nothing)]; `node scripts/wg-diff.mjs --bucket they-only --list` line 189 prints `ascended-dragonet-heritage lvl 5 missing=[grantsRecord] ours=[choice,specialStat]`. Exactly the two settles' scope, nothing extra — so restoring silences the reported difference and nothing more, and it is not stale (it answers a difference that is live right now). SIBLING PARITY: awakened-yaoguai-heritage produces the byte-identical report shape under --raw (five theirs, ours nothing) and passes normally because its settles at wg-diff.mjs:1192 (['grantsRecord']) and wg-identity.mjs:519 (['grants','options']) were untouched. RULE 4 SWEEP: grep over src/ and scripts/ finds the id only in the two overlay rows, backfill-batch15.mjs, drop-fixed-settles.mjs, the two markers, and generated data files — nothing in featGrants*.ts, featFeatGrants.ts, featPickGrants.ts, featCantripGrants.ts, companionGrants.ts, situationalBonuses.ts, counterMods.ts, modes.ts, consumable-modes.json or toggle-modes.json. NO TEST PINS THE REMOVAL: test/wg-backlog-citations.test.ts only asserts that settle comments citing the parity backlog name a real backlog record, and this comment cites no backlog entry. NOT ASK-OWNER: this is a vocabulary/carrier difference, not a WG-vs-print disagreement — both sides offer the same five dragonet heritages and hand over the same mechanics; ours additionally preserves heritage IDENTITY, which the printed sentence explicitly requires (\"allowing you to take feats and gain any benefits that require a specific dragonet heritage\") and which a copied effect block cannot deliver. TWO DETAIL CORRECTIONS, neither load-bearing: (a) the wg-diff marker is at line 1051, not 1047, and its comment block spans 1041-1050, not 1037-1046; (b) 'late-awakener' does NOT hold the identical settle — it holds none, because their side encodes nothing named for it and neither comparer reports it (verified by running both scoped). Only awakened-yaoguai-heritage is a true settled sibling. Corrected fix supplied with the right anchors.",
  "correctedFix": "[{\"kind\":\"settle\",\"detail\":\"Restore SETTLED_IDENTITIES['ascended-dragonet-heritage'] = ['grants', 'options'] in scripts/wg-identity.mjs (registry opens at line 426), replacing the removal marker at line 489 and sitting directly under the surviving comment block at 481-488 that already states the reading. Both buckets, because their five giveAbilityBlock grants and their five option titles are the same five names arriving twice — verified live: `node scripts/wg-identity.mjs --ids ascended-dragonet-heritage --verbose` reports grants and options theirs-not-ours=[feydragonet, homingdrake, housedrake, pearldragonet, tidepooldragonet], ours=[(nothing)], 'nothing' because an open choice has no baked option list. Matches the untouched sibling entry 'awakened-yaoguai-heritage': ['grants','options'] at line 519, whose --raw report is the identical shape.\",\"reader\":\"src/rules/openChoice.ts case 'heritage' (filters heritages by ancestryId==='dragonet' && !versatile — measured as exactly the same five) and src/rules/build.ts:437-445 secondHeritageIdOf, feeding the second-heritage sites in build.ts including the innateSpells loop at build.ts:6465 and feat-prerequisite evaluation; src/rules/derive.ts heritageRecords/hasHeritage\"},{\"kind\":\"settle\",\"detail\":\"Restore VERIFIED_EQUIVALENT['ascended-dragonet-heritage'] = ['grantsRecord'] in scripts/wg-diff.mjs (registry opens at line 888), replacing the removal marker at line 1051 (NOT 1047) and sitting directly under the surviving comment block at 1041-1050 (NOT 1037-1046). Their giveAbilityBlock-per-option against our secondHeritage field: same mechanic, and ours additionally preserves heritage IDENTITY, which the copied effect block cannot. Verified live: `node scripts/wg-diff.mjs --bucket they-only --list` reports `ascended-dragonet-heritage lvl 5 missing=[grantsRecord] ours=[choice,specialStat]` — exactly the settle's scope. Matches the untouched sibling 'awakened-yaoguai-heritage': ['grantsRecord'] at line 1192.\",\"reader\":\"src/rules/build.ts:437-445 secondHeritageIdOf; src/rules/build.ts:6465 both-heritages innateSpells loop; src/rules/derive.ts heritageRecords/hasHeritage\"},{\"kind\":\"note\",\"detail\":\"Correct the surviving wg-diff comment's closing sentence while restoring it: it says 'Same settle as its two siblings awakened-yaoguai-heritage and late-awakener.' Verified false for late-awakener — it holds no settle in any of the three registries, because their side encodes nothing named for it and `node scripts/wg-identity.mjs --ids late-awakener --verbose` and the wg-diff they-only list both report it zero times. Only awakened-yaoguai-heritage is a settled sibling; late-awakener is a MODELLING sibling (same secondHeritage + open-choice shape in core.json) and should be described as such so the next reader does not go looking for a settle that was never there.\",\"reader\":\"n/a — comment accuracy only; scripts/wg-diff.mjs:1049 and scripts/wg-identity.mjs:487\"}]"
 },
 {
  "id": "gates-threshold",
  "verdict": "CONFIRMED",
  "reason": "Re-read both sides independently; the core divergence is real and stat-moving.\n\nTHEIR RAW OPS (read myself, `node scripts/wg-show.mjs gates-threshold --raw`, which DOES resolve — the finding's claim that it prints \"No ability_block row\" is FALSE today; it printed all four records 23152/23145/23153/23154): one `select` optionType=CUSTOM with two options. \"Expand the Portal\" -> operations [select \"Impulse Feat\" FILTERED level{1..N} traits[Kineticist,Impulse]; select \"Gate Junction\" FILTERED traits[Junction]]. \"Fork the Path\" -> operations [select \"New element\" PREDEFINED over six ABILITY_BLOCK options 23146-23151 each with a singular `operation` giveAbilityBlock; select \"Impulse feat of new element (no composite)\" FILTERED]. The Gate Junction select is nested inside Expand the Portal ONLY. Plus adjValue KINETICIST_BLAST_DICE=1.\n\nPRINT: our own AoN-derived mirror (public/core-descriptions.json, and `npm run feat -- gates-threshold` PRINTED TEXT) reads \"Expand the Portal: … You also gain a gate junction for one of your kinetic elements\"; Fork the Path's clause names no junction. Print and WG agree, so this is not ASK-OWNER; no gold-set ruling touches it (grep of docs/gold-set-*.md finds none).\n\nOURS: public/core.json classFeatures — all four threshold records carry effectChoices[0] id 'gate-junction', 14 options, 6 elemental-resistance options with grant.resistances value \"@actor.level\" and 5 skill junctions with grant.skills (verified — these move stats). Offered/applied unconditionally: effectChoiceOffered (build.ts:1374-1382) is `if (!ch.requiresFeature) return true;` and nothing else; resolvePick (build.ts:4866-4894) is called for every owned feature at build.ts:5057; 'gates-threshold' is in the kineticist feature list at level 5; EffectChoicesPicker (shared.tsx:4202) renders it for any feature where featureAsks() is true (Builder.tsx:1527-1531). So a kineticist who Forks at 5th is still asked, and an answer grants a level-scaling resistance or a skill print does not give on that branch. Confirmed no branch test anywhere: `gate-junction` in src/ appears only at situationalBonuses.ts:3234.\n\nCARRIERS all verified: build.ts:172-177 gateForks/gateExpands/gateForkImpulses; GATE_THRESHOLD_LEVELS at build.ts:2141 (not 2135); shared.tsx:3341-3435 renders the branch and the branch-correct impulse pickers; build.ts:4120-4140 grants the impulse on BOTH branches; build.ts:2643-2650 folds the fork into extraOptions; kineticElements.ts:70 and explain.ts:223 read the same set; derive.ts:4163 (not 4155) is `const dice = 1 + [5, 9, 13, 17].filter((l) => c.level >= l).length`, settled in wg-values.mjs KINETICIST_BLAST_DICE; pinned by test/batch15-parity.test.ts:73-121.\n\nSECOND FINDING verified: second-/third-/fourth-gates-threshold each carry a record-level `choice` (overlay rows at effect-backfill.json array indices 3941/3944/3930); level-5 correctly has none. `grep gateThreshold src/` returns nothing, and fourth's flag is literal prose (\"gateForks / gateExpands / effectChoices['fourth-gates-threshold:gate-junction']\"). Their options carry no `grant` and no `grantsToken`, so build.ts:5071-5078 and 6996 apply nothing — yet Builder.tsx:1636 renders them, so the player is asked the Expand/Fork question twice at L9/13/17. Removal idiom checked: apply-backfill.mjs:63-64 `if (fix.value === null) delete target[fix.field]` — value:null does remove. No other reader: the only non-overlay reference to the sibling ids is scripts/audit/apply-archetype.ts:121, a one-shot authoring alias that never reads `.choice` (its comment \"giving four independent picks — which is the rule\" is in fact the stale premise that produced the unconditional junction).\n\nSETTLES verified: both markers exist (wg-diff.mjs:1101, wg-identity.mjs:497) under surviving branch-carrier comments (wg-diff.mjs:1082-1100, wg-identity.mjs:491-496), removed by scripts/drop-fixed-settles.mjs:32 whose id-keyed loop strips from both files; the fix that reason names is in-tree at build.ts:4120-4140. `npm run feat -- gates-threshold` does falsely name drop-fixed-settles.mjs as owner. wg-parse.mjs:57-59 flattenOps walks `optionsPredefined[].operations` and NOT the singular `operation`, so their six element grants are invisible to the comparer — as claimed.\n\nRule 8: no existing vocabulary expresses this. requiresFeature gates on owning a class feature and forking removes none; effectChoiceOffered receives no recordId, so a data gate is needed. A builder-only gate is explicitly the defect both the predicate's own doc comment and EffectChoicesPicker's comment forbid.\n\nCorrections: the fix under-specifies the new field (the predicate cannot see which record/level it is on) and, more importantly, restores the settle with no test pinning the new behaviour — the existing settle comment itself says a carrier difference \"has to be MEASURED, not asserted\", and re-settling silences the comparer for the very record that just carried two defects.",
  "correctedFix": "[{\"kind\":\"src-edit\",\"detail\":\"Gate the junction on the Expand branch, matching print and their nesting. (1) src/rules/types.ts — add to EffectChoice, next to `requiresFeature` (line 1873): `requiresNoGateFork?: number` (the Gate's Threshold level this group belongs to), documented in the same register as requiresFeature. It must carry the LEVEL because effectChoiceOffered's signature is `(ch, build, content)` — it never sees the recordId, and all four records use the same choice id 'gate-junction', so the level cannot be inferred from `ch` alone. (2) src/rules/build.ts:1374-1382 effectChoiceOffered — before the requiresFeature test, add `if (ch.requiresNoGateFork != null && build.gateForks?.[String(ch.requiresNoGateFork)]) return false;`. One predicate, so the picker (src/builder/shared.tsx:4202 EffectChoicesPicker) and the applier (resolvePick, build.ts:4866-4894, driven for owned features at build.ts:5057) hide the question and drop the stale answer together — a player who answers the junction then switches to Fork keeps neither the prompt nor the grant.\",\"reader\":\"src/rules/build.ts:1374 effectChoiceOffered -> src/rules/build.ts:4866 resolvePick (called at build.ts:5057) and src/builder/shared.tsx:4202\"},{\"kind\":\"overlay\",\"detail\":\"scripts/data/effect-backfill.json — set `requiresNoGateFork` on the junction group of each of the FOUR existing effectChoices rows {category:'classFeatures', id:'gates-threshold'|'second-'|'third-'|'fourth-gates-threshold', field:'effectChoices'} (array indices 1203-1206): 5, 9, 13, 17 respectively. Keep id 'gate-junction' on all four so saved answers under `<recordId>:gate-junction` round-trip. These are absolute assignments of the whole array, so edit the rows in place rather than adding path rows.\",\"reader\":\"public/core.json classFeatures[*].effectChoices[0] -> effectChoiceOffered\"},{\"kind\":\"overlay\",\"detail\":\"Delete the three duplicate branch pickers by setting value:null IN PLACE on the existing rows {category:'classFeatures', field:'choice'} for second-gates-threshold (index 3941), third-gates-threshold (3944), fourth-gates-threshold (3930). Verified: apply-backfill.mjs:63-64 treats value:null as `delete target[fix.field]`, and setting the existing row (rather than appending a new one) is order-independent. Verified dead and duplicated: flag 'gateThreshold' has no reader in src/, the options carry no `grant`/`grantsToken` so build.ts:5071-5078 and build.ts:6996 apply nothing, yet Builder.tsx:1636 renders a second Expand/Fork prompt beside the one shared.tsx:3341-3435 already asks through gateForks. Level-5 has no such row and must not gain one.\",\"reader\":\"src/builder/Builder.tsx:1636 renderChoice (the surface today); after removal the only branch carrier is build.gateForks / gateExpands / gateForkImpulses\"},{\"kind\":\"test\",\"detail\":\"NEW, and a precondition for the settle below: extend describe(\\\"batch 15 — Gate's Threshold really has both branches\\\") in test/batch15-parity.test.ts (line 73) with a case that MEASURES the gate. Build a kineticist 5 with extraChoices.element ['fire-gate','water-gate'] and effectChoices {'gates-threshold:gate-junction':'air-elemental-resistance'} twice — once plain, once with gateForks {'5':'air-gate'} — and assert the resistance lands on the Expand build and is ABSENT on the forked one; add the same pair for a skill junction ('air-skill-junction' -> stealth trained) so both grant shapes are covered. Without this, restoring the settle silences the only comparer that would catch the regression.\",\"reader\":\"src/rules/build.ts:1374 effectChoiceOffered -> resolvePick -> character.resistances / proficiencies.skills\"},{\"kind\":\"settle\",\"detail\":\"AFTER the three fixes and the test land, restore both settles: SETTLED_IDENTITIES['gates-threshold'] = ['options'] in scripts/wg-identity.mjs (replacing the marker at line 497, under the surviving comment at 491-496) and VERIFIED_EQUIVALENT['gates-threshold'] = ['unmapped'] in scripts/wg-diff.mjs (replacing the marker at line 1101, under the comment at 1082-1100). Amend BOTH comments to record the second fix, not just the impulse-grant one: the junction is now branch-gated (`requiresNoGateFork`), so our record and their nesting agree about WHEN it is asked and differ only in WHERE the branch is stored — ours in BuildState (gateForks/gateExpands/gateForkImpulses), theirs as a CUSTOM select. 'unmapped' covers their adjValue KINETICIST_BLAST_DICE = 1 per threshold, which is derive.ts:4163 `1 + [5, 9, 13, 17].filter((l) => c.level >= l).length`, already settled on the values side in wg-values.mjs NOT_A_SCALAR. Also drop 'gates-threshold' from the FIXED map in scripts/drop-fixed-settles.mjs:32 so the next run cannot strip the restored entries again.\",\"reader\":\"src/rules/build.ts:172-177 + 2141 + 2643-2650 + 4120-4140 and src/builder/shared.tsx:3341-3435 for the branch; src/rules/derive.ts:4163 for the blast dice; pinned by test/batch15-parity.test.ts:73-121 plus the new case above\"}]"
 },
 {
  "id": "advanced-red-mantis-magic",
  "verdict": "ASK-OWNER",
  "reason": "FACTS ALL CHECK OUT, but the finding's central premise — \"there is nothing to ask the owner about\" — is false on two counts, so this cannot be CONFIRMED.\n\n(1) AN OWNER QUESTION FOR THIS EXACT ID IS ALREADY OPEN AND UNANSWERED. C:/trying ai 2/pf2e codex/work/owner-questions.json has keys {_, authorisedExceptions, open}; open[] is 57 entries and one of them is {\"id\":\"advanced-red-mantis-magic\",\"batch\":16,...} whose question field is this same slot-schedule/dangling-castingSource issue, with the same evidence (r2@6/+r3@8/+r4@10, RED_MANTIS undefined, legacy Basic 30201 already granting r2@6 and r3@8). The finding checked only the OTHER question — basic-summoner-spellcasting, batch 17, rules-1485 bounded spellcasting — correctly ruled it inapplicable, and then concluded no owner involvement was needed at all. The record's own filed question was missed. There is no \"answered\"/\"resolved\" section in that file; open[] is the live queue. Applying the src edit now pre-empts a ruling the repo is already waiting on.\n\n(2) WG'S OP IS INERT, SO THIS IS PRINT-VS-BOTH-IMPLEMENTATIONS, NOT A WG PARITY GAP. I re-read the dump myself. `grep -o \"RED_MANTIS[A-Z_]*\" work/wg/wg-data.sql | sort | uniq -c` → RED_MANTIS x2, RED_MANTIS_ASSASSIN x6. Line 7888 is 30202 (this feat, bare RED_MANTIS); line 11112 is 30324 Mantis Form (legacy), also bare RED_MANTIS. Line 11137 is 30201 \"Basic Red Mantis Magic (legacy)\", ops = adjValue, adjValue, defineCastingSource, giveAbilityBlock, feat, giveAbilityBlock, feat, giveSpellSlot, adjValue; its defineCastingSource value is RED_MANTIS_ASSASSIN:::PREPARED-TRADITION:::DIVINE:::ATTRIBUTE_CHA and its giveSpellSlot (src RED_MANTIS_ASSASSIN) is rank 0 lvl 1-20, rank 1 lvl 4-20, rank 2 lvl 6-20, rank 3 lvl 8-20. Line 11142 is 40164 remaster Basic, the second defineCastingSource, also RED_MANTIS_ASSASSIN. Nothing defines RED_MANTIS. So WG's effective legacy Red Mantis output is r1@4, r2@6, r3@8 — byte-identical to what our RANK_UNLOCKS ladder already produces — and WG delivers NO 4th-rank slot at 10 either. The finding's \"THEIRS ... exactly the printed schedule\" is true of the table and false of the behaviour; the residue it wants to close is print-vs-us AND print-vs-them. work/owner-questions.json authorisedExceptions has the 2026-08-22 \"where WG provably contradicts print, the BOOK wins\" ruling scoped to a NAMED record list (summiting-dragonblood, monk-resiliency, barbarian-resiliency, champion-resiliency, werecreature-dedication, favored-terrain) — not standing, and this id is not on it.\n\n(3) THE FIX IS ALSO AN EDITION-SCOPE CALL THE FINDING ITSELF DECLINES TO MAKE. Its third FIXES item (\"mark it superseded instead\") ends \"I did not choose between them\" — that is the owner's decision by definition. Substantively, wiring customUnlocks splices the 2019 World Guide feat into the 2025 Prey for Death ladder we actually ship (basic feat-6520 L4 / expert feat-6526 L12 / master feat-6529 L18), producing rank 4 at level 10 for a character whose printed prerequisite — legacy Basic Red Mantis Magic, feat-896 — we do not ship at all. That hybrid exists in neither printing.\n\nMECHANICAL CHECKS THAT DID PASS, so the owner can act on the spec as written if he says yes: the customUnlocks branch (casterArchetypes.ts:432-436, not :433-437) returns early and is reached before the innateCantrip guard; archetypeProficiency (:447-455) tests profMasterFeat/profExpertFeat BEFORE `if (config.customUnlocks) return 'trained'`, so naming expert-/master-red-mantis-magic preserves expert/master; `out[rank] = 1` makes the duplicated rank-4 row idempotent; and every production caller of archetypeSlots passes a full ActiveCasterArchetype (src/builder/Builder.tsx:377 and :873, src/builder/shared.tsx:3640, src/rules/build.ts:6021 via activeCasterArchetype), so the empty-`taken` bare-Tier path that would have silently zeroed the whole table is not reachable here. Minor corrections: RANK_UNLOCKS is at casterArchetypes.ts:408-417 (not :406-414); the red-mantis config is at :256-263 (the owner-question note saying :253 is also off); applyEditionFilter is at build.ts:2510 with the superseded/legacy test at :2525-2526, and it does hide edition 'superseded' unconditionally as claimed; the feat IS reachable by default despite carrying no `archetype` field, because featSlots.ts:164 admits a category-'archetype' feat with the archetype trait into a class feat slot (132 of our 138 category-'archetype' feats likewise carry no archetype field, so that anomaly is not disqualifying).\n\nTWO THINGS THE OWNER SHOULD SEE THAT THE FINDING DID NOT SURFACE. (a) A DATA-ONLY CARRIER EXISTS and was not considered: SpellSlotBonus (src/rules/types.ts:1578-1613) has byRankAt + createRank + entryId, and vernai-training already targets entryId 'red-mantis-assassin-dedication-casting' — so `spellSlotBonus:{byRankAt:[{level:10,byRank:{\"4\":1}}],createRank:true,entryId:'red-mantis-assassin-dedication-casting'}` says the residue with no src edit. I am NOT offering it as a correctedFix, because byRank ADDS: from level 12 a character who also holds expert-red-mantis-magic would get TWO rank-4 slots, where the finding's customUnlocks spec deliberately dedupes to one. Which of those is right is itself the owner's stacking ruling. (b) The printed feat also grants SPELLBOOK ADDITIONS (\"Add two 2nd-level spells to your Red Mantis assassin spellbook\", and again at 8th and 10th) — neither WG nor either proposed fix models them, so \"the mechanic delivered\" would still be partial.\n\nVerdict: real, accurately researched, and genuinely open — but it is the owner's call, and his queue already has the question."
 },
 {
  "id": "armor-potency-1",
  "verdict": "CONFIRMED",
  "reason": "Every load-bearing claim survives independent re-reading. THEIRS (re-read via `node scripts/wg-item.mjs 6719`): exactly one op, `addBonusToValue {variable:\"AC_BONUS\", value:1, type:\"item\", text:\"\"}` — no nesting, no conditional; group RUNE, usage \"etched onto armor\", level 5, 160 gp. PRINT (re-read from the AoN mirror via `node scripts/audit/_aon.mjs equipment-2785-2570`, not from their `text` column): GM Core pg. 226, \"Magic wards deflect attacks. Increase the armor's item bonus to AC by 1. The armor can be etched with one property rune.\" Two clauses; WG encodes the first and is silent on the second. That is a WG OMISSION, not a WG-vs-print disagreement — their +1 item bonus to AC is the book's number — so rule 4 does not fire and this is not ASK-OWNER. OURS: the twin structure is real and intentional. `core.items['armor-potency-1']` is the purchasable chassis with no `ac` key (verified: none of the 159 items-bucket rune twins carries `ac`); `core.runes['armor-potency-1']` = {slot:'armor', kind:'potency', value:1, level:5, price:{gp:160}, aonOrigin:'authored'} is the carrier. The reader chain is live end to end and I re-grepped every hop: `src/rules/attachments.ts:87` `const rune = content.runes[attachment.id]` is the lookup that joins chassis to mechanics; `attachments.ts:111-116` planRune (line refs EXACT) `if (rune.kind === 'potency') { ... runes.potency = Math.min(v, 4) }`; `src/rules/derive.ts:1736` `acItem = abpOn(c) ? 0 : Math.max((worn.inv.runes as ArmorRunes)?.potency ?? 0, refAc, bfAc, actsAsPotency)` applied as an ITEM bonus at 1738 that takes the highest against Bracers/Monster-Parts/ABP — same stat, same value, same bonus type as their `type:\"item\"`. The SECOND printed clause, which their side does not encode at all, is ours via `derive.ts:2256 propertyRuneCapacity()` keyed off the same potency value and enforced at `attachments.ts:140` (\"needs a potency rune first — property-rune slots come from the potency rune\"). Covered by test/armor-runes.test.ts:41 (potency AC does not stack) and test/attach.test.ts:110. Ours is the superset; nothing to change in the data. The structural root cause is confirmed and systemic: `scripts/lib/wg-parse.mjs:124` WG_BUCKETS = ['feats','classFeatures','heritages','backgrounds','items','ancestries','classes','actions'] — no `runes`; WG_PAIRING maps `items` -> their `item` table and has no runes entry; grepping all four comparers for 'runes' hits only the unrelated `runesKnown` field name at wg-diff.mjs:299/305. I re-measured the claim myself: all 159 records in core.runes have an items-bucket twin of the same id, so every rune in the corpus is compared against its chassis. Both \"why it is open now\" causes check out: `git ls-files scripts/` returns wg-diff.mjs, lib/wg-parse.mjs, wg-batch.mjs, apply-wg-parity.mjs, wg-parity-unapply.mjs — of the four comparers only wg-diff.mjs is tracked, so wg-values.mjs is untracked and post-dates the last commit (its own header at wg-values.mjs:76-80 says \"Untaught, every one of their 1,613 AC_BONUS ops read as 'nobody has taught this'\", with `AC_BONUS: ['ac', null]` at line 80); and work/wg-batch-016-parity.json line 240 already holds this record with verdict MATCHES and the \"the mechanic lives in the runes bucket, not the items bucket\" evidence, with no settle row anywhere. I reproduced BOTH live gate rows rather than trusting the report: `node scripts/wg-batch-gate.mjs --batch work/wg-batch-016.json` prints `armor-potency-1 missing=[ac]` under KINDS, and `node scripts/wg-values.mjs --ids armor-potency-1 --verbose` prints `MISSING ac| theirs=1 ours=(nothing)`. Rule 7 satisfied: raw ops read on both sides, carrier has a live reader. Rule 6 checked for the settle path: `grep armor-potency` across wg-diff.mjs, wg-values.mjs, wg-identity.mjs and lib/wg-parse.mjs returns nothing, so both keys are unique — no duplicate-key collision, and neither settle would be stale (each matches a row the gate is printing right now). Key formats verified against neighbours: wg-diff VERIFIED_EQUIVALENT uses bare kind names (`'unbreakable-expertise': ['ac']` at wg-diff.mjs:1015) and is applied at wg-diff.mjs:1916 `const settled = RAW_SETTLES ? undefined : VERIFIED_EQUIVALENT[id]`; wg-values SETTLED_VALUES uses the pipe key with an empty detail for null-detail variables (`'uncanny-awareness': ['perception|']`, `'basic-kata': ['hp|']`) and is applied at wg-values.mjs:1270. WHERE I DIVERGE, and why a correctedFix is attached: the finding's PREFERRED fix (teach the comparers to merge the runes twin) is overstated and is the wrong thing to do inside a batch-16 settle. I measured the rune field histogram: of 159 runes only 18 carry `value`, 24 `damage`, 1 `passiveEffects`, 1 `actsAs`; 141 of them are property runes whose mechanics live in derive.ts by name rather than as any harvestable field. So the merge would not \"clear this record, assisting, and the other 157 runes at once\" — and by the finding's own admission it cannot clear even THIS row without a new derived-fact translation (kind:'potency' + value:N IS an `ac` assertion), which then needs sibling translations for striking -> damage dice, resilient -> save, reinforcing -> shield, or it manufactures fresh mismatches. That is a change to shared infrastructure consumed by all four comparers and by wg-batch-gate.mjs, and it would reclassify records inside the fifteen batches already signed off against current comparer behaviour. The finding's own narrow alternative is the correct action now, with the reader-chain citations corrected: the derive.ts line numbers it gives (1729/1731 for acItem, 2242-2258 for propertyRuneCapacity) have drifted and are actually 1736/1738 and 2246-2256, and the settle-application lines are wg-diff.mjs:1916 and wg-values.mjs:1270 rather than 1852 and 1242 — the settle comments must cite the right lines or gate 6's WHY requirement is satisfied by a pointer that goes nowhere.",
  "correctedFix": "Take the SETTLE option, not the comparer merge. Two rows, both needed because two different gates fire on this record right now (both reproduced live).\n\n1) scripts/wg-diff.mjs, in VERIFIED_EQUIVALENT (alphabetical neighbourhood, same shape as 'unbreakable-expertise': ['ac'] at line 1015; consumed at wg-diff.mjs:1916):\n\n  /* ARMOR POTENCY (+1) — the mechanic is in the RUNES bucket, which no comparer reads.\n   * Printed (GM Core p.226, AoN equipment-2785-2570): \"Increase the armor's item bonus to AC by 1.\n   * The armor can be etched with one property rune.\" Their item 6719 encodes only the first clause,\n   * as addBonusToValue AC_BONUS=1 type=item.\n   * Ours carries BOTH. core.runes/armor-potency-1 {kind:'potency', value:1} is the carrier; the\n   * items-bucket twin is the purchasable chassis and is EXPECTED to be empty, because\n   * lib/wg-parse.mjs WG_BUCKETS has no `runes` entry and every one of the 159 runes is therefore\n   * compared against its chassis. Reader chain: attachments.ts:87 content.runes[attachment.id] ->\n   * attachments.ts:111-116 planRune writes hostInv.runes.potency -> derive.ts:1736 acItem, applied\n   * as an ITEM bonus at derive.ts:1738. Same stat, same value, same bonus type as theirs.\n   * SECOND CLAUSE, ours-only: derive.ts:2256 propertyRuneCapacity() sizes the property-rune slots off\n   * the same potency value, enforced at attachments.ts:140. Asserted by test/armor-runes.test.ts:41. */\n  'armor-potency-1': ['ac'],\n\n2) scripts/wg-values.mjs, in SETTLED_VALUES (key format per the null-detail neighbours 'uncanny-awareness': ['perception|'] and 'basic-kata': ['hp|']; consumed at wg-values.mjs:1270). The key the report prints is `ac|` — confirmed by running `node scripts/wg-values.mjs --ids armor-potency-1 --verbose`:\n\n  /* Same record, same reading as the wg-diff settle above: the +1 lives on core.runes/armor-potency-1\n   * (kind:'potency', value:1) and reaches AC through attachments.ts:111-116 -> derive.ts:1736 as an\n   * item bonus. wg-values reads the items-bucket chassis, which holds no `ac` key by design. */\n  'armor-potency-1': ['ac|'],\n\nBoth comments are hedge-free and state WHY, so gate 6 (wg-settle-audit.mjs HEDGES) passes. Neither key exists today — verified unique by grepping armor-potency/weapon-potency across wg-diff.mjs, wg-values.mjs, wg-identity.mjs and lib/wg-parse.mjs — so there is no duplicate-object-key overwrite and no sibling regression. Note that after the wg-diff settle the record moves to `agree` (ours has no kinds, so `extra` is empty) and therefore appears in out.disagree, which is harmless: the gate's VALUES check is served by wg-values, which the second row settles.\n\nDo NOT fold the runes-bucket comparer merge into this. Spin it off as its own scoped change, because it is not the one-line widening the finding describes: only 18 of 159 runes carry `value`, 24 `damage`, 1 `passiveEffects`, 1 `actsAs`, and the remaining 141 property runes are encoded in derive.ts by name, so the merge clears far fewer rows than claimed while requiring a whole new derived-fact translation layer (potency+value -> ac for armor and attack for weapons, striking -> damage dice, resilient -> save, reinforcing -> shield) in code that all four comparers and wg-batch-gate.mjs share. Any such change must be followed by a re-run of batches 1-15 plus `node scripts/wg-settle-stale.mjs`, since changing what the comparers harvest can strand existing settles — and a settle that matches nothing is the trap wg-values.mjs's own header warns about.\n\nSibling note for whoever picks this up: armor-potency-2 and armor-potency-3 print the identical `MISSING ac|` row (theirs=2 and theirs=3) from the same cause, and batch 16's `assisting missing=[carry]` is the same runes-bucket blindness with core.runes/assisting.passiveEffects.bulkLimitBonus as its carrier. Settle each in its own batch on its own reading; do not generalise this row to them."
 },
 {
  "id": "assisting",
  "verdict": "CONFIRMED",
  "reason": "Every claim holds under independent re-reading. THEIRS, read raw from work/wg/wg-data.sql public.item row 11721 (wg-show.mjs cannot reach it — it walks ability_block only, returning the four \"Assisting Shot\" feats): group RUNE, usage \"etched onto armor\", level 5, 125 gp, one flat op {\"type\":\"adjValue\",\"data\":{\"variable\":\"BULK_LIMIT_BONUS\",\"value\":1}} — no nesting, no conditional. PRINT, verified independently of their dump at .import-src/pf2e/packs/pf2e/equipment/assisting.json: usage etched-onto-armor, level 5, 125 gp, \"6 + your Strength modifier before becoming encumbered … total Bulk of up to 11 + your Strength modifier\" — against derive.ts:4922 `encumberedAt: 5 + strMod + limitBonus, max: 10 + strMod + limitBonus + maxOnlyBonus`, exactly +1 to BOTH thresholds, which is what the symmetric bulkLimitBonus means (types.ts:2016-2019, and bulkMaxBonus at 2810-2817 is the asymmetric variant). OURS: public/core.json items.assisting is the chassis with no bulk field; core.json runes.assisting = {\"slot\":\"armor\",\"kind\":\"property\",\"level\":5,\"price\":{\"gp\":125},\"passiveEffects\":{\"bulkLimitBonus\":1}} is the carrier, and it has a LIVE reader — derive.ts:4873 `for (const r of propertyRuneDefs(inv, db)) runeBonus = Math.max(runeBonus, r.passiveEffects?.bulkLimitBonus ?? 0)`, gated on worn+invested, with propertyRuneDefs at derive.ts:1325 resolving db.runes[id]; test/armor-runes.test.ts:59 asserts the payload and :69-74 asserts the printed 6+Str / 11+Str, plus invest/worn/no-stack cases. CAUSE CONFIRMED: scripts/lib/wg-parse.mjs:124 WG_BUCKETS = [feats, classFeatures, heritages, backgrounds, items, ancestries, classes, actions] — no runes, so wgRecord/wgAllRecords resolve `assisting` to the chassis; wg-diff.mjs carry list and wg-values.mjs:624 both spell `passiveEffects.bulkLimitBonus` but read a record that never carries it. BOTH GATES RE-RAISE TODAY (I ran them): wg-batch-gate.mjs --batch work/wg-batch-016.json prints \"assisting missing=[carry]\", and wg-values.mjs --batch prints \"MISSING bulk| theirs=1 ours=(nothing)\". No `assisting` row in VERIFIED_EQUIVALENT or SETTLED_VALUES (grep across scripts/), and work/wg-batch-016-parity.json:250 holds the unsettled MATCHES verdict — so this is an unsettled false gap, not a regression and not a data drop. Rule 4 does not fire: print, their op and our field all say +1/+1, so nothing is being settled over a WG-vs-print disagreement. Line citations drift by ~8-11 lines (derive.ts:4862→4873, :4865→4876, :4847→4858, :4854→4865, :4873-4874→4884-4885; types.ts:2784-2788→2016-2019/2810-2817; registries are at wg-diff.mjs:888 and wg-values.mjs:786, not 1852/1242) — citation drift only, every quoted line exists as described. One real omission in the merge fix, corrected below: all 159 runes ids collide with items (so no new records), but kolss-oath and trudds-strength disagree with their items twin on `price`, which a naive runes-over-items merge would silently rewrite.",
  "correctedFix": "Both proposed routes are sound; adopt them with these corrections. (A) PREFERRED src-edit, scripts/lib/wg-parse.mjs: in wgRecord()/wgAllRecords(), when an id resolves in `items` and core.runes[id] also exists, merge the rune record ITEMS-WINS, not runes-over-items — measured, all 159/159 rune ids also live in `items`, so nothing new appears, but `kolss-oath` and `trudds-strength` carry a `price` that differs between the two records and a runes-over-items merge would silently rewrite it. Merging only keys absent from the items record (or only the mechanical keys: passiveEffects, slot, kind, damage) leaves those two untouched and still delivers `passiveEffects.bulkLimitBonus: 1` to the exact path wg-diff.mjs carry and wg-values.mjs:624 already look for, clearing both rows with no registry entry. Do not add `runes` to WG_BUCKETS as an eighth walked bucket — WG_PAIRING has no `runes` table, so the 159 records would land in noMatch and the counters would move for a reason unrelated to parity. (B) NARROW alternative, correct anchors: add `'assisting': ['carry']` inside VERIFIED_EQUIVALENT at scripts/wg-diff.mjs:888 (consumed at :1916), and `'assisting': ['bulk|']` inside SETTLED_VALUES at scripts/wg-values.mjs:786 (consumed at :1270 as `(SETTLED_VALUES[id] ?? []).includes(key)`; the printed key is exactly `bulk|`). Neither registry currently holds an `assisting` key, so there is no collision. Comment each row with the carrier (core.runes/assisting.passiveEffects.bulkLimitBonus = 1), the live reader (derive.ts:4873 via propertyRuneDefs, thresholds assembled at derive.ts:4922), the existing coverage (test/armor-runes.test.ts:69-74), and the note that the items twin is the purchase chassis because this is an etched armour property rune. Note if (A) lands, these two rows become stale and wg-settle-stale.mjs will flag them — pick one route, not both."
 },
 {
  "id": "bolkas-blessing",
  "verdict": "CONFIRMED",
  "reason": "The parity claim verifies in full, but the PREFERRED fix is refuted by the finding's own measurement, so the settle must be the narrow one.\n\nVERIFIED INDEPENDENTLY: (1) Their ops — wg-item.mjs 18177 prints exactly two `addBonusToValue {\"variable\":\"\",\"text\":\"\"}` and nothing else; no value, no type, no variable. Corroboration the finding missed: their GREATER item 18178 encodes the same effect properly (`SKILL_DIPLOMACY value 2 type item` and `PERCEPTION value 2 type item text \"to Sense Motive\"`), which is positive evidence that the base row's two blanks are unfinished stubs of those two bonuses, not a different encoding. (2) Print — AoN mirror equipment-3769-3568.json matches the quoted text verbatim (+1 item to Diplomacy and to Perception to Sense Motive; Gift of Life 1 action, once/day, 3d10). (3) Our carriers, each read directly: public/core.json items.bolkas-blessing has passiveEffects.skills.diplomacy=1, frequency {max:1,per:\"day\"}, counters[{id:\"freq\",resetsOnRest:true}], activationCost {type:\"actions\",value:1}; src/rules/situationalBonuses.ts:1989 holds the Sense Motive half, :2151 the greater. Live readers confirmed: derive.ts:279/:309 walk db.items[inv.itemId].passiveEffects, and explain.ts:278 pushes inv.runes.property into characterSituationalIds. (4) The artefact — kindOfTheirOp reads `const v = String(op.data?.variable ?? '')` at wg-diff.mjs:145; \"\" matches no pattern and hits `return 'unmapped'` at :214. work/.gate-diff.json confirms `theyOnly: {id:\"bolkas-blessing\", theirKinds:[\"unmapped\"], ourKinds:[\"skill\",\"conditional\",\"perception\"], missing:[\"unmapped\"], theirOps:2}`. work/wg-batch-016-parity.json already carries verdict MATCHES with the quoted reasoning. No WG-vs-print disagreement exists — their stubs assert nothing, so nothing contradicts print; rubric (4) does not trigger.\n\nWHY THE PREFERRED FIX IS WRONG. I re-ran the blast-radius measurement with parseCopyBlock over both tables: 8,174 value-carrying ops, exactly 6 empty-variable, across the five named records — the count is exactly right. But the finding reported only record NAMES, never the op payloads, and two of the six are not blank:\n  ability_block 57388 Instinctive Collaborator | addBonusToValue | {\"variable\":\"\",\"value\":\"2\",\"type\":\"circumstance\",\"text\":\"to Aid\"}  <- a fully specified +2 circumstance bonus to Aid, and it is that record's ONLY op\n  ability_block 57671 Undying Resilience | addBonusToValue | {\"variable\":\"\",\"text\":\"When you roll a critical failure on a Fortitude save, you get a failure instead. When you fail a Fortitude save against a damaging effect, you take half damage.\"}  <- a whole printed rule carried in text\nA blanket `if (!v) return null` deletes both from theirKinds. Instinctive Collaborator is unpaired today (not in .gate-diff.json at all), so the moment we author that record its +2 circumstance to Aid would be silently invisible — the exact failure wg-diff's own comment at :210-213 was written against (\"a differ that reports coverage it does not have is worse than one that reports a gap\"). Undying Resilience is currently paired to their better row 39100, so the blank row's clause is not load-bearing now, but the op still asserts a rule. The claim \"an op that names no variable cannot be a disagreement about a variable\" is true; the claim that therefore it \"asserts NOTHING\" and the guard \"cannot mask anything\" is false for 2 of the 6.\n\nOnly THREE of the six ops assert nothing at all (both Bolka's, plus Medium's Awareness 28411 `{\"variable\":\"\",\"text\":\"\"}` — the one other record the gate actually reports, missing=[\"unmapped\"]).\n\nALSO CHECKED: no existing 'bolkas-blessing' key in VERIFIED_EQUIVALENT, so the settle is a unique add with no collision; settle semantics at :1916 filter `missing`, moving the record to weOnly, which no gate test fails, and its theirKinds [\"unmapped\"] keeps it out of the `disagree` VALUES candidate list at :1930, so no VALUES side effect. The three cited ['unmapped'] precedents exist at :949 (untrained-improvisation, actually ['conditional','unmapped']), :1082, :1480 — the finding's line numbers are ~4-33 low throughout (map ends at :1884/:1885, not :1852), harmless but worth not trusting blind.\n\nADJACENT DEFECT — independently CONFIRMED and correctly kept out of the verdict. core.runes.bolkas-blessing carries no passiveEffects; grep shows rune passiveEffects is read at exactly two sites, derive.ts:4873 and :4876, both bulkLimitBonus only; the skill accumulators at :279/:309 are item-id inventory walks. explain.ts:275 even documents \"Etching CONSUMES the loose rune item and records its id here, so inv.itemId never names it.\" Print backs the premise: AoN category-page equipment-category-115 says attaching a filigree \"functions in the same way as etching a rune onto a weapon. A clan dagger filigree counts as a property rune.\" So the +1 Diplomacy is lost on etching while the Sense Motive half survives. The spec in the finding (rune record carries the passive AND derive.ts folds property-rune passives into the same accumulator, mirroring :4873) is the right shape. Separate note for whoever picks it up: core.json also holds a duplicate `greater-bolkas-blessing` item (passive diplomacy 2, no usage, no rune sibling) alongside `bolkas-blessing-greater`; wg-diff lists it under noMatch.",
  "correctedFix": "Do NOT apply the finding's PREFERRED fix (blanket `if (!v) return null;` before wg-diff.mjs:214). It suppresses ability_block 57388 Instinctive Collaborator's `{\"variable\":\"\",\"value\":\"2\",\"type\":\"circumstance\",\"text\":\"to Aid\"}` — a complete +2 circumstance bonus that is that record's only op — and 57671 Undying Resilience's Fortitude clause carried in `text`.\n\nAPPLY INSTEAD (either; the first is the smaller, already-precedented move):\n\n(A) The narrow settle. In scripts/wg-diff.mjs, immediately before the closing `};` of VERIFIED_EQUIVALENT (currently line 1885, after `'stylish-tricks': ['choice'],` at :1884):\n\n  /* Their two ops are UNFINISHED STUBS, quoted whole so the next reader does not have to fetch them:\n   *   {\"type\":\"addBonusToValue\",\"data\":{\"variable\":\"\",\"text\":\"\"}}   x2\n   * No variable, no value, no bonus type. Only the COUNT is legible — two bonuses, which is how many\n   * the printed text gives. Their GREATER grade (item 18178) encodes the same effect properly\n   * (SKILL_DIPLOMACY value 2 type item; PERCEPTION value 2 type item \"to Sense Motive\"), which is what\n   * establishes these blanks as stubs of those two bonuses rather than a different encoding.\n   * This is \"they encode nothing\", NOT \"we cover it elsewhere\". We carry both printed halves:\n   * items.bolkas-blessing.passiveEffects.skills.diplomacy = 1 (unconditional half, read by\n   * derive.ts:279/:309) and src/rules/situationalBonuses.ts:1989 (the Sense Motive half, reached via\n   * explain.ts:278). Empty `variable` falls through kindOfTheirOp's pattern chain to the named\n   * 'unmapped' fallback at :214, which is why this reads like a lane we have not built. */\n  'bolkas-blessing': ['unmapped'],\n\n(B) If a general guard is still wanted, narrow the predicate so it fires only on ops that assert nothing AT ALL, and place it just before `return 'unmapped'` at :214:\n\n  /* An op with no variable AND no value, bonus type or text asserts nothing there is anything to\n   * disagree about. Measured corpus-wide across ability_block + item, flattening every nested branch:\n   * 8,174 value-carrying ops, 6 with an empty `variable`, of which only THREE are wholly empty —\n   * Bolka's Blessing item 18177 (x2) and Medium's Awareness 28411. The other three are deliberately\n   * left reporting because they DO assert something without naming a variable: Instinctive\n   * Collaborator 57388 {value:\"2\",type:\"circumstance\",text:\"to Aid\"}, Undying Resilience 57671\n   * (a whole Fortitude clause in `text`), and Mystery (legacy) 26443 {value:false}. Sits with the\n   * PRIMARY_SHEET_TABS/PAGE_*, BLACKLIST_/WHITELIST_ and _FEAT_COUNT guards above — their\n   * bookkeeping, not a rule. */\n  if (!v && op.data?.value === undefined && !String(op.data?.text ?? '') && !op.data?.type) return null;\n\n(B) clears bolkas-blessing and mediums-awareness (missing=[\"unmapped\"]) and nothing else; re-measure before trusting that, since the corpus can move. The rune-etching defect stays a separate item and must not be folded into this settle."
 },
 {
  "id": "warriors-training-ring",
  "verdict": "CONFIRMED",
  "reason": "The gate is genuinely open and the lane is right; the prescribed registry row is the WRONG one and would damage the instrument.\n\nVERIFIED AS STATED: `node scripts/wg-item.mjs 14767` prints exactly one op — {\"type\":\"addBonusToValue\",\"data\":{\"variable\":\"ATTACK_ROLLS_BONUS\",\"text\":\"While wearing the Warrior's Training Ring, add your level...\"}} — no `value`, no `type` key. Their activation clause lives only in the description prose (and their text says FREE-ACTION/concentrate, not \"envision\" as the finding's PRINTED quote claims — a slip, do not copy it into a comment). Our side: situationalBonuses.ts:2422 carries BOTH clauses; core.json items.warriors-training-ring has frequency 1/day, freq counter with resetsOnRest, activationCost free, divination/invested, worn. No other carrier (grep across src/scripts/test returns only situationalBonuses.ts), no `modes` row among all 533. Reader chain confirmed live: explain.ts:239 characterSituationalIds -> situationalBonuses.ts:3486 featSituationalFor -> explain.ts:740. wg-values.mjs contains no ATTACK_ROLLS_BONUS in VAR or NOT_A_SCALAR, and `node scripts/wg-values.mjs --ids warriors-training-ring --verbose` reproduces the gate trip: \"NO VAR MAPPING — nobody has taught this yet: ATTACK_ROLLS_BONUS\". So one registry row in wg-values.mjs is the correct lane, and NO data change is right.\n\nWHY THE PRESCRIBED ROW IS WRONG: ATTACK_ROLLS_BONUS is not a prose-only variable. Walking every bucket of work/wg/wg-data.sql, it appears on ten distinct records and eight carry a real number with a `type`: Acid Flask (Major) and Dread Ampoule (Major) value 3 item, Weapon Potency (+2)/(+3) value 2/3 item, Hunter's Arrowhead value 1 item, Fated Rival value \"+1\" circumstance, Courageous Anthem value \"1\" status, Automatic Bonus Progression 1/2/3 item. Only Warrior's Training Ring and Scion Transformation (legacy) are valueless. Our side already holds that number in this comparer's own currency: acid-flask-major and dread-ampoule-major both carry attackItemBonus: 3 in core.json, and ourAssertions already puts it — wg-values.mjs:581 `if (rec.attackItemBonus) put('attack|', Number(rec.attackItemBonus));`. So \"a rider on attack rolls with no number on their side\" is false on its face for the variable, and NOT_A_SCALAR is keyed by bare variable name (`unmapped.filter((v) => NOT_A_SCALAR[v])`), not by record or by op shape.\n\nMEASURED SIBLING REGRESSION: `node scripts/wg-values.mjs --ids acid-flask-major,dread-ampoule-major --verbose` shows both tripping this exact gate TODAY. Adding the proposed row would relabel them \"not a scalar we hold\" and retire them uncompared — theirs asserts 3, ours asserts 3, and the pair would go silent behind a false claim. That is precisely the failure the file's own header forbids (\"NO SILENT CAPS\", \"A settle that matches nothing is a trap\"). It also does not match its claimed neighbours: MARTIAL_EXPERIENCE (3 hits, value=true) and UNTRAINED_IMPROVISATION (12 hits, value=\"{{level}}\") are record-named marker variables that are never a plain number anywhere; ATTACK_ROLLS_BONUS is a general stat variable, structurally the twin of AC_BONUS — which this same file DID teach a VAR mapping, with the comment \"their side has no conditional verb — it writes a plain AC_BONUS and parks the trigger in the op's own text — so the number is still the number.\" The repo already knows this: the comment above the entry the finding is defending, situationalBonuses.ts:2410, says of their side \"their numeric operations all carry `value` and `type`\".\n\nThe corrected row fixes this record too, by code reading: with a varSpec present, theirAssertions:274 (\"a prose-only bonus asserts no value\") drops the valueless op, `unmapped` comes back empty, and the reason becomes the existing 'prose-only bonus (no number asserted)' — which wg-batch-gate.mjs:92 does not match, so VALUES passes.",
  "correctedFix": "ONE REGISTRY ROW, but in VAR — not NOT_A_SCALAR.\n\nscripts/wg-values.mjs, in `VAR`, immediately after the AC_BONUS entry (~line 82), whose comment makes the identical argument for the identical shape:\n\n  /* The attack twin of AC_BONUS, and the same reason: their side has no conditional verb, so it writes\n   * a plain ATTACK_ROLLS_BONUS and parks the trigger in the op's own `text`. Numeric on eight of its ten\n   * carriers — Acid Flask/Dread Ampoule (Major) 3 item, Weapon Potency (+2)/(+3), Hunter's Arrowhead,\n   * Fated Rival, Courageous Anthem, Automatic Bonus Progression — and ours already holds that number as\n   * `attackItemBonus`, put as `attack|` a few lines below. Warrior's Training Ring is the prose case:\n   * item 14767's single op carries `text` and no `value`, so theirAssertions drops it and the record\n   * lands in the existing, non-failing 'prose-only bonus (no number asserted)' bucket. Its data was\n   * already FIXED in batch 16 — both printed clauses are on FEAT_SITUATIONAL['warriors-training-ring']\n   * (situationalBonuses.ts:2422), read by characterSituationalIds (explain.ts:239) -> featSituationalFor\n   * (situationalBonuses.ts:3486) -> explain.ts:740. This row is the comparer catching up, not a defect. */\n  ATTACK_ROLLS_BONUS: ['attack', null],\n\nDo NOT add ATTACK_ROLLS_BONUS to NOT_A_SCALAR. That lookup is by bare variable name, so it would silence acid-flask-major and dread-ampoule-major — both tripping this same gate today, both with attackItemBonus: 3 on our record matching their value 3 — behind the false claim that we hold no scalar for it.\n\nDATA: unchanged, exactly as the finding says. Both clauses carried, frequency counter and free-action activation present, and no mode authored through the overlay.\n\nONE CONSEQUENCE TO CARRY, not blocking batch 16 (neither record is in it): weapon-potency-2 / weapon-potency-3 hold no attack number on our record — a potency rune's bonus lives in the rune engine, and types.ts:3536 marks `builtInRunes` as deliberately distinct from `attackItemBonus` — so once taught they will read `attack| theirs=2/3 ours=(nothing)` when they reach a batch. The AC_BONUS precedent is the template for handling that: the same pass that taught AC_BONUS also fed `passiveEffects.ac` and `unarmoredAc.acBonus` into ourAssertions so the teach would not \"report a false gap on every magic item\". Either feed the potency value into `attack|` the same way, or settle those two rune records by hand when they surface — do not pre-emptively suppress the variable to avoid them."
 },
 {
  "id": "beastmasters-sigil",
  "verdict": "CONFIRMED",
  "reason": "Reproduced both gates and re-read both sides from source. THEIRS (raw, read directly from work/wg/wg-data.sql item table, since wg-show only scans ability_block): item 11749 carries exactly one `select` op, title \"What is the spellheart affixed to?\", modeType PREDEFINED, optionType CUSTOM, with three optionsPredefined — \"Armor\" (addBonusToValue SAVE_FORT/SAVE_REFLEX/SAVE_WILL, value 1, type item, text \"against poison\"), \"Melee Weapon\" (SKILL_ATHLETICS +1 item, text \"to Trip\"), and \"Ranged Weapon\" with `\"operations\":[]`. Ids 11747 (Greater) and 11748 (Major) carry NO operations at all. PRINT (public/core-descriptions.json, all three grades): +1/+1/1d6 for base, +1/+1/2d6 for greater, +2/+2/3d6 for major — WG and print agree completely, so this is not a WG-vs-print disagreement and ASK-OWNER does not apply. GATES: work/.gate-diff.json theyOnly holds `beastmasters-sigil` with theirKinds=[choice,save,skill], ourKinds=[spell,conditional,save,skill], missing=[\"choice\"] — so save and skill are already credited to us and only the `choice` kind is open; `node scripts/wg-identity.mjs --ids beastmasters-sigil` prints `options theirs-not-ours=[armor, meleeweapon, rangedweapon] / ours=[(nothing)]`. `node scripts/wg-values.mjs --batch work/wg-batch-016.json --verbose` prints `ok beastmasters-sigil (4 values, 0 sets agree)`, confirming the batch-16 VALUES half is right and the IDENTITY/KINDS half was never addressed — work/wg-batch-016-parity.json:300 holds the MATCHES verdict with exactly the quoted reasoning. CARRIERS, all re-checked by grep: situationalBonuses.ts:1656/1657/1658 are verbatim as quoted, with +1/+1/+2 matching the printed grades; core.json items.beastmasters-sigil/-greater/-major have keys id,name,level,price,bulk,traits,rarity,source,usage,frequency,counters,heldSpells,itemType,edition,aonId — no `choice`, no `effectChoices`; five-feather-wreath carries effectChoices in the exact proposed shape (scripts/data/effect-backfill.json:142307, id \"affix\", prompt \"What is the spellheart affixed to?\") AND its affix-gated Acrobatics star at situationalBonuses.ts:2722, so the two carriers are complements as claimed; 142 spellheart-trait items exist and 91 carry effectChoices, with the sigil family among the 51 that do not. FIX MECHANICS verified: wg-diff.mjs:305 OUR_KINDS.choice includes 'effectChoices' (so the KINDS row clears); wg-identity.mjs:258-264 loops `rec.effectChoices ?? []` calling addOption(o) which adds both o.label and o.value, and key() (l.47-53) lowercases and strips non-alphanumerics, giving armor / meleeweapon / rangedweapon — an exact match for their three keys, so the IDENTITY row clears; the comparer is one-directional (only `theirs-not-ours` is reported, wg-identity.mjs:924), so the extra greater/major rows cannot manufacture a new failure, and those two records are skipped outright by `if (!total) continue` since WG encodes nothing for them. apply-backfill.mjs:56-66 does `target[fix.field] = fix.value` with `value === null` meaning delete, and src/data/index.ts:289 lists effectChoices in UMBRELLA_MECHANICAL_FIELDS. No collision: grep finds no beastmasters-sigil row anywhere in scripts/data/effect-backfill.json, and no settle entry for it in wg-diff.mjs VERIFIED_EQUIVALENT or wg-identity.mjs SETTLED_IDENTITIES, so the \"no settle row\" call is correct and no stale settle is created. NO DOUBLE-PAY: build.ts:4866-4890 sets `g = opt?.grant`, so a note-only option records the pick (label+note reach effectPicks for display) and applies nothing; the +1s stay solely with situationalBonuses. integrity-sweeps.test.ts:70 (\"no record grants the SAME lane from both a `choice` and its `effectChoices`\") and :101 only constrain options that carry grants, so nothing regresses. ONE CORRECTION to the finding's annotation, which does not change the proposed JSON: for ITEMS the answer is stored on the inventory instance as `InventoryItem.effectChoices[ch.id]` and the picker is rendered by src/sheet/ItemDetail.tsx:332-355 (documented at src/rules/play.ts:116), not by build.effectChoices['<recordId>:<choiceId>'] via Builder.tsx/shared.tsx — build.ts:5979 does call resolvePick on each inventory item's effectChoices using the build-level key, but with note-only options neither path applies anything, so id:\"affix\" remains right for sibling consistency rather than for key resolution. Minor cosmetic imprecision: the duplicate legacy ids greater-five-feather-wreath / major-five-feather-wreath lack effectChoices while the canonical five-feather-wreath-greater / -major carry it; the sigil family uses the canonical -greater/-major form, which is what the fix targets."
 }
]