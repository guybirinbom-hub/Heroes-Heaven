confirmed 32 | refuted 2 | askOwner 3
confirmed but NOT adversarially confirmed: 0

== FAMILIES
(routed from each finding's PROPOSAL by FAMILY_RULES in scripts/wg-batch-run.mjs — first rule wins, exactly one family per finding; a family with no findings is omitted)
data-rows (19): lesson-of-protection#familiar-spell, lesson-of-dreams#familiar-spell, lesson-of-life#familiar-spell, lesson-of-shadow#familiar-spell, blessed-swiftness#mounted, blessed-swiftness#aon-parent, lesson-of-decay, lesson-of-mischief, lesson-of-snow, lesson-of-the-shark, lesson-of-the-flock, wind-seeker, wind-seeker#darkened-forest-form, deadly-mutation, skill-mastery-rogue#maxtakable, golem-grafter-dedication, unravel-mysteries, spirit-walk#resistance, eye-of-the-unseen#innate-spell
engine (8): advanced-weaponry-construct, hyper-boosters#overdrive, hyper-boosters#prerequisite, skill-mastery-rogue#duplicate-skill, lorefinder, animalistic-brutality, armored-resistance#resistance, energy-resistant
instruments (1): armored-resistance#instrument
situational (4): cultivators-keen-eye#materials, eye-of-the-unseen, composer-staff-greater, illusionists-hat#skill-bonuses

== CONFIRMED
- lesson-of-protection#familiar-spell [player]: The lesson's second half — the ordinary spell the familiar learns — has no carrier: only the hex reaches the character, so Mystic Armor never joins the witch's spellbook.
    PROPOSAL: Add to scripts/data/effect-backfill.json: {"category":"classFeatures","id":"lesson-of-protection","field":"grantedSpells","value":["mystic-armor"]} — same row shape and same reader as the existing lesson-of-vengeance grantedSpells row; spells['mystic-armor'] exists in core.json (rank 1, matching their rank:1).
- lesson-of-dreams#familiar-spell [player]: The lesson's second half — the ordinary spell the familiar learns — has no carrier: only the hex reaches the character, so Sleep never joins the witch's spellbook.
    PROPOSAL: Add to scripts/data/effect-backfill.json: {"category":"classFeatures","id":"lesson-of-dreams","field":"grantedSpells","value":["sleep"]} — mirrors the existing lesson-of-vengeance grantedSpells row; spells['sleep'] exists in core.json (rank 1).
- lesson-of-life#familiar-spell [player]: The lesson's second half — the ordinary spell the familiar learns — has no carrier: only the hex reaches the character, so Spirit Link never joins the witch's spellbook.
    PROPOSAL: Add to scripts/data/effect-backfill.json: {"category":"classFeatures","id":"lesson-of-life","field":"grantedSpells","value":["spirit-link"]} — mirrors the existing lesson-of-vengeance grantedSpells row; spells['spirit-link'] exists in core.json (rank 1).
- lesson-of-shadow#familiar-spell [player]: The lesson's second half — the ordinary spell the familiar learns — has no carrier: only the hex reaches the character, so Chilling Darkness never joins the witch's spellbook.
    PROPOSAL: Add to scripts/data/effect-backfill.json: {"category":"classFeatures","id":"lesson-of-shadow","field":"grantedSpells","value":["chilling-darkness"]} — mirrors the existing lesson-of-vengeance grantedSpells row; spells['chilling-darkness'] exists in core.json at rank 3, which the reader's `content.spells[id]?.rank` lookup files at rank 3, matching their rank:3.
- blessed-swiftness#mounted [player]: Neither side models the mounted redirect of the Speed bonus, and our own modeAdjust note tells the player the opposite of what the feature prints.
    PROPOSAL: Amend the existing modeAdjust note on classFeatures/blessed-swiftness (scripts/data/effect-backfill.json, category classFeatures, field modeAdjust) so the parenthetical states the printed condition instead of contradicting it: "(The +5-foot status bonus is yours while on foot; if you're mounted, your mount gains it instead.)"
- blessed-swiftness#aon-parent [internal]: The record's print join points at the wrong AoN document — Bracers of Devotion (an Item 11 from Treasure Vault) instead of the champion feature it is a section of — a name collision between the item's "Blessed Swiftness" section and the class feature's.
    PROPOSAL: Add to scripts/data/effect-backfill.json: {"category":"classFeatures","id":"blessed-swiftness","field":"aonParentId","value":"class-feature-877"} (supersedes the shipped equipment-2320) and {"category":"classFeatures","id":"blessed-swiftness","field":"aonSection","value":"Blessed Swiftness:"} to match the sibling blessed-shield's section spelling, so every print pass reads this record against Blessing of the Devoted 
- lesson-of-decay [player]: The familiar half of the lesson is missing: we carry only the hex (focusSpells: [mycological-malady]) and nothing delivers the Insect Form the familiar learns, while WG grants it.
    PROPOSAL: Add scripts/data/effect-backfill.json row {category:'classFeatures', id:'lesson-of-decay', field:'grantedSpells', value:['insect-form']} — same lane and same reader as the existing lesson-of-vengeance grantedSpells row (batch 035); the reader is the flat-grantedSpells branch at src/rules/build.ts:4048. spells['insect-form'] exists in core.json (rank 3, matching WG's rank=3). No existing overlay row for this field on 
- lesson-of-mischief [player]: The familiar half of the lesson is missing: we carry only the hex (focusSpells: [deceivers-cloak]) and nothing delivers the Mad Monkeys the familiar learns, while WG grants it.
    PROPOSAL: Add scripts/data/effect-backfill.json row {category:'classFeatures', id:'lesson-of-mischief', field:'grantedSpells', value:['mad-monkeys']} — same lane/reader as the lesson-of-vengeance row (src/rules/build.ts:4048). spells['mad-monkeys'] exists in core.json (rank 3, matching WG's rank=3). No existing overlay row for this field on this record.
- lesson-of-snow [player]: The familiar half of the lesson is missing: we carry only the hex (focusSpells: [personal-blizzard]) and nothing delivers the Wall of Wind the familiar learns, while WG grants it.
    PROPOSAL: Add scripts/data/effect-backfill.json row {category:'classFeatures', id:'lesson-of-snow', field:'grantedSpells', value:['wall-of-wind']} — same lane/reader as the lesson-of-vengeance row (src/rules/build.ts:4048). spells['wall-of-wind'] exists in core.json (rank 3, matching WG's rank=3). No existing overlay row for this field on this record.
- lesson-of-the-shark [player]: The familiar half of the lesson is missing: we carry only the hex (focusSpells: [blood-in-the-water]) and nothing delivers the Feet to Fins the familiar learns, while WG grants it.
    PROPOSAL: Add scripts/data/effect-backfill.json row {category:'classFeatures', id:'lesson-of-the-shark', field:'grantedSpells', value:['feet-to-fins']} — same lane/reader as the lesson-of-vengeance row (src/rules/build.ts:4048). spells['feet-to-fins'] exists in core.json (rank 3, matching WG's rank=3). No existing overlay row for this field on this record.
- lesson-of-the-flock [player]: The familiar half of the lesson is missing: we carry only the hex (focusSpells: [sheltering-wings]) and nothing delivers the Levitate the familiar learns, while WG grants it.
    PROPOSAL: Add scripts/data/effect-backfill.json row {category:'classFeatures', id:'lesson-of-the-flock', field:'grantedSpells', value:['levitate']} — same lane/reader as the lesson-of-vengeance row (src/rules/build.ts:4048). spells['levitate'] exists in core.json (rank 3, matching WG's rank=3). No existing overlay row for this field on this record.
- advanced-weaponry-construct [player]: The modification the feature tells the player to choose is not recorded anywhere on our side — the record has no effectChoices, no effectPicks handler and no reference in build.ts/Builder — while WG stores the pick.
    PROPOSAL: Adopt their select: add effectChoices to classFeatures/advanced-weaponry-construct (via scripts/data/effect-backfill.json) whose options are the 11 level-1 `weapon-innovation-modification` class features (advanced-design, blunt-shot, complex-simplicity, dynamic-weighting, entangling-form, hampering-spikes, hefty-composition, modular-head, pacification-tools, razor-prongs, segmented-frame), so the answer is stored and
- hyper-boosters#overdrive [player]: The Overdrive step of the Speed bonus is a live number on their side and prose on ours, although the inventor Overdrive state and the whileActive lane that carries exactly this shape both exist here (batch 035 built them for metallic-reactance).
    PROPOSAL: Add whileActive: [{ state: 'overdrive', speeds: { land: 10 } }] to classFeatures/hyper-boosters in scripts/data/effect-backfill.json — activeStateGrants (src/rules/derive.ts:5165) pushes it and land grants are additive (derive.ts:5241), so the sheet reads +20 while Overdrive is on, exactly as print. Keep the situational star for the legendary branch only, or extend WhileActiveClause (src/rules/types.ts:1181) with the
- hyper-boosters#prerequisite [player]: The printed prerequisite is enforced by neither side, and our +10 is stored as a +5 DELTA over speed-boosters — so an inventor who takes Hyper Boosters without Speed Boosters (which the modification picker permits) walks at +5 feet instead of the printed +10.
    PROPOSAL: Either gate the option — in inventorModificationOptions (src/rules/build.ts:2759) drop hyper-boosters unless speed-boosters is among the character's chosen modifications, matching print's requirement — or make the record self-sufficient (landSpeedBonus 10) and subtract speed-boosters' 5 when both are owned, since two status bonuses to Speed do not stack in the first place.
- wind-seeker [player]: Wind Seeker's printed spell grant reaches nothing: the record carries no spellListAdditions, so aerial form never joins the animist's apparition repertoire.
    PROPOSAL: Add to feats/wind-seeker in the data lane (effect-backfill.json, so it survives npm run data): spellListAdditions: {"spells":["aerial-form"],"as":"repertoire","entryId":"animist-apparition-casting"} — byte-identical in shape to walk-the-wilds. core.json already has spells/aerial-form at rank 4, matching their rank:4.
- wind-seeker#darkened-forest-form [player]: The Special clause that adds the bat and bird forms to the darkened forest form lists reaches nothing on our side; we carry no spellNotes for it.
    PROPOSAL: Add to feats/wind-seeker in effect-backfill.json: spellNotes: [{"spellId":"darkened-forest-form","note":"Wind Seeker: if you are attuned to an apparition of darkened boughs, add the bat and bird forms in aerial form to your darkened forest form lists."}] — same carrier and shape as scattered-fire's fireball note.
- deadly-mutation [player]: Deadly Mutation delivers nothing at all: it never modifies the Spit Ambient Magic action it upgrades, so neither the d8 damage dice nor the 1d6 persistent damage on a critical failure reaches the sheet.
    PROPOSAL: Add to feats/deadly-mutation in effect-backfill.json: modifiesGrant: [{"from":"spit-ambient-magic","actionRider":{"actionId":"spit-ambient-magic","note":"Deadly Mutation: the damage dice of your Spit Ambient Magic are d8s, and a target that critically fails its save also takes 1d6 persistent bleed damage (persistent damage of the energy type instead, if you dealt energy damage with the ability)."}}] — same carrier/sh
- skill-mastery-rogue#maxtakable [player]: Skill Mastery (Rogue) prints "up to five times" but carries no maxTakable, so maxTakes() defaults it to 1 and the player can only ever take it once.
    PROPOSAL: Add maxTakable: 5 to feats/skill-mastery-rogue in effect-backfill.json, matching feats/skill-mastery.
- skill-mastery-rogue#duplicate-skill [player]: Skill Mastery (Rogue)'s two skill-increase slots are both `options: 'any'`, and an unanswered pair defaults both to Acrobatics — so the second printed increase (trained to expert on ANOTHER skill) silently vanishes.
    PROPOSAL: Extend distinctSiblingPicks in src/rules/build.ts to exclude earlier siblings for 'any' slots too (the two 'any' slots of one grant are byte-identical, which is the condition the helper already tests) — i.e. drop the `|| slot.options === 'any'` bail-out and let the JSON.stringify comparison handle it, keeping the existing never-empty guard. Same fix covers the sibling record feats/skill-mastery.
- golem-grafter-dedication [player]: Our resistance entry omits the printed adamantine exception, so the sheet grants (and displays) resistance to ALL physical damage including adamantine.
    PROPOSAL: Amend the existing backfill row in scripts/data/effect-backfill.json {category:"feats", id:"golem-grafter-dedication", field:"resistances"} to value [{"type":"physical","value":"@actor.archetypeFeats.golem-grafter","condition":"except adamantine"}] — the exact shape already used by feats/stoney-skin ({"type":"physical","value":"@actor.archetypeFeats.stone-brawler","condition":"except adamantine"}); derive.ts:2749 ren
- unravel-mysteries [player]: Both printed effects (half-time Decipher Writing and the waived -2 circumstance penalty on a failure) reach the player nowhere in our app; their side injects them onto the Decipher Writing action.
    PROPOSAL: Add a backfill row to scripts/data/effect-backfill.json: {category:"feats", id:"unravel-mysteries", field:"recordMarks", value:[{"on":"action","id":"decipher-writing","note":"Unravel Mysteries: Deciphering Writing takes only half as long as usual (about half a minute per page), and on a failure you don't take the usual -2 circumstance penalty to further checks to decipher that text."}]} — the on:'action' recordMarks 
- spirit-walk#resistance [player]: The printed first-turn resistance to haunt/spirit damage is modelled by neither side — our situational registry entry covers only the +2 Recall Knowledge bonus and its AC/saves extension.
    PROPOSAL: Add a backfill row to scripts/data/effect-backfill.json: {category:"feats", id:"spirit-walk", field:"resistances", value:[{"type":"all","value":"floor(@actor.level/2)","against":"damage dealt by haunts or spirits, during your first turn in an encounter (you and allies in the 30-foot aura)"}]} — `against` withholds the number from the total but renders the conditional line in the IWR breakdown (derive.ts:2744), the sa
- lorefinder [player]: Our innate Locate grant never heightens, so a 14th-level Eldritch Researcher still casts it at 3rd rank instead of 5th.
    PROPOSAL: Add heightenAt to the grant on feats.lorefinder in the effect-backfill lane: innateSpells[0].heightenAt = [{"level":14,"rank":5}] (reader: src/rules/build.ts:8140; same shape as dragon-prince / shadows-assault).
- animalistic-brutality [player]: Neither side models the trait the feat adds to the animal-instinct unarmed attack — our record carries only an action cost, and WG only injects descriptive text onto the twelve bestial-rage attack items.
    PROPOSAL: Author one mode per trait in src/rules/modes.ts under a shared exclusiveGroup (e.g. 'animalistic-brutality'), feats:['animalistic-brutality'], duration:'while raging', each carrying an unarmed rider {fromRecord:'animal-instinct', add:['backswing'|'forceful'|'parry'|'razing'|'sweep']}; ModeDef currently declares weaponTraits (src/rules/types.ts:4794) but not unarmedTraits, so hoist unarmedTraits?: UnarmedRider | Unarm
- armored-resistance#resistance [player]: The feat's entire printed content — physical resistance equal to half your level while intercepting in medium or heavy armor — is carried by neither side.
    PROPOSAL: Add to feats['armored-resistance'] in the effect-backfill lane: resistances: [{"type":"physical","value":"floor(@actor.level/2)","against":"damage you take with Intercept Attack while wearing medium or heavy armor"}] — the `against` form renders as its own IWR line and is deliberately excluded from the computed total (src/rules/types.ts IwrEntry), which is right for a reaction-only resistance; formula values are alre
- armored-resistance#instrument [internal]: The 'hp' instrument flag is a comparer misread: WG's MAX_HEALTH_BONUS op on this feat is the Guardian Resiliency per-archetype-feat HP, which we already deliver from the Resiliency record.
    PROPOSAL: instrument: the comparer read WG's per-archetype-feat Guardian Resiliency HP rider (replicated onto every guardian archetype feat in WG's model) as an HP mechanic missing from this record; ours delivers the same total from the single guardian-resiliency carrier. No change to armored-resistance's HP.
- cultivators-keen-eye#materials [player]: The feat's second half — sensing precious materials at 30 feet and spending them in place of a ritual's cost — is modelled by neither side; only the lifesense clause is carried.
    PROPOSAL: Add a recordMarks note on feats['cultivators-keen-eye'] (or a situationalBonuses.ts entry keyed 'cultivators-keen-eye') reading that you also sense precious materials within 30 feet and may pay all or part of a ritual's cost in precious materials of equivalent value (not for rituals needing specific items), so the clause surfaces on the sheet beside the lifesense.
- eye-of-the-unseen [player]: Our flat passiveEffects.perception adds the +1 item bonus to EVERY Perception check, while print limits it to visual Perception, and the conditional star in situationalBonuses.ts already states the same bonus — so the sheet both over-grants it and lists it twi
    PROPOSAL: Drop passiveEffects.perception from items['eye-of-the-unseen'] (authored via effect-backfill.json so it survives npm run data) and leave the conditional +1 on src/rules/situationalBonuses.ts:1693 — the same treatment day-goggles' visual-only -2 already gets (see the passiveItemPenalty comment at src/rules/derive.ts:326-339, which names that exact precedent).
- eye-of-the-unseen#innate-spell [player]: The once-per-day See the Unseen cast is modelled by neither side: our record has the frequency counter and the 2-action activation cost but no spell grant, so the spell never reaches the Spells page.
    PROPOSAL: Add innateSpells: [{ spellId: 'see-the-unseen', tradition: 'arcane', rank: 2, usesPerDay: 1 }] to items['eye-of-the-unseen'] via effect-backfill.json (spells['see-the-unseen'] exists, rank 2, traditions arcane/divine/occult) — the same carrier hydration-cloak uses for its 1/day water-walk.
- composer-staff-greater [player]: Our flat +1 Performance passive applies to every Performance check while the staff is merely held, though print grants it only for checks made by playing the staff as an instrument; the situational star already carries the conditional bonus, so the number is b
    PROPOSAL: Drop passiveEffects.skills.performance from items['composer-staff-greater'] (effect-backfill.json) and keep the conditional entry at src/rules/situationalBonuses.ts:1822 — the shape the sibling instrument pipes-of-compulsion-greater already uses (star only, no flat passive).
- energy-resistant [player]: Nothing on our side ever delivers the rune's resistance: the effectChoices grant can only fire from a loose inventory row that is worn/held/affixed, and etching consumes that row, so an etched energy-resistant rune gives 0 resistance while WG's row grants a re
    PROPOSAL: Deliver it on the etched side: read propertyRuneDefs(inv, db)'s passiveEffects.resistances in the DefenseGrants walk at src/rules/derive.ts:~2595-2612 (mirroring the propertyRuneDefs reads already at derive.ts:318 and :406), and carry the crafter's energy pick for the etched rune on the host row — InventoryItem.effectChoices keyed by the rune id, the store ItemDetail.tsx:336-345 already writes for item choices — so t
- illusionists-hat#skill-bonuses [player]: The hat's two printed +1 item bonuses (Deception to Create a Diversion, Thievery to Palm an Object) are carried by neither our item record nor any registry, while Wanderer's Guide encodes both.
    PROPOSAL: Add to FEAT_SITUATIONAL in src/rules/situationalBonuses.ts: "illusionists-hat": [{ targets: [{ kind: 'skill', detail: 'deception' }], when: "on Deception checks to Create a Diversion", bonus: "+1 item" }, { targets: [{ kind: 'skill', detail: 'thievery' }], when: "on Thievery checks to Palm an Object", bonus: "+1 item" }] — the same shape already used for 'the-demons-lantern' and 'lightweave-scarf' (action-scoped item

== ASK OWNER
- hyper-boosters#wg-legendary (CONFIRMED): WG's legendary-Crafting conditional has an empty trueOperations and grants its +30 from falseOperations, so the +30 fires whenever the character is in Overdrive regardless of Crafting rank — a non-legendary inventor in Overdrive gets +30 (on top of the +20 the first conditional's ELSE grants) where 
- spirit-walk#sense (REFUTED): WG's Spirit Walk row grants a PRECISE 30-foot 'apparition sight' sense that Spirit Walk's printed text does not grant at all — print gives apparition sight on the prerequisite feat Apparition Sense, and grants it as an IMPRECISE sense.
- chronomancers-secrets (CONFIRMED): WG grants Stasis as a NORMAL (non-focus) spell and from the legacy Stasis record, contradicting print, which calls it a domain spell (i.e. a focus spell); their Path of Least Resistance option is correctly typed FOCUS.

== REFUTED
- wind-seeker#instrument: The baseline digest's NO-SHEET-EFFECT flag on wind-seeker is a comparer misread — the record has a live sheet effect. || work/.b036-baseline/experience.json's wind-seeker entry gives the verdict's own basis — "WG encodes 1 value-bearing op(s); not on our built character: giveSpell \"Aerial Form\"" with delivery {deliver
- illusionists-hat#daily-illusory-creature: The hat's once-per-day two-action activation that casts a 3rd-rank illusory creature is modelled by neither side, so the player never sees the spell or its dail || Refuted as the listed false-positive class 'WG encoding LESS than print (that is parity, not a finding)': the finding's own 'theirs' is empty and I confirmed it by parsing WG item row 23007 directly -
