confirmed 29 | refuted 0 | askOwner 1
confirmed but NOT adversarially confirmed: 0

== FAMILIES
(routed from each finding's PROPOSAL by FAMILY_RULES in scripts/wg-batch-run.mjs — first rule wins, exactly one family per finding; a family with no findings is omitted)
situational (6): knowledge-is-power, hazard-finder, safeguard-soul#allies, mighty-bulwark#replacement-wording, mighty-bulwark#supersedes-trait-bulwark, mantle-of-the-tikbalang#illusory-thrash
instruments (2): show-off, magical-knowledge#instrument
engine (14): prolific-prophet-spellcasting, malleable-mental-forge, occult-breadth, magical-knowledge#skill-feats, magical-knowledge#master-precondition, magical-knowledge#distinct-skills, magic-finder#tradition, projectile-snatching, improved-elemental-blast, improved-elemental-blast#no-archetype-blast, lifting-leather#max, sigil-of-the-first-clan#spell-dc, canopy-bulwark#assisting-rune, canopy-bulwark#spell-tradition
data-rows (7): safeguard-soul#undead, heal-mount, magic-finder#spellcasting-statistic, basic-beast-gunner-spellcasting#dropped-clause, second-blessing, lifting-leather, crown-of-the-fire-eater#activation-effect

== CONFIRMED
- knowledge-is-power [player]: Our record and its situationalBonuses entry carry the LEGACY text and hand the player four +1 circumstance bonuses, while the print at its own aonId (feat-5039, Player Core pg. 203, edition remaster) gives the ENEMY a -1 circumstance penalty and no bonus to th
    PROPOSAL: Delete the four legacy "+1 circumstance" entries under "knowledge-is-power" in src/rules/situationalBonuses.ts:2857 (the remaster feat gives the player no bonus; the penalty lands on the enemy, ruling F / OTHERS_ROLL territory) and refresh core-descriptions feats/knowledge-is-power from the feat-5039 mirror. Since feats/knowledge-is-power-wizard already holds feat-5039 with the correct remaster text and feats/knowled
- show-off [internal]: The packet's theirEncoding for Show-Off was taken from the wrong Wanderer's Guide row — a level-1 Performance feat — while the row matching our feat (the level 8 Acrobat/Firebrand free action, feat-4144) carries no operations at all; adopting the quoted op wou
    PROPOSAL: instrument: the WG name match picked their level-1 "Show-Off" row 55639 instead of the level-8 row 24347 that matches our feat-4144; the WG-side lane for show-off is empty (WG encodes nothing mechanical) and no Performance bonus is to be adopted.
- prolific-prophet-spellcasting [player]: The extra spells the feat adds to the repertoire reach the sheet's entry but the builder never offers a pick for them: the archetype pick-cap ignores spellSlotBonus.perRank, so at level 8 the character gets a second rank-1 known spell and slot with no picker t
    PROPOSAL: src/builder/Builder.tsx: extend `archExtraKnownAt` (the helper behind slotsAt at :449 and :1014) to apply the same entry-scoped folding build.ts:7330-7342 does — byRank, highestOnly, and `perRank` with `exceptHighest` — for feats whose spellSlotBonus.entryId is in archetypeEntryIds(archCaster), so the builder's per-rank pick cap equals the repertoire size buildCharacter slices.
- malleable-mental-forge [player]: The two weapon traits chosen each morning are recorded and then reach nothing: the record's choice flag has no reader anywhere in src/, and the mind weapon ships with an empty trait list, so the strike never gains Grapple, Modular, Nonlethal, Shove or Trip.
    PROPOSAL: Give the answer a reader: in src/rules/derive.ts, where the strike's trait list is assembled, add the daily answer to `mindWeaponTraits` (falling back to mental-forge's build-time `mentalForgeTraits`) to the traits of the mind-weapon strike for characters holding items/mind-weapon. If the owner instead wants this lane to stay recorded-only, say so on the record the way mind-smith-dedication's `note` states its statli
- occult-breadth [player]: The extra repertoire spells the feat grants are unreachable: buildCharacter adds the feat's perRank slots to the archetype entry and sizes the repertoire from that count, but the Builder's spell-pick cap for an archetype caster ignores every slot-side spellSlo
    PROPOSAL: In src/builder/Builder.tsx `archExtraKnownAt` (line 414), after folding in `extraKnown`, apply the entry-targeted slot bonuses the same way src/rules/build.ts:7327-7342 does — byRank / highestOnly / (perRank ?? 1) over `exceptHighest`-trimmed ranks — so the builder's pick cap equals the repertoire the sheet builds. No record change; occult-breadth's fields are correct.
- hazard-finder [player]: The feat's second clause — finding hazards without Searching — is modelled by neither side and is not surfaced on the Search action a player actually reads.
    PROPOSAL: Add RECORD_MARKERS['hazard-finder'] = [{ on: 'action', id: 'search', note: "you find hazards that would normally require you to Search even when you aren't Searching." }] in src/rules/situationalBonuses.ts (core.json actions.search exists) — the same carrier smoke-through-bamboo uses for a feat whose clause changes another action.
- magical-knowledge#skill-feats [player]: The printed skill feat(s) are granted by neither our record nor our registries — WG asks for one and we ask for none, so the feat's whole second sentence reaches the player as nothing.
    PROPOSAL: Add `bonusSkillFeat: true` to 'magical-knowledge' in src/rules/featGrantsLane.ts — the existing lane (FeatGrant.bonusSkillFeat, src/rules/featGrants.ts:346; applied build.ts:5074/9198, rendered Builder.tsx:2357) injects exactly the level-8 skill-feat slot WG's select is. That reaches WG parity; print grants one per chosen skill (two), which the boolean cannot express, so the lane needs a count (e.g. `bonusSkillFeat: 
- magical-knowledge#master-precondition [player]: Our master slot offers all four skills with no requirement that the picked skill is already expert, so a character trained (not expert) in a skill jumps straight to master off this feat.
    PROPOSAL: Give the master slot a precondition on the picked skill's pre-feat rank in src/rules/featGrantsLane.ts + the slot applier: either a new per-slot `requires: 'expert'` honoured by skillSlotGrant (src/builder/Builder.tsx:167) and the apply loop (src/rules/build.ts:5706), or express it with the existing `conditionalRank: { base: 'expert', upgraded: 'master' }` so a non-expert pick grants expert rather than master.
- magical-knowledge#distinct-skills [player]: Nothing stops both slots resolving to the same skill, and an unanswered pair defaults both to Arcana — so the feat silently delivers one skill's increase instead of the two it prints.
    PROPOSAL: Exclude slot 0's answer from slot 1's options in `skillSlotOptions` (src/rules/build.ts:1652) via a per-slot `distinctFromSlot: 0` flag, so the engine and the Builder picker (which shares the helper) both offer three options in the second slot and the unanswered default cannot collide.
- magical-knowledge#instrument [internal]: The identity flag "theirs-not-ours=[skillarcana, skillnature, skilloccultism, skillreligion] | ours=[(nothing)]" is an instrument misread: we do offer those four options, through FEAT_GRANTS.skillChoices, and the comparer's reader for that lane cannot see them
    PROPOSAL: instrument: scripts/wg-identity.mjs:505-517 reads a lane record's options with /options\s*:\s*\[/, which cannot match the JSON-quoted `"options": [` form every machine-written featGrantsLane/featGrantsAuto row uses (make the key optionally quoted); and it credits only key('arcana'), while the their-side label keys as key('SKILL_ARCANA') = 'skillarcana' — so it must also add key(`skill ${s}`) for each slot option, the
- safeguard-soul#undead [player]: The feat's categorical immunity to being made undead is modelled by neither side, though our feats records already have an `immunities` carrier that would display it.
    PROPOSAL: Add `immunities: ["being transformed into an undead"]` to feats.safeguard-soul via scripts/data/effect-backfill.json — the same free-text immunities carrier 16 feats already use (explosive-expert: ["splash damage from your own Strikes with bombs and firearms"], ghost-dedication: ["death effects"]), so it renders in the IWR list.
- safeguard-soul#allies [player]: The printed clause extending the +2 status bonus to allies while the spiral glows is carried by neither side, so a party reading the character's sheet sees a self-only bonus.
    PROPOSAL: Extend the entry's `when` in src/rules/situationalBonuses.ts:558 to "against death, possession, or soul-manipulation effects — and, while your spiral is glowing, allies in its light gain the same bonus", the cheapest carrier that puts the clause where the player reads the bonus; no numeric lane exists for ally auras.
- heal-mount [player]: Heal Mount changes what Lay on Hands restores when cast on your mount, and we carry nothing at all for it — no spellNote, no situational entry, no marker — so the clause reaches the player nowhere.
    PROPOSAL: Add to scripts/data/effect-backfill.json: {"category":"feats","id":"heal-mount","field":"spellNotes","value":[{"spellId":"lay-on-hands","note":"When you cast Lay on Hands on your mount, instead of the normal amount, the spell restores 10 Hit Points, plus 10 for each heightened rank."}]} — the same carrier feats/mercy and feats/greater-mercy already use to hang clauses on lay-on-hands, and the direct analogue of their
- mighty-bulwark#replacement-wording [player]: Our situational entry states Mighty Bulwark as a "+4 (bulwark)" bonus on Reflex saves, but the bulwark number is used INSTEAD OF your Dexterity modifier — for a high-Dex character it is a reduction, so the star as written overstates the save.
    PROPOSAL: src/rules/situationalBonuses.ts:443 — change the mighty-bulwark entry's bonus to "+4 instead of your Dexterity modifier" so the replacement semantics survive when the trait:bulwark line is superseded (see mighty-bulwark#supersedes-trait-bulwark).
- mighty-bulwark#supersedes-trait-bulwark [player]: A character with Mighty Bulwark wearing bulwark armor reads TWO stars on the same Reflex row — the trait's +3 and the feat's +4 — although print says the feat INCREASES the trait's number rather than adding a second one.
    PROPOSAL: Add "mighty-bulwark": ['trait:bulwark'] to SITUATIONAL_SUPERSEDES in src/rules/situationalBonuses.ts — the exact shape already used for "master-overdrive": ['expert-overdrive'] and "grave-strength": ['ghoul-dedication'] (an increase, not a second bonus); supersededIds already runs over the id list that carries both the feat id and the trait: id.
- magic-finder#spellcasting-statistic [player]: Print gives a non-caster who takes Magic Finder an Intelligence-keyed arcane spellcasting statistic; we carry no spellcastingGrant, so the innate entry falls back to Charisma and the feat's three spells roll and set DCs off the wrong attribute.
    PROPOSAL: Add to scripts/data/effect-backfill.json: {"category":"feats","id":"magic-finder","field":"spellcastingGrant","value":{"tradition":"arcane","keyAbility":"int","proficiency":"trained"}} — the shape already used by crystal-ward-spells. Same sentence, same missing field, on the two lane siblings lore-seeker and cautious-delver.
- magic-finder#tradition [player]: We hard-code tradition "arcane" on all three grants, so an existing caster's Magic Finder spells are listed as arcane even though print makes them that caster's own tradition.
    PROPOSAL: Add `traditionFromCasting?: boolean` to InnateSpellGrant (src/rules/types.ts) read at build.ts:7973 — when set, prefer `caster?.tradition` over the grant's/spell's tradition and fall back to the record's `tradition` for a non-caster — then set it on the three magic-finder grants (and the lane siblings lore-seeker, cautious-delver) via scripts/data/effect-backfill.json. Dropping the `tradition:'arcane'` alone does NOT
- projectile-snatching [player]: Projectile Snatching's return-Strike is not marked on the Deflect Projectile row, although our own recordMarks lane already carries the identical clause for the sibling feat Return Fire.
    PROPOSAL: Add to scripts/data/effect-backfill.json: {"category":"feats","id":"projectile-snatching","field":"recordMarks","value":[{"on":"feature","id":"deflect-projectile","note":"When you successfully deflect an attack with Deflect Projectile, you can immediately make a ranged Strike against the attacker with the deflected projectile as part of that same reaction; it is a thrown weapon with the triggering attack's range incr
- improved-elemental-blast [player]: The feat's whole content — +1 Elemental Blast damage die per taking — has no carrier anywhere on our side; the blast's die count is a hard-coded class-level table in derive.ts that no record can raise.
    PROPOSAL: Add `blastDiceBonus?: number` to Feat (src/rules/types.ts), set it to 1 on feats/improved-elemental-blast in the effect-backfill lane (work/effect-backfill.json, or it dies at the next `npm run data`), and read it in deriveBlastStrikes (src/rules/derive.ts:4501) as `dice = 1 + classSteps + sum over c.feats of db.feats[f.featId]?.blastDiceBonus` — c.feats is FeatChoice[] with one entry per taking, so the repeat count 
- improved-elemental-blast#no-archetype-blast [player]: A character who can take this feat (prerequisite: Kineticist Dedication, i.e. never a kineticist by class) has no Elemental Blast strike at all, so the mechanic this feat improves is not on the sheet to begin with.
    PROPOSAL: In src/rules/build.ts:8286 build `kineticist` whenever `kineticistElements(build, level)` is non-empty, not only when ownsClass('kineticist') — i.e. `const els = kineticistElements(build, level).map(id => id.replace(/-gate$/, '')); const kineticist = els.length ? { elements: els, ...blastTypes } : undefined;`. Then gate the class die-scaling table in deriveBlastStrikes on ownsClass so an archetype blast stays at 1 di
- basic-beast-gunner-spellcasting#dropped-clause [player]: Our shipped rules text drops the linked phrase that names what the feat grants, leaving the sentence 'You gain the .' — the player cannot see that they gain the basic spellcasting benefits.
    PROPOSAL: Text lane, not a mechanics field: restore the stripped link LABEL in the description importer's markdown cleaner (the same class as the deleted @Damage inline values) and add a verify guard for the residue pattern /\b(the|a|an|your)\s+\./ over core-descriptions.json — it currently matches 174 records (e.g. spells/commune-with-corazal, spells/natures-enmity), so this is systemic, not one row.
- second-blessing [player]: Our blessing picker lets the player re-take the blessing they already chose at level 3, which the printed text forbids — the record omits `disableIfOwned`, the shipped field for exactly this restriction (Manifold Modifications, the sibling "an ADDITIONAL x" pi
    PROPOSAL: effect-backfill.json row { category: 'feats', id: 'second-blessing', field: 'choice', value: { ...existing choice, disableIfOwned: true } } — the option values are already classFeature ids, so Q27's grey-with-reason path ("Already taken — Blessed Swiftness.") fires with no code change.
- lifting-leather [player]: Lifting Leather raises no Bulk threshold at all — the armor carries no passiveEffects, so the printed +2 encumbered limit is delivered by nothing, while WG delivers it.
    PROPOSAL: Add to scripts/data/effect-backfill.json: {"category":"items","id":"lifting-leather","field":"passiveEffects","value":{"bulkLimitBonus":2}} — same shape as the existing lifting-belt row, which is the only reason that item's threshold moves.
- lifting-leather#max [player]: The printed maximum Bulk limit rises by 4 while the encumbered limit rises by 2; neither side models the extra +2 on the maximum, and no item field can express it — ItemPassiveEffects has bulkLimitBonus but no bulkMaxBonus, and deriveBulk's item lane never rea
    PROPOSAL: Declare `bulkMaxBonus?: number` on the item passiveEffects interface (src/rules/types.ts, beside bulkLimitBonus at :2244), read it in deriveBulk's item loop (src/rules/derive.ts:5292-5299) into maxOnlyBonus exactly as the feat/heritage loops do, and author {"bulkLimitBonus":2,"bulkMaxBonus":2} for lifting-leather in scripts/data/effect-backfill.json.
- mantle-of-the-tikbalang#illusory-thrash [player]: Illusory Thrash's extra 4d6 mental damage on the Strike is modelled by neither side — the record carries the activation cost and the once-per-day counter but nothing that tells the player the Strike hits harder.
    PROPOSAL: Add a second target to the existing src/rules/situationalBonuses.ts entry for "mantle-of-the-tikbalang": { targets: [{ kind: 'strikeDamage' }], when: "on the melee Strike you make when you Activate Illusory Thrash (once per day)", bonus: "+4d6 mental" } — the same shape blackaxe (:2140, "+2d6 damage") and heartripper-blade (:1725) already use for activation-gated extra dice.
- sigil-of-the-first-clan#spell-dc [player]: The sigil prints a fixed DC 24 for its Command, but our heldSpells lane casts it at the WIELDER's spell DC (Int/trained when the wielder is not a caster), so the sheet shows a DC the printed text contradicts.
    PROPOSAL: Declare a fixed-DC field on Item (e.g. `heldSpellDc?: number`, beside heldSpells at src/rules/types.ts:3962), have the 'items' spellcasting entry in src/rules/build.ts (~:7735-7745) use it as the entry's DC when present, and author heldSpellDc: 24 for sigil-of-the-first-clan in scripts/data/effect-backfill.json.
- canopy-bulwark#assisting-rune [player]: Canopy Bulwark is printed as a +1 ASSISTING leaf weave, but our record carries only builtInRunes.potency:1 — the assisting property rune's bulk-limit grant is dropped, so the wearer's encumbered/maximum Bulk thresholds never move.
    PROPOSAL: Add to scripts/data/effect-backfill.json: {"category":"items","id":"canopy-bulwark","field":"passiveEffects","value":{"bulkLimitBonus":1}} — the item-level bulkLimitBonus reader already exists (src/rules/derive.ts:5291-5296, itemInUse-gated, Math.max against the rune bonus), so this delivers the assisting rune's "6 + Str encumbered / 11 + Str maximum" without inventing a builtInRunes.property reader.
- canopy-bulwark#spell-tradition [player]: Their giveSpell op names the granted Haste's tradition as PRIMAL; ours carries it as heldSpells with no tradition field, so build.ts derives the entry's tradition from haste's tradition list and labels it ARCANE.
    PROPOSAL: In scripts/data/effect-backfill.json replace the heldSpells carrier for canopy-bulwark with the innate carrier, which is the only one of ours that holds a tradition: {"category":"items","id":"canopy-bulwark","field":"innateSpells","value":[{"spellId":"haste","rank":3,"tradition":"primal","usesPerDay":1}]} and drop heldSpells (do not author both — the invested-item innateSpells reader at src/rules/build.ts:7807-7812 a
- crown-of-the-fire-eater#activation-effect [player]: The reaction's whole mechanical payload — fire resistance rising from 5 to 15 for the triggering damage, and the Hit Points regained after it — is modelled by neither side; we carry only the 1/day counter and the reaction action cost, so no number on the sheet
    PROPOSAL: Add a modes-bucket entry via scripts/data/effect-backfill.json in the same shape the resistance potions already use (core.json modes/item-potion-of-fire-resistance-greater carries resistances [{type:'fire',value:15}] with fromItemId): {"id":"item-crown-of-the-fire-eater","name":"Fire Eater (Activated)","fromItemId":"crown-of-the-fire-eater","duration":"the triggering fire damage","modifiers":[],"resistances":[{"type"

== ASK OWNER
- necromantic-tenacity (CONFIRMED): WG re-triggers the degree shift on "an effect with the death or void trait" where the printed feat triggers on "a necromancy effect" — not a narrower encoding of print but a different trigger set (legacy necromancy covers effects that carry neither trait, e.g. vitality/curse effects).

== REFUTED
