{
  "_": "BATCH 035 — GAP LANE, family gap-data-rows-1. ZERO backfill rows and zero applier edits, deliberately: the one open gap line of family data-rows-1 is a READER defect (a chosen whileActive clause reached the IWR breakdown with no source name), so nothing is owed to scripts/data/effect-backfill.json — the data was already correct. This spec exists to carry the manifest `stage` list, which is what scripts/wg-batch-commit.mjs stages by explicit path. The work itself is one two-file code fix plus its pinning test, listed in work/.b035-report-gap-data-rows-1.txt.",
  "findings": [
    {
      "id": "gap#chosen-whileactive-attribution",
      "backfillRows": [],
      "codeEdits": [],
      "note": "GAP LANE, closing work/.b035-gaps.json's only data-rows-1 line (ref work/.b035-report-data-rows-1.txt:13). A `whileActive` clause printed on a record is scanned off that record, which carries a `name`; a clause the PLAYER CHOSE is merged into Character.chosenEffects, one shared nameless DefenseGrants bag, so derive.ts's ownedWhileActive had nothing to attribute it to and fell back to the bare string 'Active state'. MEASURED before the fix on a built 9th-level Rime dragon-instinct barbarian: deriveDefenses(...).sources['resistance:piercing'] === [{from:'Active state'}], and stateGrantSummary(c, db, 'rage') the same, so the Rage card was blind too. Fixed at both ends: src/rules/build.ts:6106-6117 (mergeEffect now takes the granting record's srcName and stamps `from` on a COPY of each chosen clause — `g` is the shared content record and must not be mutated) and src/rules/derive.ts:1163-1170 (the scan reads wa.from first, then the bag's name, then the fallback). Print authority: AoN instinct-9 “You resist piercing damage and the damage type of your instinct’s dragon breath.” and instinct-4 “You resist bludgeoning damage and your choice of cold, electricity, or fire, chosen when you gain raging resistance.” — both clauses are printed ON the instinct, so both breakdown lines have to name it. Pinned by test/batch035-gap-data-rows-1.test.ts (6 tests: 3 pinned the defect and failed before the fix, 3 are controls — ligneous-instinct's record-scanned attribution, an unanswered pick granting nothing, and the state/level gates — which passed before and after). Same blindness raging-resistance.test.ts:64 closed for the instincts that ask no question."
    }
  ]
}
